---
phase: 02-authentication-authorization
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
gap_closure: false
files_modified:
  - shared-lib/src/main/java/io/restaurantos/shared/security/EncryptionService.java
  - shared-lib/src/main/java/io/restaurantos/shared/security/EncryptedStringConverter.java
  - shared-lib/src/main/java/io/restaurantos/shared/config/EncryptionAutoConfiguration.java
  - shared-lib/src/main/resources/META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports
  - shared-lib/src/test/java/io/restaurantos/shared/security/EncryptionServiceTest.java
  - services/auth-service/pom.xml
  - services/auth-service/src/main/java/io/restaurantos/auth/entity/UserEntity.java
  - services/auth-service/src/main/java/io/restaurantos/auth/entity/PasswordResetTokenEntity.java
  - services/auth-service/src/main/java/io/restaurantos/auth/entity/PasswordHistoryEntity.java
  - services/auth-service/src/main/java/io/restaurantos/auth/repository/PasswordResetTokenRepository.java
  - services/auth-service/src/main/java/io/restaurantos/auth/repository/PasswordHistoryRepository.java
  - services/auth-service/src/main/java/io/restaurantos/auth/service/TotpService.java
  - services/auth-service/src/main/java/io/restaurantos/auth/service/PasswordResetService.java
  - services/auth-service/src/main/java/io/restaurantos/auth/service/BranchSwitchService.java
  - services/auth-service/src/main/java/io/restaurantos/auth/service/AuthServiceImpl.java
  - services/auth-service/src/main/java/io/restaurantos/auth/controller/TwoFactorController.java
  - services/auth-service/src/main/java/io/restaurantos/auth/controller/PasswordResetController.java
  - services/auth-service/src/main/java/io/restaurantos/auth/controller/BranchSwitchController.java
  - services/auth-service/src/main/java/io/restaurantos/auth/dto/request/TotpVerifyRequest.java
  - services/auth-service/src/main/java/io/restaurantos/auth/dto/request/PasswordResetRequest.java
  - services/auth-service/src/main/java/io/restaurantos/auth/dto/request/PasswordResetConfirmRequest.java
  - services/auth-service/src/main/java/io/restaurantos/auth/dto/request/SwitchBranchRequest.java
  - services/auth-service/src/main/java/io/restaurantos/auth/dto/response/TotpSetupResponse.java
  - services/auth-service/src/main/resources/db/changelog/v1.0.0/060-create-password-reset-tokens.xml
  - services/auth-service/src/main/resources/db/changelog/v1.0.0/070-create-password-history.xml
  - services/auth-service/src/test/java/io/restaurantos/auth/integration/TotpFlowIT.java
  - services/auth-service/src/test/java/io/restaurantos/auth/integration/PasswordResetIT.java
  - services/auth-service/src/test/java/io/restaurantos/auth/integration/BranchSwitchIT.java
  - services/auth-service/src/test/java/io/restaurantos/auth/integration/StepUpLoginIT.java
autonomous: true

must_haves:
  truths:
    - "A user POSTs /api/v1/auth/2fa/setup to get a TOTP secret + otpauth:// URI, then /2fa/verify with a valid code sets totp_enabled=true; /2fa/disable (with a fresh code) clears it (SC3, AUTH-07)"
    - "users.totp_secret is persisted AES-256-GCM encrypted via EncryptedStringConverter (raw secret never stored in plaintext) (SC3, AUTH-09)"
    - "At login, a user whose computed permissions include rbac.manage or finance.period.close (or who has totp_enabled) is refused with 401 TOTP_REQUIRED unless a valid totpCode is supplied (SC3, AUTH-07; resolves Open Q2)"
    - "StepUpLoginIT's valid-code success path has a defined secret source: the test fixture enrolls the privileged user by writing an encrypted totp_secret + totp_enabled=true directly via EncryptionService/UserRepository, then generates a matching code with dev.samstevens.totp (no chicken-and-egg with /2fa/setup, which itself needs an authenticated JWT)"
    - "The non-step-up paths in this plan (password reset, branch switch) reuse 02-01's NON-privileged cashier@demo.local so they don't trip the new step-up rule"
    - "Password reset works end-to-end: request emits a reset event with a single-use, time-limited token; confirm sets a new bcrypt-12 hash, enforces no-reuse-of-last-5, and consumes the token (SC3, AUTH-06)"
    - "POST /api/v1/auth/switch-branch verifies an active user_branch_roles row for the target branch, recomputes permissions for that branch, and reissues an access JWT with the new branch_id/permissions while keeping the refresh session (SC2, AUTH-05)"
  artifacts:
    - path: "shared-lib/src/main/java/io/restaurantos/shared/security/EncryptionService.java"
      provides: "AES-256-GCM encrypt/decrypt (12-byte IV, 128-bit tag) keyed by FIELD_ENCRYPTION_KEY"
      contains: "AES/GCM/NoPadding"
    - path: "shared-lib/src/main/java/io/restaurantos/shared/security/EncryptedStringConverter.java"
      provides: "JPA AttributeConverter<String,byte[]> static-initialised with EncryptionService"
      contains: "AttributeConverter"
    - path: "services/auth-service/src/main/java/io/restaurantos/auth/service/TotpService.java"
      provides: "RFC-6238 TOTP via dev.samstevens.totp (secret gen, otpauth URI, code verify)"
      contains: "DefaultCodeVerifier"
    - path: "services/auth-service/src/main/java/io/restaurantos/auth/service/PasswordResetService.java"
      provides: "request/confirm reset with single-use hashed token + last-5 reuse check"
      contains: "password_reset"
    - path: "services/auth-service/src/main/java/io/restaurantos/auth/service/BranchSwitchService.java"
      provides: "branch-switch reissue of the access JWT with recomputed permissions"
      contains: "user_branch_roles"
  key_links:
    - from: "services/auth-service/src/main/java/io/restaurantos/auth/entity/UserEntity.java"
      to: "users.totp_secret BYTEA"
      via: "@Convert(converter = EncryptedStringConverter.class) on the totp_secret field"
      pattern: "EncryptedStringConverter"
    - from: "services/auth-service/src/main/java/io/restaurantos/auth/service/AuthServiceImpl.java"
      to: "TotpService (step-up at login)"
      via: "if permissions contain rbac.manage/finance.period.close OR totp_enabled → require valid totpCode else 401 TOTP_REQUIRED"
      pattern: "TOTP_REQUIRED"
    - from: "services/auth-service/src/main/java/io/restaurantos/auth/service/PasswordResetService.java"
      to: "shared EventPublisher (auth.topic) + notification (Phase 5)"
      via: "publish PASSWORD_RESET_REQUESTED with token; email delivery is a Phase-5 consumer"
      pattern: "auth.topic"
---

<objective>
Add the remaining authentication features on top of 02-01: TOTP 2FA with an AES-256-GCM-encrypted secret, mandatory step-up TOTP at login for privileged-permission holders, password reset via emailed token, and branch switching that reissues the JWT with the new branch context. Build the spec'd field-encryption primitives (`EncryptionService` + `EncryptedStringConverter`) once in shared-lib for reuse.

Purpose: Delivers SC3 (TOTP setup/verify with AES-256-GCM `totp_secret`; `rbac.manage`/`finance.period.close` refused without a valid TOTP step; password reset via emailed token) and the SC2 branch-switch half. Covers AUTH-05, AUTH-06, AUTH-07, AUTH-09. Resolves Open Q2 (step-up contract) and Open Q4 (home of EncryptionService — shared-lib, additive).
Output: shared-lib `EncryptionService`/`EncryptedStringConverter` (+ a NEW opt-in `EncryptionAutoConfiguration`, NOT an edit to `SharedAutoConfiguration` — keeps this plan parallel-safe with 02-03), the TOTP/reset/branch-switch services + controllers, `password_reset_tokens` + `password_history` migrations, the `users.totp_secret` `@Convert`, and an IT suite proving SC3 + branch switch.

WAVE-2 BUILD NOTE: 02-02 and 02-03 both add DISJOINT new files to shared-lib (this plan: `security/Encryption*` + `config/EncryptionAutoConfiguration`; 02-03: `authz/AuthorizationService`) — no file conflict. If both run in the SAME working tree, sequence the shared-lib `mvn install` (don't run two `shared-lib` builds concurrently) to avoid a `target/` race; running each plan's full reactor build serially is sufficient.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-authentication-authorization/02-RESEARCH.md
@.planning/phases/02-authentication-authorization/02-01-PLAN.md
@.planning/phases/02-authentication-authorization/02-01-SUMMARY.md

# Source of truth — transcribe/adapt, do not invent:
@Docs/agent-specs/09-security-implementation-guide.md
@Docs/agent-specs/08-database-migration-guide.md
@Docs/agent-specs/05-environment-variables.md
@Docs/agent-specs/02-event-schema-registry.md
@Docs/agent-specs/10-test-architecture-guide.md
@Docs/agent-specs/07-coding-standards.md

# Shared-lib + auth-service code to BUILD ON (do not duplicate):
@shared-lib/src/main/java/io/restaurantos/shared/config/SharedAutoConfiguration.java
@shared-lib/src/main/resources/META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports
@services/auth-service/src/main/java/io/restaurantos/auth/service/AuthServiceImpl.java
@services/auth-service/src/main/java/io/restaurantos/auth/service/JwtSigningService.java
@services/auth-service/src/main/java/io/restaurantos/auth/service/PermissionResolver.java
@services/auth-service/src/main/java/io/restaurantos/auth/entity/UserEntity.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: shared-lib field encryption (EncryptionService + EncryptedStringConverter) via a NEW opt-in auto-config</name>
  <files>shared-lib/src/main/java/io/restaurantos/shared/security/EncryptionService.java, shared-lib/src/main/java/io/restaurantos/shared/security/EncryptedStringConverter.java, shared-lib/src/main/java/io/restaurantos/shared/config/EncryptionAutoConfiguration.java, shared-lib/src/main/resources/META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports, shared-lib/src/test/java/io/restaurantos/shared/security/EncryptionServiceTest.java</files>
  <action>
    Resolves Open Q4 — these live in shared-lib (HR/Purchasing reuse them later for `employees.cnic`, `bank_account_no`, `vendors.bank_account_no`, Doc 9 §9.5/CC.1). Transcribe Doc 9 §9.5 VERBATIM:
      - `io.restaurantos.shared.security.EncryptionService`: AES-256-GCM, 12-byte IV + 128-bit tag, key from a base64 32-byte string; `byte[] encrypt(String)` / `String decrypt(byte[])` with IV prepended to ciphertext (exactly as the spec shows).
      - `io.restaurantos.shared.security.EncryptedStringConverter implements AttributeConverter<String, byte[]>` with the `static EncryptionService` + `static void init(EncryptionService)` pattern from the spec.
    Create a NEW `io.restaurantos.shared.config.EncryptionAutoConfiguration` (a SEPARATE file — DO NOT edit `SharedAutoConfiguration.java`; this keeps 02-02 and 02-03 parallel-safe and avoids a HIGH-risk edit to the shared bean graph): an `@AutoConfiguration` guarded `@ConditionalOnProperty(name="restaurantos.encryption.key")` (bound from `FIELD_ENCRYPTION_KEY`) that builds an `EncryptionService` bean AND, in an `@PostConstruct`/`InitializingBean`, calls `EncryptedStringConverter.init(encryptionService)` so the JPA converter is wired at startup. APPEND this new auto-config class to the existing `META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports` (one class per line — append, do not rewrite the file).
    NOTE for the executor: if a deviation forces editing `SharedAutoConfiguration` instead, run `gitnexus_impact({target:"SharedAutoConfiguration", direction:"upstream"})` first and report the blast radius before proceeding (CLAUDE.md rule). The new-file approach avoids this.
    `EncryptionServiceTest` (shared-lib unit test): round-trip encrypt→decrypt returns the original; two encryptions of the same plaintext differ (random IV); decrypt of tampered bytes throws.
  </action>
  <verify>`mvn -pl shared-lib -am -q -DskipTests compile` succeeds; `SharedAutoConfiguration.java` is UNCHANGED (`git diff --quiet shared-lib/.../config/SharedAutoConfiguration.java`); the imports file now lists `EncryptionAutoConfiguration`; `mvn -pl shared-lib -am -q test -Dtest=EncryptionServiceTest` is GREEN.</verify>
  <done>shared-lib ships AES-256-GCM `EncryptionService` + `EncryptedStringConverter`, wired by a new opt-in `EncryptionAutoConfiguration` (no edit to SharedAutoConfiguration), reusable by HR/Purchasing. (AUTH-09 primitive; Open Q4.)</done>
</task>

<task type="auto">
  <name>Task 2: TOTP setup/verify/disable + AES-encrypted totp_secret + step-up at login</name>
  <files>services/auth-service/pom.xml, services/auth-service/src/main/java/io/restaurantos/auth/entity/UserEntity.java, services/auth-service/src/main/java/io/restaurantos/auth/service/TotpService.java, services/auth-service/src/main/java/io/restaurantos/auth/service/AuthServiceImpl.java, services/auth-service/src/main/java/io/restaurantos/auth/controller/TwoFactorController.java, services/auth-service/src/main/java/io/restaurantos/auth/dto/request/TotpVerifyRequest.java, services/auth-service/src/main/java/io/restaurantos/auth/dto/response/TotpSetupResponse.java</files>
  <action>
    Add `dev.samstevens.totp:totp:1.7.1` to `services/auth-service/pom.xml` (RESEARCH Don't-Hand-Roll; verify the exact 1.7.x at build time). Ensure `restaurantos.encryption.key` is bound from `FIELD_ENCRYPTION_KEY` in `application.yml` so the new `EncryptionAutoConfiguration` activates and `EncryptedStringConverter` is initialized.

    On `UserEntity`, annotate the `totp_secret` field `@Convert(converter = EncryptedStringConverter.class)` + `@Column(name="totp_secret", columnDefinition="bytea")` (Doc 9 §9.5 usage) — the raw base32 secret is now stored AES-256-GCM encrypted (AUTH-09). The DB column already exists (02-01 migration 020).

    `TotpService` using `dev.samstevens.totp`: `generateSecret()` (`DefaultSecretGenerator`), `otpauthUri(secret, accountName, issuer="RestaurantOS")` (`QrData`/`QrGenerator` → otpauth:// URI; returning the URI is sufficient, QR image optional), `verify(secret, code)` (`DefaultCodeVerifier` + `SystemTimeProvider`, default time window for skew).

    `TwoFactorController` (`/api/v1/auth/2fa`, authenticated):
      - `POST /setup`: generate a secret, store it ENCRYPTED on the user with `totp_enabled=false`, return `TotpSetupResponse{otpauthUri}` (do not return the raw secret beyond setup).
      - `POST /verify`: `{code}` → verify against the stored (decrypted) secret; on success set `totp_enabled=true`.
      - `POST /disable`: require a fresh valid code, then clear `totp_secret` + set `totp_enabled=false`.

    Step-up at login (resolves Open Q2) — extend `AuthServiceImpl.login` (the method built in 02-01; this plan depends on 02-01 so editing it is in-wave). IMPORTANT cross-plan guard: this edit changes the shared `login` method, so 02-01's plain-login ITs MUST already target the NON-privileged `cashier@demo.local` (see 02-01 seed + AuthLoginIT/RefreshLogoutIT) — confirm that before editing so SC1 does not regress. Logic: AFTER a successful password match and permission resolution, BEFORE issuing tokens, if the user's computed permissions contain `rbac.manage` OR `finance.period.close`, OR the user has `totp_enabled=true`, then a valid `totpCode` is REQUIRED — if absent/invalid, throw 401 with code `TOTP_REQUIRED` (and publish USER_LOGIN_FAILED via the 02-01 LoginEventPublisher). A privileged user with no TOTP enrolled is likewise refused (must enroll).
    ENROLLMENT SEAM (resolves the chicken-and-egg warning): a privileged user cannot reach `POST /2fa/setup` (which needs an authenticated JWT) without first passing step-up, and 02-01 seeds privileged users WITHOUT a `totp_secret`. For Phase 2, first-time privileged-user TOTP enrollment is therefore an out-of-band/provisioning concern (Phase 3 provisioning, or an admin/test fixture that writes the encrypted secret directly). Document this explicitly in the SUMMARY: production first-enrollment is deferred to Phase 3; tests enroll via the fixture described in Task 4. (Optional, low priority: a `POST /api/v1/auth/2fa/step-up` stub returning a short-lived step-up marker for later modules — implement only if time permits; note it in SUMMARY as deferred otherwise.)
  </action>
  <verify>`mvn -pl services/auth-service -am -q -DskipTests compile` succeeds; `UserEntity` has `@Convert(converter = EncryptedStringConverter.class)` on `totp_secret`; `TotpService` references `DefaultCodeVerifier`; `AuthServiceImpl` references `TOTP_REQUIRED` and checks `rbac.manage`/`finance.period.close`; `dev.samstevens.totp` is on the auth-service classpath.</verify>
  <done>TOTP setup/verify/disable works with an AES-256-GCM-encrypted secret, and privileged-permission holders (or totp-enabled users) are refused login without a valid TOTP code. (AUTH-07, AUTH-09; SC3 TOTP + step-up; Open Q2.)</done>
</task>

<task type="auto">
  <name>Task 3: Password reset (emailed token, single-use, last-5 reuse check) + migrations</name>
  <files>services/auth-service/src/main/java/io/restaurantos/auth/entity/PasswordResetTokenEntity.java, services/auth-service/src/main/java/io/restaurantos/auth/entity/PasswordHistoryEntity.java, services/auth-service/src/main/java/io/restaurantos/auth/repository/PasswordResetTokenRepository.java, services/auth-service/src/main/java/io/restaurantos/auth/repository/PasswordHistoryRepository.java, services/auth-service/src/main/java/io/restaurantos/auth/service/PasswordResetService.java, services/auth-service/src/main/java/io/restaurantos/auth/controller/PasswordResetController.java, services/auth-service/src/main/java/io/restaurantos/auth/dto/request/PasswordResetRequest.java, services/auth-service/src/main/java/io/restaurantos/auth/dto/request/PasswordResetConfirmRequest.java, services/auth-service/src/main/resources/db/changelog/v1.0.0/060-create-password-reset-tokens.xml, services/auth-service/src/main/resources/db/changelog/v1.0.0/070-create-password-history.xml</files>
  <action>
    Migrations (Doc 8, FORCE-RLS + RLS changeset immediately after each createTable; BIGINT paisa N/A here; all TIMESTAMPTZ; rollback blocks):
      - `060-create-password-reset-tokens.xml`: `password_reset_tokens(id, tenant_id NOT NULL, user_id→users(id), token_hash VARCHAR NOT NULL, expires_at TIMESTAMPTZ NOT NULL, used_at TIMESTAMPTZ NULL)` + audit cols; index on token_hash; expires 30 min (AUTH-06).
      - `070-create-password-history.xml`: `password_history(id, tenant_id NOT NULL, user_id→users(id), password_hash VARCHAR NOT NULL, created_at TIMESTAMPTZ)` + audit cols (P7.1 — cannot reuse last 5).
    Entities extend `TenantAuditableEntity`; repositories `PasswordResetTokenRepository.findByTokenHash`, `PasswordHistoryRepository.findTop5ByUserIdOrderByCreatedAtDesc`.

    `PasswordResetService`:
      - `request(email, tenantSlug)`: resolve tenant (auth_tenants) → set RLS GUC → find user; if found, create a single-use token (opaque random, store only SHA-256 hash, expires_at=now+30min), set TenantContext to the resolved tenant, and `EventPublisher.publish("auth.topic","auth.user.password_reset_requested","PASSWORD_RESET_REQUESTED", null, {userId,email,token})` — actual email delivery is a Phase-5 notification consumer (this phase only publishes the trigger; ROADMAP cross-cutting decision). To avoid user enumeration, return 200 regardless of whether the email exists.
      - `confirm(token, newPassword)`: hash + look up a non-used, non-expired token; load user under that tenant's RLS; enforce no-reuse vs the last 5 `password_history` hashes (and the current hash) using `BCryptPasswordEncoder(12).matches`; on pass, set the new bcrypt-12 hash, append the old hash to `password_history`, mark the token `used_at`, and (optional) revoke active refresh sessions.

    `PasswordResetController` (`/api/v1/auth/reset-password/**`, permitAll — already in the 02-01 SecurityConfig permit list): `POST /reset-password/request` and `POST /reset-password/confirm`, returning `ApiResponse`/`ApiError`. DTOs are validated `record`s.
  </action>
  <verify>`mvn -pl services/auth-service -am -q -DskipTests compile` succeeds; both migrations have FORCE-RLS + `tenant_isolation` + rollback; grep shows `findTop5ByUserIdOrderByCreatedAtDesc` and a `PASSWORD_RESET_REQUESTED` publish to `auth.topic`; the confirm path uses bcrypt-12 and marks `used_at`.</verify>
  <done>Password reset works end-to-end: request emits a single-use, 30-min, hashed token event; confirm sets a new bcrypt-12 hash, enforces the last-5 no-reuse rule, and consumes the token. (AUTH-06; SC3 reset.)</done>
</task>

<task type="auto">
  <name>Task 4: Branch switch (reissue JWT with new branch context) + IT suite (TOTP, reset, branch switch, step-up)</name>
  <files>services/auth-service/src/main/java/io/restaurantos/auth/service/BranchSwitchService.java, services/auth-service/src/main/java/io/restaurantos/auth/controller/BranchSwitchController.java, services/auth-service/src/main/java/io/restaurantos/auth/dto/request/SwitchBranchRequest.java, services/auth-service/src/test/java/io/restaurantos/auth/integration/TotpFlowIT.java, services/auth-service/src/test/java/io/restaurantos/auth/integration/PasswordResetIT.java, services/auth-service/src/test/java/io/restaurantos/auth/integration/BranchSwitchIT.java, services/auth-service/src/test/java/io/restaurantos/auth/integration/StepUpLoginIT.java</files>
  <action>
    `BranchSwitchService.switchBranch(targetBranchId)` (AUTH-05): the caller is authenticated (TenantContext/SecurityContext populated by the shared JwtAuthenticationFilter wired in 02-01). Verify the user has an ACTIVE `user_branch_roles` row for `targetBranchId` (under RLS); if not → 403. Recompute roles/permissions/attributes for that branch via the 02-01 `PermissionResolver`; reissue a NEW access JWT with the new `branch_id`/`permissions`/`attributes` via `JwtSigningService`; KEEP the existing refresh session (do not rotate the cookie). Branch details are seeded locally in Phase 2; Phase 3's user-service later supplies real branch details via `/internal/users/branches/{branchId}` (note as a seam).
    `BranchSwitchController` (`POST /api/v1/auth/switch-branch`, authenticated): `{branchId}` → returns a new `TokenResponse` (access token + expiry). Returns `ApiResponse`/`ApiError`.

    IT suite (reuse the 02-01 `BaseIntegrationTest` + `TestFixtures`; Doc 10):
      - `TotpFlowIT` (authenticate as the NON-privileged `cashier@demo.local` — it can log in WITHOUT a TOTP code, obtaining the JWT needed to call the authenticated `/2fa/setup`): setup → returns otpauth URI; the stored `users.totp_secret` BYTEA is NOT the plaintext secret (assert the column bytes != raw secret UTF-8, proving AES-GCM at rest); verify with a generated code (use the same `dev.samstevens.totp` time-based code generation in the test) sets `totp_enabled=true`; a wrong code fails (SC3 TOTP, AUTH-09). TEST-ISOLATION NOTE: enabling TOTP on the shared `cashier` would make its later plain-login ITs require a code (the `totp_enabled` step-up branch); since these ITs share one Testcontainers DB with no guaranteed class ordering, this test MUST restore state in `@AfterEach` — clear `totp_secret` + set `totp_enabled=false` via `UserRepository` (under the demo-tenant RLS GUC) so `AuthLoginIT`/`RefreshLogoutIT` stay green regardless of order.
      - `StepUpLoginIT` (secret source is explicit — resolves the chicken-and-egg warning): in an `@BeforeEach`/fixture, ENROLL the seeded privileged user (`owner@demo.local`, holds `rbac.manage`) by generating a base32 secret with `dev.samstevens.totp` (`DefaultSecretGenerator`), encrypting it via the shared `EncryptionService` (now available) and writing it to `users.totp_secret` + `totp_enabled=true` directly through `UserRepository` (under the demo-tenant RLS GUC) — NOT via `/2fa/setup` (which would need a JWT the user can't yet obtain). Then: (1) login with NO `totpCode` → 401 `TOTP_REQUIRED`; (2) login with a code generated from that SAME secret via `dev.samstevens.totp` for the current time step → 200 with a valid access JWT (SC3 step-up, Open Q2). Also assert a non-privileged user (`cashier@demo.local`) still logs in WITHOUT a code (guards SC1 against regression).
      - `PasswordResetIT` (use the NON-privileged `cashier@demo.local` so the post-reset login isn't gated by step-up): request → a PASSWORD_RESET_REQUESTED event/token is produced; confirm with the token sets a new password (login with old fails, new succeeds); the token is single-use (second confirm fails); reusing one of the last-5 passwords is rejected (AUTH-06).
      - `BranchSwitchIT` (use the NON-privileged `cashier@demo.local`, seeded to both branches in 02-01): log in (active branch A) → switch-branch to branch B (an assigned branch) → new JWT carries `branch_id`=B and B's permissions; switching to an unassigned branch → 403; the refresh cookie/session is unchanged (AUTH-05, SC2 branch switch).
  </action>
  <verify>`mvn -pl services/auth-service -am verify` runs the new ITs GREEN under Testcontainers; `TotpFlowIT` asserts the totp_secret column is encrypted at rest; `StepUpLoginIT` asserts 401 TOTP_REQUIRED then success; `PasswordResetIT` proves single-use + last-5; `BranchSwitchIT` proves new branch_id + 403 for unassigned; JaCoCo auth-service ≥ 70%.</verify>
  <done>Branch switch reissues the JWT with the new branch context while keeping the refresh session, and the IT suite proves SC3 (TOTP + encrypted secret + step-up + reset) and the SC2 branch-switch behavior. (AUTH-05, AUTH-06, AUTH-07, AUTH-09.)</done>
</task>

</tasks>

<verification>
- `mvn -pl shared-lib,services/auth-service -am -DskipTests compile` succeeds; `SharedAutoConfiguration.java` unchanged; `EncryptionAutoConfiguration` registered in the imports file.
- `users.totp_secret` uses `@Convert(EncryptedStringConverter)`; a stored secret is AES-256-GCM ciphertext, not plaintext.
- Step-up: privileged-permission holders are refused login without a valid TOTP (401 `TOTP_REQUIRED`).
- Password reset: single-use 30-min token, last-5 reuse rejected, bcrypt-12 new hash.
- Branch switch: new JWT with new branch_id/permissions; 403 for unassigned branch; refresh session preserved.
- `mvn -pl services/auth-service -am verify` GREEN via Testcontainers; JaCoCo ≥ 70%.
</verification>

<success_criteria>
SC3 fully met (TOTP setup/verify with AES-256-GCM `totp_secret`; `rbac.manage`/`finance.period.close` refused without a valid TOTP step; password reset via emailed token) and the SC2 branch-switch behavior in place. Requirements covered: AUTH-05, AUTH-06, AUTH-07, AUTH-09. Open Q2 (step-up = mandatory TOTP at login for privileged holders) and Q4 (EncryptionService home = shared-lib, opt-in auto-config) resolved.
</success_criteria>

<output>
After completion, create `.planning/phases/02-authentication-authorization/02-02-SUMMARY.md` recording the exact step-up predicate, that `EncryptionService` lives in shared-lib via `EncryptionAutoConfiguration`, the password-reset token TTL/policy, and whether the `2fa/step-up` stub was implemented or deferred.
</output>
