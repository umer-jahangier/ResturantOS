---
phase: 02-authentication-authorization
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
gap_closure: false
files_modified:
  - pom.xml
  - shared-lib/src/main/java/io/restaurantos/shared/authz/AuthorizationService.java
  - services/authorization-service/pom.xml
  - services/authorization-service/Dockerfile
  - services/authorization-service/src/main/java/io/restaurantos/authz/AuthorizationServiceApplication.java
  - services/authorization-service/src/main/resources/application.yml
  - services/authorization-service/src/main/java/io/restaurantos/authz/config/SecurityConfig.java
  - services/authorization-service/src/main/java/io/restaurantos/authz/config/OpaConfig.java
  - services/authorization-service/src/main/java/io/restaurantos/authz/config/InternalServiceFilter.java
  - services/authorization-service/src/main/java/io/restaurantos/authz/controller/InternalAuthorizeController.java
  - services/authorization-service/src/main/java/io/restaurantos/authz/service/AuthorizeService.java
  - services/authorization-service/src/main/java/io/restaurantos/authz/dto/request/AuthorizeRequest.java
  - services/authorization-service/src/main/java/io/restaurantos/authz/dto/response/AuthorizeResponse.java
  - policies/restaurantos/common.rego
  - policies/restaurantos/pos.rego
  - policies/restaurantos/finance.rego
  - policies/restaurantos/vendor.rego
  - policies/restaurantos/rbac.rego
  - policies/tests/common_test.rego
  - policies/tests/pos_test.rego
  - policies/tests/finance_test.rego
  - policies/tests/vendor_test.rego
  - policies/tests/rbac_test.rego
  - services/authorization-service/src/test/java/io/restaurantos/authz/integration/BaseIntegrationTest.java
  - services/authorization-service/src/test/java/io/restaurantos/authz/integration/TestJwksConfig.java
  - services/authorization-service/src/test/java/io/restaurantos/authz/integration/TestFixtures.java
  - services/authorization-service/src/test/java/io/restaurantos/authz/integration/AuthorizeIT.java
  - services/authorization-service/src/test/java/io/restaurantos/authz/integration/OpaTimeoutFailClosedIT.java
autonomous: true

must_haves:
  truths:
    - "POST /internal/authorize (JWT + X-Internal-Service secret) returns an OPA decision {allow:boolean} built from a branch-aware OpaInput (SC4, AUTHZ-01/02)"
    - "The decision DENIES cross-tenant access and cross-branch access (resource.tenant_id/branch_id != user's) (SC4, AUTHZ-03)"
    - "On OPA timeout (2s) or any OPA error the result fails CLOSED (deny) — never allow (SC4, AUTHZ-01, BLR-5; Pitfall 4)"
    - "Rego policies exist for common/pos/finance/vendor/rbac with same_tenant, same_branch, has_permission helpers, all Rego v1 (if/contains) (AUTHZ-02/03; Pitfall 5)"
    - "opa test reports 100% coverage across all policies, exercising positive + cross-tenant + cross-branch denials and each helper (SC5, AUTHZ-04)"
  artifacts:
    - path: "policies/restaurantos/common.rego"
      provides: "Rego v1 helpers same_tenant, same_branch, same_tenant_and_branch, has_permission"
      contains: "same_branch"
    - path: "policies/restaurantos/finance.rego"
      provides: "finance policy: branch-isolated expense approve + tenant-wide close_period (same_tenant only)"
      contains: "finance.period.close"
    - path: "services/authorization-service/src/main/java/io/restaurantos/authz/config/OpaConfig.java"
      provides: "OpaClient/RestClient with a 2s connect+read timeout (fail-closed)"
      contains: "2"
    - path: "services/authorization-service/src/main/java/io/restaurantos/authz/controller/InternalAuthorizeController.java"
      provides: "POST /internal/authorize returning {allow} via the shared DefaultOpaClient"
      contains: "/internal/authorize"
    - path: "shared-lib/src/main/java/io/restaurantos/shared/authz/AuthorizationService.java"
      provides: "shared authorize(module, action, resource) helper (Doc 9 §9.4) for reuse by all services"
      contains: "PermissionDeniedException"
  key_links:
    - from: "services/authorization-service/src/main/java/io/restaurantos/authz/service/AuthorizeService.java"
      to: "io.restaurantos.shared.authz.DefaultOpaClient (OPA /v1/data/restaurantos/{module}/allow)"
      via: "builds OpaInput from the validated JWT claims (re-derived, never trusted from body) and 2s-timeout RestClient"
      pattern: "evaluate"
    - from: "policies/restaurantos/pos.rego"
      to: "policies/restaurantos/common.rego"
      via: "import data.restaurantos.common; allow requires common.same_tenant_and_branch(input)"
      pattern: "common.same_tenant_and_branch"
    - from: "services/authorization-service/src/main/java/io/restaurantos/authz/config/InternalServiceFilter.java"
      to: "X-Internal-Service shared secret (INTERNAL_SERVICE_SECRET)"
      via: "constant-time compare; 403 if absent/wrong (Doc 4 §4.1)"
      pattern: "X-Internal-Service"
    - from: "services/authorization-service/src/test/java/io/restaurantos/authz/integration/TestJwksConfig.java"
      to: "io.restaurantos.shared.security.JwksKeyProvider (self-seed constructor)"
      via: "@TestConfiguration @Primary JwksKeyProvider(testKid, testPublicKey) so TestFixtures-minted JWTs validate offline (no auth-service HTTP fetch in ITs)"
      pattern: "JwksKeyProvider"
---

<objective>
Stand up the `authorization-service` module (port 8083, `auth_db`) exposing `POST /internal/authorize`, which builds a branch-aware OPA input from the validated JWT and proxies to OPA fail-closed on a 2s timeout, plus the top-level `policies/` Rego tree (common/pos/finance/vendor/rbac) enforcing tenant AND branch isolation at 100% `opa test` coverage. Build the shared `AuthorizationService` helper in shared-lib for reuse by all later services.

Purpose: Delivers SC4 (`/internal/authorize` denies cross-tenant + cross-branch and fails closed on 2s OPA timeout) and SC5 (`opa test` 100% coverage across common/pos/finance/vendor/rbac with same_tenant/same_branch/has_permission exercised). Covers AUTHZ-01, AUTHZ-02, AUTHZ-03, AUTHZ-04. Runs in parallel with 02-02; both depend only on 02-01 (which freezes the JWT claim/permission shape this OPA input consumes).
Output: the `authorization-service` module (+ parent POM module line), the 2s-timeout OPA config, the `InternalAuthorizeController`, the `policies/restaurantos/*.rego` + `policies/tests/*_test.rego` tree, the shared `AuthorizationService` helper, and tests proving SC4 (incl. a fail-closed timeout test) + the 100% `opa test` gate.

WAVE-2 BUILD NOTE: 02-03 and 02-02 both add DISJOINT new files to shared-lib (this plan: `authz/AuthorizationService`; 02-02: `security/Encryption*` + `config/EncryptionAutoConfiguration`) — no file conflict, and `SharedAutoConfiguration` is untouched by both. If both run in the SAME working tree, sequence the shared-lib `mvn install` (don't build `shared-lib` concurrently with 02-02) to avoid a `target/` race; serial full-reactor builds are sufficient.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-authentication-authorization/02-RESEARCH.md
@.planning/phases/02-authentication-authorization/02-01-PLAN.md
@.planning/phases/02-authentication-authorization/02-01-SUMMARY.md

# Source of truth — transcribe/adapt, do not invent:
@Docs/agent-specs/09-security-implementation-guide.md
@Docs/agent-specs/04-internal-api-contracts.md
@Docs/agent-specs/05-environment-variables.md
@Docs/agent-specs/10-test-architecture-guide.md
@Docs/agent-specs/07-coding-standards.md
@Docs/agent-specs/01-project-scaffold.md

# Shipped shared-lib OPA primitives to REUSE (do NOT re-implement the HTTP/JSON):
@shared-lib/src/main/java/io/restaurantos/shared/authz/OpaClient.java
@shared-lib/src/main/java/io/restaurantos/shared/authz/DefaultOpaClient.java
@shared-lib/src/main/java/io/restaurantos/shared/authz/OpaInput.java
@shared-lib/src/main/java/io/restaurantos/shared/authz/OpaDecision.java
@shared-lib/src/main/java/io/restaurantos/shared/security/JwtClaims.java
@shared-lib/src/main/java/io/restaurantos/shared/exception/PermissionDeniedException.java
@shared-lib/src/main/java/io/restaurantos/shared/security/JwtAuthenticationFilter.java
@deploy/docker-compose.yml
@policies/restaurantos/common.rego
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rego policy tree (common/pos/finance/vendor/rbac), Rego v1, tenant+branch isolation</name>
  <files>policies/restaurantos/common.rego, policies/restaurantos/pos.rego, policies/restaurantos/finance.rego, policies/restaurantos/vendor.rego, policies/restaurantos/rbac.rego</files>
  <action>
    NOTE: `policies/restaurantos/common.rego` already exists as a Phase-1 placeholder ([01-01]/01-02). REPLACE/extend it with the full helper set; the OPA container already mounts `../policies:/policies:ro` (docker-compose, OPA 1.17.1). All policies MUST be Rego v1 (OPA 1.x default): every rule body uses `if`, multi-value/deny rules use `contains` (Doc 9 header note; Pitfall 5).

    `common.rego` (`package restaurantos.common`) — transcribe Doc 9 §9.4 and ADD the missing `same_branch` helper (SC5 requires `same_tenant`, `same_branch`, `has_permission` all present and exercised):
      - `same_tenant(input) if { input.resource.tenant_id == input.user.tenant_id }`
      - `same_branch(input) if { input.resource.branch_id == input.user.branch_id }`
      - `same_tenant_and_branch(input) if { same_tenant(input); same_branch(input) }`
      - `has_permission(input, perm) if { some p in input.user.permissions; p == perm }`

    `pos.rego` (`package restaurantos.pos`, `import data.restaurantos.common`, `default allow := false`) — transcribe Doc 9 §9.4: void.own (created_by == user.id, status OPEN, `common.same_tenant_and_branch`) and void.any (`common.same_tenant_and_branch`).

    `finance.rego` (`package restaurantos.finance`) — transcribe Doc 9 §9.4 (the MAJOR-11 branch-isolation fix): expense `approve` requires `finance.expense.approve` + `common.same_tenant_and_branch` + `amount_paisa <= user.attributes.approval_limit_paisa`; `close_period` requires `finance.period.close` + `common.same_tenant` ONLY (the explicit tenant-wide exception). This is the one place branch isolation is intentionally relaxed — cover both branches in tests.

    `vendor.rego` (`package restaurantos.vendor`, branch-isolated): `default allow := false`; allow rules for vendor actions (e.g. `vendor.manage` / PO approve) gated by `common.has_permission` + `common.same_tenant_and_branch`. Keep rules minimal but each must be test-covered.

    `rbac.rego` (`package restaurantos.rbac`, branch-isolated): `default allow := false`; allow gated by `common.has_permission(input,"rbac.manage")` + `common.same_tenant_and_branch` (rbac.manage is the step-up-protected permission from SC3).

    Keep resource/user field names consistent with the Java `OpaInput` (snake_case in JSON: `tenant_id`, `branch_id`, `created_by`, `amount_paisa`, `user.permissions`, `user.attributes.approval_limit_paisa`).
  </action>
  <verify>`opa check policies/` passes (valid Rego v1); `grep -l "same_branch" policies/restaurantos/common.rego` matches; every module policy except `finance.close_period` references `same_tenant_and_branch`; `finance.rego` uses `same_tenant` only for `close_period`.</verify>
  <done>Rego v1 policies for common/pos/finance/vendor/rbac exist with same_tenant/same_branch/same_tenant_and_branch/has_permission, enforcing tenant AND branch isolation (tenant-wide close_period excepted). (AUTHZ-02, AUTHZ-03; Pitfall 5.)</done>
</task>

<task type="auto">
  <name>Task 2: Rego test suite at 100% coverage (positive + cross-tenant + cross-branch denials, each helper)</name>
  <files>policies/tests/common_test.rego, policies/tests/pos_test.rego, policies/tests/finance_test.rego, policies/tests/vendor_test.rego, policies/tests/rbac_test.rego</files>
  <action>
    Write Rego v1 tests (`policies/tests/*_test.rego`) achieving 100% coverage (SC5, AUTHZ-04). For each policy:
      - a POSITIVE allow case (correct permission, same tenant + same branch),
      - a CROSS-TENANT denial (resource.tenant_id != user.tenant_id) → allow == false,
      - a CROSS-BRANCH denial (same tenant, resource.branch_id != user.branch_id) → allow == false,
      - a MISSING-PERMISSION denial.
    `common_test.rego` must exercise each helper directly: `same_tenant` true/false, `same_branch` true/false, `same_tenant_and_branch` true/false, `has_permission` true/false.
    `finance_test.rego` must cover BOTH `approve` (branch-isolated; plus an over-limit denial where `amount_paisa > approval_limit_paisa`) AND `close_period` (tenant-wide: same tenant different branch still ALLOWED with `finance.period.close`; different tenant DENIED). This proves the deliberate branch-exception is covered.
    Ensure every `allow`/helper line is hit so `opa test policies/ --coverage --format=json | jq '.coverage'` equals 100.
  </action>
  <verify>`opa test policies/ -v` is GREEN; `opa test policies/ --coverage --format=json | jq '.coverage'` returns `100`; cross-tenant and cross-branch cases assert `allow == false`; the finance close_period cross-branch-allowed case passes.</verify>
  <done>`opa test` passes at 100% coverage across common/pos/finance/vendor/rbac, exercising same_tenant/same_branch/has_permission and positive + cross-tenant + cross-branch denials. (SC5, AUTHZ-04.)</done>
</task>

<task type="auto">
  <name>Task 3: authorization-service module + shared AuthorizationService helper + 2s fail-closed OPA config + /internal/authorize</name>
  <files>pom.xml, shared-lib/src/main/java/io/restaurantos/shared/authz/AuthorizationService.java, services/authorization-service/pom.xml, services/authorization-service/Dockerfile, services/authorization-service/src/main/java/io/restaurantos/authz/AuthorizationServiceApplication.java, services/authorization-service/src/main/resources/application.yml, services/authorization-service/src/main/java/io/restaurantos/authz/config/SecurityConfig.java, services/authorization-service/src/main/java/io/restaurantos/authz/config/OpaConfig.java, services/authorization-service/src/main/java/io/restaurantos/authz/config/InternalServiceFilter.java, services/authorization-service/src/main/java/io/restaurantos/authz/controller/InternalAuthorizeController.java, services/authorization-service/src/main/java/io/restaurantos/authz/service/AuthorizeService.java, services/authorization-service/src/main/java/io/restaurantos/authz/dto/request/AuthorizeRequest.java, services/authorization-service/src/main/java/io/restaurantos/authz/dto/response/AuthorizeResponse.java</files>
  <action>
    Add `<module>services/authorization-service</module>` to the parent `pom.xml` `<modules>` (after 02-01 added auth-service; both edit pom.xml but in different waves so no conflict — RESEARCH Pitfall 7).

    Shared `AuthorizationService` (Open Q4 — shared-lib, additive): create `io.restaurantos.shared.authz.AuthorizationService` transcribing Doc 9 §9.4 — `authorize(String module, String action, OpaInput.Resource resource)` reads `JwtClaims` from `SecurityContextHolder`, builds `OpaInput` (user from claims incl. `attributes`), calls `opaClient.evaluate(module, input)`, and throws `PermissionDeniedException` when `!allow()`. This is the helper EVERY later service (POS, Finance, Purchasing) calls. Do NOT annotate it `@Service` (shared-lib is not component-scanned); each consuming service exposes it as a bean (the authorization-service does so in its own config). DO NOT edit `SharedAutoConfiguration.java` (keeps this plan parallel-safe with 02-02; if a deviation requires it, run `gitnexus_impact({target:"SharedAutoConfiguration"})` first per CLAUDE.md).

    Create `services/authorization-service/pom.xml`: parent `restaurantos-parent`, base package `io.restaurantos.authz`. Versionless deps: `io.restaurantos:shared-lib`, `spring-boot-starter-web`, `spring-boot-starter-security`, `spring-cloud-starter-netflix-eureka-client`, jjwt (for the shared JwtAuthenticationFilter), nimbus-jose-jwt; test deps spring-boot-starter-test + testcontainers (postgres/rabbitmq optional — authz mainly needs OPA; a Postgres container is fine for the shared auto-config to start, or exclude DB auto-config if no DB is used — decide and document). DB_NAME `auth_db` per Doc 5. JaCoCo ≥ 70% (Doc 10 §10.7). `Dockerfile` mirrors the 01-01 multi-stage pattern, EXPOSE 8083.

    `AuthorizationServiceApplication` + `application.yml`: `server.port: ${SERVER_PORT:8083}`, `restaurantos.opa.url: ${OPA_URL}` (so the shared OPA bean activates), `restaurantos.jwt.jwks-url: ${JWT_JWKS_URL}` (points at auth-service's JWKS), Eureka, `INTERNAL_SERVICE_SECRET` bound, actuator health/prometheus.

    `OpaConfig` (Pitfall 4 — the shared `DefaultOpaClient`'s RestClient has NO explicit timeout): build an `OpaClient` bean as `new DefaultOpaClient(restClient)` where `restClient` uses a `ClientHttpRequestFactory` (e.g. `JdkClientHttpRequestFactory`/`SimpleClientHttpRequestFactory` or Reactor/HttpComponents) with connect timeout = 2s AND read timeout = 2s, baseUrl = `OPA_URL`. Mark `@Primary` (or otherwise override the shared `@ConditionalOnProperty` OPA bean) so the 2s-timeout client is the one used. A hung OPA then surfaces as an exception → `DefaultOpaClient` already catches all exceptions and throws `PermissionDeniedException` (deny) — fail-closed (BLR-5).

    `SecurityConfig`: same Security-7 lambda DSL as Doc 9 §9.1, wiring the shipped shared-lib `JwtAuthenticationFilter` (build the bean + a `JwksKeyProvider(jwksUrl, restClient)` pointing at auth-service's JWKS — NOT self-seeded in production). Expose the `JwksKeyProvider` as a SEPARATE `@Bean` (its own factory method) so the IT can override it with a self-seeded test provider via `@TestConfiguration`/`@MockBean` without standing up auth-service — do NOT inline `new JwksKeyProvider(...)` inside the filter bean. permitAll only health/prometheus; `/internal/**` requires an authenticated JWT.

    `InternalServiceFilter` (Doc 4 §4.1): a filter/interceptor that constant-time compares the inbound `X-Internal-Service` header to `INTERNAL_SERVICE_SECRET`; 403 if absent/wrong. Applies to `/internal/**`.

    `InternalAuthorizeController` (`POST /internal/authorize`): request `{module, action, resource:{type,id,tenant_id,branch_id,created_by,status,amount_paisa}}` (DTO `AuthorizeRequest`); RE-DERIVE the user from the validated JWT (`JwtClaims` in SecurityContext) — NEVER trust a user/tenant id from the body (anti-pattern in Doc 4/RESEARCH). `AuthorizeService` builds `OpaInput` (user from claims, resource from the request) and calls `opaClient.evaluate(module, input)`; return `AuthorizeResponse{allow}`. Any OPA error/timeout → deny (the shared client throws PermissionDeniedException → map to `{allow:false}` or 403 via GlobalExceptionHandler; decide and keep it fail-closed). DTOs are validated `record`s; responses via `ApiResponse`/`ApiError`.
  </action>
  <verify>`mvn -pl services/authorization-service -am -q -DskipTests compile` succeeds; parent `<modules>` includes `services/authorization-service`; `OpaConfig` sets a 2s connect+read timeout; `InternalServiceFilter` references `X-Internal-Service`; `AuthorizeService` builds `OpaInput` from JWT claims (not the body); shared `AuthorizationService` exists and is NOT `@Service`-annotated; `SharedAutoConfiguration.java` unchanged.</verify>
  <done>authorization-service exposes `POST /internal/authorize` (JWT + X-Internal-Service) returning an OPA `{allow}` decision built from the validated JWT via a 2s-timeout fail-closed OPA client; the shared `AuthorizationService` helper ships in shared-lib for reuse. (AUTHZ-01, AUTHZ-02; SC4 endpoint; Open Q4.)</done>
</task>

<task type="auto">
  <name>Task 4: Integration tests — authorize allow/deny (cross-tenant, cross-branch) + 2s fail-closed timeout + opa-test CI gate</name>
  <files>services/authorization-service/src/test/java/io/restaurantos/authz/integration/BaseIntegrationTest.java, services/authorization-service/src/test/java/io/restaurantos/authz/integration/TestJwksConfig.java, services/authorization-service/src/test/java/io/restaurantos/authz/integration/TestFixtures.java, services/authorization-service/src/test/java/io/restaurantos/authz/integration/AuthorizeIT.java, services/authorization-service/src/test/java/io/restaurantos/authz/integration/OpaTimeoutFailClosedIT.java</files>
  <action>
    `TestFixtures`: holds a generated RS256 test keypair + a fixed `kid` (e.g. `test-key-1`); `mintJwt(sub, tenantId, branchId, roles, permissions, attributes)` builds an RS256-signed access JWT with that key (mirror the auth-service `TestFixtures` claim shape from 02-01); also exposes the matching `RSAPublicKey`/`kid`.
    `TestJwksConfig` (`@TestConfiguration` — resolves the JWKS-wiring warning; auth-service is NOT running in this IT): declare a `@Bean @Primary JwksKeyProvider` constructed with the SELF-SEED constructor `new JwksKeyProvider(TestFixtures.kid, TestFixtures.publicKey)` (the same self-seed constructor 02-01 uses), so the shared `JwtAuthenticationFilter` validates `TestFixtures`-minted JWTs purely in-process with NO HTTP fetch to auth-service. This overrides the production HTTP-backed `JwksKeyProvider` bean from `SecurityConfig`. Import it on the ITs via `@Import(TestJwksConfig.class)` (or `@ContextConfiguration`).
    `BaseIntegrationTest` (Doc 10 §10.1): `@SpringBootTest`(RANDOM_PORT) + `@Testcontainers` running an OPA container (`openpolicyagent/opa:1.17.1`) with the `policies/` directory mounted/loaded so the service evaluates the REAL Rego; `@Import(TestJwksConfig.class)` so minted JWTs verify offline; set `OPA_URL` to the container via `@DynamicPropertySource`. Provide an `X-Internal-Service` test secret (set `INTERNAL_SERVICE_SECRET` via `@DynamicPropertySource`). (Use the demo tenant/branch UUIDs recorded in 02-01-SUMMARY for realistic resources.)
    `AuthorizeIT`:
      - allow: a JWT with `pos.order.void.any` + matching tenant/branch → `POST /internal/authorize` for module=pos action=void returns `{allow:true}`.
      - cross-tenant deny: resource.tenant_id != JWT tenant_id → `{allow:false}` (AUTHZ-03, SC4).
      - cross-branch deny: same tenant, resource.branch_id != JWT branch_id → `{allow:false}` (AUTHZ-03, SC4).
      - finance close_period: same tenant, different branch, with `finance.period.close` → `{allow:true}` (tenant-wide exception); different tenant → `{allow:false}`.
      - missing `X-Internal-Service` → 403 (Doc 4 §4.1).
    `OpaTimeoutFailClosedIT` (SC4 / Pitfall 4 / BLR-5): point the OPA client at an endpoint that STALLS beyond 2s (e.g. a toxiproxy/slow stub or a non-responsive port), call `/internal/authorize`, and assert the result is DENY (never allow) within ~2s — proving fail-closed on timeout.
    Wire the `opa test policies/ --coverage` 100% gate into the build (Doc 10 §10.7; the CI pipeline is Phase 4, but expose a Make/Maven exec or document the exact command in the SUMMARY so Phase 4 wires it): `opa test policies/ --coverage --format=json | jq '.coverage'` must equal 100.
  </action>
  <verify>`mvn -pl services/authorization-service -am verify` GREEN under Testcontainers (OPA 1.17.1) with NO auth-service running — `TestJwksConfig` self-seeds the `JwksKeyProvider(kid, testPublicKey)` so `TestFixtures`-minted RS256 JWTs validate offline (no HTTP JWKS fetch); `AuthorizeIT` proves allow + cross-tenant deny + cross-branch deny + close_period exception + 403-without-secret; `OpaTimeoutFailClosedIT` proves deny-on-2s-timeout; `opa test policies/ --coverage` reports 100; JaCoCo authorization-service ≥ 70%.</verify>
  <done>ITs prove `/internal/authorize` denies cross-tenant + cross-branch and fails closed on a 2s OPA timeout, and `opa test` reports 100% coverage. (SC4, SC5; AUTHZ-01/02/03/04.)</done>
</task>

</tasks>

<verification>
- `mvn -pl services/authorization-service -am -DskipTests compile` succeeds; parent `<modules>` includes `services/authorization-service`; `SharedAutoConfiguration.java` unchanged.
- `opa check policies/` valid Rego v1; `opa test policies/ --coverage --format=json | jq '.coverage'` == 100.
- `/internal/authorize` builds OpaInput from the validated JWT (re-derived, not body), enforced by a 2s-timeout fail-closed OPA client; missing `X-Internal-Service` → 403.
- `mvn -pl services/authorization-service -am verify` GREEN proving allow + cross-tenant deny + cross-branch deny + close_period tenant-wide exception + 2s fail-closed; JaCoCo ≥ 70%.
- The shared `io.restaurantos.shared.authz.AuthorizationService` helper exists for reuse.
</verification>

<success_criteria>
SC4 fully met (`/internal/authorize` denies cross-tenant + cross-branch and fails closed on 2s OPA timeout) and SC5 fully met (`opa test` 100% coverage across common/pos/finance/vendor/rbac exercising same_tenant/same_branch/has_permission). Requirements covered: AUTHZ-01, AUTHZ-02, AUTHZ-03, AUTHZ-04. Open Q4 (AuthorizationService home = shared-lib) resolved; Pitfalls 4 (2s timeout) and 5 (Rego v1 + 100% coverage) handled.
</success_criteria>

<output>
After completion, create `.planning/phases/02-authentication-authorization/02-03-SUMMARY.md` recording the exact `opa test --coverage` command for Phase-4 CI, the OPA client timeout config, and the resource field contract used by `/internal/authorize`.
</output>
