---
phase: 08.1-pos-inventory-depletion-activation
plan: 04
type: execute
wave: 4
depends_on: ["08.1-02", "08.1-03"]
files_modified:
  - frontend/lib/api-client/schemas/inventory.schema.ts
  - frontend/lib/adapters/inventory.adapter.ts
  - frontend/lib/repositories/inventory.repository.ts
  - frontend/lib/hooks/inventory/use-inventory.ts
  - frontend/mocks/inventory.handlers.ts
  - frontend/mocks/handlers.ts
  - frontend/__tests__/lib/inventory-recipe-builder.test.ts
  - frontend/components/shared/sidebar-nav-items.ts
  - frontend/app/(tenant)/app/inventory/layout.tsx
  - frontend/app/(tenant)/app/inventory/page.tsx
  - frontend/app/(tenant)/app/inventory/recipes/page.tsx
  - frontend/components/inventory/RecipeFormDialog.tsx
  - frontend/app/(tenant)/app/inventory/coverage/page.tsx
  - frontend/e2e/inventory-recipe-builder.spec.ts
autonomous: true
requirements: [INV-10]
user_setup: []

must_haves:
  truths:
    - "/app/inventory is a real, live page (the sidebar-nav-items.ts comingSoon flag is removed) gated by inventory.item.view + FEATURE_INVENTORY, not a 404 placeholder (D-04)."
    - "An operator can author a versioned recipe by picking a real synced menu item from GET /api/v1/inventory/menu-items and adding ingredient lines (ingredient + qty + UOM + yieldPct) plus effectiveFrom, submitted through the four-layer stack to POST /api/v1/inventory/recipes (INV-10)."
    - "The coverage dashboard at /app/inventory/coverage renders GET /api/v1/inventory/recipes/coverage's totals and the list of menu items still missing a recipe."
  artifacts:
    - frontend/lib/api-client/schemas/inventory.schema.ts
    - frontend/lib/repositories/inventory.repository.ts
    - frontend/lib/hooks/inventory/use-inventory.ts
    - frontend/app/(tenant)/app/inventory/layout.tsx
    - frontend/app/(tenant)/app/inventory/recipes/page.tsx
    - frontend/app/(tenant)/app/inventory/coverage/page.tsx
    - frontend/e2e/inventory-recipe-builder.spec.ts
  key_links:
    - "InventoryRepository.createRecipe Zod-parses createRecipeInputSchema before POST and apiRecipeSchema.parse(raw) on the response, per the enforced four-layer boundary (FE-08)."
    - "useCreateRecipe's onSuccess invalidates BOTH the ['inventory','coverage'] and ['inventory','recipes', menuItemId] query keys so the coverage dashboard reflects a newly-authored recipe immediately."
    - "sidebar-nav-items.ts's Inventory NavItem in navGroups (Menu group) drops `comingSoon: true` so the sidebar link is live and routes to a real page."
  prohibitions:
    - "No component under app/(tenant)/app/inventory/** or components/inventory/** imports lib/api-client or lib/repositories directly — only lib/hooks/inventory/use-inventory.ts is imported by components (ESLint no-restricted-imports boundary, FE-08)."
    - "This is a vertical slice, not the full inventory module — no ingredients/stock/receipts/transfers/counts UI is built here (D-04); those remain out of scope."
---

<objective>
Flip `/app/inventory` from a `comingSoon` placeholder into a real, minimal recipe-builder + coverage dashboard (D-04): an operator picks a real menu item from the newly-synced catalog (08.1-02), adds ingredient lines, and submits a versioned recipe; a coverage dashboard (08.1-03) shows which active menu items still lack one.

Purpose: Without a UI, the catalog sync (08.1-01/02) and validation (08.1-02) and coverage report (08.1-03) are unreachable by an actual operator — this closes INV-10 and makes the whole activated loop usable end-to-end, not just API-reachable.
Output: The inventory four-layer data slice (schema/adapter/repository/hooks), the recipe-builder page + form, the coverage dashboard page, and a Playwright smoke spec.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.1-pos-inventory-depletion-activation/08.1-CONTEXT.md
@.planning/phases/08.1-pos-inventory-depletion-activation/08.1-RESEARCH.md
@frontend/lib/api-client/schemas/purchasing.schema.ts
@frontend/lib/adapters/purchasing.adapter.ts
@frontend/lib/repositories/purchasing.repository.ts
@frontend/lib/hooks/purchasing/use-purchasing.ts
@frontend/components/purchasing/PurchaseOrderFormDialog.tsx
@frontend/app/(tenant)/app/purchasing/layout.tsx
@frontend/app/(tenant)/app/purchasing/page.tsx
@frontend/components/shared/sidebar-nav-items.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Inventory four-layer data slice (schema -> adapter -> repository -> hooks) + MSW handlers</name>
  <files>frontend/lib/api-client/schemas/inventory.schema.ts, frontend/lib/adapters/inventory.adapter.ts, frontend/lib/repositories/inventory.repository.ts, frontend/lib/hooks/inventory/use-inventory.ts, frontend/mocks/inventory.handlers.ts, frontend/mocks/handlers.ts, frontend/__tests__/lib/inventory-recipe-builder.test.ts</files>
  <read_first>
    - frontend/lib/api-client/schemas/purchasing.schema.ts (the `qtyField` union-string/number-transform-to-string pattern for backend BigDecimal fields — apply the same to Recipe qty/yieldServings/yieldPct/reorderPoint)
    - frontend/lib/adapters/purchasing.adapter.ts + frontend/lib/repositories/purchasing.repository.ts + frontend/lib/hooks/purchasing/use-purchasing.ts (the full four-layer chain to mirror exactly — identity adapters, get/post from lib/api-client/request, useQuery/useMutation with query-key invalidation)
    - frontend/mocks/purchasing.handlers.ts + frontend/mocks/handlers.ts (MSW handler registration pattern — well-formed UUID fixture IDs, exported `handlers` array this file must be added to)
    - services/inventory-service/src/main/java/io/restaurantos/inventory/dto/InventoryDtos.java (IngredientDto: id/name/sku/baseUomCode/category/reorderPoint/active — for apiIngredientSchema) and RecipeDtos.java post-08.1-03 (RecipeDto/RecipeLineDto/CoverageResponse/MissingMenuItemDto exact field names) and MenuItemCatalogDtos.java from 08.1-02 (MenuItemCatalogDto: menuItemId/name/categoryName/active/basePricePaisa)
    - services/inventory-service/src/main/java/io/restaurantos/inventory/web/UnitOfMeasureController.java (GET /api/v1/inventory/uom -&gt; UomDto: id/code/name/baseUnitCode/toBaseFactor)
    - frontend/__tests__/lib/purchasing-vendors.test.ts (vitest + MSW round-trip test style to mirror)
  </read_first>
  <behavior>
    - Test: creating a recipe via InventoryRepository.createRecipe round-trips through MSW and returns a Recipe with the submitted menuItemId, version 1, and the submitted lines.
    - Test: listMenuItems/listIngredients/listUoms each return the MSW-seeded fixture arrays, Zod-parsed without throwing.
    - Test: getCoverage returns the MSW-seeded totals/covered/missing shape.
    - Test: submitting createRecipe with an empty lines array is rejected client-side by createRecipeInputSchema before any network call (schema-level guard, mirrors purchasing's "rejects a vendor with no name before it reaches the network" test).
  </behavior>
  <action>
    In inventory.schema.ts define: apiMenuItemCatalogSchema (menuItemId uuid, name, categoryName nullable/optional, active boolean, basePricePaisa int); apiIngredientSchema (id uuid, name, sku nullable/optional, baseUomCode, category nullable/optional, reorderPoint via the qty-union-to-string transform, active boolean); apiUomSchema (id uuid, code, name, baseUnitCode nullable/optional, toBaseFactor via the same transform); recipeLineInputSchema (ingredientId uuid, qty string min-length-1, uomCode string min-length-1, yieldPct number optional); createRecipeInputSchema (menuItemId uuid, yieldServings string min-length-1, effectiveFrom string nullable/optional, name string optional, lines array of recipeLineInputSchema min 1 "Add at least one ingredient line"); apiRecipeLineSchema (id uuid, ingredientId uuid, qty via the transform, uomCode, yieldPct via the transform nullable/optional); apiRecipeSchema (id uuid, menuItemId uuid, version int, current boolean, effectiveFrom string, yieldServings via the transform, name nullable/optional, lines array of apiRecipeLineSchema); apiMissingMenuItemSchema (menuItemId uuid, name); apiCoverageSchema (totalActiveMenuItems int, covered int, missing array of apiMissingMenuItemSchema).

    In inventory.adapter.ts, export the inferred types (MenuItemCatalogEntry, Ingredient, Uom, Recipe, RecipeInput, Coverage) and identity adapter functions (adaptMenuItemCatalogEntry, adaptIngredient, adaptUom, adaptRecipe, adaptCoverage) exactly mirroring purchasing.adapter.ts's pass-through style.

    In inventory.repository.ts, export InventoryRepository with: listMenuItems() -&gt; GET /api/v1/inventory/menu-items, map+parse+adapt each element; listIngredients() -&gt; GET /api/v1/inventory/ingredients; listUoms() -&gt; GET /api/v1/inventory/uom; createRecipe(input) -&gt; POST /api/v1/inventory/recipes with createRecipeInputSchema.parse(input), parse+adapt the response with apiRecipeSchema; listRecipeVersions(menuItemId) -&gt; GET /api/v1/inventory/recipes with { menuItemId } param, map+parse+adapt; getCoverage() -&gt; GET /api/v1/inventory/recipes/coverage, parse+adapt with apiCoverageSchema.

    In use-inventory.ts (new directory lib/hooks/inventory/), export: useMenuItemCatalog() (queryKey ["inventory","menu-items"]); useIngredients() (queryKey ["inventory","ingredients"]); useUoms() (queryKey ["inventory","uoms"]); useCreateRecipe() (useMutation&lt;Recipe, ApiError, RecipeInput&gt;, onSuccess invalidates both ["inventory","coverage"] and ["inventory","recipes"] — the recipes list key broadly, not just one menuItemId, since the picker may re-query any item); useRecipeVersions(menuItemId) (queryKey ["inventory","recipes",menuItemId], enabled on truthy menuItemId); useCoverage() (queryKey ["inventory","coverage"]). Import ApiError type-only from lib/api-client/errors, mirroring use-purchasing.ts's exact comment/pattern justifying that import from a hooks file.

    Create mocks/inventory.handlers.ts with well-formed UUID fixtures (mirrors purchasing.handlers.ts's TENANT/BRANCH/id constants) seeding 2-3 menu-item-catalog entries, 2-3 ingredients, 1-2 UOMs, and in-memory mutable arrays for created recipes so createRecipe/listRecipeVersions/getCoverage respond consistently across calls within a test (getCoverage recomputes covered/missing from the in-memory recipes array against the seeded catalog). Register the new handlers into mocks/handlers.ts's exported `handlers` array (import and spread, matching how purchasing/finance handlers are already combined there).

    Create __tests__/lib/inventory-recipe-builder.test.ts covering the four behaviors above, mirroring purchasing-vendors.test.ts's structure (describe block, MSW-backed direct repository calls, no component rendering).
  </action>
  <verify>
    <automated>npm --prefix frontend run test -- inventory-recipe-builder</automated>
  </verify>
  <done>The inventory four-layer data slice compiles, is Zod-validated end-to-end, and round-trips correctly against MSW fixtures.</done>
</task>

<task type="auto">
  <name>Task 2: Recipe-builder page + form (menu-item picker, dynamic ingredient lines) + sidebar activation</name>
  <files>frontend/components/shared/sidebar-nav-items.ts, frontend/app/(tenant)/app/inventory/layout.tsx, frontend/app/(tenant)/app/inventory/page.tsx, frontend/app/(tenant)/app/inventory/recipes/page.tsx, frontend/components/inventory/RecipeFormDialog.tsx</files>
  <read_first>
    - frontend/components/shared/sidebar-nav-items.ts lines 146-158 (the Inventory NavItem inside navGroups' "Menu" group — remove ONLY the `comingSoon: true` line and its comment, keep permission/feature/href unchanged)
    - frontend/app/(tenant)/app/purchasing/layout.tsx (PermissionGuard + FeatureGuard wrapping pattern, tabs nav component) and page.tsx (static Server Component landing linking to sub-routes) — mirror both exactly, swapping vendor.view/FEATURE_VENDOR for inventory.item.view/FEATURE_INVENTORY and the tab list for Recipes/Coverage
    - frontend/components/purchasing/PurchaseOrderFormDialog.tsx (the FULL react-hook-form + useFieldArray + createZodResolver + Dialog pattern this component mirrors: local form schema with string-typed numeric inputs, a `lines` field array with add/remove row controls, toast on success/error)
    - frontend/lib/hooks/inventory/use-inventory.ts (useMenuItemCatalog/useIngredients/useUoms/useCreateRecipe from Task 1)
  </read_first>
  <action>
    In sidebar-nav-items.ts, remove the `comingSoon: true` line (and its trailing comment) from the Inventory entry inside navGroups' "Menu" group — leave permission/feature/href/icon untouched so the link becomes live with no other behavior change.

    Create app/(tenant)/app/inventory/layout.tsx: `"use client"` component wrapping children in `PermissionGuard require="inventory.item.view" fallback={&lt;AccessDenied /&gt;}` then `FeatureGuard feature="FEATURE_INVENTORY" failOpenOnError fallback={&lt;AccessDenied /&gt;}`, with a simple tabs nav (Recipes -&gt; /app/inventory/recipes, Coverage -&gt; /app/inventory/coverage) built the same way as PurchasingLayout's PurchasingTabs (active-state via usePathname().startsWith).

    Create app/(tenant)/app/inventory/page.tsx: a static Server Component landing page (mirrors purchasing's page.tsx) with two Card links — "Recipe Builder" (-&gt; /app/inventory/recipes, "Author versioned recipes for your synced menu items.") and "Coverage" (-&gt; /app/inventory/coverage, "See which active menu items still need a recipe.").

    Create components/inventory/RecipeFormDialog.tsx: a client component taking a `trigger: React.ReactNode` prop (mirrors PurchaseOrderFormDialog's prop shape exactly). Local zod form schema: menuItemId (string, required), yieldServings (string, required), effectiveFrom (string, optional — HTML date input value), name (string, optional), lines (array of { ingredientId: string required, qty: string required-positive, uomCode: string required, yieldPct: string optional } with a min-1-line rule). Use useForm+createZodResolver+useFieldArray exactly like PurchaseOrderFormDialog. The menu-item field renders a `&lt;select&gt;` populated from useMenuItemCatalog() (value=menuItemId, label=name); each line's ingredient field renders a `&lt;select&gt;` populated from useIngredients() (value=id, label=name); each line's UOM field renders a `&lt;select&gt;` populated from useUoms() (value=code, label=code); qty/yieldPct are text Inputs (inputMode="decimal"); effectiveFrom is `&lt;Input type="date"&gt;`. On submit, build the RecipeInput: menuItemId, yieldServings (as-is string), name (trim to undefined if blank), effectiveFrom (if provided, convert the date string to a full ISO instant via `new Date(value + "T00:00:00Z").toISOString()`, else omit), lines mapped 1:1 with qty/uomCode trimmed and yieldPct parsed to a number when non-blank else omitted. Call useCreateRecipe().mutate with toast.success("Recipe version created") on success (closing the dialog) and toast.error(error.message) on failure, mirroring PurchaseOrderFormDialog's onSuccess/onError shape exactly.

    Create app/(tenant)/app/inventory/recipes/page.tsx: `"use client"` page rendering a menu-item select (from useMenuItemCatalog, shared local state) that, once a menu item is picked, shows its existing recipe versions via useRecipeVersions(selectedMenuItemId) in a simple list/table (version, current, effectiveFrom, yieldServings, line count), plus a `RecipeFormDialog` trigger button labelled "New recipe version" wrapped in `&lt;PermissionGuard require="inventory.item.manage"&gt;` (no fallback — renders nothing for a view-only user, matching this repo's existing nested-guard precedent).
  </action>
  <verify>
    <automated>npm --prefix frontend run typecheck</automated>
  </verify>
  <done>/app/inventory is live in the sidebar; the recipe-builder page lets an operator pick a real menu item, see its existing versions, and author a new one via a dynamic ingredient-line form.</done>
</task>

<task type="auto">
  <name>Task 3: Coverage dashboard + Playwright smoke</name>
  <files>frontend/app/(tenant)/app/inventory/coverage/page.tsx, frontend/e2e/inventory-recipe-builder.spec.ts</files>
  <read_first>
    - frontend/lib/hooks/inventory/use-inventory.ts (useCoverage from Task 1)
    - frontend/components/ui/card.tsx, frontend/components/ui/data-table.tsx or an existing simple list-rendering page for the stat-tile + table layout convention
    - frontend/e2e/pos-modal-revamp.spec.ts or frontend/e2e/kds-stations.spec.ts (Playwright spec structure — login helper, page navigation, assertions against real rendered text) and frontend/playwright.config.ts (base URL / project config this new spec runs under, no config change expected)
  </read_first>
  <action>
    Create app/(tenant)/app/inventory/coverage/page.tsx: `"use client"` page calling useCoverage() and rendering three stat tiles (Total Active Menu Items, Covered, Missing — using the existing Card/CardHeader/CardTitle primitives or AnimatedNumber if trivially available) followed by a simple table/list of the `missing` array (menuItemId, name) with an EmptyState ("Every active menu item has a recipe") when missing is empty.

    Create e2e/inventory-recipe-builder.spec.ts: a Playwright smoke test that logs in as a permissioned demo user (mirrors kds-stations.spec.ts or pos-modal-revamp.spec.ts's login helper), navigates to /app/inventory, asserts the sidebar Inventory link is no longer a coming-soon/404 route, navigates into /app/inventory/recipes, asserts the menu-item select renders at least one option (from the real or MSW-backed catalog depending on this repo's e2e run mode), and navigates into /app/inventory/coverage asserting the three stat tiles render numeric values. Keep this a SMOKE test (page loads + key elements render) rather than a full recipe-creation click-through, consistent with this plan's "Playwright smoke" scope.
  </action>
  <verify>
    <automated>npm --prefix frontend run lint</automated>
  </verify>
  <done>The coverage dashboard renders real totals/missing data; a Playwright smoke spec proves /app/inventory is reachable and its two sub-pages render their key elements.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> /app/inventory/** | Authenticated tenant user, permission+feature gated |
| RecipeFormDialog submit -> backend | Client-validated payload crossing into the server's own independent validation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-084-01 | Information Disclosure | Inventory recipe-builder/coverage pages | medium | mitigate | `PermissionGuard(inventory.item.view)` + `FeatureGuard(FEATURE_INVENTORY)` wrap the whole `/app/inventory` route group; the create-recipe trigger is additionally wrapped in `PermissionGuard(inventory.item.manage)`. |
| T-084-02 | Tampering | Client-submitted recipe payload | medium | mitigate | `createRecipeInputSchema` Zod-validates client-side before POST; the server independently re-validates (Bean Validation + 08.1-02's catalog check) — client validation is UX-only, never the security boundary. |
| T-084-03 | Elevation of Privilege | Components importing api-client/repositories directly | low | mitigate | ESLint `no-restricted-imports` boundary (FE-08) — verified by `npm run lint`. |
| T-084-SC | Tampering (supply chain) | npm installs | low | accept | No new npm packages — reuses `react-hook-form`/`zod`/`@tanstack/react-query` already in package.json. |
</threat_model>

<verification>
- `npm --prefix frontend run test -- inventory-recipe-builder` passes.
- `npm --prefix frontend run typecheck` passes with zero `any`.
- `npm --prefix frontend run lint` passes (no boundary violations).
</verification>

<success_criteria>
An operator can author a versioned recipe against a real synced menu item through a live `/app/inventory` UI, and see recipe-coverage gaps on a dashboard — the vertical slice mandated by D-04/INV-10.
</success_criteria>

<output>
Create `.planning/phases/08.1-pos-inventory-depletion-activation/08.1-04-SUMMARY.md` when done.
</output>
