---
status: diagnosed
phase: 08.1-pos-inventory-depletion-activation
source: [08.1-VERIFICATION.md]
started: 2026-07-19T14:05:00Z
updated: 2026-07-19T20:35:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Recipe-builder UI live click-through (/app/inventory)
expected: The RecipeFormDialog's actual click-through submission (menu-item select → ingredient lines → submit → toast) works against the real backend, and the coverage dashboard renders real numbers — not just the MSW-mocked unit-test round-trip already proven. Confirm the sidebar Inventory link is live (comingSoon flag removed) and neither sub-route 404s.
result: issue
reported: "Driven live against the full dev stack. Frontend module itself is correct: sidebar Inventory link renders (comingSoon removed), /app/inventory + /recipes + /coverage all load with no 404, feature-flags returns FEATURE_INVENTORY, JWT carries inventory.item.view/manage. BUT the recipe-builder menu-item picker is permanently EMPTY (only the placeholder option) and the coverage dashboard shows 0 total items — because the pos→inventory menu-item catalog sync is BROKEN on the live stack. GET /api/v1/inventory/menu-items returns []. After emitting MENU_ITEM_UPSERTED for all 10 demo menu items (via the authenticated pos activate path), all 10 events routed correctly to inventory.menu-item.queue but every one failed to persist: 'ERROR: new row violates row-level security policy for table menu_item_catalog' (SQLState 42501). The recipe click-through can never complete because there is nothing to select."
severity: blocker

## Summary

total: 1
passed: 0
issues: 1
pending: 0
skipped: 0
blocked: 0

## Gaps

- truth: "A synced menu item can be picked in the recipe builder and a recipe version authored against it; the coverage dashboard shows real totals — i.e. the live pos→inventory menu-item catalog sync populates menu_item_catalog."
  status: failed
  reason: "User reported: live menu-item catalog sync fails — every MenuItemCatalogConsumer insert into the FORCE-RLS menu_item_catalog table is rejected with 'new row violates row-level security policy' (SQLState 42501), so GET /api/v1/inventory/menu-items stays [] and the recipe-builder picker + coverage dashboard are empty on the real stack."
  severity: blocker
  test: 1
  root_cause: "Transaction/connection-checkout ordering bug in shared-lib's TenantAwareMessageProcessor.process(). It is @Transactional and sets TenantContext INSIDE the method body, but TenantAwareDataSource.getConnection() sets the RLS GUC app.current_tenant_id at connection CHECKOUT — which Spring performs at the @Transactional boundary (before the body runs). At checkout TenantContext is still empty, so configureTenant() sees tenantId==null and sets no GUC; the subsequent INSERT runs with an empty app.current_tenant_id, and the FORCE-RLS tenant_isolation policy (USING/WITH CHECK: tenant_id = NULLIF(current_setting('app.current_tenant_id',TRUE),'')::uuid) evaluates to NULL→false, rejecting the row. The HTTP read/write path is unaffected because a servlet filter sets TenantContext BEFORE the transactional boundary. The 08.1-02 ITs pass because TestFixtures opens a raw JDBC connection and manually 'SET app.current_tenant_id', bypassing the real DataSource checkout ordering. This is the first inventory CONSUMER write to a FORCE-RLS table (pos/kitchen RLS is inert — ENABLE not FORCE, app owns tables), so the latent shared-lib bug surfaces here. NOTE: the same code path backs OrderClosedConsumer→DepletionService writing to the FORCE-RLS V1 stock tables, so the live depletion loop is almost certainly broken the same way (only ever proven via GUC-setting ITs, never live)."
  artifacts:
    - path: "shared-lib/src/main/java/io/restaurantos/shared/tenant/TenantAwareMessageProcessor.java"
      issue: "@Transactional method sets TenantContext in its body, AFTER Spring has already checked out the connection (and run the RLS-GUC setter with an empty context) at the transaction boundary. Needs to guarantee app.current_tenant_id is set on the consumer transaction's connection BEFORE any DML — e.g. set TenantContext before the @Transactional boundary, or explicitly run set_config('app.current_tenant_id', tenantId, true) via the EntityManager at the start of the transaction (TenantGucHelper already exists for this)."
    - path: "shared-lib/src/main/java/io/restaurantos/shared/tenant/TenantAwareDataSource.java"
      issue: "Sets the RLS GUC only at connection checkout from the then-current TenantContext; empty context at checkout ⇒ no GUC ⇒ FORCE-RLS insert rejected. (Behaves correctly for HTTP because the tenant filter runs before checkout.)"
    - path: "services/inventory-service/src/main/java/io/restaurantos/inventory/consumer/MenuItemCatalogConsumer.java"
      issue: "Observed failing consumer (MenuItemCatalogConsumer.onMessage:74 → AmqpRejectAndDontRequeueException) — correct as written; it is the victim of the shared-lib ordering bug, not the cause."
    - path: "services/inventory-service/src/main/resources/db/migration/V4__menu_item_catalog.sql"
      issue: "FORCE ROW LEVEL SECURITY table whose tenant_isolation policy the consumer insert violates. Correct as designed; documents the RLS contract the consumer path fails to satisfy."
  missing:
    - "Fix TenantAwareMessageProcessor so the RLS GUC (app.current_tenant_id) is set on the consumer transaction's connection before any DML — set TenantContext before the @Transactional boundary, or set_config(...,true) via EntityManager at transaction start."
    - "Add a live (non-TestFixtures-GUC) integration test that drives a real @Transactional consumer insert into a FORCE-RLS table via the actual TenantAwareDataSource checkout path, so this class of ordering bug is caught (the current ITs manually set the GUC and cannot detect it)."
    - "Verify (and fix if needed) OrderClosedConsumer→DepletionService live depletion against FORCE-RLS V1 stock tables, which shares this exact code path."
  debug_session: ""
