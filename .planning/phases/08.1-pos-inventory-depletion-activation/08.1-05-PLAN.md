---
phase: 08.1-pos-inventory-depletion-activation
plan: 05
type: execute
wave: 5
depends_on: ["08.1-01", "08.1-02", "08.1-03", "08.1-04"]
files_modified:
  - services/pos-service/src/test/java/io/restaurantos/pos/LiveOrderClosedPayloadIT.java
  - services/inventory-service/src/test/java/io/restaurantos/inventory/LiveDepletionProofIT.java
autonomous: true
requirements: [INV-12]
user_setup: []

must_haves:
  truths:
    - "A menu item created via MenuServiceImpl.createItem (08.1-01) and referenced by a real closed pos-service order carries the SAME menuItemId in both its MENU_ITEM_UPSERTED outbox row and the ORDER_CLOSED outbox row's items[] — the sync and the sale are self-consistent."
    - "A recipe authored through the REAL RecipeService.createVersion (catalog-validated, 08.1-02) against a real catalog-synced menu item, when a real ORDER_CLOSED payload for that menu item is consumed, depletes stock FEFO, writes an inventory_movements DEPLETION row, and publishes STOCK_DEPLETED with totalCogsPaisa exactly equal to the depleted base quantity times the aggregate avg_cost_paisa, rounded HALF_UP (INV-12)."
  artifacts:
    - services/pos-service/src/test/java/io/restaurantos/pos/LiveOrderClosedPayloadIT.java
    - services/inventory-service/src/test/java/io/restaurantos/inventory/LiveDepletionProofIT.java
  key_links:
    - "LiveDepletionProofIT drives the SAME real components production uses, in the SAME order: MenuItemCatalogService.upsert (simulating the synced catalog row) -> RecipeService.createVersion (catalog-validated, NOT a direct RecipeRepository.save) -> OrderClosedConsumer.onMessage (the real @RabbitListener method, NOT DepletionService.deplete called directly) -> assert on inventory_movements + event_outbox."
    - "LiveOrderClosedPayloadIT drives menuService.createItem then the real order create/addItem/sendToKds/markItemServed/recordPayment/maybeCloseOrder chain via PosTestBase.closeViaServeAndPay, then compares the MENU_ITEM_UPSERTED and ORDER_CLOSED outbox rows' menuItemId fields for equality."
  prohibitions:
    - "The proof never seeds a Recipe row directly via RecipeRepository.save — it goes through RecipeService.createVersion so the 08.1-02 catalog validation is actually exercised, unlike 08-05's pre-existing DepletionConsumerIT (which is out of scope to modify here)."
    - "The depletion trigger under test is unchanged from D-01 (ORDER_CLOSED, Paid AND Served) — this plan does not touch OrderServiceImpl.maybeCloseOrder's trigger conditions, and kitchen-service is not touched."
---

<objective>
Prove the entire activated loop end-to-end with real service code, not hand-built shortcuts: a menu item is created (08.1-01) and synced into the catalog (08.1-02), a recipe is authored against it through the real catalog-validated `RecipeService.createVersion`, and a real closed order for that menu item — consumed through the real `OrderClosedConsumer` — FEFO-depletes stock and publishes `STOCK_DEPLETED` with correct aggregate-MAC `totalCogsPaisa` (INV-12). Provide a manual UAT checklist for optionally re-confirming the same chain against the live dev stack.

Purpose: 08-05 already unit/IT-proved the depletion mechanics in isolation (recipe seeded directly via repository, no catalog concept). This plan is the first test in the whole suite to exercise the FULL activated chain — catalog sync, validated recipe authoring, and depletion — as one coherent proof, which is what ROADMAP Phase 08.1 success criterion #4 and INV-12 actually require.
Output: Two new integration tests (pos-service, inventory-service) plus a documented manual UAT checklist in this plan's verification section.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.1-pos-inventory-depletion-activation/08.1-CONTEXT.md
@.planning/phases/08.1-pos-inventory-depletion-activation/08.1-RESEARCH.md
@services/pos-service/src/test/java/io/restaurantos/pos/SettlementSemanticsIT.java
@services/inventory-service/src/test/java/io/restaurantos/inventory/DepletionConsumerIT.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: LiveOrderClosedPayloadIT (pos-service) — menu-item sync self-consistency</name>
  <files>services/pos-service/src/test/java/io/restaurantos/pos/LiveOrderClosedPayloadIT.java</files>
  <read_first>
    - services/pos-service/src/test/java/io/restaurantos/pos/SettlementSemanticsIT.java (createSentOrder/createServedOrder helpers + `outboxRepository.findAll().stream().filter(e -&gt; "X".equals(e.getEventType()))` pattern; `closeViaServeAndPay` on PosTestBase)
    - services/pos-service/src/main/java/io/restaurantos/pos/service/MenuServiceImpl.java (post-08.1-01: createItem's real signature and its MENU_ITEM_UPSERTED publish)
    - services/pos-service/src/main/java/io/restaurantos/pos/event/PosEventPayloads.java (post-08.1-01: MenuItemUpsertedPayload field order) and PosClosePayloads.java (OrderClosedPayload.items()' ItemEntry.menuItemId())
  </read_first>
  <action>
    Create LiveOrderClosedPayloadIT extends PosTestBase. Autowire MenuService, MenuCategoryRepository, OrderService, PaymentService, OutboxRepository, TenantContext, ObjectMapper (a plain one is fine — this test only needs to read eventType/payload fields back out of the envelope JSON, not the tolerant qualifier). In @BeforeEach, clear the outbox, set a fresh tenant/branch context, seed a MenuCategory, and call openTillForCashier(branchId) (mirrors PosTestBase's helper) so order creation succeeds. Test: create a menu item via menuService.createItem(...) (capturing its returned id as menuItemId); create an order, addItem(menuItemId, qty=1), drive it to CLOSED via closeViaServeAndPay(orderService, paymentService, order, branchId) (mirrors SettlementSemanticsIT's helper usage exactly); then read the outbox: find the single row with eventType "MENU_ITEM_UPSERTED" and extract its envelope's payload.menuItemId (parse the envelope_json with the ObjectMapper into a generic tree or a Map, since this test only needs the one field), find the single row with eventType "ORDER_CLOSED" and extract items[0].menuItemId from its envelope; assert the two UUIDs are equal. This proves the menu item that was synced is the SAME menu item that ends up on a real closed order's payload — the two halves of D-02's contract are not independently drifting.
  </action>
  <verify>
    <automated>mvn -pl services/pos-service verify -Dit.test=LiveOrderClosedPayloadIT -DfailIfNoTests=false</automated>
  </verify>
  <done>A real menu item's MENU_ITEM_UPSERTED sync and its appearance on a real closed order's ORDER_CLOSED payload are proven to reference the identical menuItemId.</done>
</task>

<task type="auto">
  <name>Task 2: LiveDepletionProofIT (inventory-service) — catalog -> validated recipe -> real consumer -> FEFO + MAC COGS</name>
  <files>services/inventory-service/src/test/java/io/restaurantos/inventory/LiveDepletionProofIT.java</files>
  <read_first>
    - services/inventory-service/src/test/java/io/restaurantos/inventory/DepletionConsumerIT.java (the FULL fixture-seeding + EventEnvelope/Message-construction + OrderClosedConsumer.onMessage direct-invocation harness — reuse this exact harness shape, do NOT modify DepletionConsumerIT itself)
    - services/inventory-service/src/main/java/io/restaurantos/inventory/service/MenuItemCatalogService.java (post-08.1-02: upsert(MenuItemUpsertedPayload) — use this to seed the catalog, simulating a real consumed MENU_ITEM_UPSERTED event, rather than inserting a MenuItemCatalog row directly)
    - services/inventory-service/src/main/java/io/restaurantos/inventory/service/RecipeService.java (post-08.1-02/03: createVersion's catalog validation + getCoverage())
    - services/inventory-service/src/main/java/io/restaurantos/inventory/service/DepletionService.java (post-08.1-03: computeCogsPaisa/walkFefoAndFloor static helpers usable directly for the test's independent expected-value calculation)
    - services/inventory-service/src/main/java/io/restaurantos/inventory/dto/RecipeDtos.java (CreateRecipeVersionRequest/RecipeLineRequest shapes for calling createVersion for real)
  </read_first>
  <action>
    Create LiveDepletionProofIT extends InventoryTestBase. Autowire MenuItemCatalogService, RecipeService, IngredientRepository, IngredientBranchStockRepository, StockLotRepository, InventoryMovementRepository, OutboxRepository, OrderClosedConsumer, TenantContext, ObjectMapper (the `@Qualifier("eventObjectMapper")` bean). In the test method: (1) activate a fresh tenant/branch (TestFixtures.newTenantBranch + activateTenantContext); (2) seed one ingredient (InventoryFixtures.seedIngredient) with a known reorderPoint, one branch-stock row (InventoryFixtures.seedStock) with a known qtyOnHand and avgCostPaisa, and one FEFO lot (InventoryFixtures.seedLot) covering that quantity; (3) call menuItemCatalogService.upsert(new MenuItemUpsertedPayload(menuItemId=UUID.randomUUID(), name="Chicken Karahi", categoryId=UUID.randomUUID(), categoryName="Mains", active=true, basePricePaisa=45000, updatedAt=Instant.now())) — this is the ONLY place a catalog row is created, simulating the real consumed sync event; (4) call recipeService.createVersion(new CreateRecipeVersionRequest(that same menuItemId, yieldServings=BigDecimal.ONE, effectiveFrom=null, name="Karahi Recipe", lines=[a RecipeLineRequest referencing the seeded ingredient with a known qty+uomCode matching the ingredient's base UOM+yieldPct=100])) — asserting it does NOT throw (proves the catalog validation from 08.1-02 passes for a properly-synced item); (5) build a real InventoryEventPayloads.OrderClosedPayload (orderId, orderNo, type="DINE_IN", customerId=null, subtotal/discount/serviceCharge/tax/total set consistently, payments=[], items=[one ItemEntry(menuItemId, name, qty=known order quantity, unitPricePaisa, lineTotalPaisa)], tillSessionId=null, cashierId=null, closedAt=Instant.now()), wrap it in an EventEnvelope, serialize to an AMQP Message (mirrors DepletionConsumerIT's exact helper), and call orderClosedConsumer.onMessage(message); (6) assert: the branch-stock row's qtyOnHand decreased by exactly (recipe qty x order qty, adjusted per the UomConverter formula — compute the SAME expected value the test asserts against, do not just assert "some decrease"); the seeded lot's qty decreased accordingly (FEFO, single-lot case is trivially all-in-one-lot); an inventory_movements row exists with movement_type='DEPLETION', reference_type='ORDER_CLOSED', reference_id=orderId, and total_cost_paisa equal to DepletionService.computeCogsPaisa(expectedBaseQty, seededAvgCostPaisa) computed independently in the test; an event_outbox row exists with event_type='STOCK_DEPLETED' whose payload's totalCogsPaisa matches that same independently-computed value; (7) finally call recipeService.getCoverage() and assert this menuItemId does NOT appear in the missing list (the item is fully covered) — closing the loop back to 08.1-03's coverage report.
  </action>
  <verify>
    <automated>mvn -pl services/inventory-service verify -Dit.test=LiveDepletionProofIT -DfailIfNoTests=false</automated>
  </verify>
  <done>The full activated loop — catalog sync, catalog-validated recipe authoring, and real-consumer FEFO+MAC-COGS depletion — is proven correct end-to-end with real service code, and the coverage report reflects the newly-covered item.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cross-service payload contract | pos-service's real emitted event shapes must be exactly what inventory-service's real consumers expect |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-085-01 | Repudiation | Full-loop proof coverage | medium | mitigate | `LiveDepletionProofIT` exercises the REAL `RecipeService.createVersion` (catalog-validated) and the REAL `OrderClosedConsumer.onMessage` path — not a shortcut direct-repository seed — so a regression in either validation or consumption wiring fails this test, not just a narrower unit test. |
| T-085-02 | Tampering (data integrity) | totalCogsPaisa assertion | high | mitigate | The expected COGS is computed independently in the test (`DepletionService.computeCogsPaisa`, the same static helper production uses) and compared against the actual `STOCK_DEPLETED` outbox payload — guards against a future refactor silently reintroducing lot-cost-based COGS (Pitfall 9). |
| T-085-SC | Tampering (supply chain) | dependencies | low | accept | No new packages. |
</threat_model>

<verification>
- `mvn -pl services/pos-service verify -Dit.test=LiveOrderClosedPayloadIT -DfailIfNoTests=false` passes.
- `mvn -pl services/inventory-service verify -Dit.test=LiveDepletionProofIT -DfailIfNoTests=false` passes.
- Full regression: `mvn -pl services/pos-service verify -DfailIfNoTests=false` and `mvn -pl services/inventory-service verify -DfailIfNoTests=false` both stay green (no regression introduced across the whole phase).

### Manual UAT checklist (optional, run against the live dev stack — this plan stays autonomous/no-checkpoint; this is documentation for a human to optionally re-confirm)

1. `make dev-up` (or the project's documented equivalent) — all services healthy, including pos-service, inventory-service, and RabbitMQ with the definitions from `deploy/init/rabbitmq-definitions.json` loaded (confirm `inventory.menu-item.queue` exists in the RabbitMQ management UI with its two bindings).
2. Log in as an OWNER/MANAGER demo user; create a new menu item via the POS admin surface or `POST /api/v1/pos/menu/items` directly; confirm (via RabbitMQ management UI or pos-service logs) a `MENU_ITEM_UPSERTED` message was published.
3. In the inventory-service logs (or by querying `menu_item_catalog` directly), confirm the new menu item was synced within a few seconds.
4. Navigate to `/app/inventory/recipes`, select the new menu item, and author a recipe (ingredient + qty + UOM + yieldServings).
5. Ring a real order for that menu item through the POS UI: create -> add item -> fire -> pay -> mark served (order should auto-close on Paid AND Served, per D-01/POS-23).
6. Confirm in inventory-service logs / `inventory_movements` table: a new `DEPLETION` row for the order's ingredient(s), with `total_cost_paisa` matching qty x the ingredient's current `avg_cost_paisa`.
7. Confirm a `STOCK_DEPLETED` message was published (RabbitMQ management UI on `inventory.topic`) with the expected `totalCogsPaisa`.
8. Navigate to `/app/inventory/coverage` and confirm the menu item now shows as covered.
</verification>

<success_criteria>
The activated ORDER_CLOSED -> depletion loop is proven correct end-to-end with real service code (catalog sync, validated recipe authoring, real-consumer FEFO depletion, correct aggregate-MAC COGS) — INV-12 — and a manual checklist exists for optionally re-confirming the same chain live.
</success_criteria>

<output>
Create `.planning/phases/08.1-pos-inventory-depletion-activation/08.1-05-SUMMARY.md` when done.
</output>
