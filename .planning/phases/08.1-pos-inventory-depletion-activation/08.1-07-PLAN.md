---
phase: 08.1-pos-inventory-depletion-activation
plan: 07
type: execute
wave: 2
depends_on: [08.1-06]
files_modified: []
autonomous: false
gap_closure: true
requirements: [INV-09, INV-10]
must_haves:
  truths:
    - "On the live dev stack, after redeploying the fixed inventory-service and re-emitting MENU_ITEM_UPSERTED, GET /api/v1/inventory/menu-items returns the 10 demo menu items (no longer [])."
    - "GET /api/v1/inventory/recipes/coverage shows totalActiveMenuItems=10, and the inventory-service log shows NO new SQLState 42501 during the re-emit."
    - "The recipe-builder UI click-through works end-to-end against the live backend: the menu-item picker populates from the synced catalog, ingredient lines submit with a success toast, and the coverage dashboard renders real totals."
  artifacts: []
  key_links:
    - "fixed shared-lib (.m2) -> rebuilt inventory-service jar -> restarted inventory-service -> pos republish -> menu_item_catalog populated -> recipe-builder picker + coverage dashboard"
---

<objective>
Confirm the 08.1-06 fix end-to-end on the LIVE dev stack (currently UP), closing UAT test #1 (the original blocker) AND the outstanding VERIFICATION.md human-verification item (recipe-builder UI click-through) in one pass. This plan does no code change — it redeploys the fixed inventory-service, re-drives the pos->inventory sync, and verifies the catalog now populates and the recipe-builder UI works.

Purpose: 08.1-06's shared-lib regression IT is the authoritative MACHINE proof that the RLS-ordering bug is fixed, but the original UAT blocker was observed on the real running stack (GET /menu-items returned [], 42501 in the log). The user's standing preference is real Playwright/browser verification over unit-test evidence. This plan turns the machine fix into an observed live PASS.

Output: a live-stack confirmation that `menu_item_catalog` populates (10 demo items), coverage reports totalActiveMenuItems=10, and the recipe-builder authoring flow completes in a real browser.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08.1-pos-inventory-depletion-activation/08.1-UAT.md
@.planning/phases/08.1-pos-inventory-depletion-activation/08.1-VERIFICATION.md

# The re-emit mechanism (X-Internal-Service guarded) used by the UAT to publish MENU_ITEM_UPSERTED
@services/pos-service/src/main/java/io/restaurantos/pos/web/InternalPosController.java

# The consumer + read endpoint whose live behavior we are confirming
@services/inventory-service/src/main/java/io/restaurantos/inventory/consumer/MenuItemCatalogConsumer.java

# The e2e spec to run against the live stack
@frontend/e2e/inventory-recipe-builder.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redeploy fixed inventory-service, re-emit MENU_ITEM_UPSERTED, verify the catalog populates live</name>
  <files>services/inventory-service/target/*.jar</files>
  <action>
    Prerequisite: 08.1-06 installed the fixed shared-lib to local `.m2`.

    1. Rebuild the inventory-service jar so it links the fixed shared-lib: `mvn -f services/inventory-service/pom.xml -DskipTests clean package`. Only inventory-service needs redeploying — the pos publish path already works (the UAT confirmed all 10 MENU_ITEM_UPSERTED events routed correctly to inventory.menu-item.queue); only the inventory CONSUMER side was broken.
    2. Restart inventory-service in the running dev stack using the repo's restart procedure. Follow the dev-stack rebuild gotchas: prefer the repo's `restart-service.ps1` (or the documented equivalent), watch for jar file locks (stop the running process before `package` if the jar is locked), UTF-16-encoded logs, and the PowerShell stderr trap. Confirm inventory-service is healthy (its actuator/health endpoint or a successful authenticated GET) before proceeding.
    3. Re-emit MENU_ITEM_UPSERTED for the demo menu items via `POST /internal/menu-items/republish` on pos-service (X-Internal-Service header — the same backfill path the UAT used; it returns the count of republished active items). Alternatively re-drive the authenticated pos activate path the UAT used. Confirm the returned/expected count is 10.
    4. Verify live:
       - `GET /api/v1/inventory/menu-items` (authenticated as a demo manager/owner with inventory.item.view) returns the 10 demo menu items — no longer `[]`.
       - `GET /api/v1/inventory/recipes/coverage` shows `totalActiveMenuItems=10`.
       - Tail the inventory-service log during the re-emit and confirm ZERO new `SQLState 42501` / "new row violates row-level security policy" errors at `MenuItemCatalogConsumer`.

    Capture the actual JSON responses + a log excerpt into the SUMMARY as the live-PASS evidence.
  </action>
  <verify>
    <human-check>After redeploy + republish: GET /api/v1/inventory/menu-items returns 10 items, GET /api/v1/inventory/recipes/coverage shows totalActiveMenuItems=10, and the inventory-service log shows no new 42501 during the re-emit. Capture the responses in the SUMMARY.</human-check>
  </verify>
  <done>The live menu_item_catalog is populated (10 items via the read endpoint), coverage reports totalActiveMenuItems=10, and no new RLS-rejection appears in the inventory-service log.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human/Playwright verification of the recipe-builder UI click-through</name>
  <files>frontend/e2e/inventory-recipe-builder.spec.ts</files>
  <action>
    What was built and why this is a checkpoint: the pos->inventory menu-item catalog sync now works live (Task 1), so the recipe-builder picker is populated from the real synced catalog and the coverage dashboard has real totals. This checkpoint closes UAT test #1 AND the VERIFICATION.md outstanding UI human-verification item together — the rendered browser interaction (React Hook Form + useFieldArray dynamic rows, real select population, toast) has never been observed running against a live stack.

    Preferred (matches the user's Playwright preference): run the existing e2e spec against the live dev stack — `cd frontend && npx playwright test inventory-recipe-builder` — and confirm it passes.

    Manual fallback (or in addition):
      1. Start/confirm the dev stack, log in as a manager/owner demo user.
      2. Navigate to `/app/inventory` — confirm the sidebar Inventory link is live (not comingSoon/404).
      3. Go to `/app/inventory/recipes`, open the recipe-builder — confirm the menu-item picker now lists the 10 synced items (previously only the empty placeholder).
      4. Pick a synced menu item, add at least one ingredient line (qty + UOM), submit — confirm a success toast and the new version appears.
      5. Navigate to `/app/inventory/coverage` — confirm the stat tiles and missing-item list render real numbers (totalActiveMenuItems=10), not zeros.
  </action>
  <verify>
    <human-check>Recipe-builder click-through completes end-to-end against the live backend: menu-item picker populated from the synced catalog, recipe version authored with a success toast, coverage dashboard shows real totals. Reviewer types "approved", or describes what failed. Preferred evidence: a passing `npx playwright test inventory-recipe-builder` run.</human-check>
  </verify>
  <done>The recipe-builder authoring flow works end-to-end in a real browser and the coverage dashboard reflects the synced catalog (totalActiveMenuItems=10) — the UAT #1 blocker and the VERIFICATION.md UI human-check are both closed.</done>
</task>

</tasks>

<verification>
- Live: GET /api/v1/inventory/menu-items returns 10 items; GET /api/v1/inventory/recipes/coverage shows totalActiveMenuItems=10; no new 42501 in the inventory-service log.
- UI: recipe-builder picker populated, recipe version authored via the form with a success toast, coverage dashboard shows real totals (Playwright spec passes, or manual click-through confirmed).
</verification>

<success_criteria>
- The original UAT test #1 blocker is closed on the real stack: the catalog sync populates menu_item_catalog and the recipe-builder can select a synced item.
- The VERIFICATION.md outstanding human-verification item (live recipe-builder UI click-through) is confirmed PASS.
</success_criteria>

<output>
Create `.planning/phases/08.1-pos-inventory-depletion-activation/08.1-07-SUMMARY.md` when done. Record the live JSON responses, the log excerpt showing zero new 42501, and the Playwright/manual UI result.
</output>
