---
phase: 08.1-pos-inventory-depletion-activation
verified: 2026-07-19T00:00:00Z
status: human_needed
score: 4/4 must-haves verified (roadmap success criteria); 1 human-verification item outstanding (UI click-through UAT, non-blocking)
behavior_unverified: 0
overrides_applied: 0
human_verification:
  - test: "Drive the /app/inventory recipe-builder UI live (or via Playwright against the running dev stack): navigate to /app/inventory, confirm the sidebar link is live (not comingSoon/404), pick a real synced menu item on /app/inventory/recipes, add ingredient lines, submit, see a success toast, then check /app/inventory/coverage renders the totals/missing list."
    expected: "The RecipeFormDialog's actual click-through submission (menu-item select -> ingredient lines -> submit -> toast) works against the real backend, and the coverage dashboard renders real numbers — not just the MSW-mocked unit-test round-trip already proven."
    why_human: "frontend/e2e/inventory-recipe-builder.spec.ts was authored and lint-validated in this session but its own SUMMARY (08.1-04-SUMMARY.md, coverage items D1-D3) explicitly records human_judgment: true / status: unknown — it was never executed against a running dev stack or CI in this session. The repository/MSW-level round-trip (4/4 vitest) and typecheck are proven; the live browser interaction is not. Per user's standing preference (user-prefers-playwright-verification memory), this should be closed with a real Playwright run against the dev stack, not treated as passed on unit-test evidence alone."
---

# Phase 08.1: POS-Inventory Depletion Activation Verification Report

**Phase Goal:** The already-wired ORDER_CLOSED→depletion loop (built in Phase 8) becomes functional and trustworthy — POS menu items sync to inventory so recipes attach to real menu_item_ids with validation, operators author recipes via a UI, recipe coverage and un-recipe'd sales are surfaced instead of silently skipped, and a live order demonstrably depletes stock with correct COGS.

**Verified:** 2026-07-19
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (mapped to ROADMAP Success Criteria)

| # | Truth (Success Criterion) | Status | Evidence |
|---|---|---|---|
| 1 | POS publishes MENU_ITEM_UPSERTED/MENU_ITEM_DELETED on menu-item changes (+ backfill/republish); inventory maintains a menu_item_catalog read-model from those events; recipe creation rejects a menu_item_id not present/active in the catalog. | ✓ VERIFIED | `MenuServiceImpl` create/update/setActive/deleteItem/republishAllActive all funnel through `publishUpserted` (pos-service); `POST /internal/menu-items/republish` backfill exists (X-Internal-Service guarded). `MenuItemCatalogConsumer` (dual binding on `inventory.menu-item.queue`) populates `menu_item_catalog` (V4, FORCE-RLS, confirmed by reading the migration file). `RecipeService.createVersion` line 98-99 calls `existsByTenantIdAndMenuItemIdAndActiveTrue` and throws `MenuItemNotFoundException` before any Recipe row is created (confirmed by reading source). Proven by `MenuItemEventPublishingIT` 5/5, `MenuItemCatalogConsumerIT` 5/5, `RecipeMenuItemValidationIT` 3/3, `RecipeAccessControlIT` 2/2 regression (orchestrator-run, this session). |
| 2 | An operator can author a versioned recipe (menu item → ingredient lines w/ qty + UOM + effectiveFrom) through the /app/inventory recipe-builder UI, selecting from the real synced catalog. | ✓ VERIFIED (API/data-layer) / ⚠ see Human Verification (live UI click-through) | All 4 route files (`layout.tsx`, `page.tsx`, `recipes/page.tsx`, `coverage/page.tsx`) + `RecipeFormDialog.tsx` exist, are git-tracked (the `.gitignore` bug hiding `coverage/` was fixed), and the sidebar `comingSoon` flag is confirmed removed. Four-layer stack (schema/adapter/repository/hooks) wired to real endpoints. `inventory-recipe-builder.test.ts` 4/4 unit tests pass (MSW round-trip). However, the plan's own SUMMARY (08.1-04) explicitly flags all 3 of its coverage items as `human_judgment: true` — the Playwright spec was authored but never run against a live stack. See Human Verification below. |
| 3 | A recipe-coverage report shows which active menu items lack an effective recipe; when a sold line has no effective recipe at closedAt, depletion still processes covered lines and publishes DEPLETION_INCOMPLETE (no silent no-op). | ✓ VERIFIED | `GET /api/v1/inventory/recipes/coverage` (`RecipeService.getCoverage()`) confirmed in source, reusing `resolveEffectiveRecipe`. `DepletionService.deplete` (read in full, lines ~90-200): the 08-05 all-empty early return is deleted; the lock-and-deplete loop + STOCK_DEPLETED publish are wrapped in `if (!requiredByIngredient.isEmpty())`; `DEPLETION_INCOMPLETE` publishes independently whenever `missingMenuItemIds` is non-empty. Proven by `RecipeCoverageIT` 2/2, `DepletionIncompleteIT` 2/2 (partial-miss + fully-uncovered cases), `DepletionConsumerIT` regression pass (fully-covered path unchanged). |
| 4 | A live POS order (create→add→fire→pay→serve→ORDER_CLOSED) depletes stock FEFO, writes a DEPLETION movement, and emits STOCK_DEPLETED with correct aggregate-MAC totalCogsPaisa. | ✓ VERIFIED | `LiveDepletionProofIT` drives the real chain: `MenuItemCatalogService.upsert` → real `RecipeService.createVersion` (catalog-validated, not a direct repo save) → real `OrderClosedConsumer.onMessage`. Confirmed by reading the test source: seeded lot cost (1200) deliberately differs from seeded avg cost (850, `SEEDED_AVG_COST_PAISA`), COGS assertion computed independently via `DepletionService.computeCogsPaisa` — structurally proves aggregate-MAC valuation, not lot cost. `LiveOrderClosedPayloadIT` proves the synced menuItemId and the sold menuItemId are identical across a real closeViaServeAndPay chain. Both 1/1 PASS (independently re-run this session per orchestrator evidence). The plan's optional manual UAT checklist (live dev-stack click-through) exists in 08.1-05-PLAN.md but is explicitly non-blocking/optional per the plan's own scope note — this criterion is machine-proven and does not require human re-confirmation to be considered met. |

**Score:** 4/4 roadmap success criteria machine-verified at the API/service layer. 1 outstanding human-verification item (live UI click-through for criterion 2's UI surface) — does not fail any criterion, since the API/data-layer proof for criterion 2 is solid, but the actual browser-driven UX has not been exercised.

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `services/pos-service/.../MenuServiceImpl.java` | Menu-item write path + event publish | ✓ VERIFIED | `publishUpserted` helper confirmed wired into create/update/setActive/deleteItem/republishAllActive |
| `services/pos-service/.../InternalPosController.java` | Republish backfill endpoint | ✓ VERIFIED | `POST /internal/menu-items/republish`, X-Internal-Service guarded |
| `services/inventory-service/.../V4__menu_item_catalog.sql` | FORCE-RLS catalog table | ✓ VERIFIED | Read in full: `ENABLE`+`FORCE ROW LEVEL SECURITY`, NULLIF-guarded `tenant_isolation` policy, `UNIQUE(tenant_id, menu_item_id)`, soft-delete documented |
| `services/inventory-service/.../MenuItemCatalogConsumer.java` | Dual-event-type consumer | ✓ VERIFIED | Confirmed via `InventoryRabbitConfig` dual-binding (`pos.menu_item.upserted` + `pos.menu_item.deleted`) on one queue, both declared in `deploy/init/rabbitmq-definitions.json` (D-08) |
| `services/inventory-service/.../RecipeService.java` | Catalog-validation gate | ✓ VERIFIED | `existsByTenantIdAndMenuItemIdAndActiveTrue` check + `MenuItemNotFoundException` confirmed as `createVersion`'s first statement |
| `services/inventory-service/.../DepletionService.java` | DEPLETION_INCOMPLETE branch | ✓ VERIFIED | Full method read; independent-publish structure confirmed exactly as summarized |
| `frontend/app/(tenant)/app/inventory/{layout,page,recipes/page,coverage/page}.tsx` | Live UI route group | ✓ VERIFIED (exists+substantive+wired) | All git-tracked; `comingSoon` flag removal confirmed |
| `frontend/components/inventory/RecipeFormDialog.tsx` | Recipe-authoring form | ✓ VERIFIED (exists+substantive) | Present, no stub markers found |
| `services/pos-service/.../LiveOrderClosedPayloadIT.java` | Sync self-consistency proof | ✓ VERIFIED | 1 `@Test`, content matches plan's spec, PASS (orchestrator evidence) |
| `services/inventory-service/.../LiveDepletionProofIT.java` | Full-loop capstone proof | ✓ VERIFIED | 1 `@Test`, content matches plan's spec (aggregate-MAC guard, real consumer path), PASS (orchestrator evidence) |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `MenuServiceImpl` mutations | transactional outbox | `publishUpserted`/direct `eventPublisher.publish` | ✓ WIRED | Confirmed by source read + 5/5 IT |
| `InventoryRabbitConfig` + `rabbitmq-definitions.json` | `pos.topic` routing keys | dual binding, D-08 | ✓ WIRED | Both files declare identical topology (queue, 2 bindings, .dlq) |
| `RecipeService.createVersion` | `menu_item_catalog` | `existsByTenantIdAndMenuItemIdAndActiveTrue` | ✓ WIRED | Confirmed first statement in method body |
| `DepletionService.deplete` | outbox (STOCK_DEPLETED / DEPLETION_INCOMPLETE) | independent non-empty guards | ✓ WIRED | Confirmed by full-method read; both branches confirmed independent |
| `RecipeFormDialog` | `POST /api/v1/inventory/recipes` | four-layer (schema→adapter→repository→hook) | ✓ WIRED (data-layer proven; browser-level not exercised) | Confirmed via source + 4/4 unit test; live click-through outstanding (see Human Verification) |
| `LiveDepletionProofIT` | real `OrderClosedConsumer.onMessage` | direct invocation, tenant cleared first | ✓ WIRED | Confirmed via source read (line ~127 `orderClosedConsumer.onMessage(message)`) |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|---|---|---|---|---|
| INV-09 | 08.1-01, 08.1-02 | Menu-item sync + catalog + validation | ✓ SATISFIED | REQUIREMENTS.md marks Complete; both plans' truths verified above |
| INV-10 | 08.1-04 | Recipe-builder UI | ✓ SATISFIED (API/data-layer) — live UI click-through pending human confirmation | REQUIREMENTS.md marks Complete; UI artifacts + unit tests verified; browser-level UAT outstanding, non-blocking per plan's own scope |
| INV-11 | 08.1-03 | Coverage report + DEPLETION_INCOMPLETE | ✓ SATISFIED | REQUIREMENTS.md marks Complete; both ITs verified |
| INV-12 | 08.1-05 | Live end-to-end proof | ✓ SATISFIED | REQUIREMENTS.md marks Complete; both capstone ITs verified, machine-proven, no human gate required |

No orphaned requirements found — all four INV-09..INV-12 IDs are claimed by exactly one plan each and all four appear correctly mapped in REQUIREMENTS.md's phase-mapping table (lines 357-360).

### Anti-Patterns Found

None. Scanned all key production files modified in this phase (`MenuServiceImpl`, `MenuController`, `InternalPosController`, `MenuItemCatalogService`, `MenuItemCatalogConsumer`, `RecipeService`, `DepletionService`, `inventory.repository.ts`, `RecipeFormDialog.tsx`, `coverage/page.tsx`, `recipes/page.tsx`) for TBD/FIXME/XXX/TODO/HACK/PLACEHOLDER/stub markers — zero matches.

### Behavioral Spot-Checks / Probe Execution

Not re-run in this session per the orchestrator's explicit instruction (evidence already gathered and authoritative). Spot-checked instead via source-code reading:
- `RecipeService.createVersion`'s catalog gate: confirmed present as the literal first statement (lines 98-99).
- `DepletionService.deplete`'s independent STOCK_DEPLETED/DEPLETION_INCOMPLETE publish structure: confirmed by reading the full method body.
- `V4__menu_item_catalog.sql`: confirmed FORCE-RLS + NULLIF-guarded policy, matching V1's convention exactly.
- `InventoryRabbitConfig` + `rabbitmq-definitions.json`: confirmed dual-binding topology present in both files (D-08 compliance).
- `LiveDepletionProofIT` / `LiveOrderClosedPayloadIT`: confirmed test bodies actually exercise the real service call chain claimed (not shortcut/direct-repository seeds) — `MenuItemCatalogService.upsert`, real `RecipeService.createVersion`, real `orderClosedConsumer.onMessage`, and the 1200-vs-850 cost-divergence trick that structurally forces an aggregate-MAC (not lot-cost) assertion.
- Test-method counts cross-checked against SUMMARY claims (5+5+3+2+1+1 Java ITs, 4 vitest unit tests) — all counts match exactly; no phantom/empty test files.
- D-06 scope boundary confirmed: `grep STOCK_DEPLETED` in `services/finance-service` returns zero matches — finance does not yet consume the event, as intended.

All authoritative test results (from the orchestrator's session) are accepted as-is per instruction:
- inventory-service: 58/58 IT PASS (BUILD SUCCESS), zero regressions.
- pos-service: `MenuItemEventPublishingIT` 5/5, `LiveOrderClosedPayloadIT` 1/1 PASS; 3 pre-existing failing ITs (`OrderRevisionIT`, `OrderRlsIsolationIT`, `VoidOwnOrderIT`) proven pre-existing at commit `d7bbc71` (before any 08.1 code) and disjoint from every file this phase touched — documented in `deferred-items.md`, not regressions.
- Frontend: vitest 4/4 PASS, typecheck clean.

### Human Verification Required

### 1. Live browser click-through of the recipe-builder UI (and Playwright re-run against the dev stack)

**Test:** Start the dev stack, log in as a manager/owner demo user, navigate to `/app/inventory`, confirm the sidebar link is live; go to `/app/inventory/recipes`, pick a real synced menu item, add at least one ingredient line, submit the form, and confirm a success toast + the new version appears; then navigate to `/app/inventory/coverage` and confirm the stat tiles and missing-item list render real numbers. Optionally, actually execute `frontend/e2e/inventory-recipe-builder.spec.ts` against the running stack (`npx playwright test inventory-recipe-builder`).

**Expected:** The full authoring flow works end-to-end in a real browser against the real backend — menu-item select populates from the real synced catalog, ingredient lines can be added/removed, submission succeeds, and the coverage dashboard reflects the change.

**Why human:** This is explicitly flagged in 08.1-04-SUMMARY.md's own coverage block (`human_judgment: true`, `status: unknown` for the e2e spec) — the Playwright spec was authored and lint-validated but never executed against a live stack in this session, and no CI run of it was reported either. The repository/MSW-level round-trip is proven (4/4 vitest), and static analysis confirms the wiring is correct (four-layer boundary respected, no orphaned imports, sidebar flag removed), but the actual rendered browser interaction (React Hook Form + useFieldArray dynamic rows, real select population, toast display) has not been observed running. Per the user's standing preference for Playwright-driven UAT over manual click-throughs, this item should be closed with an actual browser automation run, not accepted on unit-test evidence alone.

### Gaps Summary

No gaps found at the API/service/data layer — all four ROADMAP success criteria are machine-proven by integration tests that exercise the real production code paths (not shortcuts), confirmed independently by reading the actual source (not just trusting SUMMARY narrative). The one outstanding item is a UI-layer human/browser verification for criterion 2 (recipe-builder UI), which the plan's own SUMMARY already flagged honestly as unexecuted rather than claiming a false pass — this is a `human_needed` classification, not a `gaps_found` one, because the underlying code is present, substantive, and wired; only the live browser behavior is unexercised. Criterion 4's live-chain proof (the other "live order" requirement) is fully machine-proven by `LiveDepletionProofIT` and does not require this same human gate — its "manual UAT checklist" in 08.1-05-PLAN.md is explicitly documented as optional, not a blocking requirement.

---

*Verified: 2026-07-19*
*Verifier: Claude (gsd-verifier)*
