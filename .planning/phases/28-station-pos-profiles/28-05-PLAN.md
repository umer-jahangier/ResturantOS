---
phase: 28-station-pos-profiles
plan: 05
type: execute
wave: 2
depends_on: ["28-02"]
files_modified:
  - services/pos-service/src/main/resources/db/migration/V15__menu_station_routes.sql
  - services/pos-service/src/main/java/io/restaurantos/pos/domain/model/MenuItemStationRoute.java
  - services/pos-service/src/main/java/io/restaurantos/pos/domain/model/MenuCategoryStationRoute.java
  - services/pos-service/src/main/java/io/restaurantos/pos/repository/MenuItemStationRouteRepository.java
  - services/pos-service/src/main/java/io/restaurantos/pos/repository/MenuCategoryStationRouteRepository.java
  - services/pos-service/src/main/java/io/restaurantos/pos/service/StationRoutingResolver.java
  - services/pos-service/src/main/java/io/restaurantos/pos/service/MenuServiceImpl.java
  - services/pos-service/src/main/java/io/restaurantos/pos/service/OrderServiceImpl.java
  - services/pos-service/src/main/java/io/restaurantos/pos/dto/MenuItemDto.java
  - services/pos-service/src/main/java/io/restaurantos/pos/web/MenuController.java
  - services/pos-service/src/test/java/io/restaurantos/pos/StationRoutingResolverTest.java
  - services/pos-service/src/test/java/io/restaurantos/pos/MenuStationRoutingIT.java
autonomous: true
requirements: [P28-SC3]
decisions: [D-28-04, D-28-05, D-28-06]

must_haves:
  truths:
    - "A tenant-scoped menu item can route to a different station in each branch, so a two-branch tenant no longer has one station column for one shared item."
    - "A category-level route exists, so 'everything in Drinks goes to the bar' is one row rather than two hundred (D-28-05)."
    - "One resolver decides an item's station, and it is unit-testable without an order."
    - "A tenant who configures no routes gets byte-identical behaviour to today."
    - "Assigning a station at branch B no longer silently re-points the same item for branch A."
  prohibitions:
    - "The add-item station snapshot MUST NOT move to fire time. Both routing keys are captured when the line is added, precisely so a later menu re-assignment never retroactively re-routes an already-fired line."
    - "`menu_items.station_id` and `menu_items.kds_station` MUST NOT be dropped or stop being written. The free-text mirror is still read downstream; retiring it is a later phase's job."
    - "The resolver MUST NOT fall back to a station belonging to a different branch. That fallback is the corruption this plan exists to close."
    - "No route row may be created for a station outside the caller's branch or a menu item outside the caller's tenant."
    - "Ticket grouping MUST NOT change. Grouping stays by station code."
  artifacts:
    - services/pos-service/src/main/resources/db/migration/V15__menu_station_routes.sql
    - services/pos-service/src/main/java/io/restaurantos/pos/service/StationRoutingResolver.java
    - services/pos-service/src/test/java/io/restaurantos/pos/StationRoutingResolverTest.java
    - services/pos-service/src/test/java/io/restaurantos/pos/MenuStationRoutingIT.java
  key_links:
    - "OrderServiceImpl.addItem -> StationRoutingResolver, replacing the direct read of menu_items.station_id"
    - "MenuServiceImpl.assignStation -> a route row for the caller's branch, with the legacy column kept in sync for one release"
    - "StationRoutingResolver step 3 -> a branch check on the legacy FK, which is the fix for the cross-branch overwrite"
---

<objective>
Make an item's destination a per-branch fact, and in doing so close a live data-integrity bug in shipped
code.

`MenuItem` has no branch. It hangs off `MenuCategory`, which is unique per tenant and name. So a
two-branch tenant has exactly one row for "Chicken Karahi" and exactly one `station_id` on it. An admin
at Branch B assigning that item to B's grill silently re-points the same item for Branch A and
overwrites the free-text mirror with B's code — after which Branch A's tickets route to a code that may
not exist there and fall through to the default station. Each write passes its own branch guard. There
is no guard against the last writer winning across branches, because the row is not branch-scoped in the
first place. `V7__stations.sql` says this in its own comment. It is currently masked only because no UI
calls the endpoint, which means the feature being dead is the only reason the bug is invisible — and
plan 28-10 is about to build that UI.

This plan is also what makes D-28-04 configurable. Split tickets already work in kitchen-service: it
groups by station code and emits one ticket per order and station. What has never existed is a
supported way for a tenant to say which items go where, per branch. Without that, "an order spanning
food and drink produces two tickets" is a sentence about a code path nobody can reach.

Purpose: 28-08 asserts the split; 28-10 builds the UI that writes these rows. Both need the resolver.
Output: per-branch routing tables, one resolver, and the legacy path made safe rather than removed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-station-pos-profiles/28-CONTEXT.md
@.planning/phases/28-station-pos-profiles/28-02-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Per-branch route tables and one resolver (D-28-05)</name>
  <files>services/pos-service/src/main/resources/db/migration/V15__menu_station_routes.sql, services/pos-service/src/main/java/io/restaurantos/pos/domain/model/MenuItemStationRoute.java, services/pos-service/src/main/java/io/restaurantos/pos/domain/model/MenuCategoryStationRoute.java, services/pos-service/src/main/java/io/restaurantos/pos/repository/MenuItemStationRouteRepository.java, services/pos-service/src/main/java/io/restaurantos/pos/repository/MenuCategoryStationRouteRepository.java, services/pos-service/src/main/java/io/restaurantos/pos/service/StationRoutingResolver.java, services/pos-service/src/test/java/io/restaurantos/pos/StationRoutingResolverTest.java</files>
  <read_first>
    - services/pos-service/src/main/resources/db/migration/V7__stations.sql lines 40-50 — the comment naming this exact problem: menu items are tenant-scoped while stations are branch-scoped, so a shared item has no single correct branch-station
    - services/pos-service/src/main/resources/db/migration/V1__pos_schema.sql — `menu_categories` uniqueness and `menu_items`, which is where the missing branch dimension is
    - services/pos-service/src/main/java/io/restaurantos/pos/domain/model/MenuItem.java — `stationId` and `kdsStation`, the two legacy keys
    - services/pos-service/src/main/java/io/restaurantos/pos/service/MenuServiceImpl.java around lines 137-162 — `assignStation` as it stands, including the branch guard that is individually correct and collectively insufficient
    - services/pos-service/src/main/java/io/restaurantos/pos/service/OrderServiceImpl.java around lines 230-250 — `addItem` and the snapshot rationale that must survive this change
    - services/pos-service/src/main/resources/db/migration/V11__force_rls_all_tenant_tables.sql — the policy posture for a new tenant table
    - services/pos-service/src/test/java/io/restaurantos/pos/RlsForcedInvariantIT.java
  </read_first>
  <behavior>
    - An item with a branch route resolves to that route's station for that branch.
    - The same item with a different route in another branch resolves to that other station, and neither assignment disturbs the other.
    - An item with no item-level route but whose category has one for the branch resolves to the category's station.
    - An item-level route wins over a category-level route for the same branch.
    - An item with neither, whose legacy station column points at a station in THIS branch, resolves to that station.
    - An item with neither, whose legacy station column points at a station in a DIFFERENT branch, does NOT resolve to it and falls through.
    - An item with no route and no usable legacy id, whose free-text key matches a station code in this branch, resolves to that station.
    - An item with none of the above resolves to the default station key, exactly as today.
    - A route row for a station outside the branch cannot be persisted.
  </behavior>
  <action>
    Write V15 creating `menu_item_station_routes` and `menu_category_station_routes`. Both are tenant
    and branch scoped, reference a station, and are unique on tenant, branch and the routed entity — one
    destination per item per branch, one per category per branch. Both carry a tenant_isolation policy
    and are ENABLEd and FORCEd, matching V11's posture; `RlsForcedInvariantIT` fails the build otherwise
    and it is not to be modified to accommodate them.

    Note in the migration header that FORCE binds the table owner too, so a future migration needing to
    touch rows across tenants must toggle the force flag around itself rather than quietly dropping it —
    auth-service already does exactly that in changeset 058 and that is the pattern to copy.

    Create `StationRoutingResolver` as a standalone service so it is unit-testable without constructing
    an order. Its single operation takes a menu item and a branch and returns a station, resolving in
    this order: the item-level route for the branch; the category-level route for the branch; the legacy
    station id ONLY IF that station belongs to this branch; the legacy free-text key matched against a
    station code in this branch; and finally nothing, which the caller renders as the default key.

    The branch check on the legacy id is the fix. Today that column applies regardless of branch, which
    is how one admin's assignment re-points another branch's routing. With the check, the legacy value
    stops applying to the wrong branch instead of mis-routing it. The last two steps reproduce today's
    behaviour byte for byte for a tenant who configures nothing, which is the whole back-compat story
    and belongs in a comment on the resolver.

    Every repository finder carries an explicit tenantId predicate as well as the policy — under FORCE
    an unscoped query returns zero rows rather than erroring, so a wiring break here would present as
    "this item has no route" and route silently to the default, which is the quietest possible failure.

    Write `StationRoutingResolverTest` as a unit test over the nine behaviours. The two that matter most
    are the cross-branch independence case and the legacy-id-in-another-branch case; those are the bug.
  </action>
  <acceptance_criteria>
    - All nine behaviours pass.
    - `RlsForcedInvariantIT` passes unmodified.
    - The resolver is constructible and testable without an order or an HTTP context.
    - The full pos-service suite passes.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/muhammadumer/Documents/Projects/ResturantOS &amp;&amp; mvn -q -pl services/pos-service -am test -Dtest='StationRoutingResolverTest,RlsForcedInvariantIT'</automated>
  </verify>
  <done>An item's destination is a per-branch answer, and the legacy column can no longer answer for the wrong branch.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the resolver into assignment and into add-item (D-28-04, D-28-06)</name>
  <files>services/pos-service/src/main/java/io/restaurantos/pos/service/MenuServiceImpl.java, services/pos-service/src/main/java/io/restaurantos/pos/service/OrderServiceImpl.java, services/pos-service/src/main/java/io/restaurantos/pos/dto/MenuItemDto.java, services/pos-service/src/main/java/io/restaurantos/pos/web/MenuController.java, services/pos-service/src/test/java/io/restaurantos/pos/MenuStationRoutingIT.java</files>
  <read_first>
    - services/pos-service/src/main/java/io/restaurantos/pos/service/OrderServiceImpl.java lines 230-260 — `addItem`, its tenant-scoped menu lookup, and the comment explaining that both routing keys are snapshotted at add time and never at fire time
    - services/pos-service/src/main/java/io/restaurantos/pos/service/OrderServiceImpl.java lines 400-540 — `sendToKds`, the batched station lookup, and the three-step code resolution it performs at fire time
    - services/pos-service/src/main/java/io/restaurantos/pos/service/MenuServiceImpl.java — `assignStation` and `requireOwnBranchIfPresent`
    - services/pos-service/src/main/java/io/restaurantos/pos/web/MenuController.java — where the assign-station endpoint is mounted and what gates it
    - .planning/phases/13-platform-tenant-access-repair/13-16-SUMMARY.md — the section on a guard that only fires when a value is present, which is the failure mode to avoid when adding a conditional resolution step
  </read_first>
  <behavior>
    - Assigning a station through the existing endpoint creates a route row for the caller's branch and leaves other branches' routing untouched.
    - Assigning at branch B and then reading the effective station at branch A returns A's own answer, not B's.
    - Clearing an assignment removes the branch's route and falls through to the category route if one exists.
    - Assigning a category-level route routes every item in that category that has no item-level route.
    - Adding an item to an order snapshots the station resolved for the ORDER's branch, at add time.
    - A route changed after a line was added does not re-route that already-added line.
    - A route changed after a line was added DOES apply to a line added afterwards on the same order.
    - Firing an order still produces the same event shape, with the code resolved from the snapshot.
    - A menu item response carries the effective station for the requested branch.
  </behavior>
  <action>
    Run GitNexus impact analysis on `OrderServiceImpl.addItem`, `OrderServiceImpl.sendToKds` and
    `MenuServiceImpl.assignStation` before editing. `addItem` and `sendToKds` are on the order path and
    the fire path respectively; this is the highest-risk edit in the phase.

    Change `assignStation` to write a route row for the caller's branch instead of overwriting the
    tenant-wide column. Keep writing the legacy column and the free-text mirror in step for the caller's
    branch as well, for one release: the mirror is still read by the order-ready consumer on the POS
    side, and retiring it is a later phase's job, not a side effect of this one. Say that in a comment
    with the condition under which it may be removed, so it does not become permanent by forgetting.

    Change `addItem` to resolve the station through `StationRoutingResolver`, using the ORDER's own
    branch — the server-derived one the order already carries — and not any branch supplied on the
    request. The order is the authority for its own branch; this is the same reasoning the existing
    branch-override pricing lookup already applies two lines away.

    Preserve the snapshot invariant exactly. Both keys are still captured at add time and never at fire
    time, so a menu re-assignment never retroactively re-routes an already-added line. That comment is
    load-bearing and must survive the edit intact; the two behaviours asserting it are the regression
    guard.

    Leave `sendToKds` alone apart from anything strictly required to keep compiling. It already resolves
    the canonical code from the snapshot with the correct fallbacks, and touching the fire path to
    achieve a routing change would put a second resolution rule in the system.

    Add the effective station to the menu item response for a requested branch, so plan 28-10's picker
    can render what an item currently does without recomputing the resolution order in TypeScript.

    Write `MenuStationRoutingIT` covering the nine behaviours. The two-branch independence case must use
    two real branches and assert in both directions; asserting only that B changed proves nothing about
    whether A moved, and A moving is the bug.
  </action>
  <acceptance_criteria>
    - All nine behaviours pass.
    - The add-time snapshot comment and behaviour are intact, with a test that fails if resolution moves to fire time.
    - The existing order, fire and menu suites pass unmodified.
    - `StationAdminIT` and `RlsForcedInvariantIT` still pass.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/muhammadumer/Documents/Projects/ResturantOS &amp;&amp; mvn -q -pl services/pos-service -am test -Dtest='MenuStationRoutingIT,StationRoutingResolverTest,OrderRevisionIT,StationAdminIT'</automated>
  </verify>
  <done>Two branches can send the same dish to two different stations, and neither can overwrite the other.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (this plan's share)

**New symbols:** `MenuItemStationRoute`, `MenuCategoryStationRoute`, their repositories,
`StationRoutingResolver`.
**Changed behaviour:** `POST` assign-station now writes a per-branch route; `addItem` resolves through
the resolver; `MenuItemDto` gains the effective station for a requested branch.
**Migrations:** pos V15.
**Deliberately retained:** `menu_items.station_id` and `menu_items.kds_station` keep being written.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tenant admin to pos-service | Route configuration arrives with client-supplied item, category and station ids |
| pos-service to pos_db | Two new tenant-scoped tables under forced row-level security |
| order path | A client-supplied menu item id becomes a routing decision written onto an order line |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-28-05-A | Tampering | Cross-branch routing overwrite | high | mitigate | Routes are branch-keyed and the legacy column is only consulted when its station belongs to the resolving branch; both directions asserted with two real branches |
| T-28-05-B | Information Disclosure | New tables shipping ENABLE-only | critical | mitigate | Both tables carry a policy and are ENABLEd and FORCEd; the invariant test is not modified to accommodate them |
| T-28-05-C | Elevation of Privilege | Route to a foreign branch's station | high | mitigate | Station ids are resolved inside the caller's branch before a route row is written, reusing the existing branch-scoped finder |
| T-28-05-D | Tampering | Client-supplied branch on add-item | high | mitigate | Resolution uses the order's own server-derived branch, never a request parameter |
| T-28-05-E | Repudiation | A fired line silently changing destination | medium | mitigate | The add-time snapshot is preserved and guarded by two behaviours that fail if resolution moves to fire time |
| T-28-05-F | Denial of Service | Silent fallthrough to the default station on a wiring break | medium | accept | Unavoidable under FORCE, where an unscoped read is empty rather than an error; mitigated by explicit tenant predicates and named in this register so it is triaged as wiring, not configuration |
| T-28-05-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new packages. If one becomes necessary, halt and require the package-legitimacy audit plus a blocking human checkpoint |
</threat_model>

<verification>
1. Both new suites pass, eighteen behaviours in total.
2. The order, revision and fire suites pass with no assertion changed.
3. `RlsForcedInvariantIT` passes unmodified.
4. `detect_changes()` before committing, with particular attention to `OrderServiceImpl` — `addItem` should appear and `sendToKds` should not, beyond compilation.
</verification>

<success_criteria>
- A shared menu item routes per branch.
- A category-level rule covers a whole section of the menu in one row.
- Assigning at one branch provably does not move another branch.
- A tenant who configures nothing sees no change at all.
</success_criteria>

<output>
Create `.planning/phases/28-station-pos-profiles/28-05-SUMMARY.md`, recording the resolver's exact
signature and its five-step order — plan 28-08 asserts against it and plan 28-10 renders it.
</output>
