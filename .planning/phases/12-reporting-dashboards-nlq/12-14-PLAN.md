---
phase: 12-reporting-dashboards-nlq
plan: 14
type: execute
wave: 1
depends_on: []
files_modified:
  - services/auth-service/src/main/java/io/restaurantos/auth/service/ProvisioningAdminService.java
  - services/auth-service/src/main/java/io/restaurantos/auth/controller/AuthProvisioningInternalController.java
  - services/platform-admin-service/src/main/java/io/restaurantos/platform/service/ImpersonationService.java
  - services/auth-service/src/test/java/io/restaurantos/auth/service/ProvisioningAdminServiceImpersonateTest.java
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "The real POST /internal/auth/users/{userId}/impersonate endpoint returns a token (200) for a user that exists, instead of 500 'Target user not found'."
    - "An impersonation JWT issued by the REAL endpoint, used against POST /api/v1/nlq/query, lands a nlq_query_log row whose impersonated_by equals the admin's UUID (proving issuance + stamp end-to-end, replacing 12-10's self-signed-JWT workaround)."
    - "ProvisioningAdminService.impersonate sets the RLS tenant GUC before userRepository.findById, reusing the established set_config pattern (2099ac0), not a TenantAwareDataSource re-roll."
  artifacts:
    - path: "services/auth-service/src/main/java/io/restaurantos/auth/service/ProvisioningAdminService.java"
      provides: "impersonate(tenantId, targetUserId, impersonatedBy, expiresInSeconds) sets set_config('app.current_tenant_id', tenantId) before the RLS-scoped findById, in the same @Transactional"
      contains: "set_config"
    - path: "services/platform-admin-service/src/main/java/io/restaurantos/platform/service/ImpersonationService.java"
      provides: "passes the known target tenantId to auth-service in the impersonate request body"
      contains: "tenantId"
  key_links:
    - from: "platform-admin ImpersonationService.impersonate(tenantId, ...)"
      to: "auth-service /internal/auth/users/{userId}/impersonate"
      via: "tenantId added to the Feign request body Map"
      pattern: "tenantId"
    - from: "ProvisioningAdminService.impersonate"
      to: "RLS-scoped users SELECT"
      via: "set_config('app.current_tenant_id', tenantId, true) before findById"
      pattern: "set_config"
---

<objective>
Close GAP C (12-10-E2E §1g) — the real impersonation-issuance endpoint 500s.
`ProvisioningAdminService.impersonate` calls `userRepository.findById(targetUserId)` with NO tenant
GUC set at all, so the RLS-scoped `users` SELECT always misses and throws
`IllegalArgumentException: Target user not found` for a user that demonstrably exists. This is the
exact bug CLASS as the historical `2099ac0` RLS-tenant-GUC fix, recurring in a new internal
endpoint. (The NLQ impersonation STAMP itself already works — proven in §6 via a self-signed JWT;
only the issuance endpoint is broken.)

Purpose: The tenant is knowable — platform-admin's `ImpersonationService.impersonate` already
receives the target `tenantId` (its controller path is `/tenants/{tenantId}/impersonate`) but drops
it at the platform-admin→auth-service Feign seam. Thread it through and set the tenant GUC before
the RLS lookup. This lets the real end-to-end impersonation flow work without the self-signed-JWT
workaround.

Output: auth-service sets the tenant GUC (established `set_config` pattern) before `findById`; the
real issuance endpoint returns a token; the stamp lands in `nlq_query_log` end-to-end on the real
stack.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/12-reporting-dashboards-nlq/12-E2E-EVIDENCE.md
@services/auth-service/src/main/java/io/restaurantos/auth/service/ProvisioningAdminService.java
@services/auth-service/src/main/java/io/restaurantos/auth/controller/AuthProvisioningInternalController.java
@services/platform-admin-service/src/main/java/io/restaurantos/platform/service/ImpersonationService.java
@services/platform-admin-service/src/main/java/io/restaurantos/platform/client/AuthInternalClient.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Thread tenantId through the seam and set the tenant GUC before findById</name>
  <files>services/auth-service/src/main/java/io/restaurantos/auth/service/ProvisioningAdminService.java, services/auth-service/src/main/java/io/restaurantos/auth/controller/AuthProvisioningInternalController.java, services/platform-admin-service/src/main/java/io/restaurantos/platform/service/ImpersonationService.java</files>
  <action>
Three coordinated edits (no shared file — safe to do in one plan):

A. **platform-admin `ImpersonationService.impersonate`** (it already has `tenantId`): add the
   tenant to the Feign request body Map it sends to auth-service:
   ```java
   authClient.impersonate(targetUserId,
       Map.of("tenantId", tenantId.toString(),
              "impersonatedBy", adminUserId.toString(),
              "expiresInSeconds", DEFAULT_TTL_SECONDS));
   ```
   (No change needed to `AuthInternalClient` — it already takes `Map<String,Object>`.)

B. **auth-service `AuthProvisioningInternalController`**: extend the `ImpersonateRequest` record to
   `record ImpersonateRequest(UUID tenantId, UUID impersonatedBy, int expiresInSeconds) {}` and pass
   `request.tenantId()` into the service call:
   `provisioningAdminService.impersonate(request.tenantId(), userId, request.impersonatedBy(), request.expiresInSeconds())`.

C. **auth-service `ProvisioningAdminService.impersonate`**: change the signature to
   `impersonate(UUID tenantId, UUID targetUserId, UUID impersonatedBy, int expiresInSeconds)` and,
   as the FIRST statement inside the (already `@Transactional`) method, set the RLS tenant GUC
   before `findById`, reusing the established `set_config` pattern already used elsewhere in
   auth-service (`AuthServiceImpl` sets it via
   `entityManager.createNativeQuery("SELECT set_config('app.current_tenant_id', :tid, true)")`):
   - Inject `jakarta.persistence.EntityManager` (add a constructor field) into
     `ProvisioningAdminService`.
   - At method start:
     ```java
     entityManager.createNativeQuery("SELECT set_config('app.current_tenant_id', :tid, true)")
         .setParameter("tid", tenantId.toString())
         .getSingleResult();
     ```
   - Then the existing `userRepository.findById(targetUserId)` (and the subsequent
     `permissionResolver.resolveDefault(targetUserId)`, which reads the same RLS-scoped tables) both
     run under the correct tenant. Because the GUC is transaction-local (`set_config(..., true)`)
     and the method is `@Transactional`, the GUC is set on the same connection/transaction as the
     query — do NOT set it outside the transaction (avoid the historical GUC-before-BEGIN bug).
   - Do NOT re-roll `TenantAwareDataSource`; do NOT weaken RLS.

   Guard: if `tenantId` is null (older caller), keep prior behaviour (no GUC) so nothing regresses —
   but the real caller (platform-admin) now always sends it.
  </action>
  <verify>
`cd services/auth-service && mvn -q -DskipTests compile` and
`cd services/platform-admin-service && mvn -q -DskipTests compile` both succeed.
`grep -n "set_config\|EntityManager" services/auth-service/src/main/java/io/restaurantos/auth/service/ProvisioningAdminService.java` shows the GUC set before findById.
`grep -n "tenantId" services/platform-admin-service/src/main/java/io/restaurantos/platform/service/ImpersonationService.java` shows the tenant added to the request.
  </verify>
  <done>tenantId flows platform-admin → auth-service; the tenant GUC is set (established set_config pattern) inside the transaction before the RLS-scoped findById/resolveDefault.</done>
</task>

<task type="auto">
  <name>Task 2: Test — impersonate sets the tenant GUC before the RLS lookup</name>
  <files>services/auth-service/src/test/java/io/restaurantos/auth/service/ProvisioningAdminServiceImpersonateTest.java</files>
  <action>
Add a unit test (follow auth-service's existing service-test conventions; mock `EntityManager`,
`UserRepository`, `PermissionResolver`, `JwtSigningService`). Assert:

1. `impersonate(tenantId, targetUserId, impersonatedBy, ttl)` issues the
   `set_config('app.current_tenant_id', tenantId, true)` native query (verify the
   `createNativeQuery(...).setParameter("tid", tenantId.toString())` interaction) BEFORE
   `userRepository.findById(targetUserId)` is invoked (use `InOrder` verification).
2. With the target user present (mocked) and a resolvable branch auth, it returns an
   `ImpersonateResult` with a non-null token stamped via
   `jwtSigningService.signImpersonationToken(claims, impersonatedBy, ttl)`.

If a real-DB RLS integration test already exists for this class, prefer extending it; otherwise the
mock-level ordering assertion is the regression guard and Task 3 provides the live proof.
  </action>
  <verify>
`cd services/auth-service && mvn -q -Dtest=ProvisioningAdminServiceImpersonateTest test` → passes.
  </verify>
  <done>The GUC-before-findById ordering is pinned against regression.</done>
</task>

<task type="auto">
  <name>Task 3: REAL-STACK proof — real issuance endpoint returns a token AND the stamp lands live</name>
  <files>services/auth-service/src/main/java/io/restaurantos/auth/service/ProvisioningAdminService.java</files>
  <action>
An IT alone is insufficient (§1g's endpoint could be green at the IT level and still miss live
under real RLS). Bring up ONLY what this needs (8GB host, OOM-sensitive):

- Docker infra via `scripts/dev-stack-up.sh` (postgres, redis, eureka, config-server, clickhouse).
  Stop any native :5432 Postgres first.
- Host-run JVMs via `nohup java -jar ... -Xmx300m & disown`, health-gated: auth-service (rebuilt),
  authorization-service (if the NLQ authorize hop needs it), user-service, nlq-service, and gateway.
  platform-admin-service only if you drive the outer `/tenants/{id}/impersonate` path; otherwise you
  can hit auth-service's internal endpoint directly with the X-Internal-Service secret.

Then:
1. Call the REAL issuance endpoint (the one §1g showed 500ing) for a user that exists
   (`c0000002-...`, tenant `a0000001-...`, admin/superadmin `ea07bc72-...`):
   `curl -s -i -X POST "http://localhost:<auth-port>/internal/auth/users/c0000002-0000-4000-8000-000000000002/impersonate" \
      -H "X-Internal-Service: <secret>" -H "Content-Type: application/json" \
      -d '{"tenantId":"a0000001-0000-4000-8000-000000000001","impersonatedBy":"ea07bc72-7c5c-4734-87d2-75ae388b5fd7","expiresInSeconds":1800}'`
   → assert HTTP 200 and a non-empty `data.token` (NOT 500 / "Target user not found"). (Or drive the
   full path through platform-admin's `POST /tenants/{tenantId}/impersonate` to prove the whole seam.)
2. Use that REAL impersonation token against the gateway:
   `curl -s -X POST http://localhost:8080/api/v1/nlq/query -H "Authorization: Bearer <real impersonation jwt>" -d '{"question":"impersonated issuance e2e"}'`.
3. Assert the stamp landed:
   `psql <nlq_db> -c "SELECT question, user_id, impersonated_by FROM nlq_query_log WHERE question='impersonated issuance e2e'"`
   → `impersonated_by` == `ea07bc72-7c5c-4734-87d2-75ae388b5fd7` (the real admin UUID), `user_id` ==
   the impersonated target. This replaces §6's self-signed-JWT workaround with a REAL issued token.
   (NLQ can run against the local Claude stub per §1i — the stamp path does not need a real key.)

Record the verbatim 200 + token and the `nlq_query_log` row in the SUMMARY. If host memory prevents
this reduced set, document exactly which service could not co-reside and give the exact runnable
command block — do NOT claim a pass that did not run.
  </action>
  <verify>
The real `/internal/auth/users/{id}/impersonate` returns 200 + a token (no 500); the issued token
drives an NLQ call whose `nlq_query_log.impersonated_by` equals the admin UUID. Verbatim captures in
the SUMMARY.
  </verify>
  <done>GAP C closed on the real stack: the real impersonation-issuance endpoint works and its stamp lands end-to-end, no self-signed workaround needed.</done>
</task>

</tasks>

<verification>
- `cd services/auth-service && mvn -q test` and `cd services/platform-admin-service && mvn -q -DskipTests compile` green.
- Real `/internal/auth/users/{id}/impersonate` → 200 + token (verbatim).
- Real issued token → `nlq_query_log.impersonated_by` == admin UUID (verbatim).
- GUC set via the established `set_config` pattern inside the transaction; no `TenantAwareDataSource` re-roll; RLS intact.
</verification>

<success_criteria>
- The §1g 500 becomes a 200 with a token for an existing user.
- The stamp is proven end-to-end with a REAL issued token (not self-signed).
- Regression test pins GUC-before-findById.
</success_criteria>

<output>
After completion, create `.planning/phases/12-reporting-dashboards-nlq/12-14-SUMMARY.md`.
</output>
