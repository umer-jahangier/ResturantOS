---
phase: 12-reporting-dashboards-nlq
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pom.xml
  - services/reporting-service/pom.xml
  - services/reporting-service/src/main/java/io/restaurantos/reporting/ReportingServiceApplication.java
  - services/reporting-service/src/main/java/io/restaurantos/reporting/config/ReportingSecurityConfig.java
  - services/reporting-service/src/main/java/io/restaurantos/reporting/config/FeignClientConfig.java
  - services/reporting-service/src/main/resources/application.yml
  - services/nlq-service/pom.xml
  - services/nlq-service/src/main/java/io/restaurantos/nlq/NlqServiceApplication.java
  - services/nlq-service/src/main/java/io/restaurantos/nlq/config/NlqSecurityConfig.java
  - services/nlq-service/src/main/java/io/restaurantos/nlq/config/FeignClientConfig.java
  - services/nlq-service/src/main/resources/application.yml
  - gateway/src/main/resources/application.yml
  - services/platform-admin-service/src/main/java/io/restaurantos/platform/config/TierFeatureDefaults.java
  - deploy/docker-compose.yml
  - deploy/.env.example
autonomous: true

user_setup:
  - service: anthropic
    why: "nlq-service calls the Claude Messages API to convert natural language to SQL (NLQ-01)."
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: "console.anthropic.com -> Settings -> API keys -> Create key"

must_haves:
  truths:
    - "reporting-service and nlq-service both build as Maven modules and boot, registering in Eureka"
    - "GET /api/v1/reporting/** routes through the gateway to reporting-service (not a gateway 404)"
    - "POST /api/v1/nlq/** routes through the gateway to nlq-service and is NOT rejected with FEATURE_DISABLED for a STARTER-tier tenant"
    - "Reporting routes are NOT feature-gated (core), so no phantom-flag 403 is possible"
  artifacts:
    - path: "services/reporting-service/pom.xml"
      provides: "reporting-service Maven module (Spring Boot, clickhouse-jdbc, amqp, websocket, flyway, feign)"
      contains: "clickhouse-jdbc"
    - path: "services/nlq-service/pom.xml"
      provides: "nlq-service Maven module (Spring Boot, JSqlParser, clickhouse-jdbc, redis, flyway, feign)"
      contains: "jsqlparser"
    - path: "services/reporting-service/src/main/java/io/restaurantos/reporting/config/FeignClientConfig.java"
      provides: "Internal Feign interceptor with caller-JWT forwarding (commit 990026a fix)"
      contains: "forwardCallerJwt"
    - path: "services/nlq-service/src/main/java/io/restaurantos/nlq/config/FeignClientConfig.java"
      provides: "Internal Feign interceptor with caller-JWT forwarding (commit 990026a fix)"
      contains: "forwardCallerJwt"
    - path: "pom.xml"
      provides: "Both new services declared as reactor modules"
      contains: "services/nlq-service"
    - path: "services/platform-admin-service/src/main/java/io/restaurantos/platform/config/TierFeatureDefaults.java"
      provides: "FEATURE_NLQ granted at some tier so the gateway's FeatureFlagGlobalFilter can pass"
      contains: "FEATURE_NLQ"
  key_links:
    - from: "gateway/src/main/resources/application.yml"
      to: "lb://reporting-service"
      via: "reporting-route Path=/api/v1/reporting/**"
      pattern: "reporting-route"
    - from: "gateway/src/main/resources/application.yml"
      to: "lb://nlq-service"
      via: "nlq-route Path=/api/v1/nlq/**"
      pattern: "id: nlq-route"
    - from: "services/reporting-service/src/main/java/io/restaurantos/reporting/config/FeignClientConfig.java"
      to: "HttpHeaders.AUTHORIZATION"
      via: "forwardCallerJwt reads the inbound ServletRequestAttributes"
      pattern: "ServletRequestAttributes"
---

<objective>
Create the two new Spring Boot services this phase needs — `reporting-service` (port 8092) and `nlq-service` (port 8094) — as buildable, bootable, Eureka-registered skeletons, wire their gateway routes, and fix the phantom `FEATURE_NLQ` flag that would otherwise 403 every NLQ request.

Purpose: This plan owns every SHARED file in Phase 12 (root `pom.xml`, `gateway/application.yml`, `TierFeatureDefaults.java`, `deploy/`). Every downstream Phase-12 plan then works only inside its own service directory, so plans 12-03..12-09 can run in parallel with zero file conflicts.

Output: Two compiling, booting services + two gateway routes + a real FEATURE_NLQ grant + docker-compose/env entries. No business logic yet.

BINDING DECISION (do not revisit): `nlq-service` is **Java / Spring Boot**, not Python. It reuses shared-lib's `JwksKeyProvider`, `TenantContext`, `JwtClaims`, Eureka client, Config Server client, and the internal-JWT Feign pattern exactly like every other service. SQL AST validation uses **JSqlParser** (not sqlglot). The 12-RESEARCH.md sections recommending Python/FastAPI/sqlglot/Alembic are SUPERSEDED — ignore them.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-reporting-dashboards-nlq/12-RESEARCH.md

# The scaffold to clone (freshest service, Phase 10):
@services/purchasing-service/pom.xml
@services/purchasing-service/src/main/resources/application.yml
@services/purchasing-service/src/main/java/io/restaurantos/purchasing/config/FeignClientConfig.java
@services/purchasing-service/src/main/java/io/restaurantos/purchasing/config/PurchasingSecurityConfig.java
@services/purchasing-service/src/main/java/io/restaurantos/purchasing/PurchasingServiceApplication.java

# Shared files this plan owns:
@pom.xml
@gateway/src/main/resources/application.yml
@gateway/src/main/java/io/restaurantos/gateway/support/RouteFeatureMap.java
@services/platform-admin-service/src/main/java/io/restaurantos/platform/config/TierFeatureDefaults.java
@deploy/docker-compose.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold reporting-service as a Maven module that builds and boots</name>
  <files>
    pom.xml
    services/reporting-service/pom.xml
    services/reporting-service/src/main/java/io/restaurantos/reporting/ReportingServiceApplication.java
    services/reporting-service/src/main/java/io/restaurantos/reporting/config/ReportingSecurityConfig.java
    services/reporting-service/src/main/java/io/restaurantos/reporting/config/FeignClientConfig.java
    services/reporting-service/src/main/resources/application.yml
  </files>
  <action>
Clone `services/purchasing-service`'s scaffold. Concretely:

1. Add `<module>services/reporting-service</module>` to the root `pom.xml` `<modules>` block (after `services/kitchen-service`).

2. `services/reporting-service/pom.xml` — copy purchasing-service's pom verbatim, change `<artifactId>` to `reporting-service`, then:
   - REMOVE the `spring-boot-starter-validation` dep? NO — keep it, reports take validated query params.
   - ADD `org.springframework.boot:spring-boot-starter-websocket` (dashboard WS, plan 12-06).
   - ADD `org.springframework.boot:spring-boot-starter-data-redis` (KPI tile cache, plan 12-06).
   - ADD `com.clickhouse:clickhouse-jdbc` with `<classifier>http</classifier>`. Resolve the current version at implementation time — run
     `curl -s "https://search.maven.org/solrsearch/select?q=g:com.clickhouse+AND+a:clickhouse-jdbc&core=gav&rows=5&wt=json" | head -c 2000`
     and pin the newest stable (non-alpha/beta) version. Do NOT guess.
   - KEEP the flyway deps (Postgres side-tables use Flyway — this resolves 12-RESEARCH Open Question 3: **Flyway, matching purchasing-service, the freshest reference**).
   - KEEP spring-boot-starter-amqp (ETL consumers, plan 12-03).
   - DROP `EncryptionRequiredConfig`-related concerns — reporting-service stores no encrypted fields, so do NOT copy purchasing's `EncryptionRequiredConfig.java` and do NOT set `restaurantos.encryption.key`.

3. `ReportingServiceApplication.java` — `@SpringBootApplication`, `@EnableFeignClients(basePackages = "io.restaurantos.reporting.feign")`, `@EnableDiscoveryClient` if purchasing has it (match exactly).

4. `ReportingSecurityConfig.java` — copy `PurchasingSecurityConfig` verbatim, renaming the class. It must permit `/actuator/**` and the WS handshake path `/api/v1/reporting/dashboard/**` (the WS handler does its own JWT-in-query-param check, exactly like kitchen-service's KDS path — check how kitchen-service's `KitchenSecurityConfig` permits its WS path and mirror that).

5. `FeignClientConfig.java` — copy `services/purchasing-service/.../config/FeignClientConfig.java` **VERBATIM** including the full `forwardCallerJwt()` method AND its javadoc. This is the commit-`990026a` fix: sending only `X-Internal-Service` without the caller's `Authorization` header makes every `/internal/authorize` call 401, which surfaces as a 503. Do not "improve" or condense it.

6. `application.yml` — copy purchasing's, change:
   - `server.port: ${SERVER_PORT:8092}`
   - `spring.application.name: reporting-service`
   - datasource: `${REPORTING_DB_URL:jdbc:postgresql://localhost:5432/reporting_db}` / `reporting_user` / `reporting_pass`
   - keep the `spring.data.redis` block and the `restaurantos.platform-admin.uri` block (the comments in purchasing's yml explain why: without them every `@RequiresFeature`-gated endpoint 500s / 403s)
   - keep `restaurantos.jwks.uri` and `restaurantos.internal.secret`
   - DROP `restaurantos.encryption.key` and `restaurantos.inventory.integration-mode`
   - ADD:
     ```yaml
     restaurantos:
       clickhouse:
         url: ${CLICKHOUSE_URL:http://localhost:8123}
         database: ${CLICKHOUSE_DB:clickhouse_analytics}
         user: ${CLICKHOUSE_USER:default}
         password: ${CLICKHOUSE_PASSWORD:}
       business-day:
         offset-hours: ${BUSINESS_DAY_OFFSET_HOURS:4}
     ```
     (`BUSINESS_DAY_OFFSET_HOURS` is already scoped to `pos, finance, reporting` in `Docs/agent-specs/05-environment-variables.md:47` — reporting-service is its intended consumer.)

DO NOT create a `TenantAwareDataSource` or any DataSource wrapper. reporting-service inherits the shared-lib one (commit `2099ac0` fixed the RLS tenant-GUC-before-BEGIN bug there); re-rolling it would reintroduce that bug.
  </action>
  <verify>
`mvn -q -pl services/reporting-service -am compile` exits 0.
`grep -c "forwardCallerJwt" services/reporting-service/src/main/java/io/restaurantos/reporting/config/FeignClientConfig.java` returns >= 2 (declaration + call site).
`grep -rn "TenantAwareDataSource" services/reporting-service/` returns NOTHING (proves we did not vendor a copy).
  </verify>
  <done>reporting-service compiles as part of the reactor build, declares clickhouse-jdbc, and its FeignClientConfig contains the verbatim forwardCallerJwt fix.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold nlq-service (Java/Spring Boot) as a Maven module that builds and boots</name>
  <files>
    pom.xml
    services/nlq-service/pom.xml
    services/nlq-service/src/main/java/io/restaurantos/nlq/NlqServiceApplication.java
    services/nlq-service/src/main/java/io/restaurantos/nlq/config/NlqSecurityConfig.java
    services/nlq-service/src/main/java/io/restaurantos/nlq/config/FeignClientConfig.java
    services/nlq-service/src/main/resources/application.yml
  </files>
  <action>
Same clone-from-purchasing procedure, for `nlq-service`.

1. Add `<module>services/nlq-service</module>` to the root `pom.xml` `<modules>` block.

2. `services/nlq-service/pom.xml` — clone purchasing's, artifactId `nlq-service`, then:
   - ADD `com.github.jsqlparser:jsqlparser`. This is the SQL AST parser for the 7-stage validator (plan 12-04). Resolve the current version:
     `curl -s "https://search.maven.org/solrsearch/select?q=g:com.github.jsqlparser+AND+a:jsqlparser&core=gav&rows=5&wt=json" | head -c 2000`
     and pin the newest stable. JSqlParser is chosen over Apache Calcite because we need *parse + AST walk + targeted WHERE-clause injection* on a small SQL subset, not a full query planner/optimizer — Calcite's value (relational algebra, cost-based planning, adapters) is dead weight here and its parser is harder to constrain.
   - ADD `com.clickhouse:clickhouse-jdbc` (`<classifier>http</classifier>`, same version as reporting-service) — the read-only query executor.
   - ADD `org.springframework.boot:spring-boot-starter-data-redis` (quota counters + 60s result cache).
   - ADD `org.springframework.boot:spring-boot-starter-webflux` ONLY if you use `WebClient` for the Anthropic call. Prefer plain `java.net.http.HttpClient` (JDK 25, zero new deps) — if you take that route, add nothing.
   - KEEP flyway + postgresql (nlq_db holds `nlq_query_log` and `nlq_allowed_tables`).
   - KEEP spring-boot-starter-amqp? NO — nlq-service consumes no events. Remove it.
   - DROP the encryption concerns as above.

3. `NlqServiceApplication.java` — `@SpringBootApplication`, `@EnableFeignClients(basePackages = "io.restaurantos.nlq.feign")`.

4. `NlqSecurityConfig.java` — clone `PurchasingSecurityConfig`. All `/api/v1/nlq/**` endpoints are authenticated (JWT validated at the service, defence-in-depth per PROJECT.md Security constraint — the gateway check is not sufficient on its own).

5. `FeignClientConfig.java` — copy purchasing's **VERBATIM** (same `forwardCallerJwt()` + javadoc). nlq-service will Feign-call user-service/authorization-service; the 990226a-class bug applies identically.

6. `application.yml`:
   - `server.port: ${SERVER_PORT:8094}`, `spring.application.name: nlq-service`
   - datasource `${NLQ_DB_URL:jdbc:postgresql://localhost:5432/nlq_db}` / `nlq_user` / `nlq_pass`, flyway enabled
   - `spring.data.redis` block (required — quota + cache)
   - `restaurantos.jwks.uri`, `restaurantos.internal.secret`, `restaurantos.platform-admin.uri` (as purchasing)
   - ADD:
     ```yaml
     restaurantos:
       clickhouse:
         url: ${CLICKHOUSE_URL:http://localhost:8123}
         database: ${CLICKHOUSE_DB:clickhouse_analytics}
         readonly-user: ${CLICKHOUSE_READONLY_USER:nlq_readonly}
         readonly-password: ${CLICKHOUSE_READONLY_PASSWORD:}
       nlq:
         anthropic:
           api-key: ${ANTHROPIC_API_KEY:}
           base-url: ${ANTHROPIC_BASE_URL:https://api.anthropic.com}
           model-sql: ${ANTHROPIC_MODEL_SQL:claude-sonnet-4-6}
           model-narrative: ${ANTHROPIC_MODEL_NARRATIVE:claude-haiku-4-5}
         monthly-quota-default: ${NLQ_MONTHLY_QUOTA_DEFAULT:500}
         user-hourly-limit: ${NLQ_USER_HOURLY_LIMIT:30}
         max-result-rows: ${NLQ_MAX_RESULT_ROWS:10000}
         default-limit: ${NLQ_DEFAULT_LIMIT:1000}
         timeout-seconds: ${NLQ_TIMEOUT_SECONDS:5}
         cache-ttl-seconds: ${NLQ_CACHE_TTL_SECONDS:60}
     ```

MODEL IDs ARE LOAD-BEARING: use `claude-sonnet-4-6` (SQL generation) and `claude-haiku-4-5` (narrative). `Docs/agent-specs/05-environment-variables.md:152-153` hardcodes `claude-sonnet-4-20250514` / `claude-haiku-4-20250514` — the first is a stale legacy snapshot, the second **is not a real model ID at all** and would hard-fail the API. Use the current aliases above. Do not invent dated IDs.
  </action>
  <verify>
`mvn -q -pl services/nlq-service -am compile` exits 0.
`grep -c "forwardCallerJwt" services/nlq-service/src/main/java/io/restaurantos/nlq/config/FeignClientConfig.java` returns >= 2.
`grep -n "jsqlparser" services/nlq-service/pom.xml` returns a match with a concrete pinned version.
`grep -rn "sqlglot\|fastapi\|alembic" services/nlq-service/` returns NOTHING (proves the Java decision was honoured).
`grep -n "claude-sonnet-4-6\|claude-haiku-4-5" services/nlq-service/src/main/resources/application.yml` returns both.
  </verify>
  <done>nlq-service compiles as a Java Spring Boot Maven module with JSqlParser pinned, current Claude model aliases configured, and the verbatim forwardCallerJwt fix.</done>
</task>

<task type="auto">
  <name>Task 3: Wire gateway routes, fix the phantom FEATURE_NLQ flag, and add deploy/env entries</name>
  <files>
    gateway/src/main/resources/application.yml
    services/platform-admin-service/src/main/java/io/restaurantos/platform/config/TierFeatureDefaults.java
    deploy/docker-compose.yml
    deploy/.env.example
  </files>
  <action>
**3a. Gateway routes** (`gateway/src/main/resources/application.yml`):
Add `reporting-route` as a REAL (uncommented) route immediately after `kitchen-route`, matching the exact shape of `purchasing-route`:
```yaml
            # ── Reporting Service (reports, FBR, realtime dashboard WS) ──────────
            # No feature gate in RouteFeatureMap — reports are core for every ACTIVE
            # tenant (same rationale as pos-route). Do NOT add /api/v1/reporting/ to
            # RouteFeatureMap: FEATURE_REPORTING_ADVANCED is GROWTH-and-above only, so
            # gating this path would 403 every STARTER tenant out of basic reporting.
            - id: reporting-route
              uri: lb://reporting-service
              predicates:
                - Path=/api/v1/reporting/**
              filters:
                - name: CircuitBreaker
                  args:
                    name: reportingCircuitBreaker
                    fallbackUri: forward:/fallback/service-unavailable
                    statusCodes:
                      - 500
                      - 503
```
Then UNCOMMENT the existing `nlq-route` stub (currently commented, in the "Feature-flagged routes (later phases)" block) and bring it to the full shape above (it is missing `statusCodes` in the stub — add them). Move it up next to the live routes; leave `hr-route`/`crm-route`/`inventory-route` commented.

Note: Spring Cloud Gateway proxies the WS upgrade transparently over the same `Path=` predicate (this is how `kitchen-route` already serves the KDS WebSocket) — no extra WS filter is needed for the dashboard socket.

**3b. Fix the phantom FEATURE_NLQ flag** (`TierFeatureDefaults.java`):
This is a REAL, CURRENTLY-BROKEN BUG, not a hypothetical. `RouteFeatureMap.java:39` maps `/api/v1/nlq/` → `FEATURE_NLQ`, and `frontend/lib/features/feature-flags.ts:24` lists `FEATURE_NLQ` in its union — but `TierFeatureDefaults.java` **never defines it in any tier set**, so `tenant_features` is never seeded with it, so `FeatureFlagGlobalFilter` finds no `true` and 403s `FEATURE_DISABLED` on every single NLQ request. This is the identical failure mode to decision 10-11-A (`FEATURE_PURCHASING` was phantom and made the whole Purchasing module unreachable).

Fix: add `"FEATURE_NLQ"` to the `GROWTH_AND_ABOVE` set in `TierFeatureDefaults.java` (NLQ is a premium/AI feature — GROWTH-and-above is the right tier, and it keeps `ALL_TIERS_ON` as the six core modules + KDS). It is automatically picked up by `ALL_KNOWN_FEATURES` via the existing static block.

Do NOT add `/api/v1/reporting/` to `RouteFeatureMap` — see the comment in 3a.

**3c. deploy** (`deploy/docker-compose.yml`, `deploy/.env.example`):
- Add `reporting-service` (8092) and `nlq-service` (8094) service entries following the exact shape of the existing `purchasing-service` entry (image/build, env, depends_on, healthcheck, networks). reporting-service `depends_on` postgres + rabbitmq + redis + clickhouse; nlq-service `depends_on` postgres + redis + clickhouse.
- Add `reporting_db` and `nlq_db` + their users to whatever mechanism creates the other per-service Postgres DBs (check `deploy/init/` for the postgres init script and follow it exactly).
- Add to `deploy/.env.example`: `CLICKHOUSE_READONLY_USER`, `CLICKHOUSE_READONLY_PASSWORD`, `ANTHROPIC_API_KEY`, `ANTHROPIC_MODEL_SQL=claude-sonnet-4-6`, `ANTHROPIC_MODEL_NARRATIVE=claude-haiku-4-5`, `NLQ_MONTHLY_QUOTA_DEFAULT=500`, `NLQ_USER_HOURLY_LIMIT=30`, `REPORTING_DB_*`, `NLQ_DB_*`, `BUSINESS_DAY_OFFSET_HOURS=4`.
  </action>
  <verify>
`grep -n "id: reporting-route" gateway/src/main/resources/application.yml` matches (uncommented — the line must not start with `#`).
`grep -n "^\s*- id: nlq-route" gateway/src/main/resources/application.yml` matches (uncommented).
`grep -n "FEATURE_NLQ" services/platform-admin-service/src/main/java/io/restaurantos/platform/config/TierFeatureDefaults.java` matches.
`grep -n "/api/v1/reporting/" gateway/src/main/java/io/restaurantos/gateway/support/RouteFeatureMap.java` returns NOTHING.
`mvn -q -pl gateway,services/platform-admin-service -am compile` exits 0.
`docker compose -f deploy/docker-compose.yml config -q` exits 0 (validates YAML + interpolation).
  </verify>
  <done>Both routes are live in the gateway, FEATURE_NLQ is granted at GROWTH+ so NLQ requests are not 403'd by the phantom-flag filter, reporting is deliberately ungated, and docker-compose validates.</done>
</task>

</tasks>

<verification>
1. Full reactor build: `mvn -q -DskipTests compile` exits 0 with 17 modules (15 existing + 2 new).
2. Boot both new services against the running dev stack and confirm Eureka registration:
   `curl -s http://localhost:8761/eureka/apps | grep -o "<name>[A-Z-]*</name>" | sort -u` includes `<name>REPORTING-SERVICE</name>` and `<name>NLQ-SERVICE</name>`.
3. Gateway routing reaches the services (a 401/404 from the SERVICE is success; a gateway-level `service-unavailable` fallback or a routing 404 is failure):
   `curl -s -o /dev/null -w "%{http_code}" http://localhost:8080/api/v1/reporting/health` — expect 401/403/404, NOT 503.
4. No Python artifacts anywhere: `find services/nlq-service -name "*.py" -o -name "pyproject.toml" -o -name "requirements.txt"` returns nothing.
</verification>

<success_criteria>
- Root `pom.xml` declares both new modules and the full reactor compiles.
- `reporting-service` (8092) and `nlq-service` (8094) boot and register in Eureka.
- `reporting-route` and `nlq-route` are live in the gateway with CircuitBreaker filters.
- `FEATURE_NLQ` exists in `TierFeatureDefaults` (phantom-flag 403 eliminated).
- Both `FeignClientConfig`s carry the verbatim `forwardCallerJwt()` from commit `990026a`.
- Neither service vendors a `TenantAwareDataSource` (commit `2099ac0` fix inherited from shared-lib).
</success_criteria>

<output>
After completion, create `.planning/phases/12-reporting-dashboards-nlq/12-01-SUMMARY.md`.
Pin in the summary, for downstream plans to consume without renegotiation:
- The exact pinned `clickhouse-jdbc` and `jsqlparser` versions.
- The exact `restaurantos.*` config property names you created.
- The tier at which FEATURE_NLQ was granted.
</output>
