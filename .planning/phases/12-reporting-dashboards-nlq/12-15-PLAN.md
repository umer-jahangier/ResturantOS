---
phase: 12-reporting-dashboards-nlq
plan: 15
type: execute
wave: 1
depends_on: []
files_modified:
  - deploy/.env
  - scripts/e2e/phase12-nlq-live-claude-e2e.sh
autonomous: true
gap_closure: true
user_setup:
  - service: anthropic
    why: "Real NL->SQL round-trip proof requires a live Anthropic API key; deploy/.env ships a placeholder (sk-ant-your-key) that 401s. A real key is an OPS secret that must NOT be committed to the repo."
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: "Anthropic Console -> API keys (https://console.anthropic.com/settings/keys); export at runtime, never commit"

must_haves:
  truths:
    - "deploy/.env's Anthropic model IDs match the code's real defaults (claude-sonnet-4-6 / claude-haiku-4-5), so a running nlq-service calls the correct model rather than the stale dated claude-sonnet-4-20250514 seen live in §1i."
    - "nlq-service at runtime resolves model-sql to claude-sonnet-4-6 (verifiable from config/logs), not the stale dated ID."
    - "A runnable, documented recipe exists to prove the REAL Claude NL->SQL round-trip once a real ANTHROPIC_API_KEY is supplied — and the plan honestly DEFERS that live proof rather than faking it (no real key on this host)."
  artifacts:
    - path: "deploy/.env"
      provides: "Corrected ANTHROPIC_MODEL_SQL / ANTHROPIC_MODEL_NARRATIVE matching the code defaults"
      contains: "claude-sonnet-4-6"
    - path: "scripts/e2e/phase12-nlq-live-claude-e2e.sh"
      provides: "Runnable real-key NL->SQL proof: fails fast if ANTHROPIC_API_KEY is a placeholder, asserts a real (non-stub) Claude response and the correct model in nlq-service logs"
      contains: "ANTHROPIC_API_KEY"
  key_links:
    - from: "deploy/.env ANTHROPIC_MODEL_SQL"
      to: "nlq-service application.yml model-sql: ${ANTHROPIC_MODEL_SQL:claude-sonnet-4-6}"
      via: "env override no longer beats the code default with a stale dated ID"
      pattern: "claude-sonnet-4-6"
---

<objective>
Close GAP D (12-10-E2E §1i) — two distinct problems:

1. **Stale model ID at runtime (fixable now).** The live log showed nlq-service calling
   `claude-sonnet-4-20250514` (a stale dated ID), NOT the `claude-sonnet-4-6` default in
   nlq-service `application.yml`. Root cause: `deploy/.env` overrides `ANTHROPIC_MODEL_SQL` (and
   `ANTHROPIC_MODEL_NARRATIVE`) with stale dated IDs that beat the code default. The application.yml
   comment even flags the dated haiku ID as "not a real model ID". So IF a real key were supplied,
   the WRONG model would be called. This is a real config bug and IS fixable.

2. **Real Claude round-trip unproven (honestly deferred).** `deploy/.env`'s
   `ANTHROPIC_API_KEY=sk-ant-your-key` is a placeholder (401 live), so the real NL->SQL round-trip
   is only IT-proven (`NlqServiceIT`). A real key is an OPS secret that must NOT be committed. Do
   NOT fake green: fix the config, and provide a documented, runnable recipe to prove the live
   round-trip once a real key is supplied.

Purpose: Make the config correct so a real key actually calls the right model, and give a runnable
path to close the live-round-trip evidence gap — without ever claiming a proof that did not run on
this host.

Output: Corrected `deploy/.env` model IDs + a runnable `phase12-nlq-live-claude-e2e.sh` recipe, with
the live round-trip explicitly deferred (real key required).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/12-reporting-dashboards-nlq/12-E2E-EVIDENCE.md
@services/nlq-service/src/main/resources/application.yml
@scripts/e2e/_claude_stub.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct the stale Anthropic model IDs in deploy/.env</name>
  <files>deploy/.env</files>
  <action>
`deploy/.env` currently has (the stale dated IDs that beat the code defaults):
```
ANTHROPIC_MODEL_SQL=claude-sonnet-4-20250514
ANTHROPIC_MODEL_NARRATIVE=claude-haiku-4-20250514
```
Change them to match nlq-service `application.yml`'s real defaults
(`model-sql: ${ANTHROPIC_MODEL_SQL:claude-sonnet-4-6}`,
`model-narrative: ${ANTHROPIC_MODEL_NARRATIVE:claude-haiku-4-5}`):
```
ANTHROPIC_MODEL_SQL=claude-sonnet-4-6
ANTHROPIC_MODEL_NARRATIVE=claude-haiku-4-5
```
Leave `ANTHROPIC_API_KEY=sk-ant-your-key` as the committed placeholder (a real key is an OPS secret,
Task 3 — do NOT commit a real key). Confirm no config-server override reintroduces the stale IDs
(`grep -rn "ANTHROPIC_MODEL\|claude-sonnet-4-2025\|claude-haiku-4-2025" config-server/ 2>/dev/null`
returns nothing — the override lives only in deploy/.env).
  </action>
  <verify>
`grep -n "ANTHROPIC_MODEL" deploy/.env` shows `claude-sonnet-4-6` and `claude-haiku-4-5`; no dated
`-2025xxxx` model IDs remain anywhere except the historical application.yml comment.
`grep -rn "claude-.*-2025" config-server/ 2>/dev/null` returns nothing.
  </verify>
  <done>deploy/.env's model IDs match the code defaults; a running nlq-service with these envs resolves model-sql to claude-sonnet-4-6.</done>
</task>

<task type="auto">
  <name>Task 2: Verify the corrected model resolves at runtime (config assertion, no real key needed)</name>
  <files>deploy/.env</files>
  <action>
Prove the config fix takes effect at runtime WITHOUT needing a real key (the model is resolved from
config regardless of key validity). Bring up ONLY infra + nlq-service (8GB host):

- Docker infra via `scripts/dev-stack-up.sh` (postgres, redis, eureka, config-server, clickhouse).
- Export the corrected `ANTHROPIC_MODEL_SQL`/`ANTHROPIC_MODEL_NARRATIVE` (source deploy/.env with the
  host-network overrides from 12-10 §1e: CLICKHOUSE_URL=http://localhost:8123 and the explicit
  SPRING_RABBITMQ_* since nlq-service has no rabbitmq block) and boot nlq-service via
  `nohup java -jar ... -Xmx300m & disown`.
- Assert the resolved model is `claude-sonnet-4-6` — the cleanest check is to fire one NLQ query
  against the local Claude stub (`ANTHROPIC_BASE_URL=http://localhost:9911`,
  `node scripts/e2e/_claude_stub.mjs`) and confirm the request nlq-service sends to the stub carries
  `"model":"claude-sonnet-4-6"` (have the stub log the inbound model, or read nlq-service's own log
  line). The point: the model string on the wire is now `claude-sonnet-4-6`, NOT the stale
  `claude-sonnet-4-20250514` from §1i.

Record the verbatim model string in the SUMMARY.
  </action>
  <verify>
The model on the wire / in nlq-service logs for an NLQ SQL call is `claude-sonnet-4-6` (verbatim
capture), not `claude-sonnet-4-20250514`.
  </verify>
  <done>Runtime confirms the corrected model resolves — the §1i stale-model defect is closed.</done>
</task>

<task type="auto">
  <name>Task 3: Runnable real-key round-trip recipe (live proof honestly deferred)</name>
  <files>scripts/e2e/phase12-nlq-live-claude-e2e.sh</files>
  <action>
Create `scripts/e2e/phase12-nlq-live-claude-e2e.sh` — a runnable recipe that proves the REAL Claude
NL->SQL round-trip when (and only when) a real key is supplied. It must:

1. Fail fast with a clear message if `ANTHROPIC_API_KEY` is unset or equals the placeholder
   `sk-ant-your-key` (so it can never masquerade as a pass with the committed placeholder).
2. Assert `ANTHROPIC_BASE_URL` is NOT pointed at the local stub (must be the real
   `https://api.anthropic.com`) — i.e. this is a REAL call, not a stub pass.
3. Log in as `owner@demo.local` through the gateway for a real JWT, then
   `POST /api/v1/nlq/query -d '{"question":"What was total revenue today?"}'` and assert:
   - HTTP 200 with a non-empty `data.sql` that is a real SELECT (not the stub's verbatim echo), and
   - nlq-service logs show a successful Anthropic call for model `claude-sonnet-4-6` (NOT HTTP 401,
     NOT the stale dated model) — i.e. the exact inverse of the §1i
     `WARN ... Anthropic API returned HTTP 401 for model claude-sonnet-4-20250514` line.
4. Print, at the top and on skip, an explicit note: the live round-trip is DEFERRED on hosts without
   a real key; supply `ANTHROPIC_API_KEY=<real>` and re-run this script to close the evidence gap.

Header-comment the script with the exact runbook (which services to boot, the env exports needed).
Do NOT run it to a green here (no real key on this host) — running it now must SKIP with the
deferral message, which is the honest outcome.
  </action>
  <verify>
`bash scripts/e2e/phase12-nlq-live-claude-e2e.sh` with the committed placeholder key SKIPs with the
deferral message (exit non-zero or a clearly-labelled SKIP, never a false PASS). Script is
executable and header-documents the runbook.
  </verify>
  <done>A runnable real-key proof exists; the live round-trip is deferred honestly with a clear recipe, never faked.</done>
</task>

</tasks>

<verification>
- deploy/.env model IDs are `claude-sonnet-4-6` / `claude-haiku-4-5`; no stale dated IDs override the code default.
- Runtime NLQ call sends model `claude-sonnet-4-6` (verbatim capture).
- `phase12-nlq-live-claude-e2e.sh` exists, is runnable, fails-fast on the placeholder key, and documents the deferred real-key runbook.
- No real Anthropic key committed to the repo.
</verification>

<success_criteria>
- The §1i stale-model config defect is fixed and proven at runtime.
- The real Claude round-trip has a runnable, documented proof recipe.
- The live round-trip is deferred honestly (real key = OPS action), not claimed.
</success_criteria>

<output>
After completion, create `.planning/phases/12-reporting-dashboards-nlq/12-15-SUMMARY.md`.
</output>
