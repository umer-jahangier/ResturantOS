---
status: complete
phase: 12-reporting-dashboards-nlq
source: [12-05-SUMMARY.md, 12-06-SUMMARY.md, 12-07-SUMMARY.md, 12-08-SUMMARY.md, 12-09-SUMMARY.md, 12-12-SUMMARY.md, 12-13-SUMMARY.md, 12-14-SUMMARY.md, 12-15-SUMMARY.md, 12-16-SUMMARY.md]
started: 2026-07-22T00:00:00Z
updated: 2026-07-22T06:00:00Z
round: 2
note: |
  Round 2 — re-test after 12-16 closed the round-1 gaps (Tests 3 + 4: browser WS
  hooks targeted localhost:3000 instead of the gateway). Round 1 is preserved in
  12-UAT.md (3 passed, 2 issues, 2 skipped).
---

## Current Test

[testing complete]

## Tests

### 1. Reports page renders named reports
expected: Reports section lists named reports (Sales Summary etc.) and they load real data without error.
result: pass

### 2. FBR Tax Summary — numbers + non-null NTN/STRN (GAP B / 12-13)
expected: The FBR Tax Summary report returns output tax, input tax, and net payable (output − input), AND the tenant's NTN and Sales-Tax Registration No (fbrStrn) show as real values, not null/blank.
result: pass

### 3. Realtime dashboard pushes within 5s through the gateway (GAP A / 12-12 + 12-16)
expected: With the Realtime Dashboard open in the browser, the status reads "Live" (not "Reconnecting…"). Closing a POS order updates the KPI tiles within ~5 seconds with NO page refresh.
result: pass
note: |
  Confirms the 12-16 fix. Status badge read "Live" (round 1 was stuck on
  "Reconnecting…" because the socket targeted ws://localhost:3000). First attempt
  showed no tile movement — correctly so: the order was Served but only PARTIALLY
  paid (Rs 25 of Rs 275), so Paid-AND-Served was not met and no ORDER_CLOSED fired.
  After recording the remaining balance the order closed and the tiles updated
  within ~5s with no page refresh. Also independently verified by the orchestrator
  earlier: tiles moved Rs 0.00 -> Rs 110.00, Orders 0 -> 1, Tax Rs 0.00 -> Rs 10.00.

### 4. KDS board reachable through the gateway WebSocket (GAP A / 12-12 + 12-16)
expected: The Kitchen Display board loads, shows the "Live" indicator, renders its New/Started/Preparing/Ready station columns, and reflects live ticket updates.
result: pass
note: |
  PASSES on retest. Phase 12 / 12-16 confirmed good: board loads through the real
  gateway, "Live" indicator green, New/Started/Preparing/Ready columns render, and
  tickets now appear on the board.

  Round-2 first attempt showed an empty board — diagnosed as an OPERATOR issue, not
  a defect: the session was logged in as cashier@demo.local. Verified
  GET /api/v1/kitchen/kds/tickets returns 403 for the cashier and returns all five
  live tickets (ORD-20260722-0001/0002/0005 PENDING, ORD-20260722-0004 +
  ORD-20260721-0001 READY) for chef@demo.local. That 403 is CORRECT — Phase 7 SC6
  strict KITCHEN_STAFF/POS role isolation. Item move-forward buttons are likewise
  gated on `pos.kds.update`, which the cashier does not hold. Re-testing as
  chef@demo.local showed the board populated.

  The round-1 pagination theory (KdsController Pageable default size=20 with no
  Sort burying new tickets behind the ORD-20260719-* backlog) did NOT reproduce
  this round — the stale 0719 tickets are all CANCELLED and therefore excluded by
  the status filter. Logged below as an observation, not a Phase 12 gap.

observations (NOT Phase 12 gaps — carry to Phase 07.3 backlog):
  - path: "services/kitchen-service/src/main/java/io/restaurantos/kitchen/web/KdsController.java"
    issue: "getTickets() Pageable has no default size and no explicit Sort (Spring default size=20, arbitrary order). Latent: once >=20 unserved tickets exist in a branch, newly-fired tickets can be paginated off the board. Contradicts KDS-05 'board scales for many orders'."
  - path: "frontend/components/kds/station-board.tsx"
    issue: "The 'Live' connection dot renders green for a principal that is 403'd on the KDS REST/WS surface (e.g. a cashier). The indicator should reflect an authorized, actually-subscribed socket."

### 5. NLQ ask page — answers valid, rejects unsafe (12-07 / 12-09)
expected: On the NLQ ask page, a plain-English question returns a SQL-backed answer; an unsafe/tenant-less query is honestly rejected with a clear reason (not a silent failure or raw DB error). NOTE: requires a real ANTHROPIC_API_KEY — without one the page should still fail honestly, not crash.
result: skipped
reason: |
  No funded ANTHROPIC_API_KEY available in this environment. `deploy/.env` holds the
  placeholder `ANTHROPIC_API_KEY=sk-ant-your-...`, so every NLQ request 401s upstream.
  User has a Claude.ai subscription, not an API key; a consumer subscription is not a
  Messages API credential and cannot authenticate nlq-service's ClaudeClient. Routing
  around it (e.g. via the Claude Agent SDK, which inherits Claude Code's local login)
  was considered and REJECTED: it is a different product from the Messages API, has no
  Java binding, could never ship to the Docker deployment, and — decisively — would
  bypass the very code path this test exists to verify, producing a false pass.

  PARTIALLY VERIFIED (the half that could be tested, and it PASSED): the honest-rejection
  UX from 12-09. Browser showed "That question couldn't be answered / Something prevented
  us from answering that question safely." — no crash, no infinite spinner, no stack trace,
  no raw DB error leaked. nlq-service caught and logged the upstream failure cleanly:
    WARN i.restaurantos.nlq.claude.ClaudeClient : [nlq-claude] Anthropic API returned
    HTTP 401 for model claude-sonnet-4-6

  UNVERIFIED (needs a funded key): valid question -> SQL-backed answer; unsafe question
  -> rejection attributable to the 7-stage AST validator rather than to the 401. Note the
  7-stage validation itself has adversarial unit coverage from 12-04.

### 6. NLQ live Claude round-trip uses the correct model (GAP D / 12-15)
expected: With a REAL ANTHROPIC_API_KEY exported, scripts/e2e/phase12-nlq-live-claude-e2e.sh gives HTTP 200 with a real SELECT and logs show model claude-sonnet-4-6 (not a 401, not the stale claude-sonnet-4-20250514). Without a real key this is an honest skip.
reason: |
  Same blocker as Test 5 — no funded ANTHROPIC_API_KEY. This test's own expected
  behavior designates this an honest skip, so it is recorded as such rather than
  presented separately.

  PARTIALLY VERIFIED — and this is the actual 12-15 deliverable: the stale model ID is
  GONE. Live nlq-service logs during Test 5 show the request going out as
  `claude-sonnet-4-6`, NOT the stale `claude-sonnet-4-20250514` that 12-15 was raised to
  fix. Confirmed in services/nlq-service/src/main/resources/application.yml:82 —
  `model-sql: ${ANTHROPIC_MODEL_SQL:claude-sonnet-4-6}`.

  UNVERIFIED (needs a funded key): the HTTP 200 + real SELECT round-trip. The 401 is an
  environment/credential gap, not a code gap — the request is correctly formed and
  correctly addressed; it is rejected at the credential layer only.
result: skipped

### 7. Impersonation issuance returns a token, no 500 (GAP C / 12-14)
expected: A SuperAdmin impersonating a tenant user returns a JWT stamped impersonated_by — no 500 on missing tenant RLS context.
result: pass
note: |
  Verified live against the running stack (API-only endpoint — no frontend surface for
  impersonation exists, so this was driven by the orchestrator via curl and the decoded
  JWT presented to the user for confirmation).

  POST /internal/auth/users/c0000002-0000-4000-8000-000000000002/impersonate
    body: {"tenantId":"a0000001-...-0001","impersonatedBy":"ea07bc72-...-5fd7","expiresInSeconds":1800}

  -> HTTP 200 (round 1 / 12-E2E-EVIDENCE §1g returned 500 "Target user not found")
  -> non-empty RS256 token; decoded claims:
       sub             = c0000002-0000-4000-8000-000000000002  (impersonated target)
       tenant_id       = a0000001-0000-4000-8000-000000000001
       roles           = ["OWNER"]
       impersonated_by = ea07bc72-7c5c-4734-87d2-75ae388b5fd7  (real seeded superadmin@restaurantos.io)
       exp - iat       = 1800s
  -> token accepted through the real gateway: GET /api/v1/reporting/reports returned 200.

  The populated roles/permissions are the load-bearing signal: they can only resolve if the
  tenant RLS GUC was set inside the transaction before findById — i.e. the 12-14 fix
  (@Transactional + set_config as first statement) working on the real stack, not only in
  the unit/reflection tests.

  SCOPE CAVEAT (honest): the INNER auth-service issuance endpoint was driven — the one that
  was broken and the one 12-14 fixed. The OUTER platform-admin wrapper
  POST /api/v1/platform/tenants/{tenantId}/impersonate was NOT driven; it needs a SUPER_ADMIN
  session JWT and is a thin pass-through that threads tenantId into the call made here.

## Summary

total: 7
passed: 5
issues: 0
pending: 0
skipped: 2

## Gaps

# No FAILED gaps — round 2 closed the two round-1 issues (Tests 3 + 4). Nothing here
# requires a Phase 12 gap-closure plan. Recorded below: 2 environment-blocked skips and
# 2 observations that belong to OTHER phases.

deferred_environment:
  - truth: "NLQ returns a SQL-backed answer for a valid question; unsafe queries rejected by the 7-stage validator"
    status: unverified_environment
    reason: "No funded ANTHROPIC_API_KEY. Consumer Claude subscription is not a Messages API credential; no supported substitute exists that exercises nlq-service's real ClaudeClient path."
    test: 5
    partially_verified: "Honest-rejection UX (12-09) PASSED — clean error, no crash/spinner/stack-trace, upstream 401 caught and logged."
    unblocks_with: "Any funded API key in deploy/.env -> restart nlq-service -> re-run Tests 5 and 6."
  - truth: "Live Claude round-trip returns HTTP 200 with a real SELECT on model claude-sonnet-4-6"
    status: unverified_environment
    reason: "Same missing key. This test's own expected behavior designates it an honest skip without one."
    test: 6
    partially_verified: "The actual 12-15 deliverable IS verified — live logs show claude-sonnet-4-6, not the stale claude-sonnet-4-20250514."

observations_other_phases:
  - phase: "07.3 (KDS-05)"
    path: "services/kitchen-service/src/main/java/io/restaurantos/kitchen/web/KdsController.java"
    issue: "getTickets() Pageable has no default size and no explicit Sort (Spring default size=20, arbitrary order). Latent: >=20 unserved tickets in a branch can paginate newly-fired tickets off the board."
    severity: minor
    note: "Did not reproduce in round 2 (stale backlog was CANCELLED and excluded by the status filter). Not a Phase 12 gap."
  - phase: "07.3 / 12-16 boundary"
    path: "frontend/components/kds/station-board.tsx"
    issue: "The 'Live' connection dot renders green for a principal that is 403'd on the KDS REST/WS surface (e.g. a cashier). Indicator should reflect an authorized, actually-subscribed socket."
    severity: minor
    note: "Surfaced during Test 4 diagnosis. Cosmetic/observability, not a data-correctness defect."

## Environment notes (round 2)

- Stack verified up before testing: gateway 8080, auth 8081, authorization 8083,
  user 8082, pos 8084, finance 8086, kitchen 8090, reporting 8092, nlq 8094,
  file 8095, platform-admin 8096. Frontend on 3000.
- audit-service is DOWN — Postgres `max_connections=100` is saturated (106 in use;
  11 services x ~10-connection Hikari pools). Not required for Phase 12.
- Gateway routes are behind Resilience4j circuit breakers: the FIRST request to an
  idle route can return 503 SERVICE_UNAVAILABLE (and a WS upgrade fails alongside
  it) until the route warms. Retry once or twice before treating a 503 as a defect.
- Phase 09 auto-posting consumer poison-loop (finance OrderClosedConsumer,
  AccountNotConfiguredException CASH — tenant RLS GUC not set on the AMQP consumer's
  DB session) was filling the disk at ~2.7 MB/s. Mitigated for this UAT by purging
  finance.order-closed.queue and removing the pos.topic -> finance.order-closed.queue
  binding. Auto-posting is therefore PARKED during this UAT; needs a Phase 09 fix.
