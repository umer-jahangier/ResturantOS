---
phase: 07.3-pos-kitchen-production-bug-fixes-ux-revamp
plan: 05
type: execute
wave: 2
depends_on: ["07.3-02", "07.3-04"]
files_modified:
  - services/kitchen-service/src/main/resources/db/migration/V5__ticket_table_number_and_station_seed.sql
  - services/kitchen-service/src/main/java/io/restaurantos/kitchen/domain/model/KdsTicket.java
  - services/kitchen-service/src/main/java/io/restaurantos/kitchen/dto/KdsTicketDto.java
  - services/kitchen-service/src/main/java/io/restaurantos/kitchen/event/KitchenEventPayloads.java
  - services/kitchen-service/src/main/java/io/restaurantos/kitchen/service/TicketRoutingService.java
  - services/kitchen-service/src/main/java/io/restaurantos/kitchen/service/TicketServiceImpl.java
  - services/kitchen-service/src/main/java/io/restaurantos/kitchen/service/TicketService.java
  - services/kitchen-service/src/main/java/io/restaurantos/kitchen/web/KdsController.java
  - services/kitchen-service/src/main/java/io/restaurantos/kitchen/repository/KdsStationRepository.java
  - services/kitchen-service/src/test/java/io/restaurantos/kitchen/KdsStationSeedTableNumberIT.java
  - services/kitchen-service/src/test/java/io/restaurantos/kitchen/ItemStatusEndpointIT.java
autonomous: true
requirements: [KDS-04]

must_haves:
  truths:
    - "An explicit item-status endpoint transitions a ticket item New→Started→Preparing→Ready via markItemStatus (wrapping, not the 2-step bump)"
    - "validateTransition permits the full New(PENDING)→Started(ACCEPTED)→Preparing(PREPARING)→Ready(READY) path"
    - "The order's table number propagates order→event→ticket→DTO (KdsTicket.tableNumber persisted, KdsTicketDto exposes it)"
    - "At least a DEFAULT station exists per branch so GET /stations is never empty and the board renders"
    - "Kitchen consumes the additive tableNumber field with exact field-name parity to the pos producer"
  artifacts:
    - "V5__ticket_table_number_and_station_seed.sql (kitchen)"
    - "POST /api/v1/kitchen/kds/tickets/{ticketId}/items/{itemId}/status endpoint"
    - "KdsTicket.tableNumber + KdsTicketDto.tableNumber"
    - "ensureDefaultStation station-seeding path"
  key_links:
    - "KdsController item-status endpoint -> TicketServiceImpl.markItemStatus (which now also emits KITCHEN_ITEM_STATUS_CHANGED from 07.3-02)"
    - "TicketRoutingService.createNewTicket -> ensureDefaultStation + set tableNumber"
    - "OrderSentToKdsPayload.tableNumber -> KdsTicket.tableNumber -> KdsTicketDto.tableNumber"
---

<objective>
Deliver the KDS-04 kitchen-service backend: an explicit item-status REST endpoint that drives the
New/Started/Preparing/Ready lifecycle (D-12), table-number propagation onto the ticket + DTO, and
station seeding so the board is never empty. This unblocks the KDS station-board redesign (10).

Purpose: `markItemStatus` supports arbitrary transitions but is not exposed via REST (only the 2-step
`bump`); the ticket has no table number (never carried on the event); and NO station rows are ever
created, so `GET /stations` is empty and the board shows "No active stations configured" — the live
KDS blocker (RESEARCH KDS-04).

Output: kitchen V5 migration (table_number column), the item-status endpoint, broadened transition
validation, the tableNumber consume field (parity with 07.3-04), and DEFAULT-station seeding.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07.3-pos-kitchen-production-bug-fixes-ux-revamp/07.3-CONTEXT.md
@.planning/phases/07.3-pos-kitchen-production-bug-fixes-ux-revamp/07.3-RESEARCH.md
@services/kitchen-service/src/main/java/io/restaurantos/kitchen/service/TicketServiceImpl.java
@services/kitchen-service/src/main/java/io/restaurantos/kitchen/service/TicketRoutingService.java
@services/kitchen-service/src/main/java/io/restaurantos/kitchen/web/KdsController.java
@services/kitchen-service/src/main/java/io/restaurantos/kitchen/event/KitchenEventPayloads.java

**Depends on 07.3-02:** that plan already added `markItemStatus`'s KITCHEN_ITEM_STATUS_CHANGED emit
and the `ItemStatusChangedPayload`. This plan reuses `markItemStatus` — the new endpoint MUST route
through it (so every endpoint-driven transition also emits the pos-sync event automatically).

**Table-number parity (KDS-04):** 07.3-04 added `tableNumber` as the LAST field of the pos
`OrderSentToKdsPayload`. Here, add the identically-named `tableNumber` field as the LAST field of the
kitchen `KitchenEventPayloads.OrderSentToKdsPayload` (consume side). Field name MUST be `tableNumber`;
never reorder/rename existing fields (KitchenEventPayloads.java:17-20 silent-drop warning).

**Station seeding — why runtime, not Flyway (design rationale, NOT a gap):** branches are provisioned
at runtime (per tenant), so a static Flyway INSERT cannot seed per-branch stations. Seed the DEFAULT
station lazily/idempotently (mirrors the finance 07.2 auto-seed-on-miss pattern): (a) ensure a
DEFAULT station row exists for the branch when a ticket is first routed to it, AND (b) auto-seed a
DEFAULT station in `GET /stations` when a branch has none. The V5 migration seeds only branch-agnostic
structure (the table_number column); the station rows are seeded at runtime.

**GitNexus (CLAUDE.md — MANDATORY):** run `impact` on `TicketServiceImpl.markItemStatus`,
`TicketRoutingService.route`, and `KdsController` before editing; warn on HIGH/CRITICAL;
`detect_changes()` before commit; use GitNexus `rename` for any rename.

**No new package installs.**
</context>

<tasks>

<task type="auto">
  <name>Task 1: V5 migration (table_number) + carry tableNumber onto the ticket (event→entity→DTO)</name>
  <files>services/kitchen-service/src/main/resources/db/migration/V5__ticket_table_number_and_station_seed.sql, services/kitchen-service/src/main/java/io/restaurantos/kitchen/domain/model/KdsTicket.java, services/kitchen-service/src/main/java/io/restaurantos/kitchen/dto/KdsTicketDto.java, services/kitchen-service/src/main/java/io/restaurantos/kitchen/event/KitchenEventPayloads.java, services/kitchen-service/src/main/java/io/restaurantos/kitchen/service/TicketRoutingService.java, services/kitchen-service/src/main/java/io/restaurantos/kitchen/service/TicketServiceImpl.java</files>
  <read_first>
    - services/kitchen-service/src/main/resources/db/migration/V4__ticket_order_notes.sql (the additive-column migration convention to mirror; kitchen-service is at V4 → V5 is next free — verify no phantom-V5 collision per project memory)
    - services/kitchen-service/src/main/java/io/restaurantos/kitchen/domain/model/KdsTicket.java (order_no/order_notes column pattern for the new table_number column)
    - services/kitchen-service/src/main/java/io/restaurantos/kitchen/event/KitchenEventPayloads.java (OrderSentToKdsPayload :21-29 — add tableNumber LAST)
    - services/kitchen-service/src/main/java/io/restaurantos/kitchen/service/TicketRoutingService.java (createNewTicket :96-113, appendToExistingTicket :77-90, toDto path via TicketServiceImpl.toDto)
    - services/kitchen-service/src/main/java/io/restaurantos/kitchen/dto/KdsTicketDto.java (record fields; toDto builds it in TicketServiceImpl.toDto :193-207)
  </read_first>
  <action>
    Create `V5__ticket_table_number_and_station_seed.sql` adding a nullable `table_number VARCHAR(50)`
    column to `kds_tickets` (mirror V4's additive-column style; keep RLS/grants intact). Add
    `String tableNumber` (column `table_number`) to the `KdsTicket` entity. Add `String tableNumber`
    as the NEW LAST field of `KitchenEventPayloads.OrderSentToKdsPayload` (identical name to the pos
    producer's field from 07.3-04). In `TicketRoutingService.createNewTicket` set
    `ticket.setTableNumber(payload.tableNumber())`; in `appendToExistingTicket` refresh it too if
    null. Add `tableNumber` to `KdsTicketDto` and populate it in `TicketServiceImpl.toDto`. Do NOT
    reorder/rename the existing OrderSentToKdsPayload or KdsTicketDto fields.
  </action>
  <acceptance_criteria>
    - V5__ticket_table_number_and_station_seed.sql contains `ALTER TABLE kds_tickets ADD COLUMN table_number`
    - `KitchenEventPayloads.OrderSentToKdsPayload` has `tableNumber` as its final field (name matches the pos side)
    - `KdsTicketDto` exposes `tableNumber`; toDto populates it
    - **Cross-service field-name parity (WARNING 1 / D-05):** grepping `services/pos-service/src/main/java/io/restaurantos/pos/event/PosEventPayloads.java` (producer, 07.3-04) AND `services/kitchen-service/src/main/java/io/restaurantos/kitchen/event/KitchenEventPayloads.java` (this consumer) each returns the identical literal field `tableNumber` — same spelling, both present (a mismatch silently drops the message)
    - `mvn -q -pl services/kitchen-service -am test-compile` passes
  </acceptance_criteria>
  <verify>
    <automated>mvn -q -pl services/kitchen-service -am test-compile</automated>
  </verify>
  <done>The table number flows order→event→ticket entity→DTO via an additive parity field and a V5 migration.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Explicit item-status endpoint + broaden validateTransition</name>
  <files>services/kitchen-service/src/main/java/io/restaurantos/kitchen/service/TicketService.java, services/kitchen-service/src/main/java/io/restaurantos/kitchen/service/TicketServiceImpl.java, services/kitchen-service/src/main/java/io/restaurantos/kitchen/web/KdsController.java, services/kitchen-service/src/test/java/io/restaurantos/kitchen/ItemStatusEndpointIT.java</files>
  <read_first>
    - services/kitchen-service/src/main/java/io/restaurantos/kitchen/service/TicketServiceImpl.java (markItemStatus :53-88, validateTransition :180-191, bumpItem :91-113)
    - services/kitchen-service/src/main/java/io/restaurantos/kitchen/web/KdsController.java (bumpItem endpoint :73-82 for the authz + branchId param pattern; authorizeUpdate)
    - services/kitchen-service/src/main/java/io/restaurantos/kitchen/domain/enums/TicketItemStatus.java
  </read_first>
  <behavior>
    - POST item-status {status: ACCEPTED} on a PENDING item -> item ACCEPTED (Started column)
    - then {status: PREPARING} -> PREPARING; then {status: READY} -> READY
    - an invalid transition (e.g. PENDING->READY skipping) is rejected with StateInvalid
    - the endpoint requires pos.kds.update (OPA authorizeUpdate) — a viewer-only principal is denied
    - each successful transition also publishes KITCHEN_ITEM_STATUS_CHANGED (inherited from markItemStatus, 07.3-02)
  </behavior>
  <action>
    Run GitNexus `impact` on `markItemStatus`/`validateTransition` before editing. Broaden
    `validateTransition` so the full New→Started→Preparing→Ready path is valid: PENDING→ACCEPTED,
    ACCEPTED→PREPARING, PREPARING/COOKING→READY (keep legacy COOKING handling). Add
    `POST /api/v1/kitchen/kds/tickets/{ticketId}/items/{itemId}/status` to `KdsController` taking a
    `{ TicketItemStatus status }` body + `branchId` param, calling `authz.authorizeUpdate(...)` then
    `ticketService.markItemStatus(ticketId, itemId, status)` (wrap the existing method — do NOT
    duplicate transition logic). Expose it on the `TicketService` interface if needed. Write
    ItemStatusEndpointIT asserting the New→Started→Preparing→Ready walk, an invalid-transition
    rejection, and the pos.kds.update authorization gate (mirror KdsAccessIsolationIT for the authz
    setup).
  </action>
  <acceptance_criteria>
    - the endpoint path is `/tickets/{ticketId}/items/{itemId}/status` (POST)
    - ItemStatusEndpointIT walks PENDING→ACCEPTED→PREPARING→READY and passes
    - the IT asserts a viewer-only principal is denied (authorizeUpdate)
    - grep KdsController shows the new endpoint calls `ticketService.markItemStatus` (wrapping, not re-implementing)
  </acceptance_criteria>
  <verify>
    <automated>mvn -q -pl services/kitchen-service -am test -Dtest=ItemStatusEndpointIT</automated>
  </verify>
  <done>The item-status endpoint drives the full 4-state lifecycle via markItemStatus, gated by pos.kds.update, and inherits the pos-sync event emit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: DEFAULT-station seeding (never-empty board) + table-number/station IT</name>
  <files>services/kitchen-service/src/main/java/io/restaurantos/kitchen/service/TicketRoutingService.java, services/kitchen-service/src/main/java/io/restaurantos/kitchen/web/KdsController.java, services/kitchen-service/src/main/java/io/restaurantos/kitchen/repository/KdsStationRepository.java, services/kitchen-service/src/test/java/io/restaurantos/kitchen/KdsStationSeedTableNumberIT.java</files>
  <read_first>
    - services/kitchen-service/src/main/java/io/restaurantos/kitchen/web/KdsController.java (getStations :117-124 — currently returns findByBranchIdAndActiveTrue with no seeding)
    - services/kitchen-service/src/main/java/io/restaurantos/kitchen/domain/model/KdsStation.java (code/name/active/escalationThresholdSeconds defaults; branchId)
    - services/kitchen-service/src/main/java/io/restaurantos/kitchen/service/TicketRoutingService.java (route/createNewTicket — where DEFAULT station code is used)
    - .planning/phases/07.2-finance-accounting-period-provisioning-guarantee-open-period/07.2-04-PLAN.md (the auto-seed-on-miss pattern this mirrors)
  </read_first>
  <behavior>
    - GET /stations for a branch with zero station rows auto-seeds and returns a single DEFAULT station
    - routing a ticket to station "DEFAULT" when no station row exists creates the DEFAULT station row idempotently (no duplicate on a second route)
    - a fresh branch's board is never empty after the first order is sent OR after a GET /stations
    - table number set on the payload appears on the persisted ticket + DTO
  </behavior>
  <action>
    Add an idempotent `ensureStation(branchId, code)` (upsert-by branchId+code; default name = code,
    active=true, escalationThresholdSeconds=900) used in `TicketRoutingService.createNewTicket` before
    creating a ticket for a station code, so a DEFAULT (or menu-item-specified) station always exists.
    In `KdsController.getStations`, when `stationRepository.findByBranchIdAndActiveTrue(branchId)` is
    empty, seed a DEFAULT station for that branch and return it (auto-seed-on-miss, mirroring finance
    07.2). Add the repository finder needed for the idempotent upsert
    (`findByBranchIdAndCode`). Write KdsStationSeedTableNumberIT asserting: a fresh branch's
    GET /stations returns a seeded DEFAULT station; routing a payload with a tableNumber persists it on
    the ticket and the DTO exposes it; a duplicate route does not create a second DEFAULT station.
  </action>
  <acceptance_criteria>
    - KdsStationSeedTableNumberIT asserts GET /stations on a fresh branch returns exactly one DEFAULT station
    - the IT asserts the ticket DTO carries the payload's tableNumber
    - the IT asserts routing twice yields exactly one DEFAULT station row (idempotent seed)
  </acceptance_criteria>
  <verify>
    <automated>mvn -q -pl services/kitchen-service -am test -Dtest=KdsStationSeedTableNumberIT</automated>
  </verify>
  <done>Every branch has at least a DEFAULT station so the board renders, and the table number reaches the ticket DTO.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → KdsController (item-status) | untrusted status transition crosses here |
| pos-service → kitchen-service (event) | additive tableNumber field crosses the boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07.3-14 | Elevation of Privilege | item-status endpoint | high | mitigate | authz.authorizeUpdate (OPA pos.kds.update) gates the endpoint; view-only principals denied (proven by IT). |
| T-07.3-15 | Tampering | invalid status transition | medium | mitigate | validateTransition rejects illegal jumps; markItemStatus is the single lifecycle seam. |
| T-07.3-16 | Spoofing | tableNumber field parity | high | mitigate | Additive last field with identical name on both sides; deserialization-safe; IT asserts the value lands on the ticket (message actually consumed). |
| T-07.3-17 | Denial of Service | station-seed duplication | low | mitigate | ensureStation is idempotent (upsert by branchId+code); no unbounded row growth. |
| T-07.3-SC | Tampering | dependencies | low | accept | No new package installs. |
</threat_model>

## Artifacts this phase produces
- **Flyway:** `V5__ticket_table_number_and_station_seed.sql` (kitchen)
- **Endpoint:** `POST /api/v1/kitchen/kds/tickets/{ticketId}/items/{itemId}/status`
- **Entity/DTO field:** `KdsTicket.tableNumber`, `KdsTicketDto.tableNumber`
- **Event field:** `KitchenEventPayloads.OrderSentToKdsPayload.tableNumber` (additive, parity)
- **Seeding path:** `TicketRoutingService.ensureStation` + `getStations` auto-seed-on-miss
- **Repository:** `KdsStationRepository.findByBranchIdAndCode`
- **Tests:** `ItemStatusEndpointIT`, `KdsStationSeedTableNumberIT`

<verification>
- `mvn -q -pl services/kitchen-service -am test -Dtest=ItemStatusEndpointIT,KdsStationSeedTableNumberIT,TicketRoutingIT,TicketRevisionRoutingIT` green
- GitNexus `detect_changes()` shows only the expected kitchen-service KDS symbols changed
</verification>

<success_criteria>
The KDS board can render (DEFAULT station always exists), item status transitions through an explicit
gated endpoint, and the table number is present end-to-end on the ticket DTO.
</success_criteria>

<output>
Create `.planning/phases/07.3-pos-kitchen-production-bug-fixes-ux-revamp/07.3-05-SUMMARY.md` when done.
</output>
