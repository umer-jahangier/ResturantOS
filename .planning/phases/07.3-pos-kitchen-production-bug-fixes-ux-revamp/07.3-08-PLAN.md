---
phase: 07.3-pos-kitchen-production-bug-fixes-ux-revamp
plan: 08
type: execute
wave: 3
depends_on: ["07.3-04", "07.3-07"]
files_modified:
  - frontend/lib/api-client/schemas/pos.schema.ts
  - frontend/lib/models/pos.model.ts
  - frontend/lib/adapters/pos.adapter.ts
  - frontend/lib/repositories/pos.repository.ts
  - frontend/lib/hooks/pos/use-orders.ts
  - frontend/components/pos/order-management.tsx
  - frontend/__tests__/components/pos/order-management-columns.test.tsx
autonomous: true
requirements: [POS-24]

must_haves:
  truths:
    - "Order Management shows closed/paid orders via settlement filters plus the existing derived-status chips, and a search box over order no. / table name (POS-24)"
    - "Every row shows a payment-status badge (Paid/Unpaid/Partially Paid/Refunded) (POS-24)"
    - "The Cover column is replaced by an Items column showing total item quantity (with optional distinct-item secondary text) (POS-24)"
    - "An Assign Table row action assigns an available table (occupied blocked) and updates order + table status immediately (POS-24)"
    - "Already-paid/closed orders open read-only and block duplicate payment while staying accessible (POS-24)"
  artifacts:
    - "OrderSummary model extension (settlementStatus, paymentStatus, amountPaidPaisa, itemQuantity, distinctItemCount)"
    - "Closed/Paid filters + search + payment-status column + Items column + Assign Table action"
    - "useAssignTable hook + PosRepository.assignTable"
  key_links:
    - "useOrderSummaries(statuses) -> Closed/terminal request path (07.3-04)"
    - "payment-status-badge (07.3-07) -> row badge column"
    - "useAssignTable -> PATCH /orders/{id}/table (07.3-04) -> table-select-combobox (07.3-03)"
---

<objective>
Complete the Order Management screen (POS-24/D-09): surface closed/paid orders with settlement
filters + search, add a payment-status badge column, replace the Cover column with an Items
(quantity) column, and add an Assign Table row action — consuming the extended DTO (07.3-04), the
payment-status badge (07.3-07), and the table-select combobox (07.3-03).

Purpose: `useOrderSummaries` is called with no statuses (non-terminal only), the filter list has only
4 derived values, there is no search, the Cover column shows `coverCount`, and assign-table exists
nowhere (RESEARCH POS-24).

Output: the extended `OrderSummary` model/schema/adapter, the filters/search/columns/assign-table UI,
and the `useAssignTable` hook.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07.3-pos-kitchen-production-bug-fixes-ux-revamp/07.3-CONTEXT.md
@.planning/phases/07.3-pos-kitchen-production-bug-fixes-ux-revamp/07.3-UI-SPEC.md
@frontend/components/pos/order-management.tsx
@frontend/lib/hooks/pos/use-orders.ts
@frontend/lib/models/pos.model.ts

**Depends on 07.3-04 (extended OrderSummaryDto + PATCH assign-table + terminal-status request path)
and 07.3-07 (payment-status-badge + table-select-combobox reuse via 07.3-03).**

**fadeOutList invariant (RESEARCH POS-24):** `order-management.tsx` `useFadeOutList` assumes
non-closed rows never disappear. When you add a Closed/Paid filter that requests terminal statuses,
ensure the fade-out logic still holds — closed rows requested by an explicit filter must not trip the
"disappeared" fade (they are present because they were requested). Keep the fade for the active list
only.

**Registry safety (UI-SPEC):** the new settlement/payment filter values must be handled in every
switch/config map; an unmapped value must not throw.

**GitNexus (CLAUDE.md — MANDATORY):** run `impact` on `useOrderSummaries` and the order-management
columns before editing; warn on HIGH/CRITICAL; `detect_changes()` before commit; use GitNexus
`rename` for renames.

**No new npm packages.**
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend OrderSummary model/schema/adapter + useAssignTable</name>
  <files>frontend/lib/api-client/schemas/pos.schema.ts, frontend/lib/models/pos.model.ts, frontend/lib/adapters/pos.adapter.ts, frontend/lib/repositories/pos.repository.ts, frontend/lib/hooks/pos/use-orders.ts</files>
  <read_first>
    - frontend/lib/api-client/schemas/pos.schema.ts (apiOrderSummarySchema :121-133)
    - frontend/lib/models/pos.model.ts (OrderSummary interface :137-147)
    - frontend/lib/adapters/pos.adapter.ts (order-summary adapter)
    - frontend/lib/hooks/pos/use-orders.ts (useOrderSummaries(statuses) :20-27; the order-summaries invalidation pattern)
    - frontend/lib/repositories/pos.repository.ts (listOrderSummaries + add assignTable)
  </read_first>
  <action>
    Extend `apiOrderSummarySchema` (Zod) and the `OrderSummary` model with the 07.3-04 fields:
    `settlementStatus` (OrderStatus enum), `paymentStatus` (PaymentStatus union), `amountPaidPaisa`,
    `itemQuantity`, `distinctItemCount`. Update the adapter to map them. Add
    `PosRepository.assignTable(orderId, tableId)` (PATCH /orders/{id}/table) and a `useAssignTable`
    hook that on success invalidates the order-summaries + order + tables query keys (mirror
    useSendToKds's multi-key invalidation so the row + table status update immediately). Keep the
    Layer boundaries (components never import api-client; repositories `.parse()`).
  </action>
  <acceptance_criteria>
    - OrderSummary has settlementStatus, paymentStatus, amountPaidPaisa, itemQuantity, distinctItemCount
    - `useAssignTable` invalidates order-summaries, order, and tables keys on success
    - `pnpm --dir frontend exec tsc --noEmit` passes
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir frontend exec tsc --noEmit</automated>
  </verify>
  <done>The summary model carries the settlement/payment/item fields and assign-table is wired through all four layers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Filters + search + payment-status column + Items column + Assign Table action</name>
  <files>frontend/components/pos/order-management.tsx, frontend/__tests__/components/pos/order-management-columns.test.tsx</files>
  <read_first>
    - frontend/components/pos/order-management.tsx (STATUS_FILTERS :29-35; covers column :156-160; columns memo :124-192; useFadeOutList :58-105)
    - frontend/components/pos/payment-status-badge.tsx (from 07.3-07)
    - frontend/components/pos/table-select-combobox.tsx (from 07.3-03, filtered to AVAILABLE for assign)
    - frontend/lib/hooks/pos/use-orders.ts (useOrderSummaries(statuses), useAssignTable from Task 1)
    - .planning/phases/07.3-pos-kitchen-production-bug-fixes-ux-revamp/07.3-UI-SPEC.md (§3 Order Management additions)
  </read_first>
  <behavior>
    - a Closed (settlement) filter requests terminal statuses and closed rows appear
    - a Paid filter shows only paymentStatus==PAID rows
    - the search box filters rows by order no. or table name (case-insensitive substring)
    - the Items column shows "8 Items" (total quantity) with optional "5 Items / 8 Qty" secondary text — NOT coverCount
    - each row shows a PaymentStatusBadge
    - Assign Table action on a tableless order opens the AVAILABLE-only selector and calls useAssignTable; occupied tables are not selectable
    - opening a closed/paid order is read-only (no charge action)
  </behavior>
  <action>
    Run GitNexus `impact` on the order-management columns before editing. Add settlement filters
    (Closed / Paid) alongside the existing derived-status chips — the Closed filter passes an explicit
    terminal `statuses` argument to `useOrderSummaries` (07.3-04 supports it); the Paid filter is a
    client-side paymentStatus filter. Add a search `Input` filtering by order no. / table name. Replace
    the "Covers" column with an **Items** column rendering `itemQuantity` ("8 Items") with optional
    `distinctItemCount` secondary text ("5 Items / 8 Qty"). Add a **payment-status** column rendering
    `PaymentStatusBadge status={row.paymentStatus}`. Add an **Assign Table** row action for tableless
    orders that opens the AVAILABLE-filtered `table-select-combobox` and calls `useAssignTable` (order
    + table status update immediately via the Task-1 invalidations); occupied tables blocked. Ensure
    closed/paid orders open read-only. Preserve the fade-out invariant for the active list. Write
    order-management-columns.test.tsx asserting the Items column value, the payment badge presence, and
    that the search filters rows.
  </action>
  <acceptance_criteria>
    - order-management-columns.test.tsx asserts a row with 3×Burger+2×Coke shows "5 Items" and renders a PaymentStatusBadge, and passes
    - grep order-management.tsx shows NO `coverCount` column cell remaining (replaced by itemQuantity)
    - the test asserts typing in the search box filters the visible rows
    - a Closed filter chip exists and passes a terminal `statuses` value to the hook
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir frontend vitest run order-management-columns</automated>
  </verify>
  <done>Order Management shows closed/paid orders with filters + search + payment badges + an Items column + Assign Table.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → PATCH /orders/{id}/table | assign-table tableId crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07.3-23 | Elevation of Privilege / IDOR | Assign Table action | high | mitigate | Server enforces tenant/branch scope + AVAILABLE re-check (07.3-04); the UI only offers AVAILABLE tables. |
| T-07.3-24 | Tampering | duplicate payment via list | medium | mitigate | Closed/paid orders open read-only (no charge action); server blocks payment on terminal orders. |
| T-07.3-25 | Information Disclosure | closed-order visibility | low | mitigate | Summaries are branch-scoped server-side (jwtBranchId check, 07.1); showing closed rows does not widen scope. |
| T-07.3-SC | Tampering | dependencies | low | accept | No new npm package installs. |
</threat_model>

## Artifacts this phase produces
- **Model/Schema:** extended `OrderSummary` (+settlementStatus, paymentStatus, amountPaidPaisa, itemQuantity, distinctItemCount)
- **Hook/Repo:** `useAssignTable`, `PosRepository.assignTable`
- **UI:** Closed/Paid filters, search box, payment-status column, Items column, Assign Table action
- **Test:** `order-management-columns.test.tsx`

<verification>
- `pnpm --dir frontend vitest run order-management-columns` green
- `pnpm --dir frontend exec tsc --noEmit` passes
- GitNexus `detect_changes()` shows only the expected order-management symbols changed
</verification>

<success_criteria>
Order Management surfaces closed/paid orders with filters + search + payment badges, replaces Cover
with item quantity, and assigns available tables live — duplicate payment blocked, paid orders
accessible.
</success_criteria>

<output>
Create `.planning/phases/07.3-pos-kitchen-production-bug-fixes-ux-revamp/07.3-08-SUMMARY.md` when done.
</output>
