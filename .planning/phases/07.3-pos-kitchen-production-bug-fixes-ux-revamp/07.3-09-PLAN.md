---
phase: 07.3-pos-kitchen-production-bug-fixes-ux-revamp
plan: 09
type: execute
wave: 3
depends_on: ["07.3-07"]
files_modified:
  - frontend/components/pos/void-refund-dialog.tsx
  - frontend/components/pos/till-session-bar.tsx
  - frontend/lib/offline/use-online-status.ts
  - frontend/__tests__/offline/use-online-status.test.ts
  - frontend/e2e/pos-modal-revamp.spec.ts
autonomous: true
requirements: [POS-25, POS-26]

must_haves:
  truths:
    - "Void/refund is a dedicated large in-place panel/page, not a hand-rolled fixed-inset-0 modal (POS-25)"
    - "Till open/close are dedicated large panels, not hand-rolled modals (POS-25)"
    - "Each converted surface carries its full analytic information and is responsive with no horizontal body overflow (POS-25)"
    - "The online-status hook no longer issues the mis-targeted HEAD /api/v1/pos/menu/categories request; the navigator.onLine event path is preserved (POS-26)"
    - "No repeated 404s appear in the console (POS-26)"
  artifacts:
    - "void-refund panel/page (converted from modal)"
    - "till open/close panels (converted from modals)"
    - "use-online-status.ts with the ping removed"
  key_links:
    - "converted surfaces mirror the 07.3-07 charge-page panel pattern"
    - "use-online-status -> navigator.onLine only (no fetch ping)"
---

<objective>
Finish the POS-25/D-10 operational modal→page sweep for the remaining surfaces — void/refund and till
open/close — converting them to dedicated large in-place panels/pages, and remove the mis-targeted
connectivity ping (POS-26/D-11).

Purpose: `void-refund-dialog.tsx` is a hand-rolled `fixed inset-0` modal and `till-session-bar.tsx`
has two hand-rolled open/close modals (RESEARCH POS-25). `use-online-status.ts` pings a relative
`HEAD /api/v1/pos/menu/categories` that hits the Next origin (no auth, no rewrite) → repeated 404s in
the console (RESEARCH POS-26). Command palette and finance modals are explicitly left as-is (D-10).

Output: void/refund + till converted to panels, and the online-status hook stripped of the ping.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07.3-pos-kitchen-production-bug-fixes-ux-revamp/07.3-CONTEXT.md
@.planning/phases/07.3-pos-kitchen-production-bug-fixes-ux-revamp/07.3-UI-SPEC.md
@frontend/components/pos/void-refund-dialog.tsx
@frontend/components/pos/till-session-bar.tsx
@frontend/lib/offline/use-online-status.ts

**Depends on 07.3-07:** reuse the charge-page panel pattern (dedicated surface, responsive, full
analytic info, no `sm:max-w-md`).

**Scope guard (D-10):** convert ONLY the POS operational surfaces (void/refund, till). Do NOT touch
the command palette, finance modals (ProvisionPeriodDialog / PeriodCloseModal), or transition
overlays. Do NOT convert genuinely-transient inline confirmations (e.g. the OrderPanel cancel-item
confirm).

**POS-26 (D-11):** delete the ping fetch + its interval + the PING_* constants; KEEP the
`navigator.onLine` online/offline event listeners. Do NOT add a HEAD handler on the backend (wrong
target, needs auth). The hook must still return `{ isOnline }` correctly from browser events.

**GitNexus (CLAUDE.md — MANDATORY):** run `impact` on `useOnlineStatus`, `VoidRefundDialog`, and the
till modals before editing; warn on HIGH/CRITICAL; `detect_changes()` before commit; use GitNexus
`rename` for renames.

**No new npm packages.**
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove the connectivity ping (POS-26) — keep navigator.onLine</name>
  <files>frontend/lib/offline/use-online-status.ts, frontend/__tests__/offline/use-online-status.test.ts</files>
  <read_first>
    - frontend/lib/offline/use-online-status.ts (PING_ENDPOINT :5; ping() :31-48; interval wiring :50-67)
    - frontend/lib/hooks/pos/use-orders.ts (consumers of useOnlineStatus — must keep working)
  </read_first>
  <behavior>
    - mounting the hook issues NO fetch to /api/v1/pos/menu/categories (no HEAD request at all)
    - the hook returns isOnline=true when navigator.onLine is true, false on an 'offline' event, true again on an 'online' event
    - no setInterval ping remains
  </behavior>
  <action>
    Run GitNexus `impact` on `useOnlineStatus` before editing. Delete the `ping` function, the
    `setInterval(ping, ...)` wiring, and the `PING_ENDPOINT`/`PING_TIMEOUT_MS`/`PING_INTERVAL_MS`
    constants. Keep the `navigator.onLine` initial state and the `online`/`offline` window event
    listeners updating `isOnline`. Simplify the effect to just add/remove the two event listeners
    (respect the repo's `react-hooks/set-state-in-effect` rule — setState only in event callbacks).
    Write use-online-status.test.ts asserting NO fetch is issued on mount (spy on global fetch) and
    that online/offline events flip isOnline.
  </action>
  <acceptance_criteria>
    - grep use-online-status.ts shows NO `fetch(` and NO `/pos/menu/categories`
    - use-online-status.test.ts asserts `fetch` is never called on mount and passes
    - the test asserts an 'offline' event sets isOnline=false and an 'online' event sets it true
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir frontend vitest run use-online-status</automated>
  </verify>
  <done>The 404-producing ping is gone; connectivity is driven purely by navigator.onLine events.</done>
</task>

<task type="auto">
  <name>Task 2: Convert void/refund to a dedicated panel</name>
  <files>frontend/components/pos/void-refund-dialog.tsx</files>
  <read_first>
    - frontend/components/pos/void-refund-dialog.tsx (fixed inset-0 modal :95-215; per-action PermissionGuards)
    - frontend/components/pos/settlement-actions.tsx (renders VoidRefundDialog cluster — preserve the caller contract)
    - .planning/phases/07.3-pos-kitchen-production-bug-fixes-ux-revamp/07.3-UI-SPEC.md (§5 modal→page conversion contract)
  </read_first>
  <action>
    Run GitNexus `impact` on `VoidRefundDialog` before editing. Convert the hand-rolled
    `fixed inset-0` overlay to a dedicated large in-place panel (or routed page) occupying the primary
    content column, responsive with no horizontal body overflow, carrying the full void/refund analytic
    info (order summary, reason input, refund scope/amount, resulting state). Preserve the existing
    per-action `PermissionGuard`s (void vs refund) and the `onDone` caller contract so
    `settlement-actions.tsx` keeps working unchanged. Do not weaken any authorization.
  </action>
  <acceptance_criteria>
    - grep void-refund-dialog.tsx shows NO `fixed inset-0` overlay wrapper (converted to a panel/page)
    - the per-action PermissionGuards remain present (grep shows both void + refund guards)
    - `pnpm --dir frontend exec tsc --noEmit` passes; eslint on the file clean
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir frontend exec eslint components/pos/void-refund-dialog.tsx</automated>
  </verify>
  <done>Void/refund is a dedicated panel with full info and unchanged authorization + caller contract.</done>
</task>

<task type="auto">
  <name>Task 3: Convert till open/close to dedicated panels + POS-25 no-dialog screenshot backstop</name>
  <files>frontend/components/pos/till-session-bar.tsx, frontend/e2e/pos-modal-revamp.spec.ts</files>
  <read_first>
    - frontend/components/pos/till-session-bar.tsx (open-till modal :89-128; close-till modal :161-239)
    - frontend/lib/hooks/pos/use-till.ts (open/close till mutations — unchanged)
    - frontend/e2e/pos-settlement.spec.ts (login/gotoRobust helpers, seed constants, SHOT_DIR=`e2e/__screenshots__` screenshot convention to mirror)
    - frontend/playwright.config.ts (testDir ./e2e — no config change)
    - .planning/phases/07.3-pos-kitchen-production-bug-fixes-ux-revamp/07.3-UI-SPEC.md (§5)
  </read_first>
  <action>
    Run GitNexus `impact` on the till component before editing. Convert both hand-rolled open-till and
    close-till modals to dedicated large in-place panels, responsive with no horizontal body overflow,
    carrying the full till analytic info (opening float, expected/declared closing, variance).
    Preserve the page-level TillSessionBar chrome and the existing open/close mutation wiring
    (use-till hooks unchanged). Keep the session-scoped placement (visible across tabs) from the POS
    page. THEN convert the POS-25 manual screenshot backstop into an executable check: create
    `frontend/e2e/pos-modal-revamp.spec.ts` (reuse the login/gotoRobust helpers + screenshot convention
    from pos-settlement.spec.ts, same live dev stack) that opens each converted POS-25 operational
    surface in this plan (void/refund from Task 2, till open/close from this task), captures a screenshot
    of each to a named path under `e2e/__screenshots__/` (e.g. `pos25-void-refund.png`,
    `pos25-till.png`), AND asserts the structural truth that each converted surface has NO `[role=dialog]`
    (it is a dedicated panel/routed page, not a popup). Use the Blocked pattern for missing
    seed/environment preconditions. Do NOT edit playwright.config.ts.
  </action>
  <acceptance_criteria>
    - grep till-session-bar.tsx shows the open/close surfaces are panels (no hand-rolled `fixed inset-0` overlay)
    - the open/close till mutations are still invoked (grep shows the use-till hook calls unchanged)
    - `pnpm --dir frontend exec tsc --noEmit` passes
    - `frontend/e2e/pos-modal-revamp.spec.ts` exists; it writes a screenshot per converted surface to `e2e/__screenshots__/` (named `pos25-void-refund.png`, `pos25-till.png`) AND asserts each converted surface has no `[role=dialog]`; `pnpm --dir frontend exec playwright test pos-modal-revamp` passes (against the live dev stack)
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir frontend exec tsc --noEmit</automated>
  </verify>
  <done>Till open/close are dedicated panels carrying full reconciliation info (wiring unchanged), and the POS-25 "not a popup" truth has an executable no-dialog screenshot backstop.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → pos-service (void/refund/till) | privileged operations cross here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07.3-26 | Elevation of Privilege | void/refund panel | high | mitigate | Per-action PermissionGuards preserved in the conversion; server OPA thresholds unchanged (no authz weakened). |
| T-07.3-27 | Spoofing | connectivity ping removal | low | accept | Removing the unauthenticated relative ping removes a needless request; connectivity now derives from browser events only (no server trust needed). |
| T-07.3-28 | Tampering | till variance entry | medium | mitigate | Declared-closing/variance computed server-side (use-till unchanged); the panel is display/entry only. |
| T-07.3-SC | Tampering | dependencies | low | accept | No new npm package installs. |
</threat_model>

## Artifacts this phase produces
- **Converted surfaces:** void/refund panel, till open/close panels
- **Hook change:** `use-online-status.ts` (ping removed)
- **Test:** `use-online-status.test.ts`
- **POS-25 backstop E2E:** `frontend/e2e/pos-modal-revamp.spec.ts` (no-dialog assertion + screenshot artifacts `e2e/__screenshots__/pos25-void-refund.png`, `e2e/__screenshots__/pos25-till.png`)

<verification>
- `pnpm --dir frontend vitest run use-online-status` green
- `pnpm --dir frontend exec tsc --noEmit` passes; eslint on touched files clean
- `pnpm --dir frontend exec playwright test pos-modal-revamp` green — executable POS-25 backstop: each converted surface has no `[role=dialog]` and a screenshot is written to `e2e/__screenshots__/` (replaces the former manual/SUMMARY screenshot note)
- no repeated `HEAD /api/v1/pos/menu/categories 404` in the console (POS-26; asserted by `use-online-status.test.ts` — no fetch on mount)
- GitNexus `detect_changes()` shows only the expected cleanup symbols changed
</verification>

<success_criteria>
Void/refund and till are dedicated panels with full info and unchanged authorization; the connectivity
404 is gone; command palette and finance modals remain untouched.
</success_criteria>

<output>
Create `.planning/phases/07.3-pos-kitchen-production-bug-fixes-ux-revamp/07.3-09-SUMMARY.md` when done.
</output>
