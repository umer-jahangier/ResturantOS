---
phase: 07.3-pos-kitchen-production-bug-fixes-ux-revamp
plan: 06
type: execute
wave: 2
depends_on: ["07.3-02", "07.3-03"]
files_modified:
  - frontend/lib/hooks/pos/use-orders.ts
  - frontend/components/pos/order-table-detail-drawer.tsx
  - frontend/components/pos/order-management.tsx
  - frontend/__tests__/components/pos/order-detail-fire.test.tsx
  - frontend/e2e/pos-kitchen-live-sync.spec.ts
  - frontend/e2e/pos-add-existing-revision.spec.ts
autonomous: true
requirements: [POS-20, POS-21, POS-25]

must_haves:
  truths:
    - "Opening an order refetches live and reflects per-item kitchen status without a manual reopen (POS-20 frontend)"
    - "The Order/Table detail surface has a revision-aware 'Send New Items (N)' action that fires ONLY newly-added (PENDING) items as a new revision (POS-21)"
    - "Added items appear instantly by seeding the query cache from the addItem mutation response (POS-21)"
    - "Order Management has a manual Refresh action that refetches the list (POS-21)"
    - "The order/table detail surface is a dedicated large panel/page, not a centered sm:max-w-md dialog (POS-25)"
  artifacts:
    - "useOrder refetchInterval-while-open (POS-20)"
    - "Send New Items (N) CTA wired into the detail surface"
    - "manual Refresh control in Order Management"
    - "order-table-detail-drawer converted to a large panel/page surface"
  key_links:
    - "KITCHEN_ITEM_STATUS_CHANGED (07.3-02) -> useOrder refetch -> live item status"
    - "useAddItem onSuccess seeds useOrder cache from the returned Order"
    - "detail surface -> useSendToKds (fires only PENDING = new revision)"
---

<objective>
Wire the frontend to the new kitchen→POS sync (POS-20/D-05), give the Order/Table detail surface a
revision-aware "Send New Items (N)" action with instant UI (POS-21/D-06), add a manual Refresh to
Order Management, and convert the detail drawer from a modal to a dedicated large panel/page
(POS-25/D-10, detail surface).

Purpose: `useOrder` already refetches on open but the data was stale because pos-service was never
told (fixed backend-side in 07.3-02) — here we add a modest `refetchInterval` so live transitions
show while the order is open. The detail drawer renders QuickAdd + Instructions + Settlement but has
NO Send-to-Kitchen CTA, so added items stay PENDING forever (RESEARCH POS-21). And the drawer is a
`Dialog` (RESEARCH POS-25) that must become a large panel/page.

Output: `use-orders.ts` refetch + cache-seed, a "Send New Items (N)" CTA in the detail surface, a
Refresh control in Order Management, and the panelized detail surface.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07.3-pos-kitchen-production-bug-fixes-ux-revamp/07.3-CONTEXT.md
@.planning/phases/07.3-pos-kitchen-production-bug-fixes-ux-revamp/07.3-UI-SPEC.md
@frontend/lib/hooks/pos/use-orders.ts
@frontend/components/pos/order-table-detail-drawer.tsx
@frontend/components/pos/order-management.tsx

**Depends on 07.3-02 (POS-20 backend event) and 07.3-03 (terminal/cart base).** The revision fire
flow is already correct backend-side (sendToKds fires only PENDING = new revision) — this is wiring
only, no backend change.

**Instant-UI seam (RESEARCH POS-21):** `useAddItem` currently discards the returned `Order`
(use-orders.ts:127-131); the repository DOES return it (pos.repository.ts:111-114). Seed the
`queryKeys.pos.order` cache with the mutation response in `useAddItem.onSuccess` so the added item
renders without waiting for a refetch.

**POS-25 detail surface:** convert `order-table-detail-drawer.tsx` from a centered/edge Dialog to a
large in-place panel or routed page occupying the primary content column (UI-SPEC §5). Preserve BOTH
callers — Order Management (orderId mode) and Table Floor View (tableId mode) — and every existing
child (line items, InstructionsField, QuickAddSearch, SettlementActions).

**GitNexus (CLAUDE.md — MANDATORY):** run `impact` on the hooks/components before editing; warn on
HIGH/CRITICAL; `detect_changes()` before commit; use GitNexus `rename` for renames.

**No new npm packages.**
</context>

<tasks>

<task type="auto">
  <name>Task 1: Live refetch on open + instant-add cache seed (use-orders.ts)</name>
  <files>frontend/lib/hooks/pos/use-orders.ts</files>
  <read_first>
    - frontend/lib/hooks/pos/use-orders.ts (useOrder :29-36; useAddItem onSuccess :127-131; useSendToKds :167-189 for the invalidation pattern)
    - frontend/lib/repositories/pos.repository.ts (addItem returns the updated Order)
    - frontend/lib/hooks/query-keys.ts (queryKeys.pos.order)
  </read_first>
  <action>
    Run GitNexus `impact` on `useOrder`/`useAddItem` before editing. Add a modest `refetchInterval`
    (e.g. 5000ms) to `useOrder` so an open order reflects live KITCHEN_ITEM_STATUS_CHANGED-driven
    updates without a manual reopen — keep `staleTime:0`/enable-on-open behaviour. In
    `useAddItem.onSuccess`, seed the `queryKeys.pos.order(branchId, variables.orderId)` cache with the
    returned `Order` (use the `_data` argument, which is the mutation response) BEFORE the existing
    invalidations, so the added line appears instantly. Do not change the offline stub branch. Respect
    the repo's `react-hooks/set-state-in-effect` ESLint rules (no setState-in-effect).
  </action>
  <acceptance_criteria>
    - `useOrder` has a numeric `refetchInterval`
    - `useAddItem.onSuccess` calls `queryClient.setQueryData(queryKeys.pos.order(...), data)` with the mutation response
    - `pnpm --dir frontend exec tsc --noEmit` passes and `pnpm --dir frontend exec eslint lib/hooks/pos/use-orders.ts` is clean
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir frontend exec eslint lib/hooks/pos/use-orders.ts</automated>
  </verify>
  <done>Open orders live-refetch and added items appear instantly via cache seeding.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 'Send New Items (N)' revision CTA + panelize the detail surface</name>
  <files>frontend/components/pos/order-table-detail-drawer.tsx, frontend/__tests__/components/pos/order-detail-fire.test.tsx</files>
  <read_first>
    - frontend/components/pos/order-table-detail-drawer.tsx (Dialog wrapper :74-84; QuickAddSearch :278-344; SettlementActions footer :134-136; order.items with itemStatus/revisionNo)
    - frontend/lib/hooks/pos/use-orders.ts (useSendToKds — fires only PENDING lines, new revision)
    - frontend/components/pos/revision-chip.tsx (deriveRevisionLog, RevisionBadge for the "new items" indication)
    - .planning/phases/07.3-pos-kitchen-production-bug-fixes-ux-revamp/07.3-UI-SPEC.md (§5 modal→page contract; §3 detail surface)
  </read_first>
  <behavior>
    - when the order has ≥1 PENDING (unfired) line, a "Send New Items (N)" button shows with N = count of PENDING lines
    - clicking it calls useSendToKds (fires only the PENDING lines as a new revision); already-fired lines are never resent
    - when there are 0 PENDING lines the button is hidden/disabled
    - the surface renders in the primary content column, not a centered sm:max-w-md dialog (no `sm:max-w-md` class)
  </behavior>
  <action>
    Run GitNexus `impact` on the drawer component. Convert `order-table-detail-drawer.tsx` from the
    `Dialog`-based surface to a dedicated large in-place panel (or a routed page) that occupies the
    primary content area with no horizontal body overflow (UI-SPEC §5) — preserve BOTH orderId and
    tableId modes and all existing children. Add a revision-aware **"Send New Items (N)"** action in
    the detail footer/header, wired to `useSendToKds(order.id)`, where N = number of PENDING lines
    (compute from `order.items`); hide/disable it when N == 0. Gate the CTA off a resolved `order` for
    both drawer modes. Keep the SettlementActions footer. Write order-detail-fire.test.tsx (RTL)
    asserting the N-count label, that clicking calls the send mutation, and that the surface has no
    `sm:max-w-md` dialog class.
  </action>
  <acceptance_criteria>
    - order-detail-fire.test.tsx asserts the button shows "Send New Items (2)" for an order with 2 PENDING lines and calls the send mutation on click
    - the test asserts the component tree contains no element with class `sm:max-w-md`
    - both orderId and tableId modes still resolve an order (tsc passes; no removed prop)
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir frontend vitest run order-detail-fire</automated>
  </verify>
  <done>The detail surface is a large panel/page with a working revision-fire CTA that sends only new items.</done>
</task>

<task type="auto">
  <name>Task 3: Manual Refresh control in Order Management</name>
  <files>frontend/components/pos/order-management.tsx</files>
  <read_first>
    - frontend/components/pos/order-management.tsx (useOrderSummaries :109; the filter header row :198-246)
    - frontend/lib/hooks/pos/use-orders.ts (useOrderSummaries — add a refetch handle if needed)
  </read_first>
  <action>
    Add a manual **Refresh** control to the Order Management header that re-fetches the summaries list
    (call the `refetch` returned by `useOrderSummaries`/`useQuery`). Show a subtle spinning/disabled
    state while `isFetching`. Do not alter the existing fade-out-list invariant. (POS-24 filters/search
    are a later plan — this task adds ONLY the Refresh control.)
  </action>
  <acceptance_criteria>
    - order-management.tsx has a Refresh button that calls the query's `refetch`
    - `pnpm --dir frontend exec tsc --noEmit` passes
    - the button has a `data-testid` (e.g. `order-management-refresh`) for E2E
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir frontend exec tsc --noEmit</automated>
  </verify>
  <done>Order Management has a working manual Refresh.</done>
</task>

<task type="auto">
  <name>Task 4: Wave-0 E2E — pos-kitchen-live-sync.spec.ts (POS-20) + pos-add-existing-revision.spec.ts (POS-21)</name>
  <files>frontend/e2e/pos-kitchen-live-sync.spec.ts, frontend/e2e/pos-add-existing-revision.spec.ts</files>
  <read_first>
    - frontend/e2e/pos-settlement.spec.ts (login/gotoRobust helpers, cashier-seed constants, Blocked pattern, live-dev-stack conventions to mirror)
    - frontend/playwright.config.ts (testDir ./e2e — no config change)
    - frontend/components/pos/order-table-detail-drawer.tsx (the panelized detail surface + "Send New Items (N)" CTA from Task 2 — the features these specs prove)
    - frontend/lib/hooks/pos/use-orders.ts (useOrder refetch-on-open + useAddItem cache-seed from Task 1)
  </read_first>
  <action>
    Create two Wave-0 Playwright specs (skeletons asserting the core truth are acceptable; reuse the
    login + gotoRobust helpers and cashier-seed constants from pos-settlement.spec.ts, same live dev
    stack).
    (a) `pos-kitchen-live-sync.spec.ts` (POS-20 / D-05): open an order that has a fired item in POS
    order detail; drive a kitchen per-item status change (a second browser context on the KDS board OR a
    direct authenticated POST to the item-status endpoint); assert the POS order-detail item status
    updates LIVE within the refetch window WITHOUT a manual page reload.
    (b) `pos-add-existing-revision.spec.ts` (POS-21 / D-06): open an existing already-fired order in the
    detail surface, add a new item, assert the "Send New Items (N)" CTA shows N = the count of new
    PENDING lines, click it, and assert ONLY the new item fires as a new revision (already-fired lines
    are not resent — observe the fire response / revision indicator). Use the Blocked pattern for missing
    seed/environment preconditions; reserve a hard failure for a real regression. Do NOT edit
    playwright.config.ts.
  </action>
  <acceptance_criteria>
    - both `frontend/e2e/pos-kitchen-live-sync.spec.ts` and `frontend/e2e/pos-add-existing-revision.spec.ts` exist under the Playwright testDir
    - the live-sync spec asserts a kitchen item-status change reflects in POS order detail with no reload; the revision spec asserts "Send New Items (N)" fires only the newly-added item as a new revision
    - `pnpm --dir frontend exec playwright test pos-kitchen-live-sync pos-add-existing-revision` runs both specs and their POS-20/POS-21 assertions pass (against the live dev stack)
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir frontend exec playwright test pos-kitchen-live-sync pos-add-existing-revision</automated>
  </verify>
  <done>POS-20 (live kitchen→POS sync) and POS-21 (add-existing new-revision fire) each have an executable Wave-0 Playwright proof that ships with the wiring it proves.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → pos-service (send/add) | user-initiated fire/add crosses to the server |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07.3-18 | Tampering | resend of already-fired lines | medium | mitigate | "Send New Items" uses useSendToKds which fires ONLY PENDING lines server-side (backend excludes fired lines) — the UI never resends served/fired items. |
| T-07.3-19 | Information Disclosure | live refetch of order detail | low | mitigate | useOrder is branch/tenant-scoped via useCurrentUser; refetchInterval does not widen scope. |
| T-07.3-SC | Tampering | dependencies | low | accept | No new npm package installs. |
</threat_model>

## Artifacts this phase produces
- **Hook change:** `useOrder` refetchInterval; `useAddItem` cache-seed
- **Component:** "Send New Items (N)" CTA + panelized `order-table-detail-drawer.tsx`
- **Control:** Order Management manual Refresh
- **Test:** `order-detail-fire.test.tsx`
- **Wave-0 E2E:** `frontend/e2e/pos-kitchen-live-sync.spec.ts` (POS-20), `frontend/e2e/pos-add-existing-revision.spec.ts` (POS-21)

<verification>
- `pnpm --dir frontend vitest run order-detail-fire` green
- `pnpm --dir frontend exec playwright test pos-kitchen-live-sync pos-add-existing-revision` green (POS-20/POS-21 Wave-0 proofs)
- `pnpm --dir frontend exec tsc --noEmit` + `eslint lib/hooks/pos/use-orders.ts` clean
- GitNexus `detect_changes()` shows only the expected frontend order-detail symbols changed
</verification>

<success_criteria>
Opening an order shows live per-item status; added items persist+appear instantly and fire only the
new items as a revision; Order Management has a Refresh; the detail surface is a large panel/page.
</success_criteria>

<output>
Create `.planning/phases/07.3-pos-kitchen-production-bug-fixes-ux-revamp/07.3-06-SUMMARY.md` when done.
</output>
