---
phase: 07.3-pos-kitchen-production-bug-fixes-ux-revamp
verified: 2026-07-13T00:00:00Z
status: passed
score: 13/13 must-haves verified
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 12/13 must-haves verified (1 partial — POS-23/SC4)
  gaps_closed:
    - "An order closes only when it is BOTH fully Paid AND fully Served — enforced on the payment and serve flows (Roadmap SC4 / cross-cutting truth / POS-23). The legacy POST /orders/{id}/close bypass (OrderServiceImpl.closeOrder) that force-closed on tender-sum validation alone, without checking derivedStatus==SERVED and without persisting OrderPayment rows, has been deleted. The endpoint now returns HTTP 410 Gone and calls no service. performClose is reachable from exactly ONE call site (maybeCloseOrder), which is the ONLY remaining path to CLOSED, and it is unconditionally Paid-AND-Served gated."
  gaps_remaining: []
  regressions: []
---

# Phase 07.3: POS & Kitchen Production Bug-Fixes & UX Revamp Verification Report

**Phase Goal:** Turn the Phase-7.1 POS/KDS into a production-grade surface by fixing the 16
issues from testing (`bugs.md`): eliminate draft-order persistence, merge item quantities,
make table optional with an order-type selector, reset the terminal after send with charge
gated on send, propagate per-item kitchen status back to POS in real time, fire only
newly-added items from Order Management as a revision, replace modal-heavy flows with
dedicated full-page/large views, decouple payment from close so an order closes only when
Paid AND Served, surface closed/paid orders with search + payment status + item-quantity +
assign-table, remove the stray connectivity 404, and redesign the KDS into
station-isolated New/Started/Preparing/Ready item-status columns with subtle
prioritization — WITHOUT regressing the Phase-7.1 revision/derivation model or the
cross-service messaging contracts.

**Verified:** 2026-07-13
**Status:** passed
**Re-verification:** Yes — after gap closure (plan 07.3-11, D-08 deprecate-and-remove strategy)

## Goal Achievement

### Observable Truths (Roadmap Success Criteria, goal-backward)

| # | Truth (Roadmap SC) | Status | Evidence |
|---|---|---|---|
| 1 | Tapping menu items never creates a DB order; DRAFT gone from user-visible flows; repeated taps merge ×N with ±. (POS-16, POS-17) | ✓ VERIFIED | Unchanged since prior verification. `pos-terminal.tsx` `handleSendToKitchen` only calls `createOrder`/`addItem` on explicit Send/Charge — menu taps only mutate local `cart` state. `cart-reducer.ts` merges on `menuItemId + sorted modifierIds + notes`. `OrderServiceImpl.listOrderSummaries` default excludes `DRAFT`. Not touched by the gap-closure plan; regression-checked by re-reading `pos-terminal.tsx`/`cart-reducer.ts` — no diff since last pass. |
| 2 | Dine-in/Takeaway/Pickup with table optional via searchable Available/Occupied selector (POS-18); terminal clears after send, Charge Now gated on send (POS-19) | ✓ VERIFIED | Unchanged. `order-type-toggle.tsx` + `table-select-combobox.tsx` wired into `PreSendCart`; `V6__order_type_pickup.sql` adds `PICKUP`. Charge Now disabled until `sentOrder` exists; `Clear / New Order` resets cart. Not touched by the gap-closure plan. |
| 3 | Kitchen item-status changes reflect on POS in real time without manual reopen (POS-20); Order-Management add-items fires only new items as a revision + manual Refresh (POS-21) | ✓ VERIFIED | Unchanged. `KitchenItemStatusConsumer` applies status with no-downgrade guard + idempotency, recomputes `derivedStatus` via `OrderStatusDerivationService` only, calls `TableService.syncStatusForOrder`. `use-orders.ts` polls via `refetchInterval`. `order-management.tsx` has an explicit `Refresh` button. Not touched by the gap-closure plan. |
| 4 | Charge Now full-page/large view with full analytics + payment history (POS-22); recording payment sets paymentStatus and persists OrderPayment WITHOUT closing; order closes only when BOTH Paid AND Served, enforced on payment AND serve flows (POS-23) | ✓ VERIFIED — gap closed | `charge/page.tsx` + `charge-summary.tsx` remain a dedicated route with full analytics/history (re-confirmed `charge-summary.tsx` still wires `useRecordPayment`, not `closeOrder`/`PaymentPanel`, after the gap-closure edit). `PaymentServiceImpl.recordPayment` persists `OrderPayment`, blocks terminal orders, and unconditionally calls `orderService.maybeCloseOrder(orderId)` (direct code read, line 68). `OrderServiceImpl.maybeCloseOrder` (L534-558) is a no-op unless `paymentStatus==PAID && derivedStatus==SERVED`; the ONLY call to `performClose` in the entire file is the one inside `maybeCloseOrder` (`grep -c "= performClose(" == 1`, confirmed at L556). `markItemServed` (L620-646) also routes exclusively through `maybeCloseOrder` when the last item is served. **The legacy bypass is now GONE**: `OrderService.closeOrder`/`OrderServiceImpl.closeOrder` are deleted (`grep -c closeOrder` == 0 in both files); `PaymentController.POST /{id}/close` (L60-71) is a bodyless-signature handler that calls no service and unconditionally returns `HttpStatus.GONE` with `Deprecation`/`Sunset`/`Link` headers and an `ENDPOINT_RETIRED` `ApiWarning` body (no internal leak) — `grep -c orderService` in PaymentController.java == 0 (field removed). `SettlementSemanticsIT` re-run independently by this verification: **6/6 green** (`mvn -pl services/pos-service -am test -Dtest=SettlementSemanticsIT` → `Tests run: 6, Failures: 0, Errors: 0`), including the new `servedAndPaidClose_publishesExactlyOneOrderClosedEvent` backstop and `fullPayment_onUnservedOrder_staysOpen_paidButNotClosed`. `PeriodLockCloseIT` re-run independently: **2/2 green**, now proving period-lock fail-closed on the surviving `recordPayment → maybeCloseOrder → performClose` seam. |
| 5 | Order Management shows closed/paid orders with filters/search/payment-status/Items column/Assign-Table; duplicate payment blocked, paid orders stay accessible (POS-24); payment/detail/void-refund/till are dedicated pages/large panels, not modals (POS-25); no `/pos/menu/categories` 404 (POS-26) | ✓ VERIFIED | Unchanged since prior verification (gap-closure plan did not touch Order Management, void/refund, till, or connectivity files). `order-management.tsx`, `void-refund-dialog.tsx`, `till-session-bar.tsx`, `use-online-status.ts` — no diff. |
| 6 | KDS station-isolated New/Started/Preparing/Ready item-status columns, slim cards, dedicated detail page; stations seeded, table number on tickets (KDS-04); subtle long-running-order highlighting, board scales (KDS-05) | ✓ VERIFIED | Unchanged since prior verification (gap-closure plan is pos-service/frontend-pos-payments only; does not touch kitchen-service or KDS frontend). |

**Score:** 6/6 Roadmap Success Criteria fully verified (SC4 upgraded from PARTIAL to VERIFIED by plan 07.3-11's gap closure).

### Independent Backend Test Re-Runs (this verification session)

| Suite | Command | Result | Status |
|---|---|---|---|
| SettlementSemanticsIT | `mvn -pl services/pos-service -am test -Dtest=SettlementSemanticsIT,PeriodLockCloseIT -Dsurefire.failIfNoSpecifiedTests=false -Dspring.rabbitmq.listener.simple.auto-startup=false` | Tests run: 6, Failures: 0, Errors: 0 | ✓ PASS |
| PeriodLockCloseIT | (same command) | Tests run: 2, Failures: 0, Errors: 0 | ✓ PASS |
| AssignTableIT, OrderSummaryDtoIT, TableOrderLookupIT, VoidRefundOpaIT | `mvn -pl services/pos-service -am test -Dtest=AssignTableIT,OrderSummaryDtoIT,TableOrderLookupIT,VoidRefundOpaIT -Dsurefire.failIfNoSpecifiedTests=false -Dspring.rabbitmq.listener.simple.auto-startup=false` | Tests run: 17, Failures: 0, Errors: 0 (4+3+4+6) | ✓ PASS |
| OrderRevisionIT | `mvn -pl services/pos-service -am test -Dtest=OrderRevisionIT ...` | Tests run: 4, Failures: 0, Errors: 1 — the 1 error is the exact, documented pre-existing `LazyInitializationException` on `Order.items` (deferred-items.md, 07.3-01/07.3-04 entries; unrelated to plan 07.3-11's single-line fixture repoint) | ✓ PASS (3/4 + 1 known deferred, not a phase regression) |
| Frontend type-check | `cd frontend && npx tsc --noEmit` | Exit 0, no errors | ✓ PASS |

`mvn` invocations used the two mandatory flags (`-Dsurefire.failIfNoSpecifiedTests=false`, `-Dspring.rabbitmq.listener.simple.auto-startup=false`) per project memory; background `OutboxRelay` scheduler `StaleObjectStateException` noise observed in raw console output is an async scheduled-task log line, not a test failure or assertion error — confirmed by the clean `Tests run: N, Failures: 0, Errors: 0` summary lines and `BUILD SUCCESS`.

### Source-Contract Verification (POS-23 retirement, independently re-derived)

| Check | Command | Result |
|---|---|---|
| 410 Gone present | `grep -n "HttpStatus.GONE" PaymentController.java` | L66 match |
| Deprecation header present | `grep -n "Deprecation" PaymentController.java` | L67 match |
| No orderService field/calls in PaymentController | `grep -c "orderService" PaymentController.java` | 0 |
| closeOrder deleted from interface | `grep -c "closeOrder" OrderService.java` | 0 |
| closeOrder deleted from impl | `grep -c "closeOrder" OrderServiceImpl.java` | 0 |
| No tender-sum-only validation path | `grep -c "validateExact" OrderServiceImpl.java` | 0 |
| Exactly one performClose call site | `grep -n "= performClose(" OrderServiceImpl.java` | 1 match (L556, inside maybeCloseOrder) |
| CloseOrderRequest.java removed | `test -f .../dto/CloseOrderRequest.java` | absent |
| OrderCloseIdempotencyIT.java removed | `test -f .../OrderCloseIdempotencyIT.java` | absent |
| No live FE reference to useCloseOrder | `grep -rn "useCloseOrder(" frontend/` | 0 matches |
| No live FE reference to payment-panel | `grep -rn "payment-panel" frontend/` | 0 matches |
| payment-panel.tsx / .test.tsx removed | `test -f ...` | both absent |
| PosRepository.closeOrder removed | `grep -n "closeOrder" pos.repository.ts` | 0 matches |
| No debt markers in any of the 15 files touched by plan 07.3-11 | `grep -nE "TBD\|FIXME\|XXX\|TODO\|HACK\|PLACEHOLDER"` across all 15 files | 0 matches |

All 13 source-contract checks from the plan's `must_haves.prohibitions` verification strings pass exactly as claimed — independently re-derived by this verification, not taken from the SUMMARY's word.

### Cross-Cutting Truths (goal-backward, ROADMAP.md-declared)

| Truth | Status | Evidence |
|---|---|---|
| No DB order exists until Send/Charge | ✓ VERIFIED | Unchanged, not touched by gap closure. |
| `derivedStatus` only via `OrderStatusDerivationService` | ✓ VERIFIED | Unchanged, not touched by gap closure. `markItemServed`'s `derivedStatus` assignment (L635) still calls `orderStatusDerivationService.derive(...)`, confirmed by direct read of the surrounding code in this session. |
| Table status only via `TableService.syncStatusForOrder` | ⚠️ MOSTLY VERIFIED (one edge-case exception, not UI-reachable — pre-existing, out of scope) | `performClose` still calls `tableService.syncStatusForOrder` (L581-582), confirmed unchanged. The previously-documented BE-CR-02 edge case (table-to-table reassignment not UI-reachable) remains open as a WARNING, unrelated to and not addressed by this gap-closure plan (POS-23 only). Does not block phase goal. |
| Cross-service event field-name parity (message actually consumed, not dropped) | ✓ VERIFIED | Unchanged, not touched by gap closure. |
| An order closes only when Paid AND Served | ✓ VERIFIED — gap closed | `maybeCloseOrder` is now the ONLY code path that can transition to CLOSED (single `performClose` call site, confirmed by grep + direct read). The previously-live legacy bypass is deleted; its HTTP surface returns 410 Gone and calls no service. |

### Required Artifacts (spot-checked, delta since prior verification)

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `PaymentController.closeOrder` (410 handler) | Retirement handler, no service call | ✓ VERIFIED | Present at L60-71; bodyless signature, `HttpStatus.GONE`, `Deprecation`/`Sunset`/`Link` headers, `ENDPOINT_RETIRED` `ApiWarning`, `@RequiresFeature("FEATURE_POS")` class guard preserved. |
| `OrderService.closeOrder` / `OrderServiceImpl.closeOrder` | Deleted | ✓ VERIFIED (absence confirmed) | `grep -c closeOrder` == 0 in both files. |
| `CloseOrderRequest.java` | Deleted | ✓ VERIFIED (absence confirmed) | File absent on disk. |
| `PosTestBase.closeViaServeAndPay(...)` | Shared serve+pay test helper | ✓ VERIFIED | Present, used by 5 migrated IT files (AssignTableIT, OrderSummaryDtoIT, TableOrderLookupIT, VoidRefundOpaIT, OrderRevisionIT), all green in this session's independent re-run. |
| `SettlementSemanticsIT` retirement backstop | `servedAndPaidClose_publishesExactlyOneOrderClosedEvent` + reaffirmed `fullPayment_onUnservedOrder_staysOpen` | ✓ VERIFIED | Both tests present (grep-confirmed) and green (6/6 independent re-run). |
| `OrderCloseIdempotencyIT.java` | Deleted (subject retired) | ✓ VERIFIED (absence confirmed) | File absent on disk. |
| `PaymentPanel`, `payment-panel.test.tsx`, `useCloseOrder` hook | Deleted, zero live refs | ✓ VERIFIED (absence + zero-refs confirmed) | All three absent/removed; repo-wide grep confirms zero live references. |
| All other 07.3 artifacts (cart-reducer, PaymentStatus derivation, KDS board, Order Management, etc.) | Present, wired | ✓ VERIFIED (unchanged) | Not touched by plan 07.3-11 — carried forward from prior verification pass, spot-checked for collateral damage (charge-summary.tsx, use-payments.ts siblings) in this session; no regression found. |

### Key Link Verification (delta since prior verification)

| From | To | Via | Status |
|---|---|---|---|
| `PaymentController.POST /{id}/close` (legacy) | (retired) | Returns 410 Gone directly, no service call | ✓ RETIRED (was NOT_WIRED-to-invariant; now not-a-close-path at all) |
| `OrderServiceImpl.markItemServed` | `maybeCloseOrder` | Direct call when derivedStatus becomes SERVED | ✓ WIRED (unchanged, re-confirmed) |
| `PaymentServiceImpl.recordPayment` | `OrderService.maybeCloseOrder` | Direct call, no-op unless Paid+Served | ✓ WIRED (unchanged, re-confirmed at L68) |
| `maybeCloseOrder` | `performClose` | The ONLY call site in OrderServiceImpl.java | ✓ WIRED (exclusively — confirmed single call site) |
| All other 07.3 key links (kitchen→POS event, settlement route, assign-table, connectivity) | — | — | ✓ WIRED (unchanged, carried forward) |

### Requirements Coverage (POS-16..26, KDS-04/05)

All 13 requirement IDs declared across the 11 plans' `requirements:` frontmatter (POS-16
through POS-26 across 07.3-01..10, plus POS-23 again in the gap-closure plan 07.3-11) match
REQUIREMENTS.md exactly — REQUIREMENTS.md marks all 13 `[x] Complete` and maps them to
Phase 07.3 in its coverage table. **No orphaned requirements.**

| Requirement | Status | Evidence |
|---|---|---|
| POS-16 | ✓ SATISFIED | See SC1 |
| POS-17 | ✓ SATISFIED | See SC1 |
| POS-18 | ✓ SATISFIED | See SC2 |
| POS-19 | ✓ SATISFIED | See SC2 |
| POS-20 | ✓ SATISFIED | See SC3 |
| POS-21 | ✓ SATISFIED | See SC3 |
| POS-22 | ✓ SATISFIED | See SC4 |
| POS-23 | ✓ SATISFIED — gap closed by plan 07.3-11 | New payment/serve paths correct AND the legacy bypass is now deleted/retired; `maybeCloseOrder` is the sole close path, independently re-verified green (SettlementSemanticsIT 6/6, PeriodLockCloseIT 2/2). |
| POS-24 | ✓ SATISFIED | See SC5 |
| POS-25 | ✓ SATISFIED | See SC5 |
| POS-26 | ✓ SATISFIED | See SC5 |
| KDS-04 | ✓ SATISFIED | See SC6 |
| KDS-05 | ✓ SATISFIED | See SC6 |

### Anti-Patterns Found

Scanned all 15 files touched by the gap-closure plan (07.3-11) for
`TBD`/`FIXME`/`XXX`/`TODO`/`HACK`/`PLACEHOLDER`: **zero debt markers found**. No stub
patterns (`return null`/empty-array/console.log-only) found in the retirement handler or
the migrated test helpers — the 410 handler is a complete, well-formed response with real
headers and body; `closeViaServeAndPay` drives real state transitions through the actual
service seam, not a mock/stub.

### Advisory Code Review Findings — Carried Forward (unaffected by this gap closure)

The 5 non-blocking production-hardening findings from the prior verification pass
(BE-CR-02, BE-CR-03, BE-CR-04, FE-CR-01, FE-CR-02) were explicitly out of scope for plan
07.3-11 (POS-23-only gap closure) and remain open, unrelated to the phase's goal
achievement. They do not block this phase's `passed` status — recommended for a dedicated
hardening pass as previously advised.

### Pre-Existing / Out-of-Scope Defects (deferred-items.md) — Re-Confirmed Accurate

- **`OrderRevisionIT` LazyInitializationException** — independently re-reproduced in this
  verification session (`Tests run: 4, Failures: 0, Errors: 1`, the 1 error being the exact
  `Cannot lazily initialize collection of role 'io.restaurantos.pos.domain.model.Order.items'`
  stack trace documented in deferred-items.md). The gap-closure plan's own SUMMARY
  additionally confirms this is unchanged from the pre-plan baseline via its own
  `git stash`-style comparison. Confirmed accurate, not a phase regression.
- **`KdsController.getTickets` unscoped-branch LazyInitializationException**,
  **KDS pagination/sort gap**, **`eslint-boundary` timeout**, **`addItem` response-relay
  hang** — none of these are touched by, or relevant to, plan 07.3-11 (pos-service payment/
  close-path + frontend dead-code deletion only). Not re-verified in this session as they
  are outside the gap-closure plan's file scope; carried forward as accurate per the prior
  verification's characterization check.

### Human Verification Required

None. This re-verification closes the single outstanding gap via deterministic,
programmatically-verifiable evidence: source-contract greps (all 13 checks reproduced
independently), two independent targeted Maven IT re-runs (23 tests total: 6+2+4+3+4+6+
1-known-deferred, all green except the pre-documented deferred item), and a clean
`tsc --noEmit`. No visual/UX/runtime-only behavior was introduced by this gap-closure plan
that requires human judgment — it is a pure backend contract retirement + dead-code
deletion.

The two previously-recommended (non-blocking) live click-through items from the prior
verification pass remain valid recommendations for whenever the dev-stack's independently-
documented RabbitMQ/gateway issues (deferred-items.md) are resolved, but are not required
to close this phase.

### Gaps Summary

None. The single BLOCKER gap from the initial verification pass (legacy `POST
/orders/{id}/close` bypassing the Paid-AND-Served invariant, BE-CR-01/POS-23/SC4) has been
closed by plan 07.3-11 using the user-locked D-08 "deprecate & remove" strategy:

- The legacy endpoint is retired to HTTP 410 Gone (no internal leak, still auth/feature-guarded).
- The `closeOrder` service method (interface + impl) is deleted entirely.
- `performClose` — the only code that can set `status = CLOSED` — is now reachable from
  exactly one call site: `maybeCloseOrder`, which is unconditionally gated on
  `paymentStatus == PAID && derivedStatus == SERVED`.
- All 8 IT fixtures that previously used the bypass were migrated to drive closure through
  the real serve+pay seam (`PosTestBase.closeViaServeAndPay`), and a regression backstop
  (`SettlementSemanticsIT.servedAndPaidClose_publishesExactlyOneOrderClosedEvent` +
  reaffirmed `fullPayment_onUnservedOrder_staysOpen_paidButNotClosed`) was added.
- Orphaned frontend dead code (`PaymentPanel`, `useCloseOrder`, `payment-panel.test.tsx`)
  that was the sole historical caller of the legacy endpoint is deleted.

Every claim in the gap-closure SUMMARY was independently re-derived in this verification
session (not taken on trust): all 13 source-contract greps reproduced with identical
results, `SettlementSemanticsIT` (6/6) and `PeriodLockCloseIT` (2/2) re-run green, the 4
migrated fixture ITs (17/17) re-run green, `OrderRevisionIT`'s 1 known-deferred failure
re-reproduced exactly as documented (not a new regression), and `tsc --noEmit` re-run clean.
No new gaps were found. Phase 07.3 goal is achieved: 13/13 must-haves verified, all 6
Roadmap Success Criteria fully verified, all 13 requirement IDs SATISFIED and traceable to
REQUIREMENTS.md with zero orphans.

---

_Verified: 2026-07-13_
_Verifier: Claude (gsd-verifier)_
