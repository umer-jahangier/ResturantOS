---
phase: 07.3-pos-kitchen-production-bug-fixes-ux-revamp
plan: 07
type: execute
wave: 2
depends_on: ["07.3-01"]
files_modified:
  - frontend/lib/api-client/schemas/pos.schema.ts
  - frontend/lib/models/pos.model.ts
  - frontend/lib/adapters/pos.adapter.ts
  - frontend/lib/repositories/pos.repository.ts
  - frontend/lib/hooks/pos/use-payments.ts
  - frontend/components/pos/payment-status-badge.tsx
  - frontend/components/pos/charge-summary.tsx
  - frontend/app/(tenant)/app/pos/orders/[orderId]/charge/page.tsx
  - frontend/components/pos/settlement-actions.tsx
  - frontend/__tests__/components/pos/payment-status-badge.test.tsx
autonomous: true
requirements: [POS-22, POS-23, POS-25]

must_haves:
  truths:
    - "Charge Now is a dedicated full-page route (/app/pos/orders/[orderId]/charge), not a sm:max-w-md dialog (POS-22/25)"
    - "The charge page shows order no., table/type, customer, cashier, order time, live status + payment-status chip, item list (qty/status/notes), full money breakdown, payment history, and remaining balance (POS-22)"
    - "Recording a payment updates amount-paid/remaining and the payment chip WITHOUT closing; a paid-but-unserved order shows Paid with its live status; Closed only when Paid AND Served (POS-23 frontend)"
    - "The payment-status badge renders hue+icon+label for PAID/PARTIALLY_PAID/UNPAID/REFUNDED (never color-only)"
    - "A paid order blocks new tenders but stays viewable"
  artifacts:
    - "payment-status-badge.tsx (4-state chip)"
    - "charge-summary.tsx + charge route page"
    - "useOrderPayments hook + PosRepository.getPayments"
  key_links:
    - "settlement-actions CHARGE NOW -> navigate to charge route (not open Dialog)"
    - "GET /orders/{id}/payments (07.3-01) -> useOrderPayments -> payment history"
    - "recordPayment -> paymentStatus chip updates, order not closed"
---

<objective>
Replace the cramped `sm:max-w-md` payment dialog with a dedicated full-page Charge surface
(POS-22/D-07, POS-25/D-10) that shows full order + payment analytics and payment history, and renders
the decoupled settlement semantics from 07.3-01 (Paid ≠ Closed until Served, POS-23/D-08 frontend).

Purpose: `settlement-actions.tsx` mounts `PaymentPanel` in a `Dialog sm:max-w-md` that shows only
totals + split-tender (RESEARCH POS-22). This plan builds the full-page charge route, a reusable
4-state payment-status badge, and the payment-history read path, and reroutes CHARGE NOW to the page.

Output: the charge route + `charge-summary` layout, `payment-status-badge.tsx`, the `useOrderPayments`
hook, and the settlement-actions rewire.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07.3-pos-kitchen-production-bug-fixes-ux-revamp/07.3-CONTEXT.md
@.planning/phases/07.3-pos-kitchen-production-bug-fixes-ux-revamp/07.3-UI-SPEC.md
@frontend/components/pos/settlement-actions.tsx
@frontend/components/pos/payment-panel.tsx
@frontend/lib/hooks/pos/use-payments.ts
@frontend/app/(tenant)/app/pos/page.tsx

**Depends on 07.3-01:** consumes `GET /orders/{id}/payments` and the decoupled payment/close
semantics (recording payment no longer closes; paymentStatus is derived).

**Finance period-lock precondition (execution dependency, NOT a gap):** closing hits finance-service;
fresh tenants return 423 PERIOD_LOCKED. E2E/manual verification of the close path requires an open
accounting period (Phase 07.2 provisions it). Surface this in the SUMMARY, not as a silent gap — the
charge page must render the 423 as a user-facing "period locked" message (reuse the existing
ApiError-code branching pattern from use-payments.ts), never crash.

**Payment-status badge (UI-SPEC Status System delta):** 4 states, each hue+icon+label, never
color-only: PAID (success/check/"Paid"), PARTIALLY_PAID (warning/half-circle/"Partial"), UNPAID
(neutral/dot/"Unpaid"), REFUNDED (danger/undo/"Refunded"). Reuse the DS `StatusBadge` visual system.
This component is also consumed by Order Management (07.3-08).

**Reused caller (RESEARCH POS-22):** `SettlementActions` is rendered in the OrderPanel footer and the
detail drawer footer. Rerouting CHARGE NOW to the page must preserve every caller — the button
navigates to the charge route instead of opening the Dialog.

**GitNexus (CLAUDE.md — MANDATORY):** run `impact` on `SettlementActions` before editing; warn on
HIGH/CRITICAL; `detect_changes()` before commit; use GitNexus `rename` for renames.

**No new npm packages** — reuse Next.js App Router, shadcn primitives, lucide icons.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: payment-status-badge (4-state hue+icon+label) + OrderPayment schema/model + useOrderPayments</name>
  <files>frontend/components/pos/payment-status-badge.tsx, frontend/lib/api-client/schemas/pos.schema.ts, frontend/lib/models/pos.model.ts, frontend/lib/adapters/pos.adapter.ts, frontend/lib/repositories/pos.repository.ts, frontend/lib/hooks/pos/use-payments.ts, frontend/__tests__/components/pos/payment-status-badge.test.tsx</files>
  <read_first>
    - frontend/components/ui/status-badge.tsx (DS StatusBadge hue/icon/label system to reuse)
    - frontend/lib/api-client/schemas/pos.schema.ts (apiOrderSummarySchema + adapter pattern to extend)
    - frontend/lib/hooks/pos/use-payments.ts (existing mutation hooks + ApiError typing pattern)
    - frontend/lib/repositories/pos.repository.ts (getOrder shape for adding getPayments)
    - .planning/phases/07.3-pos-kitchen-production-bug-fixes-ux-revamp/07.3-UI-SPEC.md (payment-status badge spec)
  </read_first>
  <behavior>
    - <PaymentStatusBadge status="PAID"/> renders a check icon + "Paid" + success hue
    - PARTIALLY_PAID renders half-circle icon + "Partial" + warning hue
    - UNPAID renders dot icon + "Unpaid" + neutral hue
    - REFUNDED renders undo icon + "Refunded" + danger hue
    - an unknown status value renders a neutral fallback (does NOT throw — registry safety)
  </behavior>
  <action>
    Add a `PaymentStatus` union ("UNPAID"|"PARTIALLY_PAID"|"PAID"|"REFUNDED") + an `OrderPayment`
    model type (id, method, amountPaisa, referenceNo, recordedAt) to pos.model.ts, a Zod schema for
    the GET-payments response in pos.schema.ts, and an adapter in pos.adapter.ts. Add
    `PosRepository.getPayments(orderId, branchId)` and a `useOrderPayments(orderId)` hook in
    use-payments.ts (Layer-3, `.parse()` through the schema). Build `payment-status-badge.tsx` reusing
    the DS StatusBadge system with the 4 hue+icon+label states above and a non-throwing neutral
    fallback for unknown values. Write payment-status-badge.test.tsx FIRST covering all 4 states + the
    unknown-value fallback (RED→GREEN).
  </action>
  <acceptance_criteria>
    - payment-status-badge.test.tsx asserts each of the 4 states renders its label AND a non-color-only icon, and passes
    - the test asserts an unknown status renders a fallback without throwing
    - `useOrderPayments` parses the GET /orders/{id}/payments response through a Zod schema
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir frontend vitest run payment-status-badge</automated>
  </verify>
  <done>The reusable 4-state payment badge and the payments read path exist and are unit-proven.</done>
</task>

<task type="auto">
  <name>Task 2: Full-page Charge route + charge-summary layout</name>
  <files>frontend/components/pos/charge-summary.tsx, frontend/app/(tenant)/app/pos/orders/[orderId]/charge/page.tsx</files>
  <read_first>
    - frontend/components/pos/payment-panel.tsx (split-tender entry to reuse/embed)
    - frontend/lib/hooks/pos/use-orders.ts (useOrder for the full order)
    - frontend/lib/hooks/pos/use-payments.ts (useOrderPayments from Task 1; useCloseOrder ApiError-code branching for 423)
    - frontend/lib/models/pos.model.ts (Order shape: orderNo, tableId, cashierId, openedAt, subtotal/tax/discount/serviceCharge/total, items[])
    - .planning/phases/07.3-pos-kitchen-production-bug-fixes-ux-revamp/07.3-UI-SPEC.md (§2 Charge page regions + states)
  </read_first>
  <action>
    Create the App Router page `app/(tenant)/app/pos/orders/[orderId]/charge/page.tsx` (FeatureGuard
    FEATURE_POS + PermissionGuard pos.order.close, mirroring the POS page guards) rendering a
    `charge-summary.tsx` layout with the UI-SPEC §2 regions: Header (order no., table or
    "Takeaway"/"Pickup", customer or "—", cashier, order time, live derived-status chip + the Task-1
    `PaymentStatusBadge`); Items (qty, per-item StatusBadge, notes, line totals via MoneyDisplay);
    Money breakdown (subtotal, discounts, service charge, taxes, total, amount paid, remaining balance
    via MoneyDisplay); Payment history (rows from useOrderPayments; "No payments yet" empty); Take
    payment (embed/adapt the split-tender entry from PaymentPanel; charge button disabled when
    remaining ≤ 0 or amount invalid). Responsive, no fixed narrow width, no horizontal body overflow.
    States: recording a payment updates amount-paid/remaining + the payment chip WITHOUT closing; show
    "Closed (Payment completed)" read-only only when Paid AND Served; show "Paid" + live kitchen status
    when Paid && !Served; a paid order blocks new tenders (duplicate-payment guard) but stays viewable.
    Render a 423 PERIOD_LOCKED response as a user-facing "period locked" message (do not crash).
  </action>
  <acceptance_criteria>
    - the route file exists at app/(tenant)/app/pos/orders/[orderId]/charge/page.tsx
    - `pnpm --dir frontend exec tsc --noEmit` passes
    - grep charge-summary.tsx / charge page shows NO `sm:max-w-md` and NO `<Dialog` usage (it is a page)
    - the page renders payment history rows from useOrderPayments and a remaining-balance MoneyDisplay
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir frontend exec tsc --noEmit</automated>
  </verify>
  <done>A dedicated Charge page renders full order + payment analytics + history + split-tender, with the decoupled Paid/Closed states.</done>
</task>

<task type="auto">
  <name>Task 3: Reroute CHARGE NOW from Dialog to the charge page (all callers preserved)</name>
  <files>frontend/components/pos/settlement-actions.tsx</files>
  <read_first>
    - frontend/components/pos/settlement-actions.tsx (Dialog + PaymentPanel :69-76; CHARGE NOW button :51-62; the 3 render sites documented in its header comment)
    - frontend/app/(tenant)/app/pos/page.tsx (route base for navigation)
  </read_first>
  <action>
    Run GitNexus `impact` on `SettlementActions` before editing. Replace the `Dialog`+`PaymentPanel`
    block with navigation to `/app/pos/orders/{order.id}/charge` (Next.js `useRouter().push` or a
    Link) so CHARGE NOW opens the full page, not the modal. Keep the CHARGE-NOW PermissionGuard
    (pos.order.close), the "Paid ✓" chip logic (now driven by the order's paymentStatus/status), and
    the `VoidRefundDialog` cluster untouched. Preserve every existing caller (OrderPanel footer + the
    two detail-drawer footers) — they all now route to the page. Remove the now-unused Dialog import if
    nothing else in the file uses it.
  </action>
  <acceptance_criteria>
    - grep settlement-actions.tsx shows NO `<Dialog` wrapping PaymentPanel (navigation replaces it)
    - CHARGE NOW navigates to a path matching `/app/pos/orders/.*/charge`
    - `pnpm --dir frontend exec tsc --noEmit` passes and eslint on the file is clean (no layer-boundary violation)
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir frontend exec eslint components/pos/settlement-actions.tsx</automated>
  </verify>
  <done>CHARGE NOW opens the dedicated Charge page from every caller; no payment modal remains.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → PaymentController | payment amount/method crosses here |
| pos-service → finance-service | period-open check on close |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07.3-20 | Tampering | client-entered tender amounts | high | mitigate | Server re-validates the sum (07.3-01) and derives paymentStatus authoritatively; the page never trusts a client "paid" flag. |
| T-07.3-21 | Tampering | duplicate payment | medium | mitigate | Paid order blocks new tenders in the UI AND the server rejects payment on terminal orders (07.3-01). |
| T-07.3-22 | Denial of Service | 423 period-lock on close | low | mitigate | 423 rendered as a user-facing message via existing ApiError-code branching; no crash. |
| T-07.3-SC | Tampering | dependencies | low | accept | No new npm package installs. |
</threat_model>

## Artifacts this phase produces
- **Component:** `payment-status-badge.tsx` (4-state), `charge-summary.tsx`
- **Route:** `app/(tenant)/app/pos/orders/[orderId]/charge/page.tsx`
- **Hook/Repo:** `useOrderPayments`, `PosRepository.getPayments`
- **Model/Schema:** `PaymentStatus` union + `OrderPayment` model/schema/adapter
- **Test:** `payment-status-badge.test.tsx`

<verification>
- `pnpm --dir frontend vitest run payment-status-badge` green
- `pnpm --dir frontend exec tsc --noEmit` passes; eslint on touched files clean
- GitNexus `detect_changes()` shows only the expected settlement/charge symbols changed
</verification>

<success_criteria>
Charge is a dedicated full page with full order + payment analytics + history; recording a payment
sets Paid without closing; Closed shows only when Paid AND Served; the payment badge is
hue+icon+label; paid orders block duplicate tenders but stay viewable.
</success_criteria>

<output>
Create `.planning/phases/07.3-pos-kitchen-production-bug-fixes-ux-revamp/07.3-07-SUMMARY.md` when done.
</output>
