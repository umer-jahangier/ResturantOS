---
phase: 11-hr-payroll
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - gateway/src/main/resources/application.yml
  - scripts/local-service-env.ps1
  - scripts/start-dev.ps1
  - scripts/restart-service.ps1
  - deploy/init/02b-ensure-runtime-roles.sql
autonomous: true

must_haves:
  truths:
    - "The gateway routes /api/v1/hr/** to hr-service (lb://hr-service) behind a circuit breaker"
    - "hr-service is gated by FEATURE_HR (already wired in RouteFeatureMap — must remain intact)"
    - "start-dev.ps1 / restart-service.ps1 build and launch hr-service on port 8088 with hr_db credentials in the environment"
  artifacts:
    - path: gateway/src/main/resources/application.yml
      provides: "hr-route + hrCircuitBreaker"
      contains: "hr-route"
    - path: scripts/local-service-env.ps1
      provides: "HR_DB_URL/HR_DB_USER/HR_DB_PASSWORD env block"
      contains: "HR_DB_URL"
    - path: scripts/restart-service.ps1
      provides: "hr-service port/module entry"
      contains: "hr-service"
  key_links:
    - from: gateway/src/main/resources/application.yml
      to: hr-service
      via: "lb://hr-service route on /api/v1/hr/**"
      pattern: "lb://hr-service"
    - from: scripts/start-dev.ps1
      to: services/hr-service
      via: "$DevMavenModules + Start-ServiceWindow"
      pattern: "hr-service"
---

<objective>
Register the new hr-service (port 8088, hr_db) into the platform's runtime wiring: the API gateway route, the dev/restart PowerShell scripts, and (if needed) a least-privilege runtime DB role. This does NOT create the service module (that is 11-01) — it only wires the surrounding infrastructure so that once 11-01 exists, the service is reachable and launchable.

Purpose: Without gateway + script registration, hr-service is unreachable through /api/v1/hr/** and cannot be started by the standard dev workflow.
Output: A gateway hr-route, an hrCircuitBreaker, HR entries in the three PowerShell scripts, and an optional 02b runtime-role block.

IMPORTANT — already scaffolded, DO NOT re-create (confirmed present in the repo):
- `gateway/.../support/RouteFeatureMap.java:37` already maps `/api/v1/hr/` -> `FEATURE_HR`. Do not touch it; just verify it is intact.
- `deploy/init/01-create-databases.sql` (hr_db), `02-create-roles.sql` (hr_user, NOSUPERUSER NOBYPASSRLS), `03-grant-schema-privileges.sql` (hr_db schema grants), `deploy/docker-compose.yml` (HR_DB_PASSWORD), `deploy/.env.example` (HR_DB_PASSWORD) all already contain HR. Do not duplicate them.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-hr-payroll/11-CONTEXT.md
@gateway/src/main/resources/application.yml
@scripts/start-dev.ps1
@scripts/restart-service.ps1
@scripts/local-service-env.ps1
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add hr-route + hrCircuitBreaker to the gateway</name>
  <files>gateway/src/main/resources/application.yml</files>
  <action>
Under `spring.cloud.gateway.server.webflux.routes:` add an `hr-route` block mirroring the existing `crm-route` and `finance-route` exactly:
```yaml
            # ── HR Service ───────────────────────────────────────────────────────
            - id: hr-route
              uri: lb://hr-service
              predicates:
                - Path=/api/v1/hr/**
              filters:
                - name: CircuitBreaker
                  args:
                    name: hrCircuitBreaker
                    fallbackUri: forward:/fallback/service-unavailable
                    statusCodes:
                      - 500
                      - 503
```
Then add an `hrCircuitBreaker` instance under `resilience4j.circuitbreaker.instances:` copied from `crmCircuitBreaker`.
Do NOT add /api/v1/hr/** to any JWT public-path or feature-flag public-prefix list — HR must stay authenticated and FEATURE_HR-gated. The device-authenticated /iclock path is handled separately in plan 11-11, NOT here.
  </action>
  <verify>`grep -n "hr-route" gateway/src/main/resources/application.yml` shows the block; `grep -n "hrCircuitBreaker" gateway/src/main/resources/application.yml` shows both the route reference and the instance. Gateway module compiles: `mvn -q -pl gateway -am compile`.</verify>
  <done>Gateway YAML contains an hr-route to lb://hr-service on /api/v1/hr/** with an hrCircuitBreaker instance defined; RouteFeatureMap FEATURE_HR mapping untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Register hr-service in the dev/restart PowerShell scripts</name>
  <files>scripts/local-service-env.ps1, scripts/start-dev.ps1, scripts/restart-service.ps1</files>
  <action>
1. `scripts/local-service-env.ps1`: mirror the crm block (currently ~lines 73-78) with:
```powershell
$env:HR_DB_URL = "jdbc:postgresql://127.0.0.1:5432/hr_db"
$env:HR_DB_USER = "hr_user"
$env:HR_DB_PASSWORD = $env:HR_DB_PASSWORD
```
2. `scripts/start-dev.ps1`: add `"services/hr-service"` to the `$DevMavenModules` array; add a start line next to the crm one: `$pids["hr-service"] = Start-ServiceWindow "hr-service" "services/hr-service"`; add `8088` to the `Stop-DevStack` port-kill list.
3. `scripts/restart-service.ps1`: add to the `$Services` ordered map: `"hr-service" = @{ Port = 8088; Module = "services/hr-service" }` (mirror the finance-service entry).
Use port 8088 consistently (confirmed free; 8089 is crm, 8086 is finance).
  </action>
  <verify>`grep -n "hr-service" scripts/start-dev.ps1 scripts/restart-service.ps1` and `grep -n "HR_DB_URL" scripts/local-service-env.ps1` all return matches. `grep -n "8088" scripts/*.ps1` shows the port in start-dev and restart-service.</verify>
  <done>All three scripts reference hr-service on port 8088 with hr_db credentials; the module is in the Maven build list and the start/stop lifecycle.</done>
</task>

<task type="auto">
  <name>Task 3: Add hr-service runtime-role block to 02b (only if the sibling pattern requires it)</name>
  <files>deploy/init/02b-ensure-runtime-roles.sql</files>
  <action>
Read `deploy/init/02b-ensure-runtime-roles.sql`. It currently ensures idempotent runtime roles for user_service/audit_writer/file_service only. hr-service connects as `hr_user` (already created in 02-create-roles.sql). If — and only if — the sibling services that connect directly as their `{svc}_user` (e.g. crm-service, finance-service) have NO entry in 02b, then hr-service needs none either: leave 02b unchanged and note this in the SUMMARY. If those siblings DO have a 02b idempotent `hr_user` ensure-block pattern, mirror it for hr_user (CREATE ROLE IF NOT EXISTS ... NOSUPERUSER NOBYPASSRLS, grants on hr_db). Do not weaken NOBYPASSRLS — RLS must be enforced against the runtime role.
  </action>
  <verify>Either 02b has an idempotent hr_user block consistent with the sibling pattern, or the SUMMARY documents that no 02b change was required because crm/finance also have none. `deploy/init/02b-ensure-runtime-roles.sql` still parses (no dangling SQL).</verify>
  <done>Runtime-role provisioning for hr-service is consistent with the sibling convention; RLS enforcement (NOBYPASSRLS) preserved.</done>
</task>

</tasks>

<verification>
- `mvn -q -pl gateway -am compile` succeeds.
- Gateway YAML defines hr-route (lb://hr-service, /api/v1/hr/**) + hrCircuitBreaker.
- The three PowerShell scripts launch/rebuild hr-service on 8088 with hr_db env.
- No HR path added to JWT public-paths or feature-flag public-prefixes.
</verification>

<success_criteria>
Once hr-service (11-01) exists and is started via start-dev.ps1, requests to /api/v1/hr/** route through the gateway to hr-service, are FEATURE_HR-gated, and fall back cleanly when the service is down.
</success_criteria>

<output>
After completion, create `.planning/phases/11-hr-payroll/11-02-SUMMARY.md`.
</output>
