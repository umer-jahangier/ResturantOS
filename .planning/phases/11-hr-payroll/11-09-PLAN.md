---
phase: 11-hr-payroll
plan: 09
type: execute
wave: 4
depends_on: [11-06, 11-07]
files_modified:
  - services/hr-service/src/main/java/io/restaurantos/hr/entity/AttendancePolicyEntity.java
  - services/hr-service/src/main/java/io/restaurantos/hr/service/LateArrivalDeductionService.java
  - services/hr-service/src/main/java/io/restaurantos/hr/service/PayrollRunService.java
  - services/hr-service/src/main/java/io/restaurantos/hr/service/LabourCostService.java
  - services/hr-service/src/main/java/io/restaurantos/hr/feign/PosRevenueClient.java
  - services/hr-service/src/main/java/io/restaurantos/hr/controller/internal/HrInternalController.java
  - services/hr-service/src/main/java/io/restaurantos/hr/controller/LabourCostController.java
  - services/hr-service/src/test/java/io/restaurantos/hr/LabourCostIT.java
autonomous: true

must_haves:
  truths:
    - "Late-arrival/early-leave (derived from attendance vs assigned shift, per attendance_policies) produces a deduction that feeds the payslip's deductions_json.late_arrival_paisa"
    - "Labour cost as a % of revenue is computed by shift and by branch: labour cost (from payslips/attendance) ÷ revenue (from POS/finance)"
    - "Revenue is pulled via an internal seam (Feign/internal endpoint), never client-supplied"
  artifacts:
    - path: services/hr-service/src/main/java/io/restaurantos/hr/service/LateArrivalDeductionService.java
      provides: "late-arrival deduction per policy feeding payroll"
      contains: "late_arrival_paisa"
    - path: services/hr-service/src/main/java/io/restaurantos/hr/service/LabourCostService.java
      provides: "labour-cost % by shift and branch"
      contains: "labourCostPct"
  key_links:
    - from: services/hr-service/src/main/java/io/restaurantos/hr/service/PayrollRunService.java
      to: LateArrivalDeductionService
      via: "calculate() adds late_arrival_paisa to deductions"
      pattern: "lateArrival"
    - from: services/hr-service/src/main/java/io/restaurantos/hr/service/LabourCostService.java
      to: PosRevenueClient
      via: "revenue pulled from POS/finance internal seam"
      pattern: "revenue"
---

<objective>
Close HR-05's late-arrival feed and deliver HR-06 labour-cost %: wire attendance-derived late/early deductions into payroll compute, and compute labour cost as a % of revenue by shift and by branch (revenue pulled from POS/finance internally).

Purpose: These connect the attendance/shift data (11-07) and payroll (11-06) into the two remaining HR success criteria — deductions feeding payroll and labour-cost tracking. Full reporting is Phase 12; here we expose the raw metric + internal seam.
Output: Late-arrival deduction service (integrated into PayrollRunService.calculate), attendance_policies, and a labour-cost metric endpoint.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-hr-payroll/11-CONTEXT.md
@services/hr-service/src/main/java/io/restaurantos/hr/service/PayrollRunService.java
@services/hr-service/src/main/java/io/restaurantos/hr/service/AttendanceService.java
@services/crm-service/src/main/java/io/restaurantos/crm/consumer/OrderClosedLoyaltyConsumer.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Late-arrival deduction service + payroll integration</name>
  <files>services/hr-service/src/main/java/io/restaurantos/hr/entity/AttendancePolicyEntity.java, services/hr-service/src/main/java/io/restaurantos/hr/service/LateArrivalDeductionService.java, services/hr-service/src/main/java/io/restaurantos/hr/service/PayrollRunService.java</files>
  <action>
- AttendancePolicyEntity maps attendance_policies (11-01): branchId nullable, lateGraceMinutes, deductionMode PER_MINUTE|PER_OCCURRENCE, deductionRatePaisa.
- LateArrivalDeductionService: computeMonthlyDeduction(employeeId, month, year): for each work day in the period, use AttendanceService.deriveLateEarly to get lateMinutes/early beyond grace; apply the branch's attendance_policies rule (PER_MINUTE -> minutes*rate; PER_OCCURRENCE -> occurrences*rate); return total late_arrival_paisa. Config-driven (never hardcode the rule).
- PayrollRunService.calculate: replace the late_arrival_paisa=0 placeholder from 11-06 with LateArrivalDeductionService.computeMonthlyDeduction; include it in net and in deductions_json.late_arrival_paisa. Keep the change minimal (one call + wiring).
  </action>
  <verify>`mvn -q -pl services/hr-service -am compile`. `grep -n "late_arrival_paisa\|computeMonthlyDeduction" services/hr-service/src/main/java/io/restaurantos/hr/service/PayrollRunService.java`.</verify>
  <done>Payslips reflect config-driven late-arrival deductions derived from attendance vs shift.</done>
</task>

<task type="auto">
  <name>Task 2: Labour-cost % service + revenue seam + endpoints</name>
  <files>services/hr-service/src/main/java/io/restaurantos/hr/service/LabourCostService.java, services/hr-service/src/main/java/io/restaurantos/hr/feign/PosRevenueClient.java, services/hr-service/src/main/java/io/restaurantos/hr/controller/internal/HrInternalController.java, services/hr-service/src/main/java/io/restaurantos/hr/controller/LabourCostController.java</files>
  <action>
- PosRevenueClient: an OpenFeign client (mirror finance-service's Feign clients) to pull branch revenue for a period from pos/finance's internal endpoint (e.g. GET /internal/pos/revenue?branchId&from&to). If no such endpoint exists yet, define the client against the documented internal contract and make LabourCostService degrade gracefully (return revenue=null / pct=null with a clear "revenue source unavailable" marker) — do NOT accept revenue from the client caller.
- LabourCostService: labourCostByBranch(branchId, from, to) = sum of payslip gross (or attendance-derived labour cost) for the period; labourCostPct = labourCost / revenue * 100; labourCostByShift(shiftId,...) analogous, apportioning labour cost to shifts via shift_assignments. Return by-shift and by-branch breakdowns.
- LabourCostController `/api/v1/hr/labour-cost`: GET by branch + by shift gated `hr.payroll.view` (or a report permission). HrInternalController `/internal/hr/labour-cost` for service-to-service (behind HrInternalServiceFilter) so Phase 12 reporting can consume it.
  </action>
  <verify>`mvn -q -pl services/hr-service -am compile`. `grep -n "labourCostPct\|revenue" LabourCostService.java`. Revenue never read from request body.</verify>
  <done>Labour-cost % by shift and branch is computed from internal labour cost ÷ POS revenue, exposed on public + internal endpoints.</done>
</task>

<task type="auto">
  <name>Task 3: Labour-cost + deduction integration test</name>
  <files>services/hr-service/src/test/java/io/restaurantos/hr/LabourCostIT.java</files>
  <action>
Extend HrTestBase. (1) Seed an employee with an assigned shift and a late clock-in + an attendance_policies row; run a payroll calculate; assert deductions_json.late_arrival_paisa > 0 and net reduced accordingly. (2) With a mocked PosRevenueClient returning a known revenue, assert LabourCostService.labourCostByBranch returns labourCostPct = labourCost/revenue*100 (± rounding), and by-shift apportionment sums to the branch total.
  </action>
  <verify>`mvn -q -pl services/hr-service -am verify -Dtest=LabourCostIT` green.</verify>
  <done>Late-arrival deduction and labour-cost % are proven end-to-end with a mocked revenue source.</done>
</task>

</tasks>

<verification>
- Late-arrival deduction (policy-driven) feeds deductions_json.late_arrival_paisa and net.
- Labour-cost % by shift and branch from labour cost ÷ POS revenue.
- Revenue only via internal seam.
</verification>

<success_criteria>
HR-05 late-arrival feed and HR-06 labour-cost % both satisfied; Phase 12 can consume the internal labour-cost endpoint for reporting.
</success_criteria>

<output>
After completion, create `.planning/phases/11-hr-payroll/11-09-SUMMARY.md`.
</output>
