---
phase: 11-hr-payroll
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pom.xml
  - services/hr-service/pom.xml
  - services/hr-service/src/main/java/io/restaurantos/hr/HrServiceApplication.java
  - services/hr-service/src/main/java/io/restaurantos/hr/config/HrSecurityConfig.java
  - services/hr-service/src/main/java/io/restaurantos/hr/config/HrInternalServiceFilter.java
  - services/hr-service/src/main/resources/application.yml
  - services/hr-service/src/main/resources/db/changelog/db.changelog-master.xml
  - services/hr-service/src/main/resources/db/changelog/v1.0.0/010-create-hr-tables.xml
  - services/hr-service/src/main/resources/db/changelog/v1.0.0/011-enable-rls-hr-tables.xml
  - services/hr-service/src/main/resources/db/changelog/v1.0.0/020-shared-infra-tables.xml
  - services/hr-service/src/main/java/io/restaurantos/hr/service/ProcessedEventService.java
  - services/hr-service/src/main/java/io/restaurantos/hr/entity/ProcessedEventEntity.java
  - services/hr-service/src/main/java/io/restaurantos/hr/entity/ProcessedEventId.java
  - services/hr-service/src/main/java/io/restaurantos/hr/repository/ProcessedEventRepository.java
  - services/hr-service/src/test/java/io/restaurantos/hr/HrTestBase.java
  - services/hr-service/src/test/java/io/restaurantos/hr/HrContextLoadsIT.java
autonomous: true

must_haves:
  truths:
    - "hr-service boots on port 8088 against hr_db, connects to Eureka, and passes Hibernate ddl-auto=validate against the migrated schema"
    - "Every tenant table in hr_db has FORCE ROW LEVEL SECURITY with a tenant_isolation policy on app.current_tenant_id"
    - "hr_db provisions the three shared-infra tables (event_outbox, idempotency_keys, processed_events) granted to hr_user — the crm-service 020 omission is NOT repeated"
    - "A cross-tenant read against an hr table returns no rows under RLS in the Testcontainers harness"
  artifacts:
    - path: services/hr-service/pom.xml
      provides: "hr-service Maven module inheriting restaurantos-parent, shared-lib + liquibase + testcontainers deps"
      contains: "hr-service"
    - path: services/hr-service/src/main/resources/db/changelog/v1.0.0/010-create-hr-tables.xml
      provides: "employees (incl device_user_ref durable-mapping column), tax_config, payroll_runs, payslips, shifts, shift_assignments, leave_types, leave_requests, leave_balances, attendance_policies, attendance_devices, attendance_punches, attendance_quarantine, biometric_templates"
      contains: "device_user_ref"
    - path: services/hr-service/src/main/resources/db/changelog/v1.0.0/011-enable-rls-hr-tables.xml
      provides: "FORCE RLS + tenant_isolation on every tenant table"
      contains: "FORCE ROW LEVEL SECURITY"
    - path: services/hr-service/src/main/resources/db/changelog/v1.0.0/020-shared-infra-tables.xml
      provides: "event_outbox + idempotency_keys + processed_events with GRANTs to hr_user"
      contains: "event_outbox"
    - path: services/hr-service/src/test/java/io/restaurantos/hr/HrTestBase.java
      provides: "Testcontainers Postgres + Liquibase IT base (mirrors FinanceTestBase)"
      min_lines: 40
  key_links:
    - from: services/hr-service/src/main/resources/application.yml
      to: hr_db
      via: "spring.datasource url HR_DB_URL / user hr_user"
      pattern: "hr_db"
    - from: services/hr-service/src/main/resources/db/changelog/db.changelog-master.xml
      to: "010/011/020 changelogs"
      via: "include"
      pattern: "include file"
    - from: services/hr-service/src/main/resources/application.yml
      to: EncryptedStringConverter
      via: "restaurantos.encryption.key property"
      pattern: "encryption"
---

<objective>
Stand up the hr-service microservice skeleton as a byte-for-byte sibling of crm-service/finance-service: Maven module, Spring Boot app, security config, application.yml, the FULL FORCE-RLS hr_db schema (all HR tables from spec §M8.3 plus the config/leave/quarantine tables), the three shared-infra tables, the per-service ProcessedEventService copy, and a Testcontainers IT base with a smoke test proving migration + RLS.

Purpose: This is the foundation every later HR plan builds on. Domain plans (employees, payroll, attendance, devices) will add @Entity classes mapping to tables created here; ddl-auto=validate must pass with an empty entity set now and as entities are added.
Output: A runnable, migrating, RLS-enforcing empty hr-service.

CRITICAL — mirror these exact siblings (read them):
- Module/build/app: services/crm-service/pom.xml, CrmServiceApplication.java, config/CrmSecurityConfig.java, config/CrmInternalServiceFilter.java, src/main/resources/application.yml.
- DB changelogs: services/crm-service/src/main/resources/db/changelog/** (010 tables, 011 enable-rls, 020 shared-infra, master).
- ProcessedEventService copy: services/crm-service/.../service/ProcessedEventService.java + entity/ProcessedEventEntity.java + entity/ProcessedEventId.java + repository/ProcessedEventRepository.java.
- IT base: services/finance-service/src/test/java/io/restaurantos/finance/FinanceTestBase.java (structure) but wire Liquibase (contexts="") not Flyway, following crm-service CrmLoyaltyIT.java's RLS-via-set_config approach.

Already scaffolded elsewhere — DO NOT create: hr_db/hr_user (deploy/init/*.sql), FEATURE_HR (RouteFeatureMap), gateway route + scripts (plan 11-02).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/11-hr-payroll/11-CONTEXT.md
@.planning/phases/11-hr-payroll/11-RESEARCH.md
@Docs/RestaurantERP_SaaS_Specification.md
@services/crm-service/pom.xml
@services/crm-service/src/main/resources/application.yml
@services/crm-service/src/main/resources/db/changelog/v1.0.0/020-shared-infra-tables.xml
@services/crm-service/src/main/resources/db/changelog/v1.0.0/011-enable-rls-crm-tables.xml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Maven module, Spring Boot app, security config, application.yml</name>
  <files>pom.xml, services/hr-service/pom.xml, services/hr-service/src/main/java/io/restaurantos/hr/HrServiceApplication.java, services/hr-service/src/main/java/io/restaurantos/hr/config/HrSecurityConfig.java, services/hr-service/src/main/java/io/restaurantos/hr/config/HrInternalServiceFilter.java, services/hr-service/src/main/resources/application.yml</files>
  <action>
- Add `<module>services/hr-service</module>` to the root reactor `pom.xml` (next to services/crm-service).
- Copy crm-service/pom.xml -> services/hr-service/pom.xml, change artifactId to `hr-service`. Keep all deps: shared-lib, spring-boot-starter-web/-data-jpa/-validation/-actuator, eureka-client, liquibase-core, postgresql, data-redis, amqp, security, lombok; test scope testcontainers (junit-jupiter, postgresql, rabbitmq), awaitility. Keep surefire (exclude **/*IT.java) + failsafe (include **/*IT.java) + TESTCONTAINERS_RYUK_DISABLED.
- HrServiceApplication.java: `@SpringBootApplication @EnableDiscoveryClient @EnableRabbit @EntityScan({"io.restaurantos.hr.entity","io.restaurantos.shared"}) @EnableJpaRepositories({"io.restaurantos.hr.repository","io.restaurantos.shared"})` (Boot 4 EntityScan import `org.springframework.boot.persistence.autoconfigure.EntityScan`).
- HrSecurityConfig.java + HrInternalServiceFilter.java: copy crm equivalents, swap permit path to `/internal/hr/**`, header `X-Internal-Service`, secret `${restaurantos.internal.secret:dev-internal-secret}`.
- application.yml: copy crm's; set `server.port: ${SERVER_PORT:8088}`, `spring.application.name: hr-service`, datasource `${HR_DB_URL:jdbc:postgresql://localhost:5432/hr_db}` / `${HR_DB_USER:hr_user}` / `${HR_DB_PASSWORD:hr_pass}`, `ddl-auto: validate`, `open-in-view: false`, liquibase change-log master + `contexts: ${LIQUIBASE_CONTEXTS:seed}`, eureka defaultZone, jwks.uri, internal.secret. ADD (crm lacks it, hr needs it for encrypted PII): `restaurantos.encryption.key: ${FIELD_ENCRYPTION_KEY:}` — the EncryptionAutoConfiguration is @ConditionalOnProperty(restaurantos.encryption.key), so this property MUST be present for @Convert(EncryptedStringConverter) fields (cnic, bank_account_no, device_token) to work; document FIELD_ENCRYPTION_KEY as required env in user_setup.
  </action>
  <verify>`mvn -q -pl services/hr-service -am compile` succeeds. `grep -n "hr-service" pom.xml` shows the module. `grep -n "8088" services/hr-service/src/main/resources/application.yml` and `grep -n "encryption.key" services/hr-service/src/main/resources/application.yml`.</verify>
  <done>hr-service compiles, is registered in the reactor, targets port 8088/hr_db, and declares the field-encryption key property.</done>
</task>

<task type="auto">
  <name>Task 2: Full FORCE-RLS hr_db schema + shared-infra tables</name>
  <files>services/hr-service/src/main/resources/db/changelog/db.changelog-master.xml, services/hr-service/src/main/resources/db/changelog/v1.0.0/010-create-hr-tables.xml, services/hr-service/src/main/resources/db/changelog/v1.0.0/011-enable-rls-hr-tables.xml, services/hr-service/src/main/resources/db/changelog/v1.0.0/020-shared-infra-tables.xml</files>
  <action>
Master includes in order: 010-create-hr-tables, 011-enable-rls-hr-tables, 020-shared-infra-tables (seed changelog 900 added by later plans).
010 — create these tables (each with tenant_id UUID NOT NULL; use spec §M8.3 DDL verbatim where given). Amounts as *_paisa BIGINT. Encrypted columns are BYTEA.
  1. employees (spec DDL: cnic BYTEA, bank_account_no BYTEA, employment_type, join_date, exit_date, basic_salary_paisa, user_id nullable, employee_no, branch_id NOT NULL). Add UNIQUE(tenant_id, employee_no); index tenant_id. ALSO add `device_user_ref TEXT` (nullable) — the durable mapping from a biometric device's user PIN to this employee, so a resolved punch auto-resolves for all FUTURE punches (prevents the quarantine-loop bug). Add UNIQUE(tenant_id, device_user_ref) (partial/allowing NULLs) and an index on (tenant_id, device_user_ref). This column is the match key used by 11-11's PunchIngestService.
  2. tax_config (research DDL: fiscal_year, effective_from/to, income_tax_slabs JSONB, surcharge_threshold_paisa, surcharge_rate_pct, eobi_employer_rate_pct, eobi_employee_rate_pct, eobi_wage_base_paisa, proration_method, is_active; UNIQUE(tenant_id, fiscal_year)).
  3. payroll_runs (spec DDL: period_month, period_year, status DEFAULT 'DRAFT', total_gross_paisa, total_net_paisa, run_by, approved_by, paid_at, branch_id nullable; UNIQUE(tenant_id, period_month, period_year) per spec — see NOTE below).
  4. payslips (spec DDL: run_id FK, employee_id FK, basic_paisa, allowances_json JSONB, gross_paisa, deductions_json JSONB, net_paisa, payslip_file_id).
  5. shifts (branch_id NOT NULL, name, role_designation, start_time TIME, end_time TIME, days_of_week INT[]).
  6. shift_assignments (shift_id FK, employee_id FK, work_date DATE; UNIQUE(tenant_id, shift_id, employee_id, work_date)).
  7. leave_types (name, is_paid BOOLEAN, accrual_days_per_month NUMERIC).
  8. leave_requests (employee_id FK, leave_type_id FK, start_date, end_date, status DEFAULT 'PENDING', approved_by, reason).
  9. leave_balances (employee_id FK, leave_type_id FK, period_year INT, balance_days NUMERIC; UNIQUE(tenant_id, employee_id, leave_type_id, period_year)).
  10. attendance_policies (branch_id nullable, late_grace_minutes INT, deduction_mode TEXT 'PER_MINUTE'|'PER_OCCURRENCE', deduction_rate_paisa BIGINT).
  11. attendance_devices (spec DDL: serial_no UNIQUE, connection_mode NETWORK_ADMS|USB_BRIDGE, device_token BYTEA, last_seen_at, is_active, branch_id NOT NULL).
  12. attendance_punches (spec DDL: device_id FK, employee_id FK nullable, device_user_ref, punch_type IN|OUT|UNKNOWN, device_reported_at, server_received_at DEFAULT now(), source_record_id; UNIQUE(device_id, device_user_ref, device_reported_at)).
  13. attendance_quarantine (device_id FK, device_user_ref, punch_type, device_reported_at, raw_line TEXT, status DEFAULT 'PENDING', resolved_employee_id nullable).
  14. biometric_templates (HR-08 opt-in, default UNUSED: employee_id FK, device_id FK, template_encrypted BYTEA, retention_expires_at TIMESTAMPTZ) — a dedicated restricted RLS table; will stay empty by default.
  Add a grants changeset: GRANT SELECT,INSERT,UPDATE,DELETE ON <every table above> TO hr_user.
011 — for EVERY table above: `ALTER TABLE t ENABLE ROW LEVEL SECURITY; ALTER TABLE t FORCE ROW LEVEL SECURITY; CREATE POLICY tenant_isolation ON t USING (tenant_id = current_setting('app.current_tenant_id', true)::uuid);` (mirror crm 011 exactly).
020 — copy crm-service 020-shared-infra-tables.xml VERBATIM but swap `crm_user`->`hr_user` and index names `crm`->`hr`: event_outbox (+ idx on status,created_at), idempotency_keys (+ idx on expires_at), processed_events (PK consumer,event_id). All three with GRANTs. DO NOT omit any of the three (this is the documented crm-service mistake).
NOTE for payroll_runs uniqueness: keep the spec's `UNIQUE(tenant_id, period_month, period_year)` for now; plan 11-06 owns the DRAFT→PAID lifecycle and will flag to the user whether per-branch runs `(tenant_id, branch_id, period_month, period_year)` are needed (RESEARCH Open Question 1). Leave branch_id nullable to keep that option open.
  </action>
  <verify>Liquibase migrates cleanly in the HrTestBase container (Task 3). `grep -c "FORCE ROW LEVEL SECURITY" 011-enable-rls-hr-tables.xml` >= 14. `grep -n "event_outbox\|idempotency_keys\|processed_events" 020-shared-infra-tables.xml` shows all three. `grep -n "UNIQUE (device_id, device_user_ref, device_reported_at)\|device_user_ref" 010-create-hr-tables.xml`.</verify>
  <done>All 14 tenant tables + 3 shared-infra tables exist with FORCE RLS + tenant_isolation and hr_user grants; migration runs clean.</done>
</task>

<task type="auto">
  <name>Task 3: ProcessedEventService copy + Testcontainers IT base + smoke/RLS test</name>
  <files>services/hr-service/src/main/java/io/restaurantos/hr/service/ProcessedEventService.java, services/hr-service/src/main/java/io/restaurantos/hr/entity/ProcessedEventEntity.java, services/hr-service/src/main/java/io/restaurantos/hr/entity/ProcessedEventId.java, services/hr-service/src/main/java/io/restaurantos/hr/repository/ProcessedEventRepository.java, services/hr-service/src/test/java/io/restaurantos/hr/HrTestBase.java, services/hr-service/src/test/java/io/restaurantos/hr/HrContextLoadsIT.java</files>
  <action>
- Copy crm ProcessedEventService + ProcessedEventEntity (@Table("processed_events") @IdClass) + ProcessedEventId + ProcessedEventRepository (existsByConsumerAndEventId) into io.restaurantos.hr packages, swapping package names only.
- HrTestBase.java: mirror FinanceTestBase.java structure — a shared static `PostgreSQLContainer<>("postgres:16")` started in a static block; @DynamicPropertySource wiring datasource to the container, disabling eureka + cloud config, `spring.rabbitmq.listener.simple.auto-startup=false`, and set `restaurantos.encryption.key` to a fixed test Base64 key. Run Liquibase against the container with `contexts=""` (skip seed data), OR enable spring.liquibase against the container with LIQUIBASE_CONTEXTS empty. @MockitoBean StringRedisTemplate, RabbitTemplate, IdempotencyKeyRepository, OutboxRepository (as FinanceTestBase does).
- HrContextLoadsIT.java (extends HrTestBase): (a) asserts the Spring context loads; (b) sets `app.current_tenant_id` to tenant A via `SELECT set_config('app.current_tenant_id', :tid, false)`, inserts a row into `leave_types` (a simple table) for tenant A; (c) switches the GUC to tenant B and asserts the SELECT returns 0 rows (RLS isolation), then switches back to A and asserts 1 row. This proves FORCE RLS + tenant_isolation are live.
  </action>
  <verify>`mvn -q -pl services/hr-service -am verify` runs HrContextLoadsIT green: context loads, Liquibase migrated all tables, and the cross-tenant SELECT returns 0 rows.</verify>
  <done>hr-service has a working Testcontainers IT harness proving migration + RLS isolation, plus the shared-infra ProcessedEventService ready for later consumers.</done>
</task>

</tasks>

<verification>
- `mvn -q -pl services/hr-service -am verify` passes (compile + HrContextLoadsIT).
- 14 tenant tables + 3 shared-infra tables migrate; all tenant tables FORCE RLS with tenant_isolation.
- Cross-tenant read isolation demonstrated in the IT.
- application.yml declares port 8088, hr_db, and restaurantos.encryption.key.
</verification>

<success_criteria>
A fresh developer can `start-dev.ps1` (after 11-02), reach an empty but healthy hr-service on 8088, and every hr_db tenant table enforces tenant isolation by construction.
</success_criteria>

<output>
After completion, create `.planning/phases/11-hr-payroll/11-01-SUMMARY.md`.
</output>

<user_setup>
  - service: field-encryption
    why: "AES-256-GCM key for encrypting employees.cnic / bank_account_no / attendance_devices.device_token"
    env_vars:
      - name: FIELD_ENCRYPTION_KEY
        source: "Base64-encoded 256-bit key (same key material convention as other services' restaurantos.encryption.key). Generate with: openssl rand -base64 32"
</user_setup>
