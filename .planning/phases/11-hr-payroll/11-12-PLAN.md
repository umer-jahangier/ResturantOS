---
phase: 11-hr-payroll
plan: 12
type: execute
wave: 5
depends_on: [11-04, 11-06, 11-07, 11-09]
files_modified:
  - frontend/lib/api-client/schemas/hr.schema.ts
  - frontend/lib/models/hr.model.ts
  - frontend/lib/adapters/hr.adapter.ts
  - frontend/lib/repositories/hr.repository.ts
  - frontend/app/(tenant)/app/hr/layout.tsx
  - frontend/app/(tenant)/app/hr/employees/page.tsx
  - frontend/app/(tenant)/app/hr/payroll/page.tsx
  - frontend/app/(tenant)/app/hr/attendance/page.tsx
  - frontend/app/(tenant)/app/hr/schedule/page.tsx
  - frontend/components/hr/shift-calendar.tsx
autonomous: false

must_haves:
  truths:
    - "An HR manager sees the HR section (gated by FEATURE_HR) and can list/create employees (PII masked)"
    - "A manager can create/calculate/approve/pay a payroll run and view payslips"
    - "A manager schedules role-based shifts on a drag-and-drop weekly calendar per branch"
    - "A manager views attendance punches and leave requests and approves leave"
  artifacts:
    - path: frontend/lib/repositories/hr.repository.ts
      provides: "HR data access over /api/v1/hr/** via the four-layer abstraction"
      contains: "hr"
    - path: frontend/components/hr/shift-calendar.tsx
      provides: "drag-and-drop weekly shift calendar (HR-04)"
      contains: "drag"
  key_links:
    - from: frontend/lib/repositories/hr.repository.ts
      to: /api/v1/hr
      via: "api-client calls through the gateway"
      pattern: "/api/v1/hr"
    - from: frontend/components/hr/shift-calendar.tsx
      to: frontend/lib/repositories/hr.repository.ts
      via: "assign/move shift on drop"
      pattern: "assign"
---

<objective>
Build the HR frontend (HR-04 drag-drop calendar + employee/payroll/attendance/leave pages) using the established four-layer API abstraction (api-client schema -> model -> adapter -> repository) and the tenant app router, gated by FEATURE_HR.

Purpose: Surfaces the HR backend to managers. The drag-and-drop shift calendar is the one explicitly-required UI (HR-04); the rest are standard list/detail + action pages.
Output: HR pages under /app/hr + the four-layer HR data modules + a drag-drop shift calendar component.

Mirror the existing finance module's four layers verbatim: frontend/lib/api-client/schemas/finance.schema.ts, frontend/lib/models/finance.model.ts, frontend/lib/adapters/finance.adapter.ts, frontend/lib/repositories/finance.repository.ts, and pages under frontend/app/(tenant)/app/finance/. FEATURE_HR gating already exists in frontend/lib/features/feature-flags.ts and frontend/components/shared/sidebar-nav-items.ts.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-hr-payroll/11-CONTEXT.md
@frontend/lib/api-client/schemas/finance.schema.ts
@frontend/lib/models/finance.model.ts
@frontend/lib/adapters/finance.adapter.ts
@frontend/lib/repositories/finance.repository.ts
@frontend/app/(tenant)/app/finance/page.tsx
@frontend/lib/features/feature-flags.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: HR four-layer API abstraction (schema/model/adapter/repository)</name>
  <files>frontend/lib/api-client/schemas/hr.schema.ts, frontend/lib/models/hr.model.ts, frontend/lib/adapters/hr.adapter.ts, frontend/lib/repositories/hr.repository.ts</files>
  <action>
Mirror finance.* four layers. Define Zod schemas + models + adapters + a repository for: employees (list/create/update/deactivate — PII arrives masked from backend), payroll-runs (create/calculate/approve[totp]/pay/list/payslips; include the Idempotency-Key header on create/calculate), shifts (list/week-grid/create/assign/move), attendance (punches list, clock-in/out, daily summary, quarantine list/resolve), leave (types, requests list/create, approve/reject, balances), labour-cost (by branch/shift). All calls go to /api/v1/hr/** through the shared api-client (which adds auth + tenant headers via the gateway).
  </action>
  <verify>`cd frontend && npm run typecheck` (or tsc --noEmit) passes. `grep -n "/api/v1/hr" frontend/lib/repositories/hr.repository.ts`.</verify>
  <done>The HR domain is fully typed and callable through the four-layer abstraction.</done>
</task>

<task type="auto">
  <name>Task 2: HR pages (employees, payroll, attendance, leave) + FEATURE_HR gating</name>
  <files>frontend/app/(tenant)/app/hr/layout.tsx, frontend/app/(tenant)/app/hr/employees/page.tsx, frontend/app/(tenant)/app/hr/payroll/page.tsx, frontend/app/(tenant)/app/hr/attendance/page.tsx</files>
  <action>
Mirror finance pages/layout. hr/layout.tsx: section shell gated by FEATURE_HR (use the existing feature-flag hook; sidebar-nav-items already has HR entries). 
- employees/page.tsx: list (masked CNIC/bank) + create/edit/deactivate forms; hide manage actions unless hr.employee.manage.
- payroll/page.tsx: list runs; create run; calculate; approve (prompt for TOTP code); pay; view payslips with the deductions breakdown (income tax, EOBI, advances, late-arrival) + labour-cost % widget.
- attendance/page.tsx: punches list + daily summary; clock-in/out; leave requests list with approve/reject (hr.leave.approve); quarantine list with resolve (map device_user_ref -> employee).
Guard action buttons by permission claims as the finance pages do.
  </action>
  <verify>`cd frontend && npm run typecheck` passes and `npm run build` compiles the new routes. Pages render list/detail states.</verify>
  <done>Employees, payroll (with TOTP approval + payslip breakdown), and attendance/leave/quarantine pages exist, permission- and FEATURE_HR-gated.</done>
</task>

<task type="auto">
  <name>Task 3: Drag-and-drop weekly shift calendar (HR-04)</name>
  <files>frontend/app/(tenant)/app/hr/schedule/page.tsx, frontend/components/hr/shift-calendar.tsx</files>
  <action>
Build shift-calendar.tsx: a per-branch weekly grid (days × shifts/roles) where employees can be dragged onto a shift+date cell (assign) and dragged between cells (move = unassign+assign), calling hr.repository shift assign/move. Use the drag-and-drop approach already used elsewhere in the frontend if one exists (grep for an existing dnd lib such as @dnd-kit or react-dnd in frontend/package.json and reuse it; otherwise use native HTML5 drag events — do not add a new heavy dependency without checking). schedule/page.tsx hosts the calendar with a branch selector and week navigation, loading the week grid from the backend and optimistically updating on drop.
  </action>
  <verify>`cd frontend && npm run typecheck` passes. Component compiles; drag assign/move issues the correct repository calls (verify in the human checkpoint).</verify>
  <done>A working drag-and-drop weekly shift calendar per branch backed by the shift API.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The full HR frontend: employees (masked PII), payroll runs with TOTP approval + payslip/labour-cost, attendance/leave/quarantine, and the drag-and-drop shift calendar — all gated by FEATURE_HR.</what-built>
  <how-to-verify>
1. Start the stack (start-dev.ps1 with hr-service + gateway) and log in as an OWNER/MANAGER of the demo tenant.
2. Confirm the HR section appears (and is hidden for a tenant with FEATURE_HR off).
3. Create an employee; confirm CNIC/bank show masked.
4. Create -> calculate -> approve (enter TOTP) -> pay a payroll run; open a payslip and confirm the deductions breakdown; confirm the labour-cost % widget renders.
5. Open the schedule calendar; drag an employee onto a shift/date and move them to another; confirm assignments persist on refresh.
6. Clock in/out; submit + approve a leave request; resolve a quarantined punch.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues.</resume-signal>
</task>

</tasks>

<verification>
- HR section gated by FEATURE_HR; four-layer HR data modules typed and calling /api/v1/hr/**.
- Employees (masked PII), payroll (TOTP approve, payslip breakdown, labour-cost), attendance/leave/quarantine pages functional.
- Drag-and-drop weekly shift calendar assigns/moves employees per branch.
</verification>

<success_criteria>
HR-04's drag-and-drop calendar requirement is met and the HR backend is fully usable by managers through the standard four-layer frontend.
</success_criteria>

<output>
After completion, create `.planning/phases/11-hr-payroll/11-12-SUMMARY.md`.
</output>
