---
phase: 11-hr-payroll
plan: 07
type: execute
wave: 3
depends_on: [11-04]
files_modified:
  - services/hr-service/src/main/java/io/restaurantos/hr/entity/ShiftEntity.java
  - services/hr-service/src/main/java/io/restaurantos/hr/entity/ShiftAssignmentEntity.java
  - services/hr-service/src/main/java/io/restaurantos/hr/entity/AttendancePunchEntity.java
  - services/hr-service/src/main/java/io/restaurantos/hr/entity/LeaveTypeEntity.java
  - services/hr-service/src/main/java/io/restaurantos/hr/entity/LeaveRequestEntity.java
  - services/hr-service/src/main/java/io/restaurantos/hr/entity/LeaveBalanceEntity.java
  - services/hr-service/src/main/java/io/restaurantos/hr/repository/HrScheduleRepositories.java
  - services/hr-service/src/main/java/io/restaurantos/hr/service/ShiftService.java
  - services/hr-service/src/main/java/io/restaurantos/hr/service/AttendanceService.java
  - services/hr-service/src/main/java/io/restaurantos/hr/service/LeaveService.java
  - services/hr-service/src/main/java/io/restaurantos/hr/controller/ShiftController.java
  - services/hr-service/src/main/java/io/restaurantos/hr/controller/AttendanceController.java
  - services/hr-service/src/main/java/io/restaurantos/hr/controller/LeaveController.java
  - services/hr-service/src/test/java/io/restaurantos/hr/AttendanceLeaveIT.java
autonomous: true

must_haves:
  truths:
    - "A manager creates role-based shifts per branch and assigns employees to dates (backend for the drag-drop calendar)"
    - "An employee clock-in/clock-out writes an attendance_punch (manual source) into the same attendance_punches table device punches use"
    - "Leave has types with monthly accrual, an approval workflow (hr.leave.approve), and queryable balances"
    - "Late-arrival/early-leave is derivable by comparing a punch to the employee's assigned shift"
  artifacts:
    - path: services/hr-service/src/main/java/io/restaurantos/hr/service/AttendanceService.java
      provides: "manual clock-in/out -> attendance_punches; late/early derivation vs shift"
      contains: "clockIn"
    - path: services/hr-service/src/main/java/io/restaurantos/hr/service/LeaveService.java
      provides: "leave types, accrual, request+approve, balances"
      contains: "approve"
    - path: services/hr-service/src/main/java/io/restaurantos/hr/controller/ShiftController.java
      provides: "shift CRUD + assignment under /api/v1/hr/shifts"
      contains: "hr.attendance.manage"
  key_links:
    - from: services/hr-service/src/main/java/io/restaurantos/hr/service/AttendanceService.java
      to: attendance_punches
      via: "manual punch shares the device-punch table"
      pattern: "AttendancePunch"
    - from: services/hr-service/src/main/java/io/restaurantos/hr/service/AttendanceService.java
      to: ShiftAssignmentEntity
      via: "late-arrival compared to assigned shift start_time"
      pattern: "late"
---

<objective>
Deliver the scheduling + time & attendance + leave backend (HR-04 backend, HR-05): shifts and assignments per branch, manual clock-in/out into attendance_punches, and a leave workflow with types/accrual/approval/balances. Late-arrival/early-leave derivation is provided here; feeding it into payroll deductions is completed in 11-09.

Purpose: These are the operational HR surfaces; attendance must land in the SAME attendance_punches table biometric devices use (11-11), so both sources unify. The drag-drop calendar UI (HR-04) consumes the shift API in 11-12.
Output: /api/v1/hr/shifts, /api/v1/hr/attendance, /api/v1/hr/leave APIs + services.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-hr-payroll/11-CONTEXT.md
@Docs/RestaurantERP_SaaS_Specification.md
@services/hr-service/src/main/java/io/restaurantos/hr/entity/EmployeeEntity.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shift scheduling (shifts + assignments) backend</name>
  <files>services/hr-service/src/main/java/io/restaurantos/hr/entity/ShiftEntity.java, services/hr-service/src/main/java/io/restaurantos/hr/entity/ShiftAssignmentEntity.java, services/hr-service/src/main/java/io/restaurantos/hr/service/ShiftService.java, services/hr-service/src/main/java/io/restaurantos/hr/controller/ShiftController.java</files>
  <action>
- ShiftEntity maps shifts (name, roleDesignation, startTime, endTime, daysOfWeek INT[], branchId). ShiftAssignmentEntity maps shift_assignments (shiftId, employeeId, workDate; unique per tenant/shift/employee/date).
- ShiftService: CRUD shifts; assign(shiftId, employeeId, workDate)/unassign; getWeekGrid(branchId, weekStart) returning shifts + assignments for the drag-drop calendar; validate employee belongs to the same branch.
- ShiftController `/api/v1/hr/shifts`: shift CRUD + assignment endpoints gated `hr.attendance.manage`; read/week-grid gated `hr.attendance.view`. Provide a bulk-assign / move endpoint suited to drag-drop (assign employee to shift+date; move = unassign+assign).
  </action>
  <verify>`mvn -q -pl services/hr-service -am compile`. Week-grid endpoint returns assignments for a branch/week.</verify>
  <done>Role-based per-branch shifts with assignments and a week-grid query for the calendar UI.</done>
</task>

<task type="auto">
  <name>Task 2: Manual attendance clock-in/out + late/early derivation</name>
  <files>services/hr-service/src/main/java/io/restaurantos/hr/entity/AttendancePunchEntity.java, services/hr-service/src/main/java/io/restaurantos/hr/service/AttendanceService.java, services/hr-service/src/main/java/io/restaurantos/hr/controller/AttendanceController.java</files>
  <action>
- AttendancePunchEntity maps attendance_punches (device_id nullable for manual? spec has device_id NOT NULL — for manual punches, use a per-tenant synthetic "MANUAL" device row seeded per branch, OR relax to allow a MANUAL source; simplest: seed a MANUAL virtual device per branch so device_id stays NOT NULL and manual+device punches share the table uniformly — document the choice). Fields: employeeId, deviceUserRef, punchType IN|OUT|UNKNOWN, deviceReportedAt, serverReceivedAt, sourceRecordId.
- AttendanceService: clockIn(employeeId)/clockOut(employeeId) -> insert a punch (deviceReportedAt=now) using the manual device, employee from context; list punches by employee/date. deriveLateEarly(employeeId, date): find the employee's ShiftAssignment for that date, compare first IN punch to shift.startTime (+ grace) -> lateMinutes; compare last OUT to shift.endTime -> earlyMinutes. Return a per-employee-per-day attendance summary. (Payroll deduction wiring is 11-09.)
- AttendanceController `/api/v1/hr/attendance`: clock-in/out gated `hr.attendance.manage` (or self-service — allow an employee to clock themselves; keep manager clock for others under manage); punch list + daily summary gated `hr.attendance.view`.
  </action>
  <verify>`mvn -q -pl services/hr-service -am compile`. `grep -n "attendance_punches\|late" AttendanceService.java`. Manual punch row lands in attendance_punches.</verify>
  <done>Manual clock-in/out unifies into attendance_punches; late/early is derivable against the assigned shift.</done>
</task>

<task type="auto">
  <name>Task 3: Leave types, accrual, approval workflow, balances + IT</name>
  <files>services/hr-service/src/main/java/io/restaurantos/hr/entity/LeaveTypeEntity.java, services/hr-service/src/main/java/io/restaurantos/hr/entity/LeaveRequestEntity.java, services/hr-service/src/main/java/io/restaurantos/hr/entity/LeaveBalanceEntity.java, services/hr-service/src/main/java/io/restaurantos/hr/repository/HrScheduleRepositories.java, services/hr-service/src/main/java/io/restaurantos/hr/service/LeaveService.java, services/hr-service/src/main/java/io/restaurantos/hr/controller/LeaveController.java, services/hr-service/src/test/java/io/restaurantos/hr/AttendanceLeaveIT.java</files>
  <action>
- Entities map leave_types (name, isPaid, accrualDaysPerMonth), leave_requests (employeeId, leaveTypeId, startDate, endDate, status PENDING|APPROVED|REJECTED, approvedBy, reason), leave_balances (employeeId, leaveTypeId, periodYear, balanceDays). Put the small JpaRepository interfaces for shifts/assignments/punches/leave in HrScheduleRepositories.java (or split files — one interface each is fine).
- LeaveService: default types (annual/sick/unpaid — seed via a changelog or on-demand ensure); request(employeeId, type, dates, reason) -> PENDING (validate balance for paid types); approve(requestId)/reject(requestId) gated hr.leave.approve, decrementing balance on approve; monthly accrual via a @Scheduled job that increments leave_balances by accrualDaysPerMonth (idempotent per period); getBalance(employeeId).
- LeaveController `/api/v1/hr/leave`: request (self) + list gated `hr.leave.view`; approve/reject gated `hr.leave.approve`; leave-types manage gated `hr.attendance.manage`.
- AttendanceLeaveIT (extends HrTestBase): clock-in an employee late vs an assigned shift -> assert deriveLateEarly reports lateMinutes; request annual leave, approve it -> assert balance decremented and status APPROVED; run accrual -> assert balance incremented once.
  </action>
  <verify>`mvn -q -pl services/hr-service -am verify` runs AttendanceLeaveIT green.</verify>
  <done>Leave types/accrual/approval/balances work; late-arrival derivation proven; all under RLS + permissions.</done>
</task>

</tasks>

<verification>
- Shifts + assignments per branch with a week grid.
- Manual clock-in/out in attendance_punches; late/early derived vs shift.
- Leave types, monthly accrual, request/approve/reject, balances.
</verification>

<success_criteria>
HR-04 backend + HR-05 satisfied: scheduling, manual attendance, and leave workflow operate per-branch under RLS and permissions, with late-arrival derivation ready to feed payroll (11-09) and the calendar UI ready to be built (11-12).
</success_criteria>

<output>
After completion, create `.planning/phases/11-hr-payroll/11-07-SUMMARY.md`.
</output>
