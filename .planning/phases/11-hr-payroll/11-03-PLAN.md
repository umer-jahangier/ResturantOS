---
phase: 11-hr-payroll
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - services/auth-service/src/main/resources/db/changelog/v1.0.0/045-hr-permissions.xml
  - services/auth-service/src/main/resources/db/changelog/db.changelog-master.xml
autonomous: true

must_haves:
  truths:
    - "The nine hr.* permissions from spec §B.7 exist in the auth permissions catalogue"
    - "OWNER and TENANT_ADMIN hold all hr.* permissions; MANAGER, ACCOUNTANT, and viewer roles hold the appropriate subset"
    - "A user with an HR role is authorized for the matching hr-service endpoints via JWT permission claims"
  artifacts:
    - path: services/auth-service/src/main/resources/db/changelog/v1.0.0/045-hr-permissions.xml
      provides: "hr.* permission rows + role_permissions grants"
      contains: "hr.payroll.approve"
  key_links:
    - from: services/auth-service/src/main/resources/db/changelog/db.changelog-master.xml
      to: 045-hr-permissions.xml
      via: "include"
      pattern: "045-hr-permissions"
---

<objective>
Seed the nine HR RBAC permissions into auth-service and grant them to the appropriate system roles, so HR-service endpoints can be authorized via the standard permission-claim mechanism.

Purpose: HR permissions are genuinely new (no hr.* rows exist yet). Without them, @PreAuthorize / OPA checks on hr-service would never pass and no role could use the module.
Output: A new Liquibase changelog seeding hr.* permissions + role grants, included in the auth master changelog.

Exact permission set (spec §B.7 — do not invent others):
hr.employee.view, hr.employee.manage, hr.attendance.view, hr.attendance.manage, hr.leave.view, hr.leave.approve, hr.payroll.run, hr.payroll.approve, hr.payroll.view
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-hr-payroll/11-CONTEXT.md
@Docs/RestaurantERP_SaaS_Specification.md
@services/auth-service/src/main/resources/db/changelog/v1.0.0/038-finance-ar-permissions.xml
@services/auth-service/src/main/resources/db/changelog/db.changelog-master.xml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 045-hr-permissions.xml seeding hr.* permissions + role grants</name>
  <files>services/auth-service/src/main/resources/db/changelog/v1.0.0/045-hr-permissions.xml</files>
  <action>
Mirror the self-contained pattern of 038-finance-ar-permissions.xml (single changeSet, no context="seed" so it always applies). Insert the nine `permissions` rows with module="hr":
  <insert tableName="permissions"><column name="code" value="hr.employee.view"/><column name="module" value="hr"/><column name="description" value="View employees"/></insert>
  ...one per permission (manage, attendance.view/manage, leave.view/approve, payroll.run/approve/view).
Then grant role_permissions:
  - OWNER and TENANT_ADMIN: all nine (use the module-scoped bulk-insert pattern from 041-pos-permissions.xml lines 161-181: `INSERT INTO role_permissions (role_code, permission_code) SELECT 'OWNER', code FROM permissions p WHERE p.module='hr' AND NOT EXISTS (...)` and same for TENANT_ADMIN).
  - MANAGER: hr.employee.view, hr.attendance.view, hr.attendance.manage, hr.leave.view, hr.leave.approve, hr.payroll.view (operational, NOT payroll.run/approve).
  - ACCOUNTANT: hr.payroll.view, hr.payroll.run, hr.employee.view (accountant executes payroll runs; approval is a separate step). hr.payroll.approve stays restricted to OWNER/TENANT_ADMIN (TOTP-gated approval in 11-06).
  - FINANCE_VIEWER: hr.payroll.view.
Use explicit <insert tableName="role_permissions"> rows for the per-role subsets (as 038 does). Add a <rollback> deleting the inserted permission codes and their role_permissions.
System roles available (from 030-create-roles-permissions.xml): OWNER, TENANT_ADMIN, MANAGER, ACCOUNTANT, INVENTORY_MANAGER, CASHIER, FINANCE_VIEWER.
  </action>
  <verify>`grep -c "tableName=\"permissions\"" 045-hr-permissions.xml` == 9. `grep -n "hr.payroll.approve" 045-hr-permissions.xml` shows it granted only to OWNER/TENANT_ADMIN. XML is valid Liquibase (well-formed changeSet id/author).</verify>
  <done>Nine hr.* permission rows + role grants defined with a rollback; payroll.approve restricted to OWNER/TENANT_ADMIN.</done>
</task>

<task type="auto">
  <name>Task 2: Include 045 in the auth master changelog</name>
  <files>services/auth-service/src/main/resources/db/changelog/db.changelog-master.xml</files>
  <action>Add `<include file="db/changelog/v1.0.0/045-hr-permissions.xml"/>` after the last permission include (044-finance-period-open-permission.xml), preserving ordering.</action>
  <verify>`grep -n "045-hr-permissions" services/auth-service/src/main/resources/db/changelog/db.changelog-master.xml`. `mvn -q -pl services/auth-service -am compile` (module still builds). If auth-service has an IT that migrates its changelog, run it to confirm the new changeset applies cleanly.</verify>
  <done>045-hr-permissions.xml is wired into the auth master changelog and migrates without error.</done>
</task>

</tasks>

<verification>
- Nine hr.* permissions present with module="hr".
- OWNER/TENANT_ADMIN hold all; MANAGER/ACCOUNTANT/FINANCE_VIEWER hold correct subsets; payroll.approve restricted.
- Changelog included in master and migrates.
</verification>

<success_criteria>
A JWT issued for an OWNER/TENANT_ADMIN/MANAGER/ACCOUNTANT user carries the correct hr.* permission claims, enabling authorization on hr-service endpoints (enforced by OPA hr.rego in 11-04).
</success_criteria>

<output>
After completion, create `.planning/phases/11-hr-payroll/11-03-SUMMARY.md`.
</output>
