---
phase: 11-hr-payroll
plan: 08
type: execute
wave: 4
depends_on: [11-06]
files_modified:
  - services/finance-service/src/main/java/io/restaurantos/finance/autopost/AutoPostingRecipeEngine.java
  - services/finance-service/src/main/java/io/restaurantos/finance/autopost/consumer/PayrollApprovedConsumer.java
  - services/finance-service/src/main/java/io/restaurantos/finance/autopost/consumer/PayrollPaidConsumer.java
  - deploy/init/rabbitmq-definitions.json
  - services/finance-service/src/test/java/io/restaurantos/finance/autopost/PayrollAutoPostingIT.java
autonomous: true

must_haves:
  truths:
    - "On PAYROLL_RUN_APPROVED, finance auto-posts a balanced JE: DR 6200 Salary Expense / CR 2300 Wages Payable for the gross"
    - "On PAYROLL_RUN_PAID, finance auto-posts: DR 2300 Wages Payable / CR Bank for the net"
    - "Both postings are idempotent (PostedSourceEvent guard) — replays post no duplicate JE"
    - "finance.payroll-paid.queue + binding exist (payroll-approved queue/binding already provisioned)"
  artifacts:
    - path: services/finance-service/src/main/java/io/restaurantos/finance/autopost/AutoPostingRecipeEngine.java
      provides: "postPayrollApproved + postPayrollPaid recipe methods"
      contains: "postPayrollApproved"
    - path: services/finance-service/src/main/java/io/restaurantos/finance/autopost/consumer/PayrollApprovedConsumer.java
      provides: "consumes finance.payroll-approved.queue"
      contains: "finance.payroll-approved.queue"
  key_links:
    - from: services/finance-service/src/main/java/io/restaurantos/finance/autopost/consumer/PayrollApprovedConsumer.java
      to: AutoPostingRecipeEngine.postPayrollApproved
      via: "tryProcess -> tenantAwareMessageProcessor.process -> recipe"
      pattern: "postPayrollApproved"
    - from: deploy/init/rabbitmq-definitions.json
      to: finance.payroll-paid.queue
      via: "hr.topic binding hr.payroll.paid"
      pattern: "finance.payroll-paid.queue"
---

<objective>
Complete HR-03's consumer side: finance-service auto-posts the payroll JE from the HR events, keeping the ledger owned by finance and balanced by construction. This mirrors the existing ORDER_CLOSED recipe seam exactly.

Purpose: HR publishes; finance posts. This closes the loop so an approved+paid payroll run produces the two balanced journal entries in the general ledger.
Output: Two new recipe methods, two consumers, the payroll-paid queue/binding, and an IT proving both JEs post idempotently.

Account codes (locked by CONTEXT + spec §11): 6200 Salary Expense, 2300 Wages Payable, Bank via accountResolver.codeBySystemTag("BANK"). Use accountResolver, not hardcoded 1100.
Already provisioned (agent-confirmed): hr.topic exchange, finance.payroll-approved.queue + .dlq + binding hr.topic/hr.payroll.approved. You must ADD finance.payroll-paid.queue (+ .dlq + binding hr.payroll.paid).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-hr-payroll/11-RESEARCH.md
@services/finance-service/src/main/java/io/restaurantos/finance/autopost/AutoPostingRecipeEngine.java
@services/finance-service/src/main/java/io/restaurantos/finance/autopost/consumer/OrderClosedConsumer.java
@services/finance-service/src/main/java/io/restaurantos/finance/autopost/ProcessedEventService.java
@deploy/init/rabbitmq-definitions.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add postPayrollApproved + postPayrollPaid recipes</name>
  <files>services/finance-service/src/main/java/io/restaurantos/finance/autopost/AutoPostingRecipeEngine.java</files>
  <action>
Add SOURCE_PAYROLL_APPROVED="PAYROLL_APPROVED" and SOURCE_PAYROLL_PAID="PAYROLL_PAID" constants. Add the two methods exactly per RESEARCH "Pattern 1" example, next to postOrderRevenue/postWastage:
- postPayrollApproved(envelope): runId = uuid(p,"runId"); if alreadyPosted(SOURCE_PAYROLL_APPROVED, runId) return; grossPaisa = longVal(p,"totalGrossPaisa"); if <=0 return; lines = [ line("6200","Salary expense", grossPaisa, 0), line("2300","Wages payable", 0, grossPaisa) ]; post(SOURCE_PAYROLL_APPROVED, runId, envelope, "Payroll approved "+runId, lines).
- postPayrollPaid(envelope): runId; if alreadyPosted(SOURCE_PAYROLL_PAID, runId) return; netPaisa = longVal(p,"totalNetPaisa"); if <=0 return; lines = [ line("2300","Wages payable", netPaisa, 0), line(accountResolver.codeBySystemTag("BANK"),"Payroll disbursement", 0, netPaisa) ]; post(SOURCE_PAYROLL_PAID, runId, envelope, "Payroll paid "+runId, lines).
Reuse the existing private post()/line()/alreadyPosted() helpers unchanged. If account codes 6200/2300 need resolving via accountResolver.codeByAccountCode rather than literals to match the tenant chart-of-accounts convention used elsewhere, follow whatever the sibling recipes do for fixed codes (WASTAGE_CODE etc. use literals, so literals for 6200/2300 are consistent).
  </action>
  <verify>`mvn -q -pl services/finance-service -am compile`. `grep -n "postPayrollApproved\|postPayrollPaid\|6200\|2300" AutoPostingRecipeEngine.java`.</verify>
  <done>Two balanced payroll recipes exist with idempotency guards, using the shared post() helper and accountResolver for BANK.</done>
</task>

<task type="auto">
  <name>Task 2: PayrollApprovedConsumer + PayrollPaidConsumer + payroll-paid queue/binding</name>
  <files>services/finance-service/src/main/java/io/restaurantos/finance/autopost/consumer/PayrollApprovedConsumer.java, services/finance-service/src/main/java/io/restaurantos/finance/autopost/consumer/PayrollPaidConsumer.java, deploy/init/rabbitmq-definitions.json</files>
  <action>
- Copy OrderClosedConsumer.java shape into PayrollApprovedConsumer: CONSUMER_NAME="finance.payroll-approved", QUEUE_NAME="finance.payroll-approved.queue" (already provisioned), calls recipeEngine::postPayrollApproved via processedEventService.tryProcess + tenantAwareMessageProcessor.process.
- PayrollPaidConsumer: CONSUMER_NAME="finance.payroll-paid", QUEUE_NAME="finance.payroll-paid.queue", calls recipeEngine::postPayrollPaid.
- rabbitmq-definitions.json: ADD queue `finance.payroll-paid.queue` (durable, x-dead-letter-exchange restaurantos.dlx, x-dead-letter-routing-key finance.payroll-paid.queue.dlq) + `finance.payroll-paid.queue.dlq` + a binding { source:"hr.topic", destination:"finance.payroll-paid.queue", destination_type:"queue", routing_key:"hr.payroll.paid" } + the DLX binding, mirroring the existing finance.payroll-approved.queue rows exactly.
  </action>
  <verify>`mvn -q -pl services/finance-service -am compile`. JSON valid: `python -c "import json;json.load(open('deploy/init/rabbitmq-definitions.json'))"`. `grep -n "finance.payroll-paid.queue\|hr.payroll.paid" deploy/init/rabbitmq-definitions.json`.</verify>
  <done>Both consumers listen on their queues; payroll-paid queue/binding added; approved queue reused.</done>
</task>

<task type="auto">
  <name>Task 3: Payroll auto-posting integration test</name>
  <files>services/finance-service/src/test/java/io/restaurantos/finance/autopost/PayrollAutoPostingIT.java</files>
  <action>
Mirror OrderCloseAutoPostingIT: @Testcontainers Postgres + RabbitMQ, declareTopology() declaring hr.topic + finance.payroll-approved.queue + finance.payroll-paid.queue + bindings. Publish a PAYROLL_RUN_APPROVED envelope (totalGrossPaisa) -> await a JE with DR 6200 / CR 2300 = gross, balanced. Publish PAYROLL_RUN_PAID (totalNetPaisa) -> await DR 2300 / CR BANK = net. Re-publish each -> assert no duplicate JE (PostedSourceEvent guard). Set tenant via InternalTenantContextHelper.activate.
  </action>
  <verify>`mvn -q -pl services/finance-service -am verify -Dtest=PayrollAutoPostingIT` green (or run the failsafe IT phase).</verify>
  <done>Approved -> gross JE (6200/2300) and Paid -> net JE (2300/BANK) post exactly once each; both balanced.</done>
</task>

</tasks>

<verification>
- PAYROLL_RUN_APPROVED -> DR 6200 / CR 2300 (gross), balanced, idempotent.
- PAYROLL_RUN_PAID -> DR 2300 / CR BANK (net), balanced, idempotent.
- finance.payroll-paid.queue + binding provisioned; approved queue reused.
</verification>

<success_criteria>
HR-03 fully closed: an approved+paid HR payroll run produces the two correct balanced journal entries in finance, with HR never touching the ledger.
</success_criteria>

<output>
After completion, create `.planning/phases/11-hr-payroll/11-08-SUMMARY.md`.
</output>
