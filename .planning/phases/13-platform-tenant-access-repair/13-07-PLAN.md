---
phase: 13-platform-tenant-access-repair
plan: 07
type: execute
wave: 2
depends_on: ["13-02"]
files_modified:
  - services/auth-service/src/main/java/io/restaurantos/auth/controller/RoleCatalogController.java
  - services/auth-service/src/main/java/io/restaurantos/auth/service/RoleCatalogService.java
  - services/auth-service/src/main/java/io/restaurantos/auth/repository/PermissionRepository.java
  - services/auth-service/src/main/java/io/restaurantos/auth/repository/RolePermissionRepository.java
  - services/auth-service/src/main/java/io/restaurantos/auth/dto/response/RoleCatalogDtos.java
  - services/auth-service/src/test/java/io/restaurantos/auth/integration/RoleCatalogIT.java
  - gateway/src/main/resources/application.yml
  - scripts/e2e/phase13-role-catalog-e2e.sh
autonomous: true
requirements: [USER-02, USER-03, GW-02]
decisions: [D-14, D-30]

must_haves:
  truths:
    - "An authenticated tenant user with user-administration authority can list the assignable roles, each with its name, its system flag and its permission codes."
    - "The same caller can list the permission catalog grouped by module."
    - "Both endpoints are reachable through the gateway on an existing routed prefix, with no new gateway route needed."
  prohibitions:
    - "The role catalog MUST NOT be readable without authentication — it enumerates the platform's entire authorization surface."
    - "The catalog MUST NOT return roles that no tenant may assign, without marking them as such."
    - "The catalog MUST NOT be derived from a hardcoded list in code; it is read from the roles and permissions tables so it can never drift from what assignment actually accepts."
  artifacts:
    - services/auth-service/src/main/java/io/restaurantos/auth/controller/RoleCatalogController.java
    - services/auth-service/src/main/java/io/restaurantos/auth/service/RoleCatalogService.java
    - scripts/e2e/phase13-role-catalog-e2e.sh
  key_links:
    - "RoleCatalogService <- the roles, permissions and role_permissions tables — the same source assignment validates against in plan 13-06"
    - "Gateway reachability <- the existing users-and-branches route prefix, extended rather than replaced"
---

<objective>
Deliver the role and permission catalog endpoints (D-14). CONTEXT.md states plainly that a role-picker
UI cannot exist without them, and plan 13-06 makes an unknown role code a hard 400 — which is only
usable if a caller can discover the valid codes.

Purpose: this is the discovery half of user administration; plan 13-12's public user API is unusable
without it.
Output: two authenticated read endpoints backed by the same tables assignment validates against.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-platform-tenant-access-repair/13-CONTEXT.md
@AUDIT-REPORT-2026-08-06.md
@.planning/phases/13-platform-tenant-access-repair/13-02-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Role and permission catalog read endpoints (D-14)</name>
  <files>services/auth-service/src/main/java/io/restaurantos/auth/controller/RoleCatalogController.java, services/auth-service/src/main/java/io/restaurantos/auth/service/RoleCatalogService.java, services/auth-service/src/main/java/io/restaurantos/auth/repository/PermissionRepository.java, services/auth-service/src/main/java/io/restaurantos/auth/repository/RolePermissionRepository.java, services/auth-service/src/main/java/io/restaurantos/auth/dto/response/RoleCatalogDtos.java, services/auth-service/src/test/java/io/restaurantos/auth/integration/RoleCatalogIT.java</files>
  <read_first>
    - services/auth-service/src/main/java/io/restaurantos/auth/entity/RoleEntity.java and PermissionEntity.java and RolePermissionEntity.java
    - services/auth-service/src/main/java/io/restaurantos/auth/repository/RoleRepository.java — extended with a code finder by plan 13-06
    - services/auth-service/src/main/java/io/restaurantos/auth/repository/RolePermissionRepository.java — has one projection query; a second is needed
    - services/auth-service/src/main/resources/db/changelog/v1.0.0/030-create-roles-permissions.xml — the roles and permissions table shapes, including the module column and the system flag
    - services/auth-service/src/main/java/io/restaurantos/auth/config/SecurityConfig.java — the catch-all authenticated rule
    - .planning/phases/13-platform-tenant-access-repair/13-02-SUMMARY.md — the three new administration codes to gate on
    - shared-lib/src/main/java/io/restaurantos/shared/api/ApiResponse.java — the response envelope
  </read_first>
  <behavior>
    - Listing roles returns every row in the roles table with its code, display name, system flag and sorted permission codes.
    - Listing permissions returns every row grouped by module, with codes sorted within each module.
    - Both endpoints require authentication and return 401 without a token.
    - Both endpoints require either the umbrella administration authority or the new user-administration authority, and return 403 for a caller holding neither.
    - The role list includes the role seeded by plan 13-02 and its order-taking permission codes, proving the catalog reflects the database rather than a compiled list.
    - Neither response includes any user, tenant or assignment data.
  </behavior>
  <action>
    Create a PermissionRepository if none exists, and add a projection to RolePermissionRepository that
    returns role-code and permission-code pairs for all roles in one query, so building the catalog does
    not become an N-plus-one over the role list.

    Create RoleCatalogService with two read operations. Both read from the roles, permissions and
    role_permissions tables. These tables are global rather than tenant-scoped, so no tenant GUC is
    required; note that in the javadoc, because every other read path in this service sets one.

    Create response records in a RoleCatalogDtos holder following the existing DTO-holder convention in
    this codebase. A role entry carries its code, name, system flag and permission codes. A permission
    entry carries its code, module and description. Group the permission response by module in the
    service, not in the caller.

    Create RoleCatalogController mapped at a roles path and a permissions path under the API version
    prefix. Gate both with hasAnyAuthority naming the umbrella administration code and the new
    user-administration code from plan 13-02. Do not use a dotted platform-style code here — the
    permission-catalog closure test scans these expressions and every dotted code must exist in the
    catalog, which is exactly the property being relied on.

    Sort deterministically everywhere so responses are stable and diffable.

    Write RoleCatalogIT covering the six behaviours.
  </action>
  <acceptance_criteria>
    - All six behaviours pass.
    - The role list is built with a bounded number of queries regardless of role count.
    - PermissionCatalogClosureTest still passes with the new gate expressions in place.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/muhammadumer/Documents/Projects/ResturantOS &amp;&amp; mvn -q -pl services/auth-service -am test -Dtest='RoleCatalogIT,PermissionCatalogClosureTest'</automated>
  </verify>
  <done>The set of assignable roles and the permission vocabulary are discoverable by an authorised caller.</done>
</task>

<task type="auto">
  <name>Task 2: Make the catalog reachable through the gateway</name>
  <files>gateway/src/main/resources/application.yml</files>
  <read_first>
    - gateway/src/main/resources/application.yml — the auth route, the user route and its two-prefix predicate, and the declaration-order semantics
    - gateway/src/main/java/io/restaurantos/gateway/support/RouteFeatureMap.java — confirm the chosen prefix carries no feature gate, since role discovery must work for every tenant regardless of module entitlement
    - gateway/src/main/java/io/restaurantos/gateway/filter/JwtGlobalFilter.java — the public-path list, which must NOT gain these paths
  </read_first>
  <action>
    Decide the public path prefix for the two endpoints and record it in the SUMMARY. Prefer routing them
    to auth-service, which owns the tables, and add the chosen prefix to the existing auth-service route
    predicate rather than creating a new route — the auth route already carries the tighter rate limit and
    the auth circuit breaker, both of which suit a small read endpoint.

    If the chosen prefix would collide with the auth route's tighter per-IP budget in a way that harms a
    role-picker page, prefer a dedicated route with the general rate limit and say so in the SUMMARY. Do
    not silently leave the endpoints unroutable — a controller that no gateway route reaches is exactly
    the failure shape blocker B1 had, and it is what plan 13-12 would then trip over.

    Confirm the prefix maps to no feature code in RouteFeatureMap. Role discovery must not be gated on a
    tenant's module entitlement.

    Do not add these paths to the gateway public-path list; they require a token.
  </action>
  <acceptance_criteria>
    - A request to each endpoint through the gateway with a valid tenant-admin token returns 200.
    - A request without a token returns 401 at the gateway.
    - Neither endpoint returns a disabled-feature response for any tenant.
    - Gateway routing tests still pass.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/muhammadumer/Documents/Projects/ResturantOS &amp;&amp; mvn -q -pl gateway -am test -Dtest='GatewayRoutingIT,FeatureFlagFilterIT'</automated>
  </verify>
  <done>The catalog endpoints are reachable at the edge, not merely present in a service.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the catalog matches what assignment accepts (D-30)</name>
  <files>scripts/e2e/phase13-role-catalog-e2e.sh</files>
  <read_first>
    - scripts/e2e/_phase13-lib.sh — produced by plan 13-01
    - scripts/e2e/phase13-roles-e2e.sh — produced by plan 13-02; reuse its tenant-admin persona setup rather than duplicating it
    - services/auth-service/src/main/java/io/restaurantos/auth/controller/AuthInternalController.java — the assignment path whose acceptance must match the catalog
  </read_first>
  <action>
    Create scripts/e2e/phase13-role-catalog-e2e.sh sourcing the phase 13 harness.

    Log in a tenant-admin persona through the gateway and fetch the role catalog. Assert 200 and that the
    returned codes include the roles seeded by plan 13-02.

    Then close the loop that makes this catalog worth having: for every role code the catalog returns,
    attempt a branch-role assignment for a disposable user and assert none of them is rejected as unknown.
    A catalog that advertises a code assignment refuses is worse than no catalog. Then attempt an
    assignment with a code the catalog does not return and assert a 400. Together these two assertions
    prove the catalog and the validator read the same source, which is the D-13 and D-14 pairing.

    Fetch the permission catalog and assert it is grouped by module and non-empty, and that the codes
    attached to the roles in the role catalog are all present in it.

    Assert 401 without a token and 403 for a persona holding neither administration authority — use the
    waiter persona from plan 13-02's script for the 403 case.

    Clean up the disposable user in a trap.
  </action>
  <acceptance_criteria>
    - The script exits zero against the running stack.
    - Every advertised role code is accepted by assignment.
    - A code absent from the catalog is rejected with 400.
    - The waiter persona receives 403 and an anonymous request receives 401.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/muhammadumer/Documents/Projects/ResturantOS &amp;&amp; bash scripts/e2e/phase13-role-catalog-e2e.sh</automated>
    <human-check>Confirm auth-service and the gateway were rebuilt and restarted before the run.</human-check>
  </verify>
  <done>The catalog and the assignment validator provably share one source of truth.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (this plan's share)

**New symbols:** `RoleCatalogService`, `RoleCatalogController`, `PermissionRepository`, `RoleCatalogDtos`, a role-code/permission-code pair projection.
**New endpoints:** role catalog and permission catalog reads (exact paths recorded in the SUMMARY), authenticated + administration-gated.
**Gateway config:** the chosen prefix added to an existing route predicate.
**Migrations:** none.
**New files:** `scripts/e2e/phase13-role-catalog-e2e.sh`, `RoleCatalogIT`.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tenant user to gateway | An authenticated caller requests the platform's authorization vocabulary |
| auth-service to auth_db | Global, non-tenant-scoped catalog tables are read |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-07-A | Information Disclosure | The permission catalog enumerates the whole authorization surface | high | mitigate | Both endpoints require authentication and an administration authority; a 401 and a 403 case are asserted live |
| T-13-07-B | Information Disclosure | Cross-tenant leakage through the catalog | low | accept | The catalog tables are global and carry no tenant, user or assignment data; the response records are asserted to contain none |
| T-13-07-C | Tampering | Catalog drifting from what assignment accepts | high | mitigate | Both read the same tables, and the live script assigns every advertised code to prove it |
| T-13-07-D | Denial of Service | N-plus-one query on the role list | medium | mitigate | A single pair projection replaces per-role lookups; asserted by a bounded-query behaviour |
| T-13-07-E | Elevation of Privilege | Feature-gating role discovery | medium | mitigate | The chosen prefix is confirmed to map to no feature code, so entitlement changes cannot make role administration unreachable |
| T-13-07-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new packages. If one becomes necessary, halt and require the package-legitimacy audit plus a blocking human checkpoint |
</threat_model>

<verification>
1. Catalog integration tests and the permission-catalog closure test pass.
2. Gateway routing tests pass and both endpoints answer through the gateway.
3. The live catalog script exits zero, including the assign-every-advertised-code loop.
4. `detect_changes()` before committing.
</verification>

<success_criteria>
- Part of ROADMAP SC3 satisfied: per-branch roles are assignable from a role catalog endpoint, and an unknown role code is rejected with 400.
- The catalog cannot drift from the validator.
</success_criteria>

<output>
Create `.planning/phases/13-platform-tenant-access-repair/13-07-SUMMARY.md`, recording the two endpoint
paths and the gateway routing decision. Plans 13-12 and 13-15 both consume them.
</output>
