---
phase: 07.1-pos-production-operations
plan: 08
type: execute
wave: 6
depends_on: ["07.1-06"]
files_modified:
  - frontend/components/pos/menu-grid.tsx
  - frontend/components/pos/pos-terminal.tsx
  - frontend/__tests__/pos/menu-grid.test.tsx
  - frontend/__tests__/pos/pos-terminal.test.tsx
autonomous: true
requirements: [POS-15]
must_haves:
  truths:
    - "A menu search field filters the loaded category's items quickly (150ms debounce, clearable)"
    - "The first menu tap adds the item on the first click (the fixed first-item stale-closure bug stays fixed — regression-tested)"
    - "Items can be added past the previous cap — the 'can't select after N items' bug is located and fixed (investigated, not blindly patched)"
    - "Tapping an AVAILABLE table binds its tableId so createOrder links the new order to the table"
  artifacts:
    - "frontend/__tests__/pos/menu-grid.test.tsx"
    - "frontend/__tests__/pos/pos-terminal.test.tsx"
  key_links:
    - "pos-terminal reads the page-level selectedTableId (plan 06) and passes it into createOrder"
    - "the item-cap fix is located via GitNexus query/context, not guessed"
---

<objective>
Deliver the POS-15 cashier terminal UX: a fast menu search, the two terminal state-bug fixes (first-item add regression + the "can't select items after N" cap), and the table→order binding (tableId reaches createOrder). This plan OWNS `menu-grid.tsx` and `pos-terminal.tsx` for the phase (no overlap with plan 06's order-panel.tsx/page.tsx or plan 09's drawer), so it runs alongside plan 09.

The item-cap bug location was NOT found during research (RESEARCH.md Open Question #3 — MenuGrid was not fully read; menu-grid.tsx itself shows no obvious cap, so the bug likely lives in the add/order-state path). This plan REQUIRES an explicit investigation task using GitNexus (`query`/`context`) before any fix — no blind patch.

Purpose: POS-15 — fast, correct order creation and item entry for real service.
Output: menu search + investigated bug fixes + tableId binding + regression tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07.1-pos-production-operations/07.1-UI-SPEC.md
@.planning/phases/07.1-pos-production-operations/07.1-RESEARCH.md
@.planning/phases/07-point-of-sale-kitchen-display/07-UAT.md
@CLAUDE.md
@frontend/components/pos/menu-grid.tsx
@frontend/components/pos/pos-terminal.tsx
@frontend/lib/hooks/pos/use-orders.ts
@.planning/phases/07.1-pos-production-operations/07.1-06-SUMMARY.md
</context>

<artifacts_produced>
This plan creates/extends:
- `menu-grid.tsx`: full-width debounced (150ms) search input above the category pills, clearable, Search icon.
- `pos-terminal.tsx`: item-cap fix (post-investigation), first-item-add regression coverage, tableId binding from page-level selectedTableId into createOrder.
- Tests: menu-grid.test.tsx (search filter), pos-terminal.test.tsx (first-item + item-cap regression + tableId binding).
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Menu search input (debounced, clearable) in MenuGrid</name>
  <read_first>
    - frontend/components/pos/menu-grid.tsx (category pills; activeItems filter; grid render)
    - .planning/phases/07.1-pos-production-operations/07.1-UI-SPEC.md (§3 Menu search: full-width input above pills, 44px, "Search menu…", 150ms debounced client-side filter over the loaded category, clear (×), Search icon left-aligned)
  </read_first>
  <files>frontend/components/pos/menu-grid.tsx, frontend/__tests__/pos/menu-grid.test.tsx</files>
  <action>
    Add a full-width search input ABOVE the category pills in `menu-grid.tsx`: 44px height (touch-target floor), placeholder "Search menu…", a left-aligned `Search` (lucide) icon, and a clear (×) affordance. Debounce the input 150ms and filter the currently-loaded category's `activeItems` client-side by name (case-insensitive contains). No list virtualization (current menu is ~10 items; UI-SPEC defers virtualization until >60). Keep the existing skeleton-first loading and empty-state behavior.

    Write `menu-grid.test.tsx`: typing a query filters the grid to matching items; clearing restores the full list.

    Per CLAUDE.md: before editing the existing `MenuGrid` component, run GitNexus `impact({target: "MenuGrid", direction: "upstream"})` — report the blast radius and warn on HIGH/CRITICAL; run `detect_changes()` before commit. The new `menu-grid.test.tsx` is net-new (nothing indexed to analyze).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | tail -10 && npx vitest run __tests__/pos/menu-grid.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Search input (44px, debounced 150ms, clearable, Search icon) filters the loaded category by name.
    - menu-grid.test.tsx passes (filter + clear).
  </acceptance_criteria>
  <done>Cashiers can quickly find a menu item by typing.</done>
</task>

<task type="auto">
  <name>Task 2: Investigate + fix the item-cap bug; regression-cover first-item; bind tableId</name>
  <read_first>
    - frontend/components/pos/pos-terminal.tsx (handleItemSelect first-item stale-closure fix already in the working tree; activeOrderId state)
    - frontend/lib/hooks/pos/use-orders.ts (useAddItem binding to activeOrderId; offline branches)
    - .planning/phases/07.1-pos-production-operations/07.1-RESEARCH.md (Open Question #3 — item-cap not yet located; grep candidates menu-grid/order-panel/use-orders; MenuGrid not fully read)
    - .planning/phases/07-point-of-sale-kitchen-display/07-UAT.md (bug #4 first-item; item-cap "can't select after N")
  </read_first>
  <files>frontend/components/pos/pos-terminal.tsx, frontend/__tests__/pos/pos-terminal.test.tsx</files>
  <action>
    INVESTIGATE FIRST (no blind patch). Per CLAUDE.md, use GitNexus to locate the item-cap root cause: `query({search_query: "add item to order"})` and `context({name: "handleItemSelect"})` / `context({name: "useAddItem"})` to trace the add-item flow, and inspect for any hardcoded array length, `.slice(0, N)`, pagination limit, key collision, or a stale-closure/state-batching bound that stops accepting taps after N items. Report the located root cause in the SUMMARY before fixing. Candidate surfaces per research: the add-item mutation binding in pos-terminal/use-orders, the order-item list keying in order-panel (owned by plan 06 — if the cap lives there, coordinate: this plan may only touch pos-terminal/menu-grid, so if the fix must land in order-panel, record it as a finding for a plan-06 follow-up rather than editing order-panel here to avoid a file-ownership conflict).

    FIX the located cap in pos-terminal.tsx (or use-orders.ts if that is the seam) so an unbounded number of items can be added. Keep the existing first-item stale-closure fix (handleItemSelect adds the first item directly with the freshly-known order id) — do not regress it. Add tableId binding: read the page-level `selectedTableId` (passed as a prop from plan 06's page.tsx) and include it in the `createOrder` request so a new order links to the tapped AVAILABLE table (closes the UAT gap where tableId never reached createOrder).

    Write `pos-terminal.test.tsx`: (a) first menu tap adds exactly one item on the first click (no empty DRAFT + dropped item); (b) adding N+ items (past the previous cap) all register; (c) when selectedTableId is set, createOrder is called with that tableId.

    Per CLAUDE.md: beyond the investigation `query`/`context` above, before editing the existing `handleItemSelect`/`PosTerminal` (pos-terminal.tsx) — and `useAddItem` (use-orders.ts) if the fix lands there — run GitNexus `impact({target: "PosTerminal", direction: "upstream"})` on each edited symbol, report the blast radius and warn on HIGH/CRITICAL; run `detect_changes()` before commit.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | tail -10 && npx vitest run __tests__/pos/pos-terminal.test.tsx 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - The item-cap root cause is located (documented in SUMMARY) and fixed; items add without a cap.
    - First-item add works on the first click (regression-tested); tableId from page state reaches createOrder.
    - pos-terminal.test.tsx passes all three cases.
  </acceptance_criteria>
  <done>The terminal accepts the first tap and unlimited subsequent items, and binds the selected table — with the cap bug investigated, not guessed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cashier UI → pos-service | createOrder carries tableId + branchId; branchId/tenant resolved server-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07.1h-01 | Tampering | client-supplied tableId in createOrder | medium | mitigate | tableId is validated server-side against the caller's branch/tenant (plan 04); tenant_id always from JWT — a forged tableId cannot bind an order to another tenant's/branch's table |
| T-07.1h-02 | Denial of Service | menu search debounce | low | accept | Pure client-side filter over ≤ ~60 items; no server round-trip per keystroke; no resource-exhaustion surface |
| T-07.1h-SC | Tampering | npm installs | high | mitigate | No new packages (RESEARCH.md audit: none) |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` clean; `npx eslint components/pos/menu-grid.tsx components/pos/pos-terminal.tsx --max-warnings=0`.
- `npx vitest run __tests__/pos/menu-grid.test.tsx __tests__/pos/pos-terminal.test.tsx` green.
- Item-cap root cause recorded in the SUMMARY.
</verification>

<success_criteria>
- Menu search filters quickly; first-item add works on first click; items add without a cap; tableId binds the order to its table.
</success_criteria>

<output>
Create `.planning/phases/07.1-pos-production-operations/07.1-08-SUMMARY.md` when done.
</output>
