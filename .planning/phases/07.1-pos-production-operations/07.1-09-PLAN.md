---
phase: 07.1-pos-production-operations
plan: 09
type: execute
wave: 6
depends_on: ["07.1-06"]
files_modified:
  - frontend/components/pos/order-table-detail-drawer.tsx
  - frontend/components/pos/order-management.tsx
  - frontend/__tests__/pos/order-table-detail-drawer.test.tsx
  - frontend/__tests__/pos/order-management.test.tsx
autonomous: true
requirements: [POS-09]
must_haves:
  truths:
    - "The Order Management tab lists active orders in a DataTable with derived status, table, server, covers, total (MoneyDisplay), and age — with an 'Open' row action"
    - "A non-closed order never disappears from the active list; closing/voiding removes it via a smooth fade-out, not an abrupt reflow"
    - "The 'My Orders / All Branch' toggle appears ONLY if the user holds the all-branch permission; otherwise the screen is silently scoped to own orders (no disabled control shown)"
    - "Row click opens a single shared Order/Table Detail drawer (status + revision chip, line items with per-item status, instructions, Add Items quick-add, shared Settlement Actions footer)"
  artifacts:
    - "frontend/components/pos/order-table-detail-drawer.tsx"
    - "frontend/components/pos/order-management.tsx"
    - "frontend/__tests__/pos/order-table-detail-drawer.test.tsx"
    - "frontend/__tests__/pos/order-management.test.tsx"
  key_links:
    - "order-management uses the DS-04 DataTable primitive + useOrderSummaries (plan 05); status via StatusBadge"
    - "the drawer is the ONE shared component reused by Table Floor View (plan 10); its footer renders settlement-actions (plan 06)"
---

<objective>
Build the POS-09 Order Management screen and the single shared Order/Table Detail drawer that both it (POS-09) and the Table Floor View (POS-10, plan 10) open. The screen is the third POS tab (scaffolded by plan 06's page.tsx); this plan creates the `OrderManagement` component it references + the shared `order-table-detail-drawer.tsx`. Building the drawer here (its primary consumer) and having plan 10 consume it enforces the UI-SPEC's hard rule: ONE drawer component, not two divergent "detail" UIs.

Deliverables: a DataTable list of active orders (derived status, table, server, covers, total, age, "Open" action) with status filter chips and a permission-gated My/All-Branch toggle where non-closed orders never disappear (fade-out on close); and the shared right-side drawer (status + revision chip, per-item-status line items, order instructions, inline Quick Add search + "Full Menu →" escape hatch, shared Settlement Actions footer).

This plan touches only two new pos component files + their tests — no overlap with plan 06 (order-panel/page) or plan 08 (menu-grid/pos-terminal), so it runs alongside plan 08.

Purpose: POS-09 — a dedicated surface to list, open, edit, reopen, and take payment on active orders.
Output: shared detail drawer + Order Management screen + two vitest files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07.1-pos-production-operations/07.1-UI-SPEC.md
@.planning/phases/07.1-pos-production-operations/07.1-PATTERNS.md
@.planning/phases/07.1-pos-production-operations/07.1-RESEARCH.md
@CLAUDE.md
@frontend/components/pos/payment-panel.tsx
@frontend/components/pos/settlement-actions.tsx
@frontend/components/shared/status-badge.tsx
@frontend/components/pos/revision-chip.tsx
@.planning/phases/07.1-pos-production-operations/07.1-05-SUMMARY.md
@.planning/phases/07.1-pos-production-operations/07.1-06-SUMMARY.md
</context>

<artifacts_produced>
This plan creates:
- `order-table-detail-drawer.tsx` (new, shared): right-side Sheet/Dialog-side-panel; header (order #, derived StatusBadge, revision-count chip, Void/Refund via settlement-actions), body (line items with per-item StatusBadge + notes, order-level instructions, inline Quick Add search + "Full Menu →" link), footer (shared Settlement Actions). Used by Order Management (this plan) and Table Floor View (plan 10).
- `order-management.tsx` (new): DataTable of active orders, status filter chips, permission-gated My/All-Branch toggle, row "Open" → drawer, non-closed-never-disappears + fade-out.
- Tests: order-table-detail-drawer.test.tsx, order-management.test.tsx.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Shared Order/Table Detail drawer</name>
  <read_first>
    - frontend/components/pos/payment-panel.tsx (permission-guarded panel composition analog — PATTERNS.md role-match; check components/ui for an existing shadcn Sheet/Dialog primitive before inventing drawer chrome)
    - frontend/components/pos/settlement-actions.tsx (plan-06 shared settlement footer)
    - frontend/components/shared/status-badge.tsx + frontend/components/pos/revision-chip.tsx + frontend/lib/hooks/pos/use-orders.ts (useOrder, useUpdateInstructions, useMarkServed) + use-tables.ts (useTableDetail)
    - .planning/phases/07.1-pos-production-operations/07.1-UI-SPEC.md (§1 Order Detail drawer contents; §2 Add Items Quick Add + Full Menu escape; §7 Settlement Actions footer; Copywriting Contract)
  </read_first>
  <files>frontend/components/pos/order-table-detail-drawer.tsx, frontend/__tests__/pos/order-table-detail-drawer.test.tsx</files>
  <action>
    Create `order-table-detail-drawer.tsx` as the ONE shared drawer (right-side Sheet, 420px desktop / full-screen mobile, scale-in/slide 200ms — reuse an existing shadcn Sheet/Dialog primitive from components/ui if present). Accept either an orderId or a tableId (it resolves the active order via useOrder / useTableDetail). Contents per UI-SPEC §1/§2: header with "Order #{orderNo}", derived `StatusBadge`, revision-count chip (revision-chip), and the Void/Refund controls via `settlement-actions`; body with the table/type + server, line items each showing the per-item `StatusBadge` + notes + a "Mark Served" affordance (useMarkServed), the order-level Special Instructions (editable via useUpdateInstructions, offline-safe), and an inline "Quick Add" search field + result list (fast single-item add-on path, POS-15 quick search) plus a "Full Menu →" link that switches to the Terminal tab preloaded with this order/table (via the page-level state from plan 06); footer with the shared `settlement-actions`. All money via MoneyDisplay. Do NOT embed the full MenuGrid in the drawer (UI-SPEC Assumption 5).

    Write `order-table-detail-drawer.test.tsx` (mirror payment-panel.test.tsx: seedSession, createQueryWrapper, MSW): a raw order fixture with revisionNo=2 items renders per-item status badges + the revision chip; the Quick Add search adds an item; the settlement footer is present; editing instructions calls updateInstructions.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | tail -10 && npx vitest run __tests__/pos/order-table-detail-drawer.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - One shared drawer resolves an order from orderId or tableId; shows status + revision chip, per-item-status line items, editable instructions, Quick Add + Full Menu escape, and the shared settlement footer.
    - order-table-detail-drawer.test.tsx passes.
  </acceptance_criteria>
  <done>A single shared drawer renders full order detail with per-item status, revisions, instructions, quick-add, and settlement.</done>
</task>

<task type="auto">
  <name>Task 2: Order Management screen (DataTable + filters + permission-gated toggle + drawer)</name>
  <read_first>
    - frontend/components/ui (DataTable DS-04 primitive; built-in skeleton)
    - frontend/lib/hooks/pos/use-orders.ts (useOrderSummaries from plan 05)
    - frontend/components/pos/menu-grid.tsx (category-pill visual pattern to reuse for status filter chips)
    - frontend/components/shared/permission-guard.tsx (own-vs-all-branch toggle gating — show only if permission held, not a disabled control)
    - .planning/phases/07.1-pos-production-operations/07.1-UI-SPEC.md (§1 columns, filters, My/All-Branch rule, row→drawer, non-closed never disappear + fade-out, EmptyState copy, skeleton-first)
  </read_first>
  <files>frontend/components/pos/order-management.tsx, frontend/__tests__/pos/order-management.test.tsx</files>
  <action>
    Create `order-management.tsx` rendering the DS-04 `DataTable` of active orders from `useOrderSummaries`: columns Order/Table | Status (derived StatusBadge) | Server/Cashier | Covers | Total (MoneyDisplay) | Age (elapsed since opened) | Actions ("Open"). Status filter chips above the table (All / Draft / In Progress / Partially Served / Served) reusing the menu-grid category-pill visual pattern. A "My Orders / All Branch" toggle that renders ONLY when the user holds the all-branch view permission (PermissionGuard — never a disabled control); otherwise the screen is silently scoped to own orders. Row "Open" opens the shared `order-table-detail-drawer`. Enforce POS-09's hard invariant: a non-closed order never disappears from the active filter set — closing/voiding removes the row via a framer-motion `fade-out` (200ms), not an abrupt reflow. EmptyState (DS-04) with the Copywriting-Contract copy ("No active orders" …). Loading uses the DataTable built-in skeleton (no spinner).

    Write `order-management.test.tsx`: the list renders active orders with derived status; a filter chip narrows the set; the My/All-Branch toggle is hidden without the all-branch permission and shown with it; clicking "Open" opens the drawer.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run __tests__/pos/order-management.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Order Management lists active orders in a DataTable with derived status, table, server, covers, total, age, and "Open".
    - Status filter chips narrow the set; the My/All-Branch toggle is permission-gated (hidden without it, not disabled).
    - A non-closed order never disappears; closing fades the row out; row "Open" opens the shared drawer.
    - order-management.test.tsx passes; ESLint four-layer boundary intact.
  </acceptance_criteria>
  <done>The Order Management screen lists permission-scoped active orders and opens any of them in the shared detail drawer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cashier UI → pos-service | order-summary list + all-branch toggle; server enforces the actual scope |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07.1i-01 | Information Disclosure / Elevation of Privilege | My/All-Branch toggle | high | mitigate | The toggle is UI convenience only; the server (plan 04) is authoritative — a user without the all-branch permission receives only own-branch/own orders regardless of any client toggle state; tenant isolation via RLS + JWT |
| T-07.1i-02 | Elevation of Privilege | settlement footer in the drawer | high | mitigate | Reuses the plan-06 PermissionGuard-wrapped settlement-actions; OPA fail-closed remains the real gate server-side |
| T-07.1i-SC | Tampering | npm installs | high | mitigate | No new packages (RESEARCH.md audit: none; DataTable/framer-motion already installed) |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` clean; `npx eslint components/pos/order-management.tsx components/pos/order-table-detail-drawer.tsx --max-warnings=0` (four-layer boundary + tokens).
- `npx vitest run __tests__/pos/order-table-detail-drawer.test.tsx __tests__/pos/order-management.test.tsx` green.
</verification>

<success_criteria>
- Order Management lists permission-scoped active orders that never disappear while open, and opens each in the shared detail drawer with per-item status, revisions, instructions, and settlement.
</success_criteria>

<output>
Create `.planning/phases/07.1-pos-production-operations/07.1-09-SUMMARY.md` when done.
</output>