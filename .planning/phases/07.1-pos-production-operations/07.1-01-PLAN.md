---
phase: 07.1-pos-production-operations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/pos-service/src/main/java/io/restaurantos/pos/domain/enums/OrderItemStatus.java
  - services/pos-service/src/main/java/io/restaurantos/pos/domain/enums/DerivedOrderStatus.java
  - services/pos-service/src/main/java/io/restaurantos/pos/domain/enums/TableStatus.java
  - services/pos-service/src/main/java/io/restaurantos/pos/domain/model/OrderItem.java
  - services/pos-service/src/main/java/io/restaurantos/pos/domain/model/Order.java
  - services/pos-service/src/main/resources/db/migration/V4__item_status_revision.sql
  - services/pos-service/src/main/java/io/restaurantos/pos/service/OrderStatusDerivationService.java
  - services/pos-service/src/main/java/io/restaurantos/pos/service/OrderServiceImpl.java
  - services/pos-service/src/main/java/io/restaurantos/pos/dto/OrderDto.java
  - services/pos-service/src/test/java/io/restaurantos/pos/OrderStatusDerivationServiceUnitTest.java
autonomous: true
requirements: [POS-11]
must_haves:
  truths:
    - "A pure function maps a set of line statuses to the correct derived order status across all boundary cases (DRAFT / IN_PROGRESS / PARTIALLY_SERVED / SERVED)"
    - "Every order line carries its own status field from the 7-value lifecycle (PENDING, SENT, ACCEPTED, PREPARING, READY, SERVED, CANCELLED)"
    - "pos-service compiles and every existing pos-service IT stays green after the KdsItemStatus->OrderItemStatus reconciliation"
  artifacts:
    - "services/pos-service/.../domain/enums/OrderItemStatus.java"
    - "services/pos-service/.../domain/enums/DerivedOrderStatus.java"
    - "services/pos-service/.../service/OrderStatusDerivationService.java"
    - "services/pos-service/.../db/migration/V4__item_status_revision.sql"
    - "services/pos-service/.../test/.../OrderStatusDerivationServiceUnitTest.java"
  key_links:
    - "OrderItem.itemStatus (was kdsStatus) is the single per-line source of truth read by OrderStatusDerivationService.derive()"
    - "V4 migration adds order_items.revision_no / order_items.fired_at / orders.derived_status additively; existing rows default safely (kds_status column retained, values widened)"
---

<objective>
Lay the POS-11 foundation that gates the rest of the phase: replace the shallow 3-value per-line `KdsItemStatus` with the full 7-value `OrderItemStatus` lifecycle, add the revision-tracking fields to `OrderItem`, add `NEEDS_BUSSING` to `TableStatus`, add a persisted `derived_status` column + `DerivedOrderStatus` enum on `Order`, and create the pure, unit-tested `OrderStatusDerivationService` that computes the aggregate order status from its line statuses (per RESEARCH.md Pattern 3, following the existing `recomputeOrderTotals` pure-derivation discipline).

This plan ONLY establishes the data model + derivation seam + mechanical reconciliation. It does NOT yet wire derivation into the mutation paths or add revision firing — that is plan 03. Keeping this split lets plan 03 and every downstream plan build on a compiling, test-green foundation.

Purpose: POS-11 is foundational — the expanded line-status enum and the `deriveOrderStatus` function gate revisions (POS-12), order management (POS-09), table-centric flow (POS-10), and the KDS redesign (KDS-03).
Output: New enums, extended entities, an additive Flyway V4 migration, a pure derivation service, and its unit test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07.1-pos-production-operations/07.1-CONTEXT.md
@.planning/phases/07.1-pos-production-operations/07.1-RESEARCH.md
@.planning/phases/07.1-pos-production-operations/07.1-PATTERNS.md
@.planning/phases/07.1-pos-production-operations/07.1-VALIDATION.md
@CLAUDE.md
@services/pos-service/src/main/java/io/restaurantos/pos/domain/enums/OrderStatus.java
@services/pos-service/src/main/java/io/restaurantos/pos/domain/model/OrderItem.java
@services/pos-service/src/main/resources/db/migration/V3__pos_tills_payments.sql
</context>

<artifacts_produced>
This plan creates:
- Enum `OrderItemStatus` (values: PENDING, SENT, ACCEPTED, PREPARING, READY, SERVED, CANCELLED) — replaces `KdsItemStatus` as the per-line status type.
- Enum `DerivedOrderStatus` (values: DRAFT, IN_PROGRESS, PARTIALLY_SERVED, SERVED) — the computed aggregate; settlement states (CLOSED/VOIDED/REFUNDED) stay on `OrderStatus`.
- Enum value `TableStatus.NEEDS_BUSSING` (added to existing enum).
- `OrderItem` fields: `itemStatus` (OrderItemStatus, mapped to existing `kds_status` column), `revisionNo` (int, default 0 = unfired), `firedAt` (Instant, nullable).
- `Order` field: `derivedStatus` (DerivedOrderStatus, persisted column `derived_status`, default DRAFT).
- Class + method `OrderStatusDerivationService.derive(List<OrderItem>) : DerivedOrderStatus`.
- Flyway migration `V4__item_status_revision.sql`.
- Test `OrderStatusDerivationServiceUnitTest`.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Add item-status + revision + derived-status data model and additive V4 migration</name>
  <read_first>
    - services/pos-service/src/main/java/io/restaurantos/pos/domain/enums/OrderStatus.java (bare-enum shape to mirror)
    - services/pos-service/src/main/java/io/restaurantos/pos/domain/enums/KdsItemStatus.java (the 3-value enum being superseded)
    - services/pos-service/src/main/java/io/restaurantos/pos/domain/model/OrderItem.java (lines 42-44 kdsStatus field; @Column conventions)
    - services/pos-service/src/main/resources/db/migration/V3__pos_tills_payments.sql (exact migration header/ALTER/index/grant conventions — PATTERNS.md §V4 migration)
  </read_first>
  <files>services/pos-service/src/main/java/io/restaurantos/pos/domain/enums/OrderItemStatus.java, services/pos-service/src/main/java/io/restaurantos/pos/domain/enums/DerivedOrderStatus.java, services/pos-service/src/main/java/io/restaurantos/pos/domain/enums/TableStatus.java, services/pos-service/src/main/java/io/restaurantos/pos/domain/model/OrderItem.java, services/pos-service/src/main/java/io/restaurantos/pos/domain/model/Order.java, services/pos-service/src/main/resources/db/migration/V4__item_status_revision.sql</files>
  <action>
    Create `OrderItemStatus` as a bare enum with values PENDING, SENT, ACCEPTED, PREPARING, READY, SERVED, CANCELLED (mirror the shape of OrderStatus.java, no methods). Create `DerivedOrderStatus` bare enum with values DRAFT, IN_PROGRESS, PARTIALLY_SERVED, SERVED (settlement states CLOSED/VOIDED/REFUNDED remain on OrderStatus, per RESEARCH.md Pitfall 3 split). Add value NEEDS_BUSSING to the existing `TableStatus` enum (append after OCCUPIED).

    In `OrderItem`: rename the field `kdsStatus` (type `KdsItemStatus`) to `itemStatus` (type `OrderItemStatus`), keeping the existing `@Column(name = "kds_status", length = 20)` mapping (do NOT rename the DB column — additive-safe per PATTERNS.md; verify PREPARING/CANCELLED fit length=20). Default `OrderItemStatus.PENDING`. Append two new fields after `notes`, before `modifiers`, following existing @Column conventions: `revisionNo` (`@Column(name="revision_no", nullable=false)` int, default 0 — 0 means not yet fired) and `firedAt` (`@Column(name="fired_at")` Instant, nullable).

    In `Order`: add field `derivedStatus` (`@Column(name="derived_status", length=20, nullable=false)` `@Enumerated(EnumType.STRING)` DerivedOrderStatus, default DerivedOrderStatus.DRAFT). Do NOT remove or repurpose the existing `status` (OrderStatus) field — the settlement lifecycle stays on it.

    Create `V4__item_status_revision.sql` following V3's header-comment + ALTER-TABLE-ADD-COLUMN conventions exactly (additive only, never DROP/RENAME per RESEARCH.md Pitfall 4): add `order_items.revision_no INT NOT NULL DEFAULT 0`, `order_items.fired_at TIMESTAMPTZ`, `orders.derived_status VARCHAR(20) NOT NULL DEFAULT 'DRAFT'`. The `kds_status` VARCHAR values widen only (no DDL needed — Java enums persist as EnumType.STRING VARCHAR; confirm the column is VARCHAR not a Postgres ENUM type before assuming otherwise). Add index `idx_order_items_revision_no ON order_items (order_id, revision_no)`. No new grants (column additions inherit table grants); no new RLS policy (altering existing RLS-covered tables only).

    Per CLAUDE.md: before editing `OrderItem`/`Order`, run GitNexus `impact({target: "OrderItem", direction: "upstream"})` and `impact({target: "Order", direction: "upstream"})`; report blast radius; warn if HIGH/CRITICAL. Do the rename via `rename` (call-graph-aware), not find-and-replace, where GitNexus supports it.
  </action>
  <verify>
    <automated>cd services/pos-service && mvn -q -o compile 2>&1 | tail -20 || mvn -q compile 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - OrderItemStatus, DerivedOrderStatus exist with the exact value sets above; TableStatus has NEEDS_BUSSING.
    - OrderItem exposes itemStatus (OrderItemStatus), revisionNo (int, default 0), firedAt (Instant); DB column kds_status retained.
    - Order exposes derivedStatus (DerivedOrderStatus, persisted derived_status, default DRAFT).
    - V4 migration is additive only (no DROP/RENAME); pos-service module compiles (blocked only pending Task 3 reference updates — Task 3 makes it fully green).
  </acceptance_criteria>
  <done>New enums + entity fields + additive V4 migration created following the V3 migration and OrderStatus enum conventions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create the pure OrderStatusDerivationService with a truth-table unit test</name>
  <read_first>
    - services/pos-service/src/main/java/io/restaurantos/pos/service/SplitTenderCalculator.java (analog: pure calculation @Service, no repo/no side effects — PATTERNS.md)
    - .planning/phases/07.1-pos-production-operations/07.1-RESEARCH.md (Pattern 3, lines ~307-330 — exact target logic)
    - .planning/phases/07.1-pos-production-operations/07.1-VALIDATION.md (Critical Invariant 1 — derived status boundary cases + property-based backstop)
    - .planning/phases/07.1-pos-production-operations/07.1-UI-SPEC.md ("Derived order status" table — derivation rules)
  </read_first>
  <files>services/pos-service/src/main/java/io/restaurantos/pos/service/OrderStatusDerivationService.java, services/pos-service/src/test/java/io/restaurantos/pos/OrderStatusDerivationServiceUnitTest.java</files>
  <behavior>
    - No non-cancelled lines, or all non-cancelled lines PENDING -> DRAFT.
    - >=1 line SENT/ACCEPTED/PREPARING/READY, none SERVED -> IN_PROGRESS.
    - >=1 non-cancelled line SERVED AND >=1 non-cancelled line not yet SERVED -> PARTIALLY_SERVED.
    - All non-cancelled lines SERVED -> SERVED.
    - CANCELLED lines are excluded from the computation entirely (an all-CANCELLED order derives DRAFT).
    - Property-based backstop: over random line-status vectors, the result is always a valid DerivedOrderStatus and PARTIALLY_SERVED implies at least one SERVED and at least one non-SERVED non-cancelled line.
  </behavior>
  <action>
    Create `OrderStatusDerivationService` as a `@Service` with a single pure method `DerivedOrderStatus derive(List<OrderItem> items)` — no repository, no side effects, mirroring the SplitTenderCalculator pure-service pattern. Implement exactly the truth table in the behavior block (RESEARCH.md Pattern 3 is the reference logic). This is the POS-11 "computed, never hand-set" seam — it must be the ONLY place derived status is computed.

    Write `OrderStatusDerivationServiceUnitTest` first (RED): a data-driven / parameterized test covering each boundary row above plus the mixed 3-line case from the acceptance criteria (mark one of three lines SERVED -> PARTIALLY_SERVED; mark all SERVED -> SERVED). Include a property-based-style test iterating randomized status vectors asserting the PARTIALLY_SERVED invariant. Then implement `derive()` to GREEN. Use plain `new OrderItem()` + `setItemStatus(...)` fixtures (no DB, no Testcontainers — this is a unit test, must run under `mvn test` in the Surefire phase).
  </action>
  <verify>
    <automated>cd services/pos-service && mvn -q test -Dtest=OrderStatusDerivationServiceUnitTest 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - OrderStatusDerivationServiceUnitTest passes and covers all four boundary cases + the mixed 3-line case + the property-based backstop.
    - derive() has no repository dependency and no side effects (pure function).
    - The test runs in the Surefire (unit) phase without Testcontainers.
  </acceptance_criteria>
  <done>OrderStatusDerivationService derives the correct aggregate status for every line-status combination, proven by a green unit test.</done>
</task>

<task type="auto">
  <name>Task 3: Mechanically reconcile all KdsItemStatus references to OrderItemStatus; keep existing ITs green</name>
  <read_first>
    - services/pos-service/src/main/java/io/restaurantos/pos/service/OrderServiceImpl.java (every getKdsStatus/KdsItemStatus reference — grep confirmed present)
    - services/pos-service/src/main/java/io/restaurantos/pos/dto/OrderDto.java (nested item DTO status field)
  </read_first>
  <files>services/pos-service/src/main/java/io/restaurantos/pos/service/OrderServiceImpl.java, services/pos-service/src/main/java/io/restaurantos/pos/dto/OrderDto.java</files>
  <action>
    Update every remaining reference to `KdsItemStatus` / `getKdsStatus()` / `setKdsStatus()` in `OrderServiceImpl` and `OrderDto` to use `OrderItemStatus` / `getItemStatus()` / `setItemStatus()`. Where the code previously set `KdsItemStatus.PENDING` on new lines, set `OrderItemStatus.PENDING`. Do NOT yet add derivation calls or change firing behavior (that is plan 03) — this is a compile-fix + behavior-preserving reconciliation only. Leave `Order.derivedStatus` at its DRAFT default for now (plan 03 wires the recompute).

    Per CLAUDE.md: run GitNexus `impact({target: "OrderServiceImpl", direction: "upstream"})` before editing; report blast radius; warn on HIGH/CRITICAL. After edits, run `detect_changes({scope: "compare", base_ref: "main"})` and confirm only the expected symbols/flows changed.

    Leave the now-unreferenced `KdsItemStatus.java` enum file in place but unused (removing it is out of scope — it may still be referenced by the frontend contract mapping until plan 05; deleting it is deferred). Confirm the module compiles fully and the existing pos-service IT suite (OrderLifecycleIT, VoidRefundOpaIT, OrderCloseIdempotencyIT, etc.) still passes unchanged.
  </action>
  <verify>
    <automated>cd services/pos-service && mvn -q test 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - Zero remaining compile references to the removed getKdsStatus/setKdsStatus API in production code.
    - pos-service compiles fully and all existing unit tests pass.
    - No behavioral change to firing/close/void yet (that is plan 03).
  </acceptance_criteria>
  <done>All KdsItemStatus references reconciled to OrderItemStatus; pos-service builds and existing tests stay green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB migration → running service | Flyway applies V4 on startup against tenant-scoped tables |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07.1a-01 | Tampering | V4 migration on order_items/orders | high | mitigate | Additive columns only (ADD COLUMN … DEFAULT); no DROP/RENAME of existing columns or data; existing RLS policies on order_items/orders remain in force (no new tenant-scoped table introduced, so no missing-RLS exposure) |
| T-07.1a-02 | Information Disclosure | new derived_status column | low | accept | derived_status carries no PII and is tenant-scoped by the existing orders RLS policy; no cross-tenant exposure introduced |
| T-07.1a-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new packages introduced (RESEARCH.md Package Legitimacy Audit: none) — no install task exists in this plan |
</threat_model>

<verification>
- `mvn -q -pl services/pos-service test` green (unit tests incl. OrderStatusDerivationServiceUnitTest).
- Flyway V4 additive-only (grep the migration for absence of DROP/RENAME of pre-existing columns).
- Full existing pos-service IT suite unaffected (behavior-preserving reconciliation).
</verification>

<success_criteria>
- OrderStatusDerivationService.derive() returns the correct DerivedOrderStatus for DRAFT / IN_PROGRESS / PARTIALLY_SERVED / SERVED boundary cases, unit-tested.
- OrderItem carries a 7-value itemStatus + revisionNo + firedAt; Order carries a persisted derivedStatus; TableStatus has NEEDS_BUSSING.
- pos-service compiles and all existing tests pass.
</success_criteria>

<output>
Create `.planning/phases/07.1-pos-production-operations/07.1-01-SUMMARY.md` when done.
</output>
