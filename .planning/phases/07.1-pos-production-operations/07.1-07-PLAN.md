---
phase: 07.1-pos-production-operations
plan: 07
type: execute
wave: 5
depends_on: ["07.1-05"]
files_modified:
  - frontend/components/kds/kds-ticket-card.tsx
  - frontend/components/kds/kds-board.tsx
  - frontend/components/kds/kds-ticket-detail.tsx
  - frontend/__tests__/kds/kds-board-sort.test.ts
  - frontend/__tests__/kds/kds-ticket-card.test.tsx
autonomous: true
requirements: [KDS-03, POS-13]
must_haves:
  truths:
    - "KDS cards keep a stable position — the sort key is computed once per fetch/socket batch and a card does NOT move when only its items' statuses change"
    - "Each ticket item shows a per-item status (7-state icon+label), not just a single ticket-level status; the large text-2xl item name stays readable"
    - "Rev 1 items are unbadged; Rev 2+ items carry the accent 'REV n' pill; the aging border (green/amber/red) remains a separate, preserved channel"
    - "Tapping a ticket opens a detail view with revisions grouped by 'Rev n · time', per-item status + notes, and the order-level Kitchen Notes callout"
    - "The board stays always-dark (does not respect useTheme)"
  artifacts:
    - "frontend/components/kds/kds-ticket-detail.tsx"
    - "frontend/__tests__/kds/kds-board-sort.test.ts"
    - "frontend/__tests__/kds/kds-ticket-card.test.tsx"
  key_links:
    - "kds-board sort key = receivedAt asc, tie-break ticket.id, computed once per batch — no reorder on item-status update"
    - "kds-ticket-detail consumes the plan-05 ticket-detail hook + revision-chip; Kitchen Notes = order-level orderNotes"
---

<objective>
Redesign the KDS board for KDS-03: stable (non-jumping) cards, per-item 7-state status (replacing the 3-value statusColors dict), revision highlighting via the shared revision-chip/accent pill, a tap-to-open ticket-detail view (revisions grouped, per-item status + notes, order-level Kitchen Notes callout), while preserving the always-dark board and the aging-border urgency channel exactly as Phase 7 shipped. The bouncing-cards complaint was a real UAT finding; the fix is a deterministic sort key computed once per batch.

This plan touches only `frontend/components/kds/*` + kds tests — zero overlap with the pos-surface plans, so it runs in parallel with plan 06.

Purpose: KDS-03 — the kitchen sees stable cards, per-item status, revisions, and full order detail + instructions.
Output: redesigned ticket card + board sort + new ticket-detail view + two vitest files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07.1-pos-production-operations/07.1-UI-SPEC.md
@.planning/phases/07.1-pos-production-operations/07.1-PATTERNS.md
@.planning/phases/07.1-pos-production-operations/07.1-RESEARCH.md
@CLAUDE.md
@frontend/components/kds/kds-ticket-card.tsx
@frontend/components/kds/kds-board.tsx
@frontend/components/shared/status-badge.tsx
@frontend/components/pos/revision-chip.tsx
@.planning/phases/07.1-pos-production-operations/07.1-05-SUMMARY.md
@.planning/phases/07.1-pos-production-operations/07.1-02-SUMMARY.md
</context>

<artifacts_produced>
This plan creates/extends:
- `kds-ticket-card.tsx`: per-item 7-state status badge (icon+label) replacing the 3-value statusColors dict; accent REV-n pill on Rev 2+ items; preserved text-2xl name + aging border; START/DONE bump buttons kept for PREPARING/READY only (ACCEPTED automatic; no SERVED/cancel on KDS).
- `kds-board.tsx`: deterministic stable sort (receivedAt asc, tie ticket.id, computed once per batch); 60s auto-remove extended to fully-resolved items; fade-in on new-ticket insert.
- `kds-ticket-detail.tsx` (new): tap-to-open modal/drawer — revisions grouped by "Rev n · time", per-item status + notes, order-level Kitchen Notes callout.
- Tests: kds-board-sort.test.ts, kds-ticket-card.test.tsx.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: 7-state per-item status + revision pill on the ticket card (preserve name size + aging border)</name>
  <read_first>
    - frontend/components/kds/kds-ticket-card.tsx (current 3-value statusColors dict; text-2xl item name; START/DONE buttons; aging border logic)
    - frontend/components/shared/status-badge.tsx + frontend/components/pos/revision-chip.tsx (plan-05 primitives — compose, do not redefine)
    - .planning/phases/07.1-pos-production-operations/07.1-UI-SPEC.md (§4 rendering rule: small icon+label badge above/beside the name, keep text-2xl; §8 revision highlighting; Status System — the three distinct visual channels must not be conflated)
  </read_first>
  <files>frontend/components/kds/kds-ticket-card.tsx</files>
  <action>
    Replace the current 3-value `statusColors` dict with the 7-state icon+label badge system from plan-05 `StatusBadge` (kitchen-relevant states render: PENDING/ACCEPTED/PREPARING/READY; SENT arrives-as pre-render; SERVED/CANCELLED are POS-side and shown read-only if present). Render the badge as a small icon+label above/beside each item name — do NOT recolor the large `text-2xl` item name for all states (too noisy at 2m). Keep the existing bump buttons (START → PREPARING, DONE → READY) for the two kitchen-actionable transitions only; ACCEPTED requires no button (automatic on render); do NOT add SERVED or cancel buttons on the KDS (POS-side per the Architectural Responsibility Map).

    Apply the revision marker: Rev 1 items unbadged; Rev 2+ items get the accent "REV {n}" pill (reuse revision-chip). Keep the three visual channels distinct and unconflated: aging border color (green<10min / amber 10-15min+pulse / red 15min+bounce — PRESERVED exactly from Phase 7 = ticket-level urgency); the per-item status hue (pipeline stage); and the accent REV pill (later addition). Board stays always-dark (does not respect useTheme, preserved per [07-04-D]).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - Each item shows a 7-state icon+label badge; text-2xl name preserved; START/DONE only for PREPARING/READY; no SERVED/cancel on KDS.
    - Rev 2+ items show the accent REV-n pill; aging border + status hue + REV pill are three distinct channels.
    - Board stays always-dark; tsc clean.
  </acceptance_criteria>
  <done>The ticket card shows per-item status + revision highlighting with the aging channel preserved and the name still readable.</done>
</task>

<task type="auto">
  <name>Task 2: Deterministic stable board sort + resolved-item auto-remove</name>
  <read_first>
    - frontend/components/kds/kds-board.tsx (current sort/render; WebSocket merge into TanStack cache + 10s poll; auto-remove-after-READY rule)
    - .planning/phases/07.1-pos-production-operations/07.1-UI-SPEC.md (§8 — sort key receivedAt asc, tie ticket.id, computed once per batch, no reorder on item-status change; new-ticket fade-in; 60s auto-remove extended to fully-resolved items)
    - .planning/phases/07.1-pos-production-operations/07.1-VALIDATION.md (Manual-Only: cards do not visually jump)
  </read_first>
  <files>frontend/components/kds/kds-board.tsx, frontend/__tests__/kds/kds-board-sort.test.ts</files>
  <action>
    Implement a deterministic, stable sort: the card ordering key is `receivedAt` ascending with ties broken by `ticket.id`, computed ONCE per fetch/socket-frame batch. A card's position must NOT change when only its items' statuses update within an already-rendered card (this is the fix for the UAT "cards jump/bounce" complaint). New tickets insert at their sorted position (fade-in 200ms). Extend the existing "auto-remove 60s after READY" rule from ticket-level to fully-resolved-item-level: a ticket leaves the board 60s after ALL its items reach a terminal state (READY handled here; SERVED/CANCELLED resolved elsewhere).

    Write `kds-board-sort.test.ts`: given a batch of tickets, assert the sort key is stable; then simulate an item-status change within an already-rendered ticket and assert the card ordering is unchanged (no reorder/bounce).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run __tests__/kds/kds-board-sort.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Sort key = receivedAt asc, tie ticket.id, computed once per batch; card order is invariant under item-status updates.
    - Fully-resolved tickets auto-remove after 60s; new tickets fade in at sorted position.
    - kds-board-sort.test.ts passes (stable-order assertion).
  </acceptance_criteria>
  <done>Cards no longer jump on refresh; ordering is deterministic and stable under item-status changes.</done>
</task>

<task type="auto">
  <name>Task 3: Ticket-detail view (revisions grouped + per-item status/notes + Kitchen Notes callout)</name>
  <read_first>
    - frontend/lib/hooks/kds/use-tickets.ts (plan-05 ticket-detail hook)
    - frontend/components/pos/revision-chip.tsx (revision log grouping)
    - .planning/phases/07.1-pos-production-operations/07.1-UI-SPEC.md (§6 Kitchen Notes amber callout bg-warning/10 + MessageSquare; §8 ticket detail — revisions grouped "Rev n · time", per-item status + notes)
    - frontend/__tests__/kds/kds-ticket-card.test.tsx (or existing kds-board.test — vitest scaffold)
  </read_first>
  <files>frontend/components/kds/kds-ticket-detail.tsx, frontend/components/kds/kds-ticket-card.tsx, frontend/__tests__/kds/kds-ticket-card.test.tsx</files>
  <action>
    Create `kds-ticket-detail.tsx`: tapping a ticket's header opens a modal/drawer (shadcn Dialog/Sheet) showing the full order via the plan-05 ticket-detail hook — all items grouped under "Rev {n} · {time}" headers (revision-chip log), each item with its per-item status badge + per-item notes (italic, small), and the order-level "Kitchen Notes" callout at the top (amber `bg-warning/10`, icon MessageSquare, from the ticket's orderNotes). Wire the open-on-header-tap trigger into `kds-ticket-card.tsx`. Board stays always-dark inside the detail too.

    Extend `kds-ticket-card.test.tsx` (revision-pill test from the plan-05 fixture pattern): assert a ticket with revisionNo=2 items renders the REV-2 pill on those items and not on Rev 1 items, and that opening the detail shows grouped revisions + the Kitchen Notes callout when orderNotes is present.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run __tests__/kds/kds-ticket-card.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Tapping a ticket opens a detail with revisions grouped by "Rev n · time", per-item status + notes, and the Kitchen Notes callout.
    - kds-ticket-card.test.tsx asserts revision pill placement + detail grouping + Kitchen Notes.
  </acceptance_criteria>
  <done>Kitchen staff can open a ticket for full order detail with revisions, per-item status, and instructions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| KITCHEN_STAFF client → KDS ticket detail | ticket-detail read stays within kds.view + tenant/branch |
| notes → KDS render | free-text order/item notes rendered on the board |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07.1g-01 | Information Disclosure | KDS ticket-detail render | medium | mitigate | Detail fetched via the plan-02 permission-guarded endpoint (pos.kds.view, tenant/branch scoped); KITCHEN_STAFF isolation preserved — no pos.order.*/finance data surfaced on the board |
| T-07.1g-02 | Tampering (XSS) | order/item notes on the board | medium | mitigate | Notes rendered as plain text via JSX auto-escaping; no dangerouslySetInnerHTML; server enforces char limits (plan 03) |
| T-07.1g-SC | Tampering | npm installs | high | mitigate | No new packages (RESEARCH.md audit: none) |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` clean; `npx eslint components/kds --max-warnings=0`.
- `npx vitest run __tests__/kds/kds-board-sort.test.ts __tests__/kds/kds-ticket-card.test.tsx` green.
- Manual (VALIDATION.md): fire 3 revisions in a browser → cards stable, new items highlighted, detail opens.
</verification>

<success_criteria>
- Cards are stable (no bounce), show per-item status + revision pills, preserve the aging border + always-dark board.
- Tapping a ticket opens full order detail with grouped revisions, per-item status, and Kitchen Notes.
</success_criteria>

<output>
Create `.planning/phases/07.1-pos-production-operations/07.1-07-SUMMARY.md` when done.
</output>
