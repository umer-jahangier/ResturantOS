---
phase: 07.1-pos-production-operations
plan: 10
type: execute
wave: 7
depends_on: ["07.1-09"]
files_modified:
  - frontend/components/pos/table-floor-view.tsx
  - frontend/__tests__/pos/table-floor-view.test.tsx
autonomous: true
requirements: [POS-10]
must_haves:
  truths:
    - "Table tiles use semantic tokens (migrated off hardcoded green/orange/blue) with an icon per state (never color alone)"
    - "Tiles render three states — AVAILABLE, OCCUPIED (with a derived-order-status badge), NEEDS_BUSSING"
    - "Tapping an AVAILABLE table binds its tableId (page-level state) and switches to the Terminal tab to start a new order against that table"
    - "Tapping an OCCUPIED/NEEDS_BUSSING table opens the SAME shared Order/Table Detail drawer used by Order Management"
  artifacts:
    - "frontend/__tests__/pos/table-floor-view.test.tsx"
  key_links:
    - "table-floor-view sets the page-level selectedTableId (plan 06) on AVAILABLE tap; PosTerminal (plan 08) reads it into createOrder"
    - "OCCUPIED/NEEDS_BUSSING tap reuses order-table-detail-drawer (plan 09) — no second table-detail UI"
---

<objective>
Make the table floor view the primary dine-in entry point (POS-10): migrate `table-floor-view.tsx` off hardcoded Tailwind colors onto semantic tokens, add the three-state tile lifecycle (AVAILABLE / OCCUPIED-with-derived-status-badge / NEEDS_BUSSING) each with an icon (never color-alone), and wire both entry paths — tap AVAILABLE binds the table and starts an order on the Terminal tab; tap OCCUPIED/NEEDS_BUSSING opens the SAME shared Order/Table Detail drawer built in plan 09 (UI-SPEC's hard rule: one drawer, not a second table-detail UI).

This plan touches only `table-floor-view.tsx` + its test. It depends on the shared drawer (plan 09) and the page-level `selectedTableId` state + drawer-open plumbing (plan 06). The tableId hand-off to the terminal goes through page state (plan 06 owns page.tsx; plan 08 owns pos-terminal.tsx) — this plan only calls the setter prop, so it never touches those files.

Purpose: POS-10 — table-centric dine-in with live order visibility and table-state at a glance.
Output: redesigned table floor view + vitest.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07.1-pos-production-operations/07.1-UI-SPEC.md
@.planning/phases/07.1-pos-production-operations/07.1-PATTERNS.md
@.planning/phases/07.1-pos-production-operations/07.1-RESEARCH.md
@CLAUDE.md
@frontend/components/pos/table-floor-view.tsx
@frontend/components/pos/order-table-detail-drawer.tsx
@frontend/components/shared/status-badge.tsx
@frontend/lib/hooks/pos/use-tables.ts
@.planning/phases/07.1-pos-production-operations/07.1-06-SUMMARY.md
@.planning/phases/07.1-pos-production-operations/07.1-09-SUMMARY.md
</context>

<artifacts_produced>
This plan creates/extends:
- `table-floor-view.tsx`: semantic-token tile colors (migrated off hardcoded green/orange/blue), three-state lifecycle (AVAILABLE / OCCUPIED + derived-status badge / NEEDS_BUSSING) each with an icon, AVAILABLE-tap → bind tableId + switch to Terminal, OCCUPIED/NEEDS_BUSSING-tap → open the shared drawer.
- Test: table-floor-view.test.tsx.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Table tile color-token migration + three-state lifecycle + both tap paths</name>
  <read_first>
    - frontend/components/pos/table-floor-view.tsx (current hardcoded green-400/50/100, orange-400/50/100, blue-100/700; existing AVAILABLE/OCCUPIED handling; table.tableName)
    - frontend/components/pos/order-table-detail-drawer.tsx (plan-09 shared drawer — the OCCUPIED/NEEDS_BUSSING tap target)
    - frontend/components/shared/status-badge.tsx (derived-order-status badge for OCCUPIED tiles)
    - frontend/lib/hooks/pos/use-tables.ts (table list + derived status)
    - .planning/phases/07.1-pos-production-operations/07.1-UI-SPEC.md (§2 tile states + exact tokens: AVAILABLE border-success bg-success/10 CheckCircle2; OCCUPIED border-info bg-info/10 Utensils + derived-status badge; NEEDS_BUSSING border-warning bg-warning/10 Sparkles; tap behaviors; Color section Rule 3 migration mandate)
  </read_first>
  <files>frontend/components/pos/table-floor-view.tsx, frontend/__tests__/pos/table-floor-view.test.tsx</files>
  <action>
    Migrate every hardcoded Tailwind palette class in `table-floor-view.tsx` (green-400/50/100, orange-400/50/100, blue-100/700) to the semantic tokens per UI-SPEC §2 / Color section (this is a mandated correction, Rule 3): AVAILABLE = `border-success bg-success/10` + `CheckCircle2` icon + "Available"; OCCUPIED = `border-info bg-info/10` + `Utensils` icon + "Occupied" PLUS a small derived-order-status `StatusBadge` beneath the label (from useTableDetail/table summary — at-a-glance order progress, POS-15 "clear status indicators"); NEEDS_BUSSING = `border-warning bg-warning/10` + `Sparkles` icon + "Needs Bussing". Every state shows an icon, never color alone (WCAG). Touch targets floor at 44px.

    Wire both entry paths: tapping an AVAILABLE tile calls the page-level `setSelectedTableId(tableId)` setter (prop from plan 06) and switches the POS view to the Terminal tab so a new DRAFT order is created bound to that table (createOrder+tableId is plan 08). Tapping an OCCUPIED/NEEDS_BUSSING tile opens the SHARED `order-table-detail-drawer` (plan 09) for that table's active order — do NOT build a second table-detail UI. Keep the existing "No tables configured" empty state.

    Write `table-floor-view.test.tsx`: the three tile states render with their expected token classes + icons; tapping AVAILABLE calls the setSelectedTableId setter (+ view switch); tapping OCCUPIED opens the shared drawer.

    Per CLAUDE.md: before editing the existing `TableFloorView` component, run GitNexus `impact({target: "TableFloorView", direction: "upstream"})` — report the blast radius and warn on HIGH/CRITICAL; run `detect_changes()` before commit. The new `table-floor-view.test.tsx` is net-new (nothing indexed to analyze).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | tail -10 && npx vitest run __tests__/pos/table-floor-view.test.tsx 2>&1 | tail -20 && npx eslint components/pos/table-floor-view.tsx --max-warnings=0 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - No hardcoded palette classes remain; tiles use semantic tokens + an icon per state (AVAILABLE/OCCUPIED+badge/NEEDS_BUSSING).
    - AVAILABLE tap binds tableId (page state) + switches to Terminal; OCCUPIED/NEEDS_BUSSING tap opens the shared drawer.
    - table-floor-view.test.tsx passes; ESLint (tokens + four-layer) clean.
  </acceptance_criteria>
  <done>The table floor view is the dine-in entry point: three-state tiles with at-a-glance status, opening a new order or the shared detail drawer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cashier UI → pos-service | table detail fetch scoped by branch/tenant server-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07.1j-01 | Information Disclosure | table detail on OCCUPIED tap | high | mitigate | The shared drawer fetches via the plan-04 tenant/branch-scoped table→order endpoint (branchId validated against JWT); no cross-tenant/branch order surfaced |
| T-07.1j-02 | Tampering | client-supplied tableId | medium | mitigate | tableId flows to createOrder where the server validates it against the caller's branch/tenant (plan 04); a forged id cannot bind cross-branch |
| T-07.1j-SC | Tampering | npm installs | high | mitigate | No new packages (RESEARCH.md audit: none) |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` clean; `npx eslint components/pos/table-floor-view.tsx --max-warnings=0` (no hardcoded palette classes; four-layer intact).
- `npx vitest run __tests__/pos/table-floor-view.test.tsx` green.
- Full frontend suite green before phase verify (`npx vitest run`).
</verification>

<success_criteria>
- Table tiles show three states with semantic tokens + icons + at-a-glance derived status; tapping starts a table-bound order or opens the shared detail drawer.
</success_criteria>

<output>
Create `.planning/phases/07.1-pos-production-operations/07.1-10-SUMMARY.md` when done.
</output>
