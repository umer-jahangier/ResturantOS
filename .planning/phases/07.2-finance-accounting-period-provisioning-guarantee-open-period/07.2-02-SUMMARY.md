---
phase: 07.2-finance-accounting-period-provisioning-guarantee-open-period
plan: 02
subsystem: auth
tags: [liquibase, rbac, permissions, postgres, auth-service]

# Dependency graph
requires:
  - phase: 07.2-01
    provides: FIN-08 requirement definition (RBAC prerequisite for the self-service provision endpoint)
provides:
  - finance.period.open permission code (module finance) seeded via Liquibase changeset 044
  - Explicit role_permissions grants: OWNER, TENANT_ADMIN, ACCOUNTANT -> finance.period.open
  - FinancePeriodOpenPermissionIT DB-assertion proving the exact grant set
affects: [07.2-05 (self-service POST /api/v1/finance/periods/provision endpoint gated on this permission), 07.2-06 (live E2E verification)]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Liquibase incremental permission changeset (never edit prior runOnChange=false seed changesets 030/036)"
    - "Explicit OWNER/TENANT_ADMIN role_permissions grants for any brand-new permission code (036's wildcard SELECT only ran once at initial DB creation and does not retroactively apply)"

key-files:
  created:
    - services/auth-service/src/main/resources/db/changelog/v1.0.0/044-finance-period-open-permission.xml
    - services/auth-service/src/test/java/io/restaurantos/auth/FinancePeriodOpenPermissionIT.java
  modified:
    - services/auth-service/src/main/resources/db/changelog/db.changelog-master.xml

key-decisions:
  - "Changeset 044 grants finance.period.open explicitly to OWNER, TENANT_ADMIN, and ACCOUNTANT (not just ACCOUNTANT) because 036's wildcard SELECT-based OWNER/TENANT_ADMIN grants are runOnChange=false and do not retroactively pick up new permission codes on already-migrated databases (RESEARCH.md Pitfall 4)."
  - "MANAGER, CASHIER, INVENTORY_MANAGER, and KITCHEN_STAFF deliberately excluded — least privilege, symmetric to finance.period.close which none of them hold either."

patterns-established:
  - "DB-assertion IT pattern for RBAC changesets: extend BaseIntegrationTest (so Liquibase applies the full changelog against Testcontainers Postgres), inject JdbcTemplate, assert role_permissions role-set via containsExactlyInAnyOrder to prove both presence of intended roles and absence of all others in one assertion."

requirements-completed: [FIN-08]

coverage:
  - id: D1
    description: "finance.period.open permission code exists in the permissions table after auth-service migrations run"
    requirement: "FIN-08"
    verification:
      - kind: integration
        ref: "FinancePeriodOpenPermissionIT#financePeriodOpen_permissionRowExists"
        status: pass
    human_judgment: false
  - id: D2
    description: "role_permissions grants finance.period.open to exactly OWNER, TENANT_ADMIN, and ACCOUNTANT (no other role)"
    requirement: "FIN-08"
    verification:
      - kind: integration
        ref: "FinancePeriodOpenPermissionIT#financePeriodOpen_grantedToExactlyOwnerTenantAdminAccountant"
        status: pass
      - kind: integration
        ref: "FinancePeriodOpenPermissionIT#financePeriodOpen_notGrantedToCashierOrManager"
        status: pass
    human_judgment: false
  - id: D3
    description: "Changeset 044 is registered in db.changelog-master.xml after 043 and applies cleanly on a fresh Testcontainers DB; changesets 030/036 remain byte-identical"
    requirement: "FIN-08"
    verification:
      - kind: other
        ref: "grep -c 044-finance-period-open-permission.xml db.changelog-master.xml == 1; git diff 030-create-roles-permissions.xml == empty"
        status: pass
    human_judgment: false

# Metrics
duration: 9min
completed: 2026-07-12
status: complete
---

# Phase 07.2 Plan 02: Finance Period Open RBAC Permission Summary

**New `finance.period.open` Liquibase changeset granting exactly OWNER/TENANT_ADMIN/ACCOUNTANT, with a DB-assertion IT proving the exact grant set — the RBAC prerequisite for the self-service accounting-period provision endpoint.**

## Performance

- **Duration:** 9 min
- **Started:** 2026-07-11T19:51:20Z (approx, following 07.2-01 completion)
- **Completed:** 2026-07-11T19:57:19Z
- **Tasks:** 2
- **Files modified:** 3 (2 created, 1 modified)

## Accomplishments
- Created Liquibase changeset `044-finance-period-open-permission.xml` inserting the `finance.period.open` permission (module `finance`) plus three explicit `role_permissions` grant rows (OWNER, TENANT_ADMIN, ACCOUNTANT)
- Registered the new changeset in `db.changelog-master.xml` immediately after `043-cashier-void-own-permission.xml`, without touching any prior include ordering
- Added `FinancePeriodOpenPermissionIT`, a Testcontainers-backed DB-assertion test proving: (1) the permission row exists, (2) the grant set is exactly `{OWNER, TENANT_ADMIN, ACCOUNTANT}` via `containsExactlyInAnyOrder`, and (3) `CASHIER`/`MANAGER`/`KITCHEN_STAFF`/`INVENTORY_MANAGER` are absent
- Verified `030-create-roles-permissions.xml` and `036`'s seed changesets remain byte-identical (`git diff` empty) — no prior seed changeset was edited

## Task Commits

Each task was committed atomically:

1. **Task 1: Create changeset 044 (finance.period.open permission + 3 explicit role grants) and register it in the master changelog** - `3b445ff` (feat)
2. **Task 2: DB-assertion IT proving the 3 grants exist and disallowed roles are excluded** - `eef1ab2` (test)

**Plan metadata:** (this commit) - `docs: complete plan`

## Files Created/Modified
- `services/auth-service/src/main/resources/db/changelog/v1.0.0/044-finance-period-open-permission.xml` - New Liquibase changeset: permission insert + 3 explicit role_permissions grants
- `services/auth-service/src/main/resources/db/changelog/db.changelog-master.xml` - Registers the new changeset include after 043
- `services/auth-service/src/test/java/io/restaurantos/auth/FinancePeriodOpenPermissionIT.java` - DB-assertion IT (3 tests, all green)

## Decisions Made
- Explicit OWNER/TENANT_ADMIN/ACCOUNTANT grants (not relying on 036's wildcard SELECT) per RESEARCH.md Pitfall 4 — the wildcard grants are `runOnChange="false"` and only ran once at initial DB creation, so a new permission code would not reach OWNER/TENANT_ADMIN without an explicit insert here.
- MANAGER, CASHIER, INVENTORY_MANAGER, KITCHEN_STAFF deliberately excluded from the grant set (least privilege, symmetric to `finance.period.close`'s existing grant pattern).

## Deviations from Plan

None - plan executed exactly as written. No GitNexus MCP tools were available in this session; per the plan's fallback instruction, a manual review of `db.changelog-master.xml`'s include ordering was performed instead before editing.

## Issues Encountered
None. `mvn -pl services/auth-service test -Dtest=FinancePeriodOpenPermissionIT` ran successfully against Docker/Testcontainers (Postgres 18, Redis 8, RabbitMQ 4.3) in this environment — 3/3 tests passed in 68.23s, so no CI/human-run flag is needed.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- `finance.period.open` permission is now live in the auth-service Liquibase changelog and will apply to any fresh or existing Testcontainers/dev/staging/prod DB on next migration run.
- Plan 05 (self-service `POST /api/v1/finance/periods/provision` endpoint) can now gate on `@PreAuthorize("hasAuthority('finance.period.open')")` with confidence that OWNER/TENANT_ADMIN/ACCOUNTANT will have it and no other role will.
- No blockers for downstream plans in this phase.

---
*Phase: 07.2-finance-accounting-period-provisioning-guarantee-open-period*
*Completed: 2026-07-12*

## Self-Check: PASSED

- FOUND: services/auth-service/src/main/resources/db/changelog/v1.0.0/044-finance-period-open-permission.xml
- FOUND: services/auth-service/src/test/java/io/restaurantos/auth/FinancePeriodOpenPermissionIT.java
- FOUND: .planning/phases/07.2-finance-accounting-period-provisioning-guarantee-open-period/07.2-02-SUMMARY.md
- FOUND commit: 3b445ff
- FOUND commit: eef1ab2
