---
phase: 08.2-inventory-master-data-procurement-catalog
plan: 05
subsystem: ui
tags: [react, radix-ui, cmdk, tanstack-query, vitest, tailwind, nextjs]

# Dependency graph
requires:
  - phase: 08.2-01..04
    provides: inventory category tree V5, GET /api/v1/inventory/stock, three-state recipe coverage backend field, purchasing vendor-catalog V5 (no file overlap — this plan is frontend-only)
provides:
  - "components/ui/popover.tsx — Popover/PopoverTrigger/PopoverAnchor/PopoverContent, thin wrapper over radix-ui's unified Popover export"
  - "components/shared/catalog-item-combobox.tsx — CatalogItemCombobox + CatalogItemOption type, anchored cmdk-based search picker"
  - "components/shared/section-switcher.tsx — SectionSwitcher, raw button-group tabs replacement"
  - "components/inventory/allergen-pill-toggle.tsx — AllergenPillToggle + ALLERGENS (14 standard allergens)"
  - "components/ui/status-badge.tsx — COVERED/NO_RECIPE/SCHEDULED icon+label variants, archived legacy variant"
  - "lib/hooks/query-keys.ts — queryKeys.inventory.* and queryKeys.purchasing.* branch-scoped namespaces"
  - "lib/hooks/use-debounce.ts — useDebouncedValue<T>(value, delayMs)"
  - "lib/forms/calendar-date.ts — calendarDateToInstant, extracted from RecipeFormDialog.tsx with its explanatory comment intact"
  - "__tests__/lib/calendar-date.test.ts — 4-case regression guard for the local-midnight-vs-UTC coverage bug (mutation-tested)"
  - "lib/api-client/errors.ts — SERVICE_UNAVAILABLE entry in USER_FACING_BY_CODE"
affects: [08.2-12, 08.2-13, 08.2-14, 08.2-15, 08.2-16, 08.2-17, 08.2-18, 08.2-19]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Anchored (non-full-screen) combobox pattern: cmdk Command + new Popover wrapper, shouldFilter={false} with caller-controlled substring filtering"
    - "Branch-scoped query-key factories: every entry takes branchId first, returns as const tuple"
    - "Shared institutional-memory comments (calendarDateToInstant) move verbatim across extraction, never summarized"

key-files:
  created:
    - frontend/components/ui/popover.tsx
    - frontend/components/shared/catalog-item-combobox.tsx
    - frontend/components/shared/section-switcher.tsx
    - frontend/components/inventory/allergen-pill-toggle.tsx
    - frontend/lib/hooks/use-debounce.ts
    - frontend/lib/forms/calendar-date.ts
    - frontend/__tests__/lib/calendar-date.test.ts
  modified:
    - frontend/components/ui/status-badge.tsx
    - frontend/lib/hooks/query-keys.ts
    - frontend/components/inventory/RecipeFormDialog.tsx
    - frontend/lib/api-client/errors.ts

key-decisions:
  - "archived status variant added to LegacyStatusVariant/legacyClassMap (label-only, no icon) per the task's explicit action text, not to posStatusMap — diverges from the UI-SPEC's ARCHIVED table row (which shows an Archive icon) but matches the plan's mechanically-checked acceptance criteria exactly"
  - "TIMEOUT was not added to USER_FACING_BY_CODE — grepped the whole frontend tree first and found zero emitters of a TIMEOUT code, so per the task's own instruction ('do not invent codes the client never emits') it was skipped"
  - "query-keys.ts: filter/entity-scoped argument shapes for keys the plan didn't fully enumerate (categories, uoms, menuItems, purchaseOrders, invoices, spendAnalytics, vendorCategories) were inferred from the finance namespace's established shape and the existing local KEY arrays in use-inventory.ts/use-purchasing.ts — kept branchId-first, as const throughout"
  - "CatalogItemCombobox uses a fixed w-64 popover width rather than a Radix trigger-width CSS var, to avoid a speculative Tailwind arbitrary-value dependency not exercised anywhere else in the codebase"

patterns-established:
  - "Popover primitive: PopoverContent defaults align=start, sideOffset=4, portal-rendered, mirrors dialog.tsx's data-slot/cn/data-open|data-closed animation convention exactly"
  - "CatalogItemCombobox is data-source-agnostic (no lib/api-client import, ESLint layer-boundary clean) — callers own fetching via lib/hooks/** and pass options/onSearchChange"

requirements-completed: [INV-13, INV-14, INV-15, PUR-07, PUR-08]

coverage:
  - id: D1
    description: "popover.tsx primitive (Popover/PopoverTrigger/PopoverAnchor/PopoverContent) wraps radix-ui's Popover export, no new dependency"
    requirement: PUR-08
    verification:
      - kind: unit
        ref: "npx tsc --noEmit (frontend) — pass"
      - kind: other
        ref: "grep -c 'from \"radix-ui\"' components/ui/popover.tsx == 1; git diff --stat package.json empty"
        status: pass
    human_judgment: false
  - id: D2
    description: "CatalogItemCombobox shared picker (search-as-you-type, cmdk-based, MoneyDisplay price row, disabled placeholder, empty-state block, no lib/api-client import)"
    requirement: PUR-08
    verification:
      - kind: unit
        ref: "npx tsc --noEmit && npx eslint components/shared/catalog-item-combobox.tsx — pass"
        status: pass
      - kind: other
        ref: "grep -c 'from \"cmdk\"' == 1; grep -c 'lib/api-client' == 0"
        status: pass
    human_judgment: true
    rationale: "Visual/interaction behavior (keyboard nav, popover anchoring, empty-state rendering) is not exercised by an automated UI test in this plan — no consuming screen exists yet to drive it end-to-end; downstream plans 08.2-14..19 are the first real consumers."
  - id: D3
    description: "section-switcher.tsx (role=tablist/tab button group) and allergen-pill-toggle.tsx (14 ALLERGENS, aria-pressed pills)"
    requirement: INV-14
    verification:
      - kind: unit
        ref: "npx tsc --noEmit && npx eslint — pass"
        status: pass
      - kind: other
        ref: "grep -c 'role=\"tab\"' section-switcher.tsx >= 1; grep -c 'aria-pressed' allergen-pill-toggle.tsx >= 1"
        status: pass
    human_judgment: false
  - id: D4
    description: "status-badge.tsx COVERED/NO_RECIPE/SCHEDULED (icon+label, semantic tokens) and archived legacy variant"
    requirement: INV-15
    verification:
      - kind: unit
        ref: "npx tsc --noEmit && npx eslint — pass"
        status: pass
      - kind: other
        ref: "grep -cE 'bg-(amber|blue|green|red|slate|gray|yellow)-[0-9]{2,3}' status-badge.tsx == 0"
        status: pass
    human_judgment: false
  - id: D5
    description: "queryKeys.inventory.* and queryKeys.purchasing.* branch-scoped registries published alongside finance/pos/kds"
    requirement: INV-13
    verification:
      - kind: unit
        ref: "npx tsc --noEmit — pass"
        status: pass
      - kind: other
        ref: "grep -c 'inventory:' == 1; grep -c 'purchasing:' == 1; grep -c 'stockLevels' == 1; grep -c 'vendorItemPrices' == 1"
        status: pass
    human_judgment: false
  - id: D6
    description: "useDebouncedValue<T> hook for the plate-cost panel's 400ms recompute trigger"
    requirement: INV-15
    verification:
      - kind: unit
        ref: "npx tsc --noEmit && npx eslint — pass"
        status: pass
    human_judgment: false
  - id: D7
    description: "calendarDateToInstant extracted verbatim (function body + comment) into lib/forms/calendar-date.ts; RecipeFormDialog.tsx imports it, no local copy remains"
    requirement: INV-15
    verification:
      - kind: unit
        ref: "grep -c 'function calendarDateToInstant' RecipeFormDialog.tsx == 0; grep -c 'lib/forms/calendar-date' RecipeFormDialog.tsx == 1; npx tsc --noEmit pass"
        status: pass
    human_judgment: false
  - id: D8
    description: "4-case regression test for the local-midnight coverage bug, mutation-tested (fails 3/4 when reverted to Date.UTC)"
    requirement: INV-15
    verification:
      - kind: unit
        ref: "__tests__/lib/calendar-date.test.ts — npx vitest run, 4/4 pass"
        status: pass
      - kind: other
        ref: "Mutation check: temporarily swapped local Date constructor for Date.UTC(...) — 3/4 tests failed as expected, then reverted; final vitest run confirmed 4/4 passing again"
        status: pass
    human_judgment: false
  - id: D9
    description: "SERVICE_UNAVAILABLE entry added to USER_FACING_BY_CODE (errors.ts), replacing raw gateway text"
    requirement: INV-15
    verification:
      - kind: unit
        ref: "grep -c 'SERVICE_UNAVAILABLE' lib/api-client/errors.ts >= 1; npx tsc --noEmit pass"
        status: pass
    human_judgment: false

duration: 25min
completed: 2026-07-22
status: complete
---

# Phase 08.2 Plan 05: Shared Frontend Foundation Summary

**Popover primitive + CatalogItemCombobox + SectionSwitcher + AllergenPillToggle + 3-state coverage badges + branch-scoped inventory/purchasing query-key registry + debounce hook + extracted-and-guarded calendarDateToInstant + SERVICE_UNAVAILABLE copy — zero new npm dependencies.**

## Performance

- **Duration:** ~25 min
- **Completed:** 2026-07-22T22:35:41Z
- **Tasks:** 3
- **Files modified:** 11 (7 created, 4 modified)

## Accomplishments

- Built the one new UI primitive this phase adds (`popover.tsx`) and the one shared picker every downstream screen plan reuses (`catalog-item-combobox.tsx`), both zero-dependency (wrap the already-installed unified `radix-ui` package and `cmdk`).
- Extended `status-badge.tsx` with the 3-state recipe-coverage variants (`COVERED`/`NO_RECIPE`/`SCHEDULED`) and a legacy `archived` variant, all semantic-token-only.
- Published `queryKeys.inventory.*` / `queryKeys.purchasing.*` branch-scoped registries matching the existing `finance`/`pos`/`kds` shape.
- Extracted `calendarDateToInstant` out of `RecipeFormDialog.tsx` into `lib/forms/calendar-date.ts` (verbatim, including its bug-history comment) and wrote a 4-case regression test that is mutation-verified to fail if the UTC-midnight inversion ever ships again.
- Closed the carried-over `SERVICE_UNAVAILABLE` gap in `errors.ts` so a gateway outage now shows product copy instead of raw upstream text.

## Task Commits

1. **Task 1: popover primitive, catalog-item-combobox, section-switcher, allergen-pill-toggle** - `00ab014` (feat)
2. **Task 2: status-badge variants, query-key namespaces, debounce hook** - `724c068` (feat)
3. **Task 3: extract calendarDateToInstant behind a regression test; SERVICE_UNAVAILABLE copy** - `e707603` (fix)

**Plan metadata:** (this commit, docs)

## Files Created/Modified

- `frontend/components/ui/popover.tsx` - `Popover`, `PopoverTrigger`, `PopoverAnchor`, `PopoverContent` — thin wrapper over `radix-ui`'s `Popover` export, mirrors `dialog.tsx`'s structure (`data-slot`, `cn()`, `data-open`/`data-closed` animation classes). `PopoverContent` defaults `align="start"`, `sideOffset={4}`, renders inside `PopoverPrimitive.Portal`.
- `frontend/components/shared/catalog-item-combobox.tsx` - `CatalogItemCombobox` component + `CatalogItemOption` type export. Props: `options: CatalogItemOption[]`, `value: string | null`, `onSelect(option: CatalogItemOption): void`, `disabled?: boolean`, `disabledPlaceholder?: string` (default `"Select a vendor first."`), `placeholder?: string`, `emptyHeading?: string`, `emptyBody?: string`, `isLoading?: boolean`, `onSearchChange?(q: string): void`, `className?: string`. `CatalogItemOption = { id: string; name: string; secondary?: string; sku?: string; pricePaisa?: number | null }`. No `lib/api-client` import (ESLint layer-boundary clean); uses `MoneyDisplay` for the price column.
- `frontend/components/shared/section-switcher.tsx` - `SectionSwitcher` component. Props: `sections: { id: string; label: string }[]`, `activeId: string`, `onChange(id: string): void`, `ariaLabel: string`, `className?: string`. Renders `role="tablist"` of `<button role="tab" aria-selected aria-controls={`section-${id}`}>`.
- `frontend/components/inventory/allergen-pill-toggle.tsx` - `AllergenPillToggle` component + `ALLERGENS` frozen array export (`ReadonlyArray<{ code: string; label: string }>`, 14 standard allergens). Props: `value: string[]`, `onChange(next: string[]): void`, `disabled?: boolean`, `className?: string`.
- `frontend/components/ui/status-badge.tsx` - added `CoverageStatusVariant = "COVERED" | "NO_RECIPE" | "SCHEDULED"` (exported), widened `StatusVariant`, three new `posStatusMap` entries (icon+label+semantic token), `archived` added to `LegacyStatusVariant`/`legacyClassMap` (muted token, label-only).
- `frontend/lib/hooks/query-keys.ts` - added `queryKeys.inventory` (`ingredients`, `ingredient`, `categories`, `categoryTree`, `uoms`, `menuItems`, `recipes`, `recipeVersions`, `coverage`, `stockLevels`, `costPreview`) and `queryKeys.purchasing` (`vendors`, `vendor`, `vendorItems`, `vendorItem`, `vendorItemPrices`, `vendorCategories`, `purchaseOrders`, `purchaseOrder`, `invoices`, `spendAnalytics`, `scorecard`) — every factory takes `branchId: string` first, returns `as const`. Existing local `const X_KEY` arrays in `use-inventory.ts`/`use-purchasing.ts` untouched (plans 08.2-12/13 own that migration).
- `frontend/lib/hooks/use-debounce.ts` - `useDebouncedValue<T>(value: T, delayMs: number): T`.
- `frontend/lib/forms/calendar-date.ts` - `calendarDateToInstant(value: string): string | undefined`, moved verbatim from `RecipeFormDialog.tsx` with its full explanatory comment.
- `frontend/__tests__/lib/calendar-date.test.ts` - 4-case regression guard (local-midnight components, UTC-offset delta, literal `+05:00` reproduction pinned to `"2026-07-21T19:00:00.000Z"`, malformed/edge-case input contract). Mutation-tested against a `Date.UTC(...)` revert (3/4 tests failed, then reverted).
- `frontend/components/inventory/RecipeFormDialog.tsx` - removed the local `calendarDateToInstant` function + comment; imports `calendarDateToInstant` from `@/lib/forms/calendar-date`.
- `frontend/lib/api-client/errors.ts` - `USER_FACING_BY_CODE` gains `SERVICE_UNAVAILABLE: "This module is temporarily unavailable. Try again in a moment."`.

## Decisions Made

- `archived` status variant lives in `LegacyStatusVariant`/`legacyClassMap` (label-only, no icon), per the task's explicit action text — this diverges from the UI-SPEC's Status System table (which shows `ARCHIVED` with an `Archive` icon in `posStatusMap`-style treatment) but matches the plan's own acceptance criteria word-for-word, which is the mechanically-checked contract.
- Did not add a `TIMEOUT` entry to `USER_FACING_BY_CODE` — grepped the whole `frontend/` tree first and found zero emitters of a `TIMEOUT` code anywhere, so per the task's own instruction ("do not invent codes that the client never emits") this was skipped.
- Filled in query-key argument shapes the plan text didn't fully spell out (`categories`, `uoms`, `menuItems`, `purchaseOrders`, `invoices`, `spendAnalytics`, `vendorCategories`) by pattern-matching the `finance` namespace's existing shape and the local `KEY` constants already in use in `use-inventory.ts`/`use-purchasing.ts`.
- `CatalogItemCombobox`'s popover content uses a fixed `w-64` rather than a Radix trigger-width CSS variable, avoiding a speculative Tailwind arbitrary-value pattern not used elsewhere in this codebase.

## Deviations from Plan

None - plan executed exactly as written. All three tasks' explicit `<action>` instructions were followed; the only judgment calls made were filling gaps the plan itself flagged as "Claude's Discretion" (query-key argument shapes not fully enumerated) or explicitly instructed ("check before adding" for `TIMEOUT`).

## Issues Encountered

None. GitNexus MCP tools mandated by `CLAUDE.md` were not available in this execution environment (no `mcp__gitnexus__*` tools were present in the tool list) — per the plan's `<mcp_tools>` note that the GitNexus TS/Java symbol index has been observed to return "not found" on this repo, impact analysis and change detection were performed via `Grep`/`Glob` reads of the affected files (`dialog.tsx`, `command-palette.tsx`, `order-type-toggle.tsx`, `status-badge.tsx`, `query-keys.ts`, `use-inventory.ts`, `use-purchasing.ts`, `RecipeFormDialog.tsx`, `errors.ts`) plus `git diff`/`git log` for change-scope verification, and this fallback is recorded here as instructed.

## Known Stubs

None. Every exported component in this plan is presentational/data-source-agnostic by design (per the plan's own spec — e.g. `CatalogItemCombobox` takes `options` as a prop rather than fetching), not a stub awaiting a data source; downstream plans 08.2-12/13 wire the data layer and 08.2-14..19 consume these components in real screens.

## Threat Flags

None. All three threats in this plan's `<threat_model>` were mitigated exactly as specified: `SERVICE_UNAVAILABLE` now has a curated `USER_FACING_BY_CODE` entry (T-08.2-051); no new npm dependency was added, `git diff --stat package.json` is empty (T-08.2-052); `CatalogItemCombobox`/`AllergenPillToggle`/`SectionSwitcher` render all user-controlled strings as React text children only, no `dangerouslySetInnerHTML` anywhere in the new files (T-08.2-053).

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Every shared building block plans 08.2-12 through 08.2-19 depend on now exists and is importable: `Popover`/`PopoverTrigger`/`PopoverAnchor`/`PopoverContent`, `CatalogItemCombobox`/`CatalogItemOption`, `SectionSwitcher`, `AllergenPillToggle`/`ALLERGENS`, the `COVERED`/`NO_RECIPE`/`SCHEDULED`/`archived` status-badge variants, `queryKeys.inventory.*`/`queryKeys.purchasing.*`, `useDebouncedValue`, and `calendarDateToInstant`.
- Both halves of CONTEXT.md's coverage-bug regression-guard pair now exist (backend in 08.2-03, frontend here) — the local-midnight helper cannot silently regress to UTC without a collected test failing.
- No blockers. Full plan-level verification (`npx tsc --noEmit`, `npx eslint` over all 11 changed/created files, `npx vitest run __tests__/lib/calendar-date.test.ts`, `git diff --stat package.json`) passed clean at the end of this plan.

## Self-Check: PASSED

All 12 created/modified files verified present on disk; all 3 task commit hashes (`00ab014`, `724c068`, `e707603`) verified present in `git log --oneline --all`.

---
*Phase: 08.2-inventory-master-data-procurement-catalog*
*Completed: 2026-07-22*
