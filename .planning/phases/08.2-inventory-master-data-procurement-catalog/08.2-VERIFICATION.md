---
phase: 08.2-inventory-master-data-procurement-catalog
verified: 2026-07-24T00:00:00Z
status: human_needed
score: 8/8 success criteria verified (codebase evidence); 1 requirements-doc reconciliation gap; 2 carried-debt items noted
behavior_unverified: 0
overrides_applied: 0
requirements_reconciliation:
  finding: "REQUIREMENTS.md is internally inconsistent for all 5 phase requirements: the v1 checklist marks INV-13/INV-14/INV-15/PUR-07/PUR-08 as `[x]` complete, but the Traceability table (last touched at 08.2-16, 4 plans before the phase actually finished) still reads 'Not started' (INV-13, INV-14, PUR-07, PUR-08) or 'In progress ... stock-operations UI slice remains' (INV-15). Plans 06-20 never updated the Traceability table. Codebase verification in this report confirms the underlying capability IS now fully delivered (including the stock-operations UI slice plan 17 shipped), so the checklist's [x] is factually correct as of phase-end even though it was set prematurely at wave 1 (08.2-01/02-03/04, before any UI existed) and never revisited. The Traceability table is the stale artifact and must be updated to 'Complete' for all five rows before this phase is considered administratively closed."
  action_required: "Update .planning/REQUIREMENTS.md Traceability table rows for INV-13, INV-14, INV-15, PUR-07, PUR-08 to 'Complete', replacing the stale 'Not started'/'In progress' text. This is a documentation fix, not a code fix — no gap in delivered capability was found."
carried_debt:
  - item: "GET /api/v1/inventory/transfers/pending (plan 08.2-17 scope deviation) has zero backend integration test coverage — grep across services/inventory-service/src/test confirms no test references this endpoint, TransferService.listPendingForBranch, or the pending-transfer read path."
    code_review: "Controller enforces authz.authorizeView() (inventory.item.view) AND rejects any branchId not equal to the caller's own JWT branch claim (mirrors StockLevelController's already-proven own-branch-only pattern exactly); service method adds an explicit tenantId predicate via findByTenantIdAndToBranchIdAndStatusOrderByShippedAtAsc. Code inspection shows the same two-layer (permission + branch-ownership) scoping used everywhere else in this phase — no anomaly found — but this is a code-review conclusion, not a passing-test proof, for a phase where every other new endpoint has a dedicated IT."
    severity: "warning — human/regression-test follow-up recommended, not a functional gap"
  - item: "Pre-existing latent bug (purchasing-service @ResponseStatus + bare RuntimeException exceptions — ApprovalLimitExceededException, DuplicateApproverException, InvalidPoStateException — silently resolve to 500 through GlobalExceptionHandler's Exception.class catch-all, since @ExceptionHandler resolution outranks @ResponseStatus resolution) remains UNRESOLVED, exactly as plan 08.2-08 flagged and deliberately left out of scope."
    code_review: "Confirmed this phase's OWN new exceptions (BackdatedPriceException, VendorItemCatalogMismatchException, CategoryInUseException, CategoryValidationException, IngredientCategoryInvalidException) all correctly extend RestaurantOsException or have a local @RestControllerAdvice — none of this phase's new error paths hit the 500 catch-all. The pre-existing three classes are untouched and still carry the bug."
    severity: "info — carried debt, correctly out of this phase's scope, not introduced or worsened here"
minor_debt:
  - item: "CatalogItemCombobox's trigger aria-label never updates to the selected item's name (pre-existing since 08.2-05, documented in deferred-items.md by plan 08.2-19) — a screen-reader user always hears the placeholder text, never the actual selection, on every consumer (PO line picker, vendor-item picker)."
    severity: "info — accessibility gap, not a functional/goal-blocking issue"
---

# Phase 08.2: Inventory Master Data & Procurement Catalog Verification Report

**Phase Goal:** The inventory and procurement modules become operable by a restaurant manager
without SQL — ingredients and their categories are first-class, editable master data; recipes can
be viewed and revised with live plate cost; vendors carry a real item catalog with effective-dated
pricing; and a purchase order line is chosen from that catalog instead of a hand-typed UUID.

**Verified:** 2026-07-24
**Status:** human_needed (all 8 success criteria pass codebase verification; status is not `passed`
purely because of the REQUIREMENTS.md documentation-reconciliation gap and the untested transfers/pending
endpoint, both of which need a human decision/follow-up rather than being silent passes)
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (ROADMAP Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Manager creates/edits/re-parents/archives ingredient categories in a 3-level-capped tree; every ingredient carries exactly one required primary category; archiving a category in use is refused (not cascading) | ✓ VERIFIED | `V5__item_categories.sql` DB trigger `enforce_item_category_depth()` hard-caps depth (SQLSTATE 23514); `ItemCategoryService.archive()` throws `CategoryInUseException` (409) when `ingredientCount>0 \|\| childCount>0`; `ItemCategoryService.move()` validates cycle/depth before write; `ingredients.category_id` is `NOT NULL FK ... ON DELETE RESTRICT`; `CreateIngredientRequest.categoryId`/`UpdateIngredientRequest.categoryId` are `@NotNull`; frontend `/app/inventory/categories/page.tsx` + `category-tree.tsx` + `CategoryFormDialog.tsx` (238+ lines) wire Edit/Add-subcategory (hidden at level 3)/Move-to (valid-targets-only)/Archive-Restore, no Delete action anywhere (`grep '>\s*Delete'` returns 0 across all three files) |
| 2 | Manager creates/searches/edits/archives ingredients entirely through `/app/inventory/ingredients` — incl. purchase/stock/recipe UOM with conversions, par level, reorder point, storage location, allergens — no hard delete once stock movements exist | ✓ VERIFIED | `V6__ingredient_master_data.sql` + `IngredientService`/`IngredientController`: full CRUD, `isMeasureTypeLocked()` gates measure-type changes once `InventoryMovementRepository`/`StockLotRepository` show any row for that ingredient (409 pre-submit-disabled via `measureTypeLocked` DTO field); `archiveIngredient()` only sets `archivedAt`/`active=false`, never calls `.delete(`; no `@DeleteMapping` anywhere in `IngredientController.java`; frontend `ingredients/page.tsx` (324 lines) + `IngredientFormDialog.tsx` render grouped General/Units/Stock/Compliance sections, `AllergenPillToggle` (no checkboxes, confirmed via grep), required category select, disabled measure-type + tooltip |
| 3 | An existing recipe's ingredient lines are viewable; a revision can be authored pre-filled from the current version (never destructive), with a live plate-cost panel showing batch cost, cost/portion, food-cost %, per-line share | ✓ VERIFIED | `POST /api/v1/inventory/recipes/preview` (`RecipeCostPreviewService`, no `.save(`/`.saveAll(`/`.delete(`/`.flush(` calls, BigDecimal-only, no `double`/`float`) returns batch/portion/food-cost%/per-line share, excludes unpriceable lines with a warning instead of failing; `recipes/[menuItemId]/page.tsx` (475 lines) + `RecipeVersionStrip.tsx` + `recipe-cost-panel.tsx` render version-by-version viewing, every save routes through `useCreateRecipe` only (`grep -ciE 'useUpdateRecipe\|saveInPlace\|updateRecipeVersion'` on the detail page returns 0), panel dims + shows "Recalculating…" during recompute, never blanks |
| 4 | Coverage report distinguishes "no recipe" from "recipe scheduled from `<date>`" | ✓ VERIFIED | `RecipeService.getCoverage()` now returns 3-state `items[]` (COVERED/SCHEDULED/NO_RECIPE) derived exclusively from `effectiveFrom <= now()`, never `isCurrent()` (grep confirms no `isCurrent()` call inside `getCoverage`); bounded 2-query implementation (was N+1); `RecipeRepository` gained a tenant-scoped lookup replacing the un-scoped original; `coverage/page.tsx` renders 4 stat cards (Total/Covered/Scheduled/No Recipe) + a table rendering `item.state` verbatim via `StatusBadge`, with `scheduledFrom` shown for SCHEDULED rows; MSW mock (`inventory.handlers.ts`) rewritten to the same effective-from definition |
| 5 | Vendor linked to ingredients via a vendor item catalog (SKU, pack size, purchase UOM, MOQ, lead time) with APPEND-ONLY effective-dated pricing; category tags filter/suggest only, never authorize | ✓ VERIFIED | `purchasing V5__vendor_item_catalog.sql`: `vendor_items`, `vendor_item_prices` (effective_from/effective_to), `vendor_categories`, all RLS tenant-isolated; `VendorItemPriceService.recordNewPrice()` only ever closes the prior row's `effectiveTo` and inserts a new row — never mutates `unitPricePaisa` on an existing row (verified by reading the method body); `PurchaseOrderService` never references `vendorCategory`/`categoryId` (grep returns 0 hits); `VendorCategoryTags.tsx` always renders "Used to filter and suggest — does not restrict what can be ordered." |
| 6 | PO line selected via search-as-you-type picker (pack size/vendor SKU/contract price), filtered to vendor's catalog; hand-typed ingredient UUID field GONE | ✓ VERIFIED | `PurchaseOrderFormDialog.tsx` line row uses `CatalogItemCombobox` scoped to `useVendorItems(selectedVendorId)`; no `ingredientId` field anywhere in the file (grep confirms 0 matches for both `lines.${idx}.ingredientId` and the literal string "ingredient id"); server-side `PurchaseOrderService.create()` derives `ingredientId`/uom/price from `vendorItemId` and rejects a cross-vendor id with a 422 `VendorItemCatalogMismatchException` |
| 7 | Spend-by-category analytics from real ingredient categories; `MockIngredientCategoryResolver` + `spend-category-map.yml` DELETED | ✓ VERIFIED | `grep -rl "MockIngredientCategoryResolver\|spend-category-map" services/purchasing-service/src` returns nothing (only stale `target/` build artifacts remain, not source); `IngredientCategoryResolver` interface is now batch-only (`resolveAll(Collection<UUID>)`, no single-id overload); `FeignIngredientCategoryResolver` calls the real `GET /internal/inventory/ingredient-categories` (added by 08.2-09), chunks batches of 500, degrades to "Uncategorized" + WARN log on a transport failure without breaking the report |
| 8 | Stock receipts/transfers/counts/opening balances driveable from UI; on-hand stock per branch readable via a real endpoint | ✓ VERIFIED | `GET /api/v1/inventory/stock` (`StockLevelController`/`StockLevelService`, read-only — 0 write mappings) returns per-ingredient qty/avg-cost/`belowReorderPoint`/`nonPositive` (server-computed, not client-derived — confirmed no comparison operator applied to `qtyOnHand` in `stock/page.tsx`); `/app/inventory/stock/page.tsx` + `OpeningBalanceDialog`/`StockReceiptDialog`/`StockTransferDialog`/`StockCountDialog` (all four import `CatalogItemCombobox`, 0 `placeholder="uuid"` matches) drive all four operations from the UI |

**Score:** 8/8 ROADMAP success criteria verified against actual code (not SUMMARY claims).

### Requirements Traceability Reconciliation

| Requirement | REQUIREMENTS.md checklist | REQUIREMENTS.md Traceability table (as of last edit, 08.2-16) | Codebase verification (this report) | Reconciled status |
|---|---|---|---|---|
| INV-01 (re-open — UI) | `[x]` (set at Phase 8, unchanged) | Phase 8 / Complete | Ingredient CRUD UI now genuinely exists (was previously SQL-only) — SC-2 above | Confirmed delivered |
| INV-13 | `[x]` (set 08.2-01, wave 1 — premature, before UI existed) | "Not started" (never updated) | Fully delivered — SC-1 | Checklist is now correct by coincidence; **table is stale, needs "Complete"** |
| INV-14 | `[x]` (set 08.2-02/03 combined commit, wave 1 — premature) | "Not started" (never updated) | Fully delivered — SC-2 | **Table stale, needs "Complete"** |
| INV-15 | `[x]` (set 08.2-02/03 combined commit, wave 1 — premature) | "In progress ... stock-operations UI slice remains" (last touched 08.2-16, 4 plans before phase-end) | Fully delivered — SC-3, SC-4, SC-8 (plan 17 shipped the stock-ops UI slice the table still calls "remaining") | **Table stale, needs "Complete"** |
| PUR-07 | `[x]` (set 08.2-04, wave 1 — premature) | "Not started" (never updated) | Fully delivered — SC-5 | **Table stale, needs "Complete"** |
| PUR-08 | `[x]` (set 08.2-04, wave 1 — premature) | "Not started" (never updated) | Fully delivered — SC-6, SC-7 | **Table stale, needs "Complete"** |

**Root cause:** git blame shows the v1 checklist `[x]` markers for these five requirements were all
set during wave 1 (08.2-01 through 08.2-04) — i.e., immediately after the backend migrations landed,
before ANY frontend UI existed — which was premature at the time. Plans 06–20 (waves 2–5, the
majority of the phase's actual delivery) never revisited either the checklist or the Traceability
table for these five requirements; only `INV-15`'s table row was touched once more (at 08.2-16), and
even that edit is now four plans stale. **No functional gap was found** — every success criterion the
requirements map to is independently verified above — but the Traceability table's text is
factually wrong today (it undersells what's delivered) and must be corrected before the phase is
administratively closed, per this phase's own reconciliation instruction. This is the exact
"plans 02/03 reverted an in-progress flip, plan 05 marked complete" pattern the task asked to
check for, except the flip/revert cycle happened even earlier (08.2-01 through 08.2-04) and the
table was simply never brought current afterward.

**Orphaned requirements:** None. All five requirement IDs (INV-13, INV-14, INV-15, PUR-07, PUR-08)
are claimed by at least one plan's frontmatter `requirements:` field, and INV-01 is explicitly
re-opened per the phase's own requirements line.

### Required Artifacts (spot-checked, not exhaustive)

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `services/inventory-service/.../V5__item_categories.sql` | 3-level tree, RLS, RESTRICT FK | ✓ VERIFIED | Read in full; trigger + backfill + FORCE-RLS re-close all present |
| `services/inventory-service/.../V6__ingredient_master_data.sql` | Ingredient master-data columns, D-07 3 yield numbers | ✓ VERIFIED | Present; `IngredientService` reads all 3 (`default_yield_pct`, `recipe_lines.yield_pct` pre-existing, `recipes.net_yield_pct`) |
| `services/purchasing-service/.../V5__vendor_item_catalog.sql` | vendor_items/vendor_item_prices/vendor_categories, PO line FK | ✓ VERIFIED | Present, RLS confirmed via grep of migration file structure mirrored in 08.2-04-SUMMARY |
| `ItemCategoryService.java` / `ItemCategoryController.java` | Full CRUD + move + archive-refusal | ✓ VERIFIED | Read in full — matches all must_haves truths |
| `IngredientService.java` / `IngredientController.java` | Full CRUD, measure-lock, archive-no-refusal | ✓ VERIFIED | Read in full |
| `VendorItemPriceService.java` | Append-only pricing | ✓ VERIFIED | Read in full — `recordNewPrice` never mutates a prior row's price |
| `PurchaseOrderService.java` | Catalog-driven line derivation, no category consultation | ✓ VERIFIED | grep + read confirm |
| `RecipeCostPreviewService.java` | Non-persisting cost preview | ✓ VERIFIED | grep confirms 0 save/flush calls, 0 double/float |
| `frontend/.../inventory/categories/page.tsx`, `ingredients/page.tsx`, `recipes/[menuItemId]/page.tsx`, `coverage/page.tsx`, `stock/page.tsx` | Full UI screens | ✓ VERIFIED | All exist, substantial (116–475 lines), no stub markers found |
| `frontend/.../purchasing/vendors/[id]/page.tsx`, `PurchaseOrderFormDialog.tsx` | Vendor catalog UI, PO picker | ✓ VERIFIED | Both exist, substantial, wired |
| `GET /api/v1/inventory/transfers/pending` (`TransferController`/`TransferService`) | Read-only, own-branch-scoped | ⚠️ WIRED, UNTESTED | Code correctly scoped (view permission + own-branch + tenant predicate) but zero IT coverage exists anywhere in the test tree — see carried-debt above |

### Key Link Verification

| From | To | Via | Status |
|---|---|---|---|
| `ingredients.category_id` | `item_categories.id` | FK RESTRICT | ✓ WIRED |
| `IngredientController`/`ItemCategoryController` | `InventoryAuthorizationService` | `authorizeView`/`authorizeManage` on every endpoint | ✓ WIRED |
| `InternalIngredientCategoryController` (inventory) | `FeignIngredientCategoryResolver` (purchasing) | `GET /internal/inventory/ingredient-categories` | ✓ WIRED |
| `PurchaseOrderFormDialog` line row | `useVendorItems(selectedVendorId)` → `CatalogItemCombobox` | vendor-scoped catalog picker | ✓ WIRED |
| `VendorItemController` | `@PreAuthorize("hasAuthority('vendor.view'/'vendor.manage')")` | reused permission codes, no new codes | ✓ WIRED |
| `recipe-cost-panel.tsx` | `useRecipeCostPreview` (400ms debounce, placeholderData) | `POST /recipes/preview` | ✓ WIRED |
| `stock/page.tsx` row wash | `StockLevelDto.belowReorderPoint`/`nonPositive` | server-computed, no client threshold math | ✓ WIRED |

### Behavioral / Compile Spot-Checks

| Check | Command | Result | Status |
|---|---|---|---|
| inventory-service compiles + test-compiles | `mvn -q -pl services/inventory-service -am test-compile -DskipTests` | exit 0, no output | ✓ PASS |
| purchasing-service compiles + test-compiles | `mvn -q -pl services/purchasing-service -am test-compile -DskipTests` | exit 0, no output | ✓ PASS |
| gateway compiles + test-compiles | `mvn -q -pl gateway -am test-compile -DskipTests` | exit 0, no output | ✓ PASS |
| Frontend type-checks | `npx tsc --noEmit` | exit 0, no output | ✓ PASS |
| DepletionService/MacCalculator untouched by this phase | `git log --oneline <phase-start>..HEAD -- .../DepletionService.java .../MacCalculator.java` | no commits | ✓ PASS — 08.1 depletion loop's load-bearing files were never modified |
| All 14 spot-checked test files exist on disk | file existence check | all present | ✓ PASS |

### Anti-Patterns Found

None of severity Blocker. No `TBD`/`FIXME`/`XXX` unreferenced markers found in the files read.
Two carried-debt items and one minor a11y gap are documented above (all pre-existing or explicitly
scoped out by the plans themselves, not new debt introduced silently).

### Human Verification Required

1. **Confirm `GET /api/v1/inventory/transfers/pending` tenant/branch scoping is acceptable without a dedicated IT.**
   - **Test:** Have two branches under one tenant (and, separately, two tenants) each with a shipped-but-not-received transfer; call the endpoint as a user scoped to branch/tenant A and confirm branch/tenant B's transfer never appears, including via a crafted `branchId` query-param override attempt.
   - **Expected:** Only tenant A / branch A's pending transfers are returned; a foreign `branchId` is rejected with 403 (per the code's `AccessDeniedException` check), not silently ignored.
   - **Why human:** Code inspection shows correct double-scoping (permission + own-branch-only + tenant predicate) mirroring the already-proven `StockLevelController` pattern, but this is the only new endpoint in the entire 20-plan phase with zero backend integration test — a human/regression-test decision is needed on whether to accept this as-is or require a follow-up IT before shipping.

2. **Decide whether to fix REQUIREMENTS.md's Traceability table before closing the phase.**
   - **Test:** Read the Traceability table rows for INV-13, INV-14, INV-15, PUR-07, PUR-08.
   - **Expected:** They should read "Complete", matching the v1 checklist's `[x]` and this report's codebase verification.
   - **Why human:** This is a documentation/process decision (who updates the table, and when), not something this verifier should silently patch.

3. **(Optional, low-priority) Visual/UX pass on the five new screens** (categories tree, ingredient form, recipe detail + cost panel, coverage report, stock operations) and the vendor catalog / PO picker — this report verified code-level correctness (no stubs, correct wiring, correct data flow) but did not render the UI in a browser.

### Gaps Summary

No functional gaps were found — all 8 ROADMAP success criteria are independently verified against
the actual codebase (schema, service, controller, and frontend layers), all four affected Maven
modules and the frontend compile cleanly, and the 08.1 depletion-loop's load-bearing files were
confirmed untouched. The reason this report is not `passed` is procedural, not functional: (1)
`REQUIREMENTS.md`'s Traceability table is stale and needs a human-approved documentation update to
match reality, and (2) one new endpoint (transfers/pending) has code that appears correctly scoped
on inspection but carries zero automated test evidence, which this phase's own standard (every
other new endpoint has a dedicated IT) says should not pass silently as `passed`.

---

*Verified: 2026-07-24*
*Verifier: Claude (gsd-verifier)*
