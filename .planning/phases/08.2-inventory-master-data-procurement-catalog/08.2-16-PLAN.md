---
phase: 08.2-inventory-master-data-procurement-catalog
plan: 16
type: execute
wave: 5
depends_on: ["08.2-05", "08.2-12"]
files_modified:
  - frontend/app/(tenant)/app/inventory/recipes/[menuItemId]/page.tsx
  - frontend/app/(tenant)/app/inventory/recipes/page.tsx
  - frontend/app/(tenant)/app/inventory/coverage/page.tsx
  - frontend/components/inventory/recipe-cost-panel.tsx
  - frontend/components/inventory/RecipeVersionStrip.tsx
  - frontend/components/inventory/__tests__/recipe-cost-panel.test.tsx
autonomous: true
requirements: [INV-15]

must_haves:
  truths:
    - "An existing recipe's ingredient lines are viewable version by version at /app/inventory/recipes/[menuItemId]."
    - "Create-new-revision opens a line form pre-filled from the currently selected version — never blank — and saving always creates a new version."
    - "The plate-cost panel shows batch cost, cost per portion, food-cost percentage and each line's share of plate cost, recomputed 400ms after the last edit through the server preview endpoint."
    - "While a recompute is in flight the previous values stay visible and dimmed with a Recalculating caption; the panel never goes blank."
    - "A line the server could not price shows an inline warning, is excluded from the total, and the panel reports how many lines were excluded without blocking save."
    - "The coverage report shows four figures — total, covered, scheduled, no recipe — and every row carries the server's coverage state as a chip."
  artifacts:
    - frontend/app/(tenant)/app/inventory/recipes/[menuItemId]/page.tsx
    - frontend/components/inventory/recipe-cost-panel.tsx
    - frontend/components/inventory/RecipeVersionStrip.tsx
  key_links:
    - "useRecipeCostPreview (debounced, placeholderData-preserving) -> recipe-cost-panel is the live-cost contract; the panel renders only what the server computed."
    - "apiCoverageSchema.items[].state -> the coverage chip; the page never re-derives coverage from a boolean."
  prohibitions:
    - statement: "There must be no save-in-place action anywhere on the recipe detail page — every save creates a new version (D-05)."
      status: required
      verification: "grep -ciE '(useUpdateRecipe|saveInPlace|updateRecipeVersion)' frontend/app/(tenant)/app/inventory/recipes/[menuItemId]/page.tsx returns 0"
    - statement: "The coverage page must not re-derive coverage state client-side from the recipe current flag."
      status: required
      verification: "grep -cE '\\.current' frontend/app/(tenant)/app/inventory/coverage/page.tsx returns 0"
    - statement: "The effective-from field must reuse the shared local-midnight helper, not a re-implementation."
      status: required
      verification: "grep -c 'lib/forms/calendar-date' frontend/app/(tenant)/app/inventory/recipes/[menuItemId]/page.tsx returns 1 and grep -c 'function calendarDateToInstant' in that file returns 0"
---

<objective>
Ship the recipe detail and revision-authoring page with its live plate-cost panel, turn the existing
create-only recipes screen into an index, and make the coverage report three-state.

Purpose: ROADMAP Success Criteria 3 and 4, and INV-15. Today the recipes page renders only
`lines.length` — the actual ingredient lines are unviewable — there is no way to revise a recipe,
and a future-dated recipe is silently uncounted on the coverage dashboard.
Output: a new routed detail page, two new components, two rewritten pages, and a component test for
the panel's live contract.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08.2-inventory-master-data-procurement-catalog/08.2-UI-SPEC.md
@.planning/phases/08.2-inventory-master-data-procurement-catalog/08.2-PATTERNS.md
@.planning/phases/08.2-inventory-master-data-procurement-catalog/08.2-CONTEXT.md
@.planning/phases/08.2-inventory-master-data-procurement-catalog/08.2-12-SUMMARY.md
</context>

## Artifacts this phase produces (this plan)

| Kind | Name |
|---|---|
| Route | `/app/inventory/recipes/[menuItemId]` |
| Page | `recipes/[menuItemId]/page.tsx` — two-column detail + revision authoring |
| Component | `recipe-cost-panel.tsx` — sticky live plate-cost panel with recalculating and degraded states |
| Component | `RecipeVersionStrip.tsx` — version chip strip with current / scheduled markers |
| Rewritten | `recipes/page.tsx` (becomes an index linking into the detail route) |
| Rewritten | `coverage/page.tsx` (four stat cards + three-state chips) |
| Test | `__tests__/recipe-cost-panel.test.tsx` |

<tasks>

<task type="auto">
  <name>Task 1: RecipeVersionStrip and recipe-cost-panel</name>
  <files>frontend/components/inventory/RecipeVersionStrip.tsx, frontend/components/inventory/recipe-cost-panel.tsx</files>
  <read_first>
    - frontend/components/purchasing/PeriodPicker.tsx — the nearest existing chip-strip-over-a-list selector shape; `RecipeVersionStrip` follows it.
    - frontend/components/ui/money-display.tsx — the mandated paisa-to-PKR renderer; every money figure in the panel uses it, never a hand-rolled formatter.
    - frontend/components/ui/skeleton.tsx — the loading primitive; the panel uses a dimmed-previous-values treatment rather than a skeleton once it has data, but needs the skeleton for first load.
    - frontend/components/ui/status-badge.tsx — the `COVERED` / `NO_RECIPE` / `SCHEDULED` variants added by plan 08.2-05, used for the version strip's scheduled marker.
    - frontend/lib/adapters/inventory.adapter.ts — the `RecipeCostPreview` and `RecipeCostLine` types from plan 08.2-12, including the per-line `warning` and `sharePctOfBatch` fields and the top-level `excludedLineCount`.
    - .planning/phases/08.2-inventory-master-data-procurement-catalog/08.2-UI-SPEC.md Screen 3 in full — especially the Live contract bullet list (recompute trigger, 400ms debounce, rounding rules, loading treatment, error treatment) and the Color section's rule that the panel's two headline numbers are the one place a number gets accent treatment.
  </read_first>
  <action>
`RecipeVersionStrip({ versions, selectedId, onSelect, now })` renders one chip per version, newest
first, labelled "v{n}" with a suffix: " · current" when the version is the effective one at `now`,
" · scheduled from {date}" when its `effectiveFrom` is in the future. The selected chip uses the
accent token; the others use muted. Chips are real buttons with `aria-pressed`.

`recipe-cost-panel.tsx` exports `RecipeCostPanel({ preview, isFetching, isFirstLoad, menuItemName })`.
It is presentational — it fires no request itself and takes the already-fetched preview from the page.

Contents, in this order: a "v{n} · {state}" banner slot rendered by the page above it (not owned
here), then batch cost and cost per portion as the two headline figures using `MoneyDisplay` with the
accent token, then food-cost percentage to one decimal place (or an em dash with a tooltip reading
that there is no menu price to compare against when `foodCostPct` is null), then a caption stating
the figure is estimated from current ingredient cost and that actual cost is set at depletion time.

Loading semantics, exactly as the UI contract specifies: on first load render `Skeleton` blocks. On
a subsequent recompute (`isFetching` true with previous data present) keep the previous values
visible with `opacity-60` and show a small inline "Recalculating…" caption — never a blank panel and
never a full-panel spinner.

Degraded lines: when `excludedLineCount` is greater than zero render a footnote reading how many
lines were excluded from cost pending a fix. Export a small `RecipeCostLineShare({ line })`
subcomponent the page uses next to each ingredient row, rendering the whole-number share percentage
plus a mini horizontal bar (a div whose width is the share percentage) and, when the line has a
warning, the warning text in the warning token instead of a share.

Rounding is display-only: money through `MoneyDisplay`, food-cost percentage to one decimal, per-line
share as a whole number. Do no cost arithmetic in this component — every number it shows comes from
the server payload.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx tsc --noEmit &amp;&amp; npx eslint components/inventory/recipe-cost-panel.tsx components/inventory/RecipeVersionStrip.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'MoneyDisplay' frontend/components/inventory/recipe-cost-panel.tsx` returns at least 2.
    - `grep -c 'opacity-60' frontend/components/inventory/recipe-cost-panel.tsx` returns at least 1.
    - `grep -ciE 'recalculating' frontend/components/inventory/recipe-cost-panel.tsx` returns at least 1.
    - `grep -c 'excludedLineCount' frontend/components/inventory/recipe-cost-panel.tsx` returns at least 1.
    - `grep -cE '(\*|/|\+ *[a-z]+Paisa)' frontend/components/inventory/recipe-cost-panel.tsx` shows no cost arithmetic — verify by reading the component; the only permitted arithmetic is the mini-bar's width percentage.
    - `grep -c 'aria-pressed' frontend/components/inventory/RecipeVersionStrip.tsx` returns at least 1.
    - `grep -c 'lib/api-client' over both files returns 0.
    - `cd frontend && npx tsc --noEmit` exits 0 and `npx eslint` over both files exits 0.
  </acceptance_criteria>
  <done>The version strip and the live cost panel exist with the contract's exact loading and degraded behaviours.</done>
</task>

<task type="auto">
  <name>Task 2: Recipe detail / revision page and the recipes index</name>
  <files>frontend/app/(tenant)/app/inventory/recipes/[menuItemId]/page.tsx, frontend/app/(tenant)/app/inventory/recipes/page.tsx</files>
  <read_first>
    - frontend/app/(tenant)/app/inventory/recipes/page.tsx — the whole file; the menu-item select, the versions table that renders only `lines.length`, and the `RecipeFormDialog` trigger it currently hosts.
    - frontend/components/inventory/RecipeFormDialog.tsx — the whole file; the `useFieldArray` line-row grid (line labels at `text-xs`, ingredient select, unit select, qty and yield inputs, remove button) that the new page's revision form reuses, and the `toRecipeInput` mapper.
    - frontend/lib/forms/calendar-date.ts — the shared local-midnight helper extracted by plan 08.2-05; the effective-from field must import it, not re-declare it. Re-implementing it is how the origin bug returns.
    - frontend/lib/hooks/inventory/use-inventory.ts — `useRecipeVersions`, `useCreateRecipe`, `useRecipeCostPreview`, `useMenuItemCatalog`, `useIngredients`, `useUoms`.
    - frontend/app/(tenant)/app/purchasing/purchase-orders/[id]/page.tsx — an existing dynamic-route detail page in this app, for the params-typing convention and the back-link treatment (`text-primary`).
    - .planning/phases/08.2-inventory-master-data-procurement-catalog/08.2-UI-SPEC.md Screen 3's layout, version banner, saving-a-revision and empty-state paragraphs, and the Modal-vs-Full-Page table's justification for this being a routed page rather than a dialog.
  </read_first>
  <action>
Create `recipes/[menuItemId]/page.tsx` as a client component with a two-column layout: the left
column is the version strip plus the line viewer or the revision form; the right column is the
sticky cost panel (`position: sticky` on desktop, stacking below the form on narrow viewports — do
not set a fixed pixel width that breaks at small sizes).

Read state: `useRecipeVersions(menuItemId)`; select the effective-at-now version by default. Above
the selected version render the version banner — "v{n} · active since {date}", "v{n} · scheduled from
{date}", or "Draft — unsaved" while a revision is being authored. Below it render that version's
lines read-only: ingredient name, quantity, unit, yield percentage.

Revision authoring: a "Create new revision" primary CTA (relabelled "Create first recipe version"
when the menu item has zero versions) switches the left column into an editable `useFieldArray` line
form PRE-FILLED from the currently selected version — never blank. Reuse `RecipeFormDialog`'s line-
row grid shape. Header fields: yield servings, name, and an effective-from date input converted with
the shared `calendarDateToInstant` import. Saving calls `useCreateRecipe`, which always creates a new
version; there is no save-in-place control anywhere on this page and no update hook may be imported.

Live cost: build the preview input from the current draft (branch id, menu item id, yield servings,
lines) and pass it to `useRecipeCostPreview`, which already debounces at 400ms and preserves previous
data while fetching. Pass the result plus `isFetching` into `RecipeCostPanel`, and render
`RecipeCostLineShare` next to each line row. The recompute triggers are any change to a line's
quantity, unit or yield percentage, or to the header's yield servings — which follows automatically
from deriving the input from form state.

Empty state (no versions for this menu item): the same `EmptyState` treatment the current recipes
page uses, with the CTA relabelled per the UI contract.

Rewrite `recipes/page.tsx` as an index: keep the menu-item select, but replace the versions table
with a list of menu items showing their coverage chip and version count, each row linking to
`/app/inventory/recipes/{menuItemId}`. Keep the `RecipeFormDialog` trigger for creating a first
version from the index, and add a muted note that revision authoring now lives on the detail page.
Do not modify `inventory/layout.tsx` — plan 08.2-14 owns it.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx tsc --noEmit &amp;&amp; npx eslint "app/(tenant)/app/inventory/recipes/[menuItemId]/page.tsx" "app/(tenant)/app/inventory/recipes/page.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/app/(tenant)/app/inventory/recipes/[menuItemId]/page.tsx` exists.
    - `grep -c 'lib/forms/calendar-date' "frontend/app/(tenant)/app/inventory/recipes/[menuItemId]/page.tsx"` returns 1 and `grep -c 'function calendarDateToInstant'` in that file returns 0.
    - `grep -ciE '(useUpdateRecipe|saveInPlace|updateRecipeVersion)' "frontend/app/(tenant)/app/inventory/recipes/[menuItemId]/page.tsx"` returns 0.
    - `grep -c 'useRecipeCostPreview' "frontend/app/(tenant)/app/inventory/recipes/[menuItemId]/page.tsx"` returns 1.
    - `grep -c 'sticky' "frontend/app/(tenant)/app/inventory/recipes/[menuItemId]/page.tsx"` returns at least 1.
    - `grep -c 'useFieldArray' "frontend/app/(tenant)/app/inventory/recipes/[menuItemId]/page.tsx"` returns 1.
    - `grep -c 'recipes/' "frontend/app/(tenant)/app/inventory/recipes/page.tsx"` shows the index links into the detail route.
    - `git diff --stat "frontend/app/(tenant)/app/inventory/layout.tsx"` shows no change from this plan.
    - `cd frontend && npx tsc --noEmit` exits 0 and `npx eslint` over both files exits 0.
  </acceptance_criteria>
  <done>Recipe lines are viewable, revisions are authored pre-filled and saved as new versions, and the plate cost is live.</done>
</task>

<task type="auto">
  <name>Task 3: Three-state coverage page + cost-panel component test</name>
  <files>frontend/app/(tenant)/app/inventory/coverage/page.tsx, frontend/components/inventory/__tests__/recipe-cost-panel.test.tsx</files>
  <read_first>
    - frontend/app/(tenant)/app/inventory/coverage/page.tsx — the whole file; the three `Card` stat tiles with `AnimatedNumber`, the plain missing-items table, and the `EmptyState`. Note it currently renders "Missing" from `coverage.missing.length` and has no notion of a scheduled state.
    - frontend/lib/adapters/inventory.adapter.ts — the `Coverage`, `MenuItemCoverage` and `CoverageState` types from plan 08.2-12; the page must consume `items[].state` rather than deriving anything.
    - frontend/components/ui/status-badge.tsx — the `COVERED` / `NO_RECIPE` / `SCHEDULED` variants; SCHEDULED's date is passed through the existing `label` prop.
    - frontend/components/inventory/recipe-cost-panel.tsx — the component from Task 1.
    - frontend/components/inventory/__tests__/category-tree.test.tsx — the test harness plan 08.2-14 established; reuse it.
    - .planning/phases/08.2-inventory-master-data-procurement-catalog/08.2-UI-SPEC.md Screen 4 and the Copywriting Contract's three coverage chip labels.
  </read_first>
  <action>
`coverage/page.tsx`: replace the three stat cards with four — Total Active Menu Items, Covered,
Scheduled, No Recipe — reading `totalActiveMenuItems`, `covered`, `scheduled` and `noRecipe` straight
off the response. Raise the heading to `text-2xl font-semibold` to match the phase's other primary
module pages.

Replace the missing-items table with a single table over `items`, one row per active menu item,
showing the name and a `StatusBadge` whose status is the server's `state` value. For a `SCHEDULED`
row pass `label={`Scheduled from ${formattedDate}`}` so the date is inside the chip rather than in an
extra column. Default the table to showing only rows whose state is not `COVERED`, with a toggle to
show all — a coverage report is a worklist. Never inspect a recipe's `current` flag anywhere on this
page.

Keep the `EmptyState` for the case where every active menu item is covered.

`__tests__/recipe-cost-panel.test.tsx`:
1. `firstLoadRendersSkeletonNotABlankPanel`.
2. `recomputeKeepsPreviousValuesVisibleAndDimmed` — render with data and `isFetching` true; assert
   the previous batch cost text is still present and that the dimming class and the recalculating
   caption are applied.
3. `foodCostPercentRendersToOneDecimalPlace`.
4. `nullFoodCostRendersADashWithAnExplanation`.
5. `degradedLinesShowTheWarningAndAreReportedInTheFootnote` — a preview with one warned line; assert
   the warning text renders next to that line and the excluded-count footnote is present.
6. `perLineSharesRenderAsWholeNumbers`.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npx tsc --noEmit &amp;&amp; npx vitest run components/inventory &amp;&amp; npx eslint "app/(tenant)/app/inventory/coverage/page.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '\.current' "frontend/app/(tenant)/app/inventory/coverage/page.tsx"` returns 0.
    - `grep -c 'scheduled' "frontend/app/(tenant)/app/inventory/coverage/page.tsx"` returns at least 2.
    - `grep -c 'StatusBadge' "frontend/app/(tenant)/app/inventory/coverage/page.tsx"` returns at least 1.
    - `grep -c 'AnimatedNumber' "frontend/app/(tenant)/app/inventory/coverage/page.tsx"` returns 4.
    - `frontend/components/inventory/__tests__/recipe-cost-panel.test.tsx` declares at least 6 test cases.
    - `cd frontend && npx vitest run components/inventory` exits 0.
    - `cd frontend && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Coverage shows three distinct states from the server's own field, and the panel's live contract is test-enforced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → cost preview endpoint | Draft lines and branch id sent on every debounced edit. |
| Server preview payload → rendered cost figures | Untrusted numbers and warning strings rendered into the panel. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08.2-161 | Repudiation | A destructive edit silently rewriting the cost history the depletion loop resolves against | high | mitigate | The page has no save-in-place control and imports no update hook; every save calls `useCreateRecipe`, which always inserts a new version (D-05). Grep-gated. |
| T-08.2-162 | Repudiation | The client re-deriving coverage state and disagreeing with the backend again | high | mitigate | The coverage page renders `items[].state` verbatim and never inspects the recipe `current` flag; grep-gated, and the MSW mock was realigned in plan 08.2-12. |
| T-08.2-163 | Denial of Service | A preview request per keystroke | medium | mitigate | `useRecipeCostPreview` debounces at 400ms and is disabled with no lines. |
| T-08.2-164 | Information Disclosure | Ingredient cost shown to a read-only role | medium | mitigate | The preview endpoint authorizes at manage level server-side; the page's authoring surface is additionally wrapped in a manage-level permission guard. |
| T-08.2-165 | Tampering | Re-implementing the calendar-date conversion and reintroducing the UTC-midnight bug | high | mitigate | The page imports the shared `lib/forms/calendar-date` helper; a local re-declaration is grep-gated to zero. |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` exits 0.
- `cd frontend && npx eslint components/inventory/recipe-cost-panel.tsx components/inventory/RecipeVersionStrip.tsx "app/(tenant)/app/inventory/recipes/[menuItemId]/page.tsx" "app/(tenant)/app/inventory/recipes/page.tsx" "app/(tenant)/app/inventory/coverage/page.tsx"` exits 0.
- `cd frontend && npx vitest run components/inventory` exits 0.
</verification>

<success_criteria>
- Success Criterion 3 is satisfied: recipe lines are viewable, a revision is authored pre-filled from the current version and never overwrites it, and the plate-cost panel is live with batch cost, cost per portion, food-cost percentage and per-line share.
- Success Criterion 4 is satisfied: the coverage report distinguishes no recipe from scheduled-from-a-date.
</success_criteria>

<output>
Create `.planning/phases/08.2-inventory-master-data-procurement-catalog/08.2-16-SUMMARY.md` when done.
</output>
