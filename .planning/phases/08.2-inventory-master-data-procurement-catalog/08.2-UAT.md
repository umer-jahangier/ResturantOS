---
status: complete
phase: 08.2-inventory-master-data-procurement-catalog
source: [08.2-01..20-SUMMARY.md]
started: 2026-07-24T20:28:40Z
updated: 2026-07-24T20:52:20Z
---

## Current Test
<!-- OVERWRITE each test - shows where we are -->

[testing complete]

## Tests

### 1. Cold Start Smoke Test
expected: Kill the running dev stack, then start fresh via scripts/start-dev. All services — including purchasing-service (the one previously missing from the dev scripts) — boot, migrations complete without "remaining connection slots reserved" errors (max_connections=300), and each registers in Eureka. Frontend loads; ingredients list and vendors list return live data, no "service temporarily unavailable" / 503 fallback text.
result: pass

### 2. Ingredients as editable master data (no SQL)
expected: Open /app/inventory/ingredients. A DataTable lists ingredients with search + category/status/allergen filters. Click "Add ingredient" — the form has grouped General/Units/Stock/Compliance sections, a required single primary category, allergen pill toggles (not checkboxes), and a measure-type lock. Create one, then Edit it — changes save and appear in the grid. Archive then Restore a row via row actions. All achievable with no raw SQL.
result: pass

### 3. Categories as first-class master data
expected: Open /app/inventory/categories. A recursive 3-level category tree renders with GL-account chips. Row actions let you Edit, Add subcategory (hidden at level 3), Move-to (valid targets only), and Archive/Restore. Adding a subcategory and moving a category work; archiving a category that's in use shows a 409 refusal dialog that stays open with the server's live-count message inline.
result: pass

### 4. Recipes viewable & revisable with live plate cost
expected: Open /app/inventory/recipes — a table lists active menu items with a coverage chip + version count, each linking to recipes/{menuItemId}. Open one: view existing versions read-only, click "Create new revision" (pre-filled from the selected version, never blank), edit ingredient lines choosing ingredients from the searchable catalog picker (no UUID typing). A sticky live cost panel updates the plate cost as you edit. Save always creates a NEW version (never edits in place) and it appears in the version list.
result: pass

### 5. Recipe coverage reflects scheduled recipes
expected: Open /app/inventory/coverage. Four stat cards (Total / Covered / Scheduled / No Recipe) and a table showing each item's three-state status via badge, default-filtered to non-covered rows with a toggle. Create a recipe with a future effective date — it appears as SCHEDULED (date interpolated into the chip), visibly pending rather than silently uncounted (the previously-reported "stuck at 1 covered" bug is resolved).
result: pass

### 6. On-hand stock & stock operations via catalog picker
expected: Open /app/inventory/stock — on-hand list with server-driven row emphasis (warning/destructive), search + category filter. The four stock-operation dialogs (opening balance / receipt / transfer ship + receive / count) each select ingredients via the searchable CatalogItemCombobox — no free-text identifier input anywhere. The Transfer-receive list is populated by pending transfers (GET /transfers/pending). A count session groups by storage location with live variance = counted − expected.
result: pass

### 7. Vendor item catalog with effective-dated pricing
expected: Open a vendor at /app/purchasing/vendors/[id]. Editable category tags (gated by vendor.manage) and a SectionSwitcher between Catalog and Price Changes. Add a catalog row (ingredient + initial price); Edit one (ingredient locked in edit mode). Add a new price via the price dialog — it's append-only (create-only write) and shows the current price for comparison. The Price Changes report shows deltas with icon + token coloring. The vendors list exposes a "Manage catalog" link.
result: pass

### 8. PO line chosen from vendor catalog (not typed UUID)
expected: Create/edit a Purchase Order at /app/purchasing/purchase-orders. The line-item ingredient is chosen from the selected vendor's catalog via the searchable picker (disabled with an explanatory placeholder until a vendor is picked) — the old hand-typed ingredient UUID input is gone. Result rows show item name, pack size, vendor SKU, and current contract price; selecting one fills the line's unit and unit price (both remain editable). Submitting without a picker-confirmed selection shows a field error on submit only. Changing the header vendor after lines exist clears every line's item/unit/price with an explanatory toast.
result: pass

## Automated Coverage (not presented — deterministically covered)

66 phase deliverables across plans 08.2-01..20 are recorded as `result: pass, source: automated` —
each is backed by passing integration/unit tests referenced in its SUMMARY `coverage:` block
(migrations, RLS/FORCE-RLS isolation, MAC/plate-cost math, recipe versioning invariants, OPA/permission
gating, backend endpoint contracts, tsc/eslint/vitest structural gates). These are not re-tested by hand.

3 backend/infra items flagged human_judgment are folded into the flows above:
- transfers/pending endpoint → exercised in Test 6 (Transfer-receive list)
- circuit-breaker timelimiter + start-dev.sh parity → exercised in Test 1 (Cold Start)

## Summary

total: 8
passed: 8
issues: 0
pending: 0
skipped: 0
blocked: 0
automated_passed: 66

## Gaps

[none yet]
