---
phase: 10-purchasing-accounts-payable
plan: 03
type: gap-closure
wave: 3
depends_on: ["10-02"]
files_modified:
  - services/purchasing-service/src/main/java/io/restaurantos/purchasing/service/VendorAnalyticsService.java
  - services/purchasing-service/src/main/java/io/restaurantos/purchasing/service/IngredientCategoryResolver.java
  - services/purchasing-service/src/main/java/io/restaurantos/purchasing/dto/SpendAnalyticsDto.java
  - services/purchasing-service/src/main/java/io/restaurantos/purchasing/dto/SpendBucketDto.java
  - services/purchasing-service/src/main/java/io/restaurantos/purchasing/web/VendorAnalyticsController.java
  - services/purchasing-service/src/test/java/io/restaurantos/purchasing/SpendAnalyticsIT.java
  - services/purchasing-service/src/test/resources/spend-analytics-fixtures.sql
  - frontend/lib/api-client/schemas/purchasing.schema.ts
  - frontend/lib/adapters/purchasing.adapter.ts
  - frontend/lib/repositories/purchasing.repository.ts
  - frontend/lib/hooks/purchasing/use-purchasing.ts
  - frontend/mocks/purchasing.handlers.ts
  - frontend/app/(tenant)/app/purchasing/analytics/page.tsx
  - frontend/components/purchasing/SpendAnalyticsTable.tsx
  - .planning/REQUIREMENTS.md
  - .planning/phases/10-purchasing-accounts-payable/10-MOCK-FIXTURES.md
autonomous: true

must_haves:
  truths:
    - "GET /api/v1/purchasing/analytics/spend?branchId=&from=&to= returns spend aggregated by vendor AND by category for the requested period"
    - "Same endpoint includes prior-period comparison (equal-length window immediately before from/to, or explicit compareFrom/compareTo) with delta amount and delta percent per vendor and category"
    - "Category resolution works mock-first via IngredientCategoryResolver (YAML/seed map ingredient_id → category); no inventory-service required"
    - "Spend totals derive from vendor_invoices (MATCHED or PAID only) joined to invoice lines; tenant + branch scoped"
    - "SpendAnalyticsIT seeds mock invoices across 2 vendors, 3 categories, 2 periods; asserts bucket totals and period-over-period deltas"
    - "MSW purchasing.handlers.ts serves /analytics/spend with F8 fixture (multi-vendor, multi-category, two periods)"
    - "/app/purchasing/analytics renders vendor table + category table with current vs prior columns"
    - "Existing GET /analytics/scorecard unchanged; spend is a separate endpoint (PUR-06 distinct from PUR-05 scorecard)"
  artifacts:
    - path: "services/purchasing-service/src/main/java/io/restaurantos/purchasing/dto/SpendAnalyticsDto.java"
      provides: "Period spend report with byVendor and byCategory buckets + prior comparison"
    - path: "services/purchasing-service/src/test/java/io/restaurantos/purchasing/SpendAnalyticsIT.java"
      provides: "Mock-data IT proving PUR-06 aggregation"
      contains: "byCategory"
    - path: "frontend/app/(tenant)/app/purchasing/analytics/page.tsx"
      provides: "Spend analytics UI"
    - path: "frontend/mocks/purchasing.handlers.ts"
      provides: "F8 spend analytics MSW fixture"
      contains: "analytics/spend"
  key_links:
    - from: "VendorAnalyticsService.spendReport()"
      to: "vendor_invoices + vendor_invoice_lines"
      via: "JPQL aggregation filtered by branchId, invoiceDate range, status IN (MATCHED, PAID)"
    - from: "VendorAnalyticsService.spendReport()"
      to: "IngredientCategoryResolver"
      via: "resolve(ingredientId) → category label for line grouping"
    - from: "frontend/app/(tenant)/app/purchasing/analytics/page.tsx"
      to: "GET /api/v1/purchasing/analytics/spend"
      via: "PurchasingRepository.getSpendAnalytics()"
---

<objective>
Close the PUR-06 gap deferred from 10-02: spend analytics by vendor and by category with period comparison — implemented and tested with mock invoice/line data (no Phase 8 inventory dependency).

Purpose: Satisfies ROADMAP Phase 10 success criterion #5 spend-analytics half; completes requirement PUR-06 without changing mock-first GRN strategy.
Output: `/analytics/spend` API, mock category resolver, SpendAnalyticsIT, MSW F8 fixture, analytics frontend page.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-purchasing-accounts-payable/10-VERIFICATION.md (gap: PUR-06 deferred)
@.planning/phases/10-purchasing-accounts-payable/10-RESEARCH.md
@.planning/phases/10-purchasing-accounts-payable/10-MOCK-FIXTURES.md
@.planning/phases/10-purchasing-accounts-payable/10-02-PLAN.md
@.planning/REQUIREMENTS.md (PUR-06)
@Docs/RestaurantERP_SaaS_Specification.md (M4.2 spend analytics)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Spend analytics API + mock category resolver (backend)</name>
  <steps>
    1. Add `SpendBucketDto` (label, spendPaisa, priorSpendPaisa, deltaPaisa, deltaPct) and `SpendAnalyticsDto` (branchId, from, to, compareFrom, compareTo, byVendor[], byCategory[]).
    2. Add `IngredientCategoryResolver` interface + `MockIngredientCategoryResolver` loading a classpath map `spend-category-map.yml` (ingredient UUID → category name). Document Phase 8 swap to inventory Feign lookup in Javadoc — same pattern as GrnDataPort mock/feign.
    3. Extend `VendorAnalyticsService` with `spendReport(UUID branchId, LocalDate from, LocalDate to, LocalDate compareFrom, LocalDate compareTo)`:
       - Default prior window: same day-count immediately before `from` when compareFrom/compareTo omitted.
       - Aggregate invoice line totals grouped by vendorId (join vendors for name) and by resolved category.
       - Include only invoices with status MATCHED or PAID; exclude MISMATCHED/DRAFT.
    4. Add `GET /api/v1/purchasing/analytics/spend` on `VendorAnalyticsController` with query params branchId, from, to, optional compareFrom/compareTo; `@RequiresFeature("FEATURE_VENDOR")`.
    5. Seed map covers at least 3 categories used in tests (e.g. Produce, Dairy, Meat) mapped to fixture ingredient UUIDs from 10-MOCK-FIXTURES.md.
  </steps>
  <verify>
    - Unit test `VendorAnalyticsServiceSpendTest` (pure service, mocked repos) asserts grouping + delta math
    - Controller returns ApiResponse envelope consistent with other purchasing endpoints
  </verify>
</task>

<task type="auto">
  <name>Task 2: SpendAnalyticsIT with mock invoice fixtures (F8)</name>
  <steps>
    1. Document fixture **F8 — Spend analytics multi-period** in `10-MOCK-FIXTURES.md`:
       - Vendor A: Produce $50k Jun, $40k May; Dairy $30k Jun, $25k May
       - Vendor B: Meat $20k Jun only
       - All invoices MATCHED with lines referencing mapped ingredient IDs
    2. Add `SpendAnalyticsIT` extending `PurchasingTestBase`:
       - Seed vendors, POs, invoices via repositories or SQL fixture `spend-analytics-fixtures.sql`
       - Call spendReport for Jun 2026 vs May 2026
       - Assert byCategory totals, byVendor totals, deltaPct for Produce (+25%), Dairy (+20%), Meat (prior=0)
    3. No finance-service container required — analytics reads purchasing_db only.
  </steps>
  <verify>
    - `mvn -pl services/purchasing-service -Dtest=SpendAnalyticsIT test` green
    - F8 documented alongside F1–F7 in 10-MOCK-FIXTURES.md
  </verify>
</task>

<task type="auto">
  <name>Task 3: Frontend analytics page + MSW F8 fixture</name>
  <steps>
    1. Extend four-layer stack: Zod schemas for SpendAnalyticsDto/SpendBucketDto; adapter + repository + `useSpendAnalytics(branchId, from, to)` hook.
    2. Add MSW handler `GET */api/v1/purchasing/analytics/spend` returning F8-shaped JSON (two vendors, three categories, prior comparison).
    3. Create `/app/purchasing/analytics/page.tsx` with date-range picker (default: current month vs prior month), `SpendAnalyticsTable` for vendor and category sections (columns: label, current, prior, delta %).
    4. Link from purchasing layout nav if not already present.
    5. Optional vitest: schema parse test for F8 MSW payload.
  </steps>
  <verify>
    - `pnpm tsc` and purchasing-related vitest pass
    - Frontend renders analytics page against MSW only (no purchasing-service required)
  </verify>
</task>

<task type="auto">
  <name>Task 4: Close PUR-06 in planning docs</name>
  <steps>
    1. Update `.planning/REQUIREMENTS.md` traceability: PUR-06 → Complete.
    2. Append gap closure note to `10-VERIFICATION.md` (PUR-06 resolved by 10-03).
    3. Add UAT test #12 for spend analytics page + API in `10-UAT.md`.
  </steps>
  <verify>
    - REQUIREMENTS.md shows PUR-06 Complete
    - 10-VERIFICATION.md gaps section updated
  </verify>
</task>

</tasks>

<verification_criteria>
- [ ] PUR-06 fully satisfied: spend by vendor, by category, period comparison — all verifiable via mock data
- [ ] No dependency on inventory-service or Phase 8 for category resolution in mock mode
- [ ] Scorecard endpoint behavior unchanged (no regression in existing ITs)
- [ ] All purchasing-service ITs still green (including new SpendAnalyticsIT)
- [ ] MSW F8 enables frontend-only analytics dev
</verification_criteria>

<success_criteria>
- ROADMAP Phase 10 criterion #5 spend-analytics portion is TRUE
- PUR-06 marked Complete in REQUIREMENTS.md
- Mock-first testing story intact: seeded invoices + MSW F8, no real inventory
</success_criteria>

<notes>
**Scope boundary:** Price variance on scorecard (PUR-05 polish) and vendor catalogue REST API remain out of scope — only PUR-06 gap closure.

**Phase 8 swap:** Replace `MockIngredientCategoryResolver` with inventory ingredient category lookup when available; keep API contract stable.

**Human verification:** After execute, run `/gsd-verify-work 10` UAT #12 — open /app/purchasing/analytics, confirm category and vendor tables show current vs prior period.
</notes>
