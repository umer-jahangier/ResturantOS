---
phase: 10-purchasing-accounts-payable
plan: 01
type: execute
wave: 1
depends_on: ["06-02"]
files_modified:
  - pom.xml
  - deploy/docker-compose.yml
  - gateway/src/main/resources/application.yml
  - gateway/src/main/java/io/restaurantos/gateway/support/RouteFeatureMap.java
  - .github/workflows/coverage-gates.json
  - services/purchasing-service/pom.xml
  - services/purchasing-service/Dockerfile
  - services/purchasing-service/src/main/resources/application.yml
  - services/purchasing-service/src/main/resources/db/migration/V1__purchasing_schema.sql
  - services/purchasing-service/src/main/java/io/restaurantos/purchasing/PurchasingServiceApplication.java
  - services/purchasing-service/src/main/java/io/restaurantos/purchasing/config/PurchasingSecurityConfig.java
  - services/purchasing-service/src/main/java/io/restaurantos/purchasing/config/FeignClientConfig.java
  - services/purchasing-service/src/main/java/io/restaurantos/purchasing/domain/enums/PoStatus.java
  - services/purchasing-service/src/main/java/io/restaurantos/purchasing/domain/model/Vendor.java
  - services/purchasing-service/src/main/java/io/restaurantos/purchasing/domain/model/VendorCatalogue.java
  - services/purchasing-service/src/main/java/io/restaurantos/purchasing/domain/model/PurchaseOrder.java
  - services/purchasing-service/src/main/java/io/restaurantos/purchasing/domain/model/PurchaseOrderLine.java
  - services/purchasing-service/src/main/java/io/restaurantos/purchasing/domain/model/PoApprovalRecord.java
  - services/purchasing-service/src/main/java/io/restaurantos/purchasing/domain/model/PoApprovalTier.java
  - services/purchasing-service/src/main/java/io/restaurantos/purchasing/repository/VendorRepository.java
  - services/purchasing-service/src/main/java/io/restaurantos/purchasing/repository/PurchaseOrderRepository.java
  - services/purchasing-service/src/main/java/io/restaurantos/purchasing/service/VendorService.java
  - services/purchasing-service/src/main/java/io/restaurantos/purchasing/service/PurchaseOrderService.java
  - services/purchasing-service/src/main/java/io/restaurantos/purchasing/service/PoApprovalService.java
  - services/purchasing-service/src/main/java/io/restaurantos/purchasing/config/InventoryIntegrationProperties.java
  - services/purchasing-service/src/main/java/io/restaurantos/purchasing/domain/model/MockGrnReceipt.java
  - services/purchasing-service/src/main/java/io/restaurantos/purchasing/port/GrnDataPort.java
  - services/purchasing-service/src/main/java/io/restaurantos/purchasing/adapter/MockGrnAdapter.java
  - services/purchasing-service/src/main/java/io/restaurantos/purchasing/web/VendorController.java
  - services/purchasing-service/src/main/java/io/restaurantos/purchasing/web/PurchaseOrderController.java
  - services/purchasing-service/src/main/java/io/restaurantos/purchasing/web/InternalPurchasingController.java
  - services/purchasing-service/src/main/java/io/restaurantos/purchasing/feign/AuthorizationClient.java
  - services/purchasing-service/src/test/java/io/restaurantos/purchasing/VendorIT.java
  - services/purchasing-service/src/test/java/io/restaurantos/purchasing/PurchaseOrderApprovalIT.java
  - services/auth-service/src/main/resources/db/changelog/v1.0.0/038-vendor-tier-permissions.xml
autonomous: true

must_haves:
  truths:
    - "GET /api/v1/purchasing/vendors returns tenant-scoped paginated vendors; bank_account_no is never returned in plaintext (masked or omitted)"
    - "POST /api/v1/purchasing/vendors persists bank_account_no encrypted via EncryptionService; round-trip decrypt works internally only"
    - "POST /api/v1/purchasing/purchase-orders creates DRAFT PO with lines; total_paisa = sum(qty × unit_price_paisa)"
    - "POST .../purchase-orders/{id}/submit transitions DRAFT → PENDING_APPROVAL and records required approval tiers from po_approval_tiers config"
    - "POST .../purchase-orders/{id}/approve calls OPA; exceeding approval_limit_paisa returns 403 APPROVAL_LIMIT_EXCEEDED; success → APPROVED and publishes PO_APPROVED"
    - "POST .../purchase-orders/{id}/reject requires reason → REJECTED; PO editable only after withdraw-to-DRAFT"
    - "GET /internal/purchasing/branches/{branchId}/open-receipts returns POs in SENT state with no mock GRN yet (count > 0 when applicable)"
    - "restaurantos.inventory.integration-mode defaults to mock; mock_grn_receipts table exists with RLS"
    - "GrnDataPort wired to MockGrnAdapter when integration-mode=mock"
    - "Gateway blocks /api/v1/purchasing/** with 403 FEATURE_DISABLED when FEATURE_VENDOR off"
  artifacts:
    - path: "services/purchasing-service/src/main/resources/db/migration/V1__purchasing_schema.sql"
      provides: "vendors, vendor_catalogues, purchase_orders, purchase_order_lines, po_approval_records, po_approval_tiers with RLS"
      contains: "ENABLE ROW LEVEL SECURITY"
    - path: "services/purchasing-service/src/main/java/io/restaurantos/purchasing/service/PoApprovalService.java"
      provides: "OPA-gated approve/reject with multi-tier tracking"
      min_lines: 80
    - path: "services/purchasing-service/src/test/java/io/restaurantos/purchasing/PurchaseOrderApprovalIT.java"
      provides: "IT: submit → approve happy path; OPA deny path"
      contains: "APPROVAL_LIMIT_EXCEEDED"
  key_links:
    - from: "PoApprovalService.approve()"
      to: "AuthorizationClient.authorize()"
      via: "action vendor.po.approve + amount_paisa resource"
      pattern: "vendor\\.po\\.approve|approval_limit"
    - from: "PurchaseOrderService.submit()"
      to: "DomainEventPublisher"
      via: "notification trigger on PENDING_APPROVAL (optional) — PO_APPROVED only on APPROVED"
      pattern: "PO_APPROVED"
    - from: "RouteFeatureMap"
      to: "FEATURE_VENDOR"
      via: "/api/v1/purchasing/ prefix"
      pattern: "FEATURE_VENDOR"
---

<objective>
Scaffold purchasing-service and implement vendor master + purchase order lifecycle with OPA-gated multi-tier approval.

Purpose: Establishes the procurement foundation (PUR-01, PUR-02) plus the **mock GRN data layer** so 10-02 can run full PO→receive→invoice→pay without Phase 8.
Output: Running purchasing-service on 8087, vendor/PO REST APIs, PO_APPROVED events, gateway feature gate, mock GRN schema + GrnDataPort adapter.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-purchasing-accounts-payable/10-RESEARCH.md
@Docs/RestaurantERP_SaaS_Specification.md (M4.1–M4.2)
@Docs/RestaurantERP_UserStories_FlowDiagrams.md (US-5.1–US-5.3, FD-8)
@Docs/agent-specs/04-internal-api-contracts.md
@.planning/phases/10-purchasing-accounts-payable/10-MOCK-FIXTURES.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: purchasing-service scaffold + Flyway schema + infra wiring</name>
  <steps>
    1. Add `services/purchasing-service` module to parent `pom.xml` (mirror finance-service dependencies: web, data-jpa, flyway, shared-lib, testcontainers).
    2. Create `V1__purchasing_schema.sql`: tables per 10-RESEARCH (vendors through po_approval_tiers) **plus `mock_grn_receipts`**; all extend tenant auditable columns; RLS policies on every table; seed default `po_approval_tiers` (3 tiers) and `tenant_match_tolerances` on first vendor/PO access or via tenant bootstrap hook.
    3. `application.yml`: port 8087, datasource `purchasing_db`, Eureka registration, RabbitMQ, Flyway locations; **`restaurantos.inventory.integration-mode: mock`** (default).
    4. Register in `deploy/docker-compose.yml`, gateway route `/api/v1/purchasing/**` → `purchasing-service`, `RouteFeatureMap` entry `FEATURE_VENDOR`.
    5. Add `purchasing-service` to `coverage-gates.json` (≥60%).
    6. `PurchasingSecurityConfig`: JWT filter from shared-lib pattern; `@RequiresFeature("FEATURE_VENDOR")` on controllers.
    7. **Mock GRN foundation:** `InventoryIntegrationProperties`, `GrnDataPort` interface, `MockGrnAdapter` reading `mock_grn_receipts`, `@ConditionalOnProperty(integration-mode=mock)`. Register bean; feign adapter stubbed as no-op until Phase 8.
    8. `InternalPurchasingController`: `GET /internal/purchasing/branches/{branchId}/open-receipts` → POs in SENT with zero mock GRN rows; `GET .../pending-match-invoices` → `{ count: 0, invoiceIds: [] }` until 10-02.
  </steps>
  <verify>
    - `mvn -pl services/purchasing-service -am test` passes
    - Service registers with Eureka; `curl` via gateway returns 401 without JWT, 403 without FEATURE_VENDOR
  </verify>
</task>

<task type="auto">
  <name>Task 2: Vendor master + vendor catalogue APIs (PUR-01)</name>
  <steps>
    1. `Vendor` entity with `bankAccountNo` encrypted via `EncryptionService` (opt-in auto-config); API never exposes decrypted value — return `bankAccountLast4` or null.
    2. `VendorCatalogue` entity: vendor_id, ingredient_id (UUID, no cross-DB FK), unit_price_paisa, valid_from/to.
    3. `VendorService` + `VendorController`:
       - `GET /api/v1/purchasing/vendors` (paginated, search by name)
       - `POST /api/v1/purchasing/vendors` (US-5.1 fields: name, contact, phone, payment_terms NET15/30/45/COD, ntn, strn optional)
       - `PUT /api/v1/purchasing/vendors/{id}`
       - `GET/POST /api/v1/purchasing/vendors/{id}/catalogue`
    4. Permissions: `@PreAuthorize` with `vendor.view`, `vendor.manage` (add Liquibase 038 for tier permissions if missing from 030).
    5. `VendorIT`: create vendor, assert bank field encrypted in DB, API response masked.
  </steps>
  <verify>
    - VendorIT green
    - Duplicate name allowed with warning header `X-Duplicate-Vendor-Name: true` (US-5.1 AC3)
  </verify>
</task>

<task type="auto">
  <name>Task 3: Purchase order CRUD + OPA approval workflow (PUR-02)</name>
  <steps>
    1. `PurchaseOrder` + `PurchaseOrderLine` entities; `PoStatus` enum matching spec state machine.
    2. `PurchaseOrderService`:
       - Create/update DRAFT only; lines pre-fill unit_price from vendor_catalogue when ingredient match exists
       - `submit(id)`: DRAFT → PENDING_APPROVAL; compute required tiers from po_approval_tiers vs total_paisa
       - `withdraw(id)`: PENDING_APPROVAL → DRAFT
       - `send(id)`: APPROVED → SENT
    3. `PoApprovalService`:
       - `approve(id)`: OPA authorize `vendor.po.approve` with `{ amount_paisa, branch_id }`; record PoApprovalRecord; when all tiers satisfied → APPROVED; publish `PO_APPROVED` via DomainEventPublisher
       - `reject(id, reason)`: → REJECTED (mandatory reason)
    4. `PurchaseOrderController` endpoints per REST conventions (Appendix A ApiResponse envelope).
    5. `PurchaseOrderApprovalIT`: mock OPA allow/deny; assert state transitions and event published.
  </steps>
  <verify>
    - PurchaseOrderApprovalIT green
    - PO total recalculated on line add/remove in same transaction
    - Edit blocked when status != DRAFT (except withdraw flow)
  </verify>
</task>

</tasks>

<verification_criteria>
- [ ] purchasing-service builds and tests pass in CI matrix
- [ ] All PUR-01 and PUR-02 requirements traceable to endpoints above
- [ ] No plaintext bank_account_no in API responses or logs
- [ ] OPA fail-closed: timeout → deny approval
</verification_criteria>

<success_criteria>
- Vendor CRUD operational with field encryption
- PO lifecycle DRAFT through APPROVED with OPA limits enforced
- PO_APPROVED event published on final approval
- Gateway + FEATURE_VENDOR enforced
</success_criteria>
