---
phase: 04-frontend-shell-ci-cd
plan: 07
type: execute
wave: 3
depends_on: ["04-05", "04-06", "04-08"]
gap_closure: true
files_modified:
  - frontend/components/shared/sidebar.tsx
  - frontend/components/shared/sidebar-nav-items.ts
  - frontend/components/shared/top-bar.tsx
  - frontend/components/shared/mobile-bottom-nav.tsx
  - frontend/app/(tenant)/layout.tsx
autonomous: true

must_haves:
  truths:
    - "The upgraded Sidebar has grouped navigation sections (Overview, Orders, Menu, Finance, Settings), a brand/logo area at top, a collapse toggle that shrinks to icon-only mode with tooltips, notification badges on nav items, and KEEPS the existing `PermissionGuard` + `FeatureGuard` wrapper pattern — no guard removal, only extension (DS-05)"
    - "A `TopBar` renders across the top of the tenant layout with: breadcrumb trail (from route segments), ⌘K search button (opens CommandPalette from 04-06), a notification bell icon (placeholder count badge), the `ThemeToggle` from 04-04, and a profile dropdown with avatar + name + logout link (DS-05)"
    - "A `MobileBottomNav` renders at the bottom of the tenant layout on mobile screens only (`md:hidden`), showing 4-5 primary nav icons (Dashboard, Orders, Menu, Settings) with active-state highlighting, replacing the sidebar on small screens (DS-05)"
    - "The `(tenant)/layout.tsx` integrates the upgraded Sidebar, TopBar, MobileBottomNav, and tenant theme injection (a `<link rel='stylesheet' href='/api/theme?brandColor=...' />` in the `<head>` if a tenant brand colour is set) — layout renders `SidebarSkeleton` via Suspense during sidebar data loading (DS-05, DS-06 integration)"
  artifacts:
    - path: "frontend/components/shared/sidebar.tsx"
      provides: "Upgraded collapsible grouped sidebar with brand area and badges"
      contains: "PermissionGuard"
    - path: "frontend/components/shared/sidebar-nav-items.ts"
      provides: "Extended nav items with groups, badges, and Appearance settings link"
      contains: "NavGroup"
    - path: "frontend/components/shared/top-bar.tsx"
      provides: "Top bar with breadcrumb, search, notifications, theme toggle, profile"
      contains: "TopBar"
    - path: "frontend/components/shared/mobile-bottom-nav.tsx"
      provides: "Mobile bottom navigation bar"
      contains: "MobileBottomNav"
    - path: "frontend/app/(tenant)/layout.tsx"
      provides: "Integrated tenant layout with all shell chrome"
      contains: "TopBar"
  key_links:
    - from: "frontend/components/shared/sidebar.tsx"
      to: "frontend/components/shared/sidebar-nav-items.ts"
      via: "Consumes NavGroup[] for grouped section rendering"
      pattern: "NavGroup"
    - from: "frontend/components/shared/top-bar.tsx"
      to: "frontend/components/ui/command-palette.tsx"
      via: "Opens CommandPalette on ⌘K button click"
      pattern: "CommandPalette"
    - from: "frontend/components/shared/top-bar.tsx"
      to: "frontend/components/ui/theme-toggle.tsx"
      via: "Renders ThemeToggle in the top bar actions area"
      pattern: "ThemeToggle"
    - from: "frontend/app/(tenant)/layout.tsx"
      to: "frontend/components/shared/top-bar.tsx"
      via: "Mounts TopBar above main content area"
      pattern: "TopBar"
    - from: "frontend/app/(tenant)/layout.tsx"
      to: "frontend/components/shared/mobile-bottom-nav.tsx"
      via: "Mounts MobileBottomNav at layout bottom for mobile"
      pattern: "MobileBottomNav"
---

<objective>
Upgrade the shell chrome: enhance the existing Sidebar with grouped sections, brand area, collapse mode, and badges (preserving PermissionGuard/FeatureGuard). Build a new TopBar with breadcrumb, ⌘K search (CommandPalette), notifications placeholder, ThemeToggle, and profile menu. Build a MobileBottomNav for mobile screens. Integrate everything into the `(tenant)/layout.tsx` along with tenant theme CSS injection.

Purpose: Closes DS-SHELL-05 (shell chrome upgrade) and integrates DS-06 theme injection into the layout. This is the integration plan that ties together the tokens (04-04), skeletons (04-05), primitives (04-06), and theming (04-08) into the visible shell.
Output: Upgraded sidebar, new top bar, mobile bottom nav, integrated tenant layout.

Covers: DS-05 (primary), DS-06 (theme injection in layout), DS-07 (ThemeToggle mounted).
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@Docs/RestaurantOS_UI_UX_Design_System.md
@.planning/phases/04-frontend-shell-ci-cd/04-04-SUMMARY.md
@.planning/phases/04-frontend-shell-ci-cd/04-05-SUMMARY.md
@.planning/phases/04-frontend-shell-ci-cd/04-06-SUMMARY.md
@.planning/phases/04-frontend-shell-ci-cd/04-08-SUMMARY.md

@frontend/components/shared/sidebar.tsx
@frontend/components/shared/sidebar-nav-items.ts
@frontend/app/(tenant)/layout.tsx
@frontend/components/ui/command-palette.tsx
@frontend/components/ui/theme-toggle.tsx
@frontend/components/skeletons/sidebar-skeleton.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade Sidebar + nav items structure</name>
  <files>frontend/components/shared/sidebar.tsx, frontend/components/shared/sidebar-nav-items.ts</files>
  <action>
    **`sidebar-nav-items.ts` — extend the data model:**
    - Define a new `NavGroup` interface: `{ label: string; items: NavItem[] }` where `NavItem` keeps its existing shape (href, label, icon, permission?, feature?) BUT add optional `badge?: number | string` for notification counts
    - Restructure the existing flat `navItems` array into grouped arrays:
      - **Overview:** Dashboard
      - **Orders:** (placeholder — items will be added by module phases)
      - **Menu:** (placeholder)
      - **Finance:** (placeholder)
      - **Settings:** General, Appearance (new — links to `/settings/appearance` from 04-08), Users
    - Export `navGroups: NavGroup[]` alongside the existing `navItems` for backward compat
    - Add the Appearance settings link: `{ href: '/settings/appearance', label: 'Appearance', icon: Palette }` (from lucide-react)

    **`sidebar.tsx` — upgrade the component:**
    - Add `'use client'` if not already present (needs useState for collapse)
    - **Brand area:** At the top, render a brand section with a placeholder logo (an icon or the restaurant name from a future context — for now use a `<span>` with "RestaurantOS" and a `ChefHat` icon from lucide-react), and below it the existing `BranchSwitcher` component (DO NOT remove or replace it)
    - **Grouped sections:** Replace flat item list with grouped rendering: for each `NavGroup`, render a `<div>` with a `<p className="text-xs font-semibold uppercase text-muted-foreground tracking-wider px-3 mb-1">{group.label}</p>` heading, then the items. Each item keeps its existing `FeatureGuard` → `PermissionGuard` → `Link` wrapping — DO NOT REMOVE GUARDS.
    - **Collapse mode:** Add `const [collapsed, setCollapsed] = useState(false)`. When collapsed:
      - Sidebar width shrinks from `w-64` to `w-16`
      - Only icons show (text hidden via `hidden` or `sr-only`)
      - Wrap icon-only items in `<Tooltip>` (from shadcn/ui — should already exist) showing the label
      - Group labels hidden
      - A toggle button (ChevronLeft/ChevronRight icon) at the bottom of the sidebar toggles the state
    - **Badges:** If `item.badge` is defined, render a small `<span>` with the badge count (bg-destructive text-destructive-foreground text-xs rounded-full min-w-5 h-5 flex items-center justify-center) to the right of the item label (hidden when collapsed)
    - **Transitions:** Use Tailwind `transition-all duration-200` on the sidebar width and icon/text visibility for smooth collapse animation
    - Preserve ALL existing functionality: active link highlighting, guard composition, BranchSwitcher placement
  </action>
  <verify>`pnpm --dir frontend exec tsc --noEmit` passes; `grep -q "PermissionGuard" frontend/components/shared/sidebar.tsx`; `grep -q "FeatureGuard" frontend/components/shared/sidebar.tsx`; `grep -q "NavGroup" frontend/components/shared/sidebar-nav-items.ts`; `grep -q "Appearance" frontend/components/shared/sidebar-nav-items.ts`; `grep -q "collapsed" frontend/components/shared/sidebar.tsx`; `pnpm --dir frontend run lint` passes; `pnpm --dir frontend build` passes.</verify>
  <done>Sidebar has grouped navigation sections, brand area with BranchSwitcher, collapse toggle with tooltip-on-icon mode, optional badges on items, and the Appearance settings link. PermissionGuard and FeatureGuard wrappers preserved on all items. Smooth collapse transition.</done>
</task>

<task type="auto">
  <name>Task 2: TopBar + MobileBottomNav + tenant layout integration</name>
  <files>frontend/components/shared/top-bar.tsx, frontend/components/shared/mobile-bottom-nav.tsx, frontend/app/(tenant)/layout.tsx</files>
  <action>
    **`components/shared/top-bar.tsx`** (`'use client'`):
    - Renders a `<header>` with `className="sticky top-0 z-30 flex h-14 items-center gap-4 border-b bg-background/95 backdrop-blur px-4 lg:px-6"` — blurred sticky top bar
    - **Left side:**
      - A mobile menu toggle button (hamburger icon, `md:hidden`) — toggles the sidebar visibility on mobile (use a shared state or callback prop `onMobileMenuToggle`)
      - Breadcrumb trail: Use `usePathname()` from `next/navigation`, split segments, render as breadcrumb links with `ChevronRight` separators. Capitalize/prettify segment names. Only show last 2-3 segments to avoid overflow.
    - **Right side (flex items-center gap-2):**
      - **⌘K search button:** A ghost button with `Search` icon + "Search..." label + kbd badge "⌘K". On click, opens `CommandPalette` (from 04-06). Manage open state locally with useState.
      - **Notifications bell:** A ghost button with `Bell` icon and a small red dot badge (hardcoded `3` for now — real notification system in later phase). Uses `touch-target` class.
      - **ThemeToggle:** Import and render `<ThemeToggle />` from 04-04
      - **Profile menu:** A `DropdownMenu` (shadcn) triggered by an avatar circle (user initial letter, `bg-primary text-primary-foreground rounded-full size-8`). Menu items: "Profile" (link to `/settings/profile` — placeholder), "Settings" (link to `/settings`), separator, "Log out" (calls the existing logout function from auth hooks). Use touch-target class.
    - Render `<CommandPalette open={open} onOpenChange={setOpen}>` at the bottom of the component with placeholder command items (Dashboard, Settings, Appearance — these are navigation commands).

    **`components/shared/mobile-bottom-nav.tsx`** (`'use client'`):
    - Renders `<nav className="fixed bottom-0 inset-x-0 z-40 flex items-center justify-around border-t bg-background/95 backdrop-blur h-16 md:hidden">` — only visible below `md` breakpoint
    - Shows 5 bottom nav items: Dashboard (LayoutDashboard icon), Orders (ShoppingCart), Menu (UtensilsCrossed), Finance (DollarSign), Settings (Settings) — each is a `<Link>` with the icon + small label
    - Active item highlighted via `usePathname()` — if current path starts with the item's href, apply `text-primary` instead of `text-muted-foreground`
    - Each item uses `touch-target` class for 44px minimum tap area
    - Items are guarded by the same `PermissionGuard` used in the sidebar — import and wrap each bottom nav link

    **`app/(tenant)/layout.tsx` — integrate all shell chrome:**
    - Import `TopBar`, `MobileBottomNav`, `SidebarSkeleton`
    - Structure:
      ```
      <div className="flex h-screen">
        <Suspense fallback={<SidebarSkeleton />}>
          <Sidebar />
        </Suspense>
        <div className="flex flex-1 flex-col overflow-hidden">
          <TopBar onMobileMenuToggle={...} />
          <main className="flex-1 overflow-y-auto p-4 lg:p-6 pb-20 md:pb-6">
            {children}
          </main>
        </div>
        <MobileBottomNav />
      </div>
      ```
    - **Tenant theme injection:** If tenant has a `brandColor` stored (check localStorage for now — `tenant-theme-settings` key from 04-08), inject `<link rel="stylesheet" href={`/api/theme?brandColor=${encodeURIComponent(color)}`} />` via Next.js `<head>` or the `metadata` API. If no brand colour, skip — defaults to the base theme tokens in globals.css.
    - **Mobile sidebar toggle:** Use a `useState(false)` for `mobileOpen`. Pass `onMobileMenuToggle={() => setMobileOpen(!mobileOpen)}` to TopBar. Pass `mobileOpen` to Sidebar which shows/hides via `translate-x-0`/`-translate-x-full` + overlay backdrop on mobile.
    - KEEP existing layout behavior: auth session check, BranchSwitcher, any middleware redirects.
    - Add `pb-20 md:pb-6` padding-bottom on main content to clear the MobileBottomNav on small screens.
  </action>
  <verify>`pnpm --dir frontend exec tsc --noEmit` passes; `pnpm --dir frontend run lint` passes; `grep -q "TopBar" frontend/app/(tenant)/layout.tsx`; `grep -q "MobileBottomNav" frontend/app/(tenant)/layout.tsx`; `grep -q "SidebarSkeleton" frontend/app/(tenant)/layout.tsx`; `grep -q "CommandPalette" frontend/components/shared/top-bar.tsx`; `grep -q "ThemeToggle" frontend/components/shared/top-bar.tsx`; `grep -q "PermissionGuard" frontend/components/shared/mobile-bottom-nav.tsx`; `pnpm --dir frontend build` passes; navigate to any tenant page and verify: top bar renders with breadcrumb, sidebar collapses, mobile view shows bottom nav (manual/visual check — optional).</verify>
  <done>TopBar renders with breadcrumb, ⌘K search (CommandPalette), notifications, ThemeToggle, and profile dropdown. MobileBottomNav shows 5 guarded icons on mobile. Tenant layout integrates sidebar (with Suspense skeleton), TopBar, MobileBottomNav, and dynamic theme CSS injection. Build green. (DS-05 complete; DS-06 theme injection integrated; DS-07 ThemeToggle mounted.)</done>
</task>

</tasks>

<verification>
- Sidebar has grouped sections, brand area, collapse mode, tooltips, badges (DS-05, §6)
- PermissionGuard + FeatureGuard preserved on all sidebar items (DS-05 constraint)
- TopBar has breadcrumb, ⌘K search, notifications, ThemeToggle, profile menu (DS-05, §6)
- MobileBottomNav shows 5 icons on mobile with PermissionGuard and touch targets (DS-05, §6)
- Tenant layout renders SidebarSkeleton via Suspense during loading (DS-02 integration)
- Tenant theme CSS injected via <link> when brand colour is set (DS-06 integration)
- ThemeToggle mounted in TopBar (DS-07 integration)
- All existing behaviour preserved: auth guards, BranchSwitcher, proxy.ts, login/TOTP
- Build, typecheck, lint all green
</verification>

<success_criteria>
DS-05 (shell chrome upgrade) is complete — the shell has a professional, collapsible sidebar, a feature-rich top bar, and mobile bottom navigation. DS-06 theme injection and DS-07 ThemeToggle are integrated into the visible shell. The full gap-closure design system retrofit is now reflected in the running application.
</success_criteria>

<output>
After completion, create `.planning/phases/04-frontend-shell-ci-cd/04-07-SUMMARY.md` recording: the sidebar grouped structure, collapse state management, TopBar component hierarchy, MobileBottomNav guard pattern, tenant theme injection approach, and any layout decisions made during integration.
</output>
