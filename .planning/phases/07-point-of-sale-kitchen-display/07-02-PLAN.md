---
phase: 07-point-of-sale-kitchen-display
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - services/pos-service/src/main/resources/db/migration/V3__pos_tills_payments.sql
  - services/pos-service/src/main/java/io/restaurantos/pos/domain/enums/PaymentMethod.java
  - services/pos-service/src/main/java/io/restaurantos/pos/domain/enums/TillStatus.java
  - services/pos-service/src/main/java/io/restaurantos/pos/domain/model/TillSession.java
  - services/pos-service/src/main/java/io/restaurantos/pos/domain/model/OrderPayment.java
  - services/pos-service/src/main/java/io/restaurantos/pos/domain/model/OrderRefund.java
  - services/pos-service/src/main/java/io/restaurantos/pos/repository/TillSessionRepository.java
  - services/pos-service/src/main/java/io/restaurantos/pos/repository/OrderPaymentRepository.java
  - services/pos-service/src/main/java/io/restaurantos/pos/repository/OrderRefundRepository.java
  - services/pos-service/src/main/java/io/restaurantos/pos/service/TillService.java
  - services/pos-service/src/main/java/io/restaurantos/pos/service/TillServiceImpl.java
  - services/pos-service/src/main/java/io/restaurantos/pos/service/PaymentService.java
  - services/pos-service/src/main/java/io/restaurantos/pos/service/PaymentServiceImpl.java
  - services/pos-service/src/main/java/io/restaurantos/pos/service/SplitTenderCalculator.java
  - services/pos-service/src/main/java/io/restaurantos/pos/service/RefundService.java
  - services/pos-service/src/main/java/io/restaurantos/pos/service/RefundServiceImpl.java
  - services/pos-service/src/main/java/io/restaurantos/pos/service/OrderServiceImpl.java
  - services/pos-service/src/main/java/io/restaurantos/pos/feign/FinancePeriodClient.java
  - services/pos-service/src/main/java/io/restaurantos/pos/authz/PosAuthorizationService.java
  - services/pos-service/src/main/java/io/restaurantos/pos/dto/OpenTillRequest.java
  - services/pos-service/src/main/java/io/restaurantos/pos/dto/CloseTillRequest.java
  - services/pos-service/src/main/java/io/restaurantos/pos/dto/CloseOrderRequest.java
  - services/pos-service/src/main/java/io/restaurantos/pos/dto/VoidOrderRequest.java
  - services/pos-service/src/main/java/io/restaurantos/pos/dto/RefundRequest.java
  - services/pos-service/src/main/java/io/restaurantos/pos/dto/TillSessionDto.java
  - services/pos-service/src/main/java/io/restaurantos/pos/event/PosClosePayloads.java
  - services/pos-service/src/main/java/io/restaurantos/pos/web/TillController.java
  - services/pos-service/src/main/java/io/restaurantos/pos/web/PaymentController.java
  - services/pos-service/src/main/java/io/restaurantos/pos/web/InternalPosController.java
  - services/pos-service/src/test/java/io/restaurantos/pos/SplitTenderCalculatorUnitTest.java
  - services/pos-service/src/test/java/io/restaurantos/pos/OrderCloseIdempotencyIT.java
  - services/pos-service/src/test/java/io/restaurantos/pos/TillReconciliationIT.java
  - services/pos-service/src/test/java/io/restaurantos/pos/VoidRefundOpaIT.java
  - services/pos-service/src/test/java/io/restaurantos/pos/PeriodLockCloseIT.java
  - services/pos-service/src/test/java/io/restaurantos/pos/OpenOrdersCountInternalIT.java
  - services/pos-service/src/test/java/io/restaurantos/pos/KitchenRoleDeniedPosIT.java
  - policies/restaurantos/pos.rego
  - policies/tests/pos_test.rego
  - frontend/lib/api-client/schemas/pos.schema.ts
  - frontend/lib/adapters/pos.adapter.ts
  - frontend/lib/repositories/pos.repository.ts
  - frontend/lib/hooks/pos/use-till.ts
  - frontend/lib/hooks/pos/use-payments.ts
  - frontend/components/pos/payment-panel.tsx
  - frontend/components/pos/till-session-bar.tsx
  - frontend/components/pos/void-refund-dialog.tsx
  - frontend/__tests__/pos/payment-panel.test.tsx
autonomous: true

must_haves:
  truths:
    - "Till open creates an OPEN session (one OPEN per cashier; second open -> 409 TILL_ALREADY_OPEN), publishes TILL_OPENED; close computes variance_paisa (declared - expected), blocks if non-closed/non-voided orders remain in the session, publishes TILL_CLOSED"
    - "Order close validates sum(payments)==total_paisa exactly; split-tender across multiple methods is supported; equal-split remainder is added to the FIRST share only"
    - "Order close is idempotent via Idempotency-Key: a duplicate close returns the stored original response and does NOT re-publish ORDER_CLOSED"
    - "ORDER_CLOSED payload carries customerId (nullable), all money fields, payments[], items[], tillSessionId, cashierId, closedAt — exact field names per research event table"
    - "Closing into a LOCKED period fails closed with 423 PERIOD_LOCKED (synchronous call to Finance GET /internal/finance/periods/status; never silently closes)"
    - "Void (OPEN/SENT_TO_KDS) and refund (CLOSED) are OPA-gated and idempotent; void.own requires ownership+OPEN, void.any is manager-level; refund threshold uses approval_limit_paisa; events published once"
    - "GET /internal/orders/open-count?periodStart=&periodEnd= returns a bare long (NOT ApiResponse-wrapped) — the EXACT contract Finance's already-implemented PosInternalClient.getOpenOrderCount(periodStart,periodEnd) decodes; counts non-terminal orders (OPEN/SENT_TO_KDS/PARTIAL_READY/READY/SERVED) whose business date is within [periodStart,periodEnd], tenant-scoped via X-Tenant-Id/RLS; this is the FD-14 pre-close dependency"
    - "A KITCHEN_STAFF-style JWT (only pos.kds.* permissions, no pos.order.*) is 403 on POST /api/v1/pos/orders — reverse-direction access isolation proven at the POS enforcing service"
    - "policies/restaurantos/pos.rego covers void.own/void.any/refund/discount.override/split_bill; opa test reports 100% coverage"
  artifacts:
    - path: "services/pos-service/src/main/java/io/restaurantos/pos/service/SplitTenderCalculator.java"
      provides: "equal-split (remainder to first share) + by-item proportional split; sum==total validation"
      min_lines: 40
    - path: "services/pos-service/src/main/java/io/restaurantos/pos/feign/FinancePeriodClient.java"
      provides: "Feign GET /internal/finance/periods/status?branchId=&date= -> {status} for period-lock fail-closed"
      contains: "/internal/finance/periods/status"
    - path: "services/pos-service/src/main/java/io/restaurantos/pos/web/InternalPosController.java"
      provides: "GET /internal/orders/open-count?periodStart=&periodEnd= -> bare long, matching Finance's implemented PosInternalClient (FD-14 pre-close dependency)"
      contains: "/internal/orders/open-count"
    - path: "policies/restaurantos/pos.rego"
      provides: "void.own/void.any/refund/discount.override/split_bill allow rules with same_tenant_and_branch"
      contains: "pos.order.refund"
    - path: "services/pos-service/src/test/java/io/restaurantos/pos/OrderCloseIdempotencyIT.java"
      provides: "IT: split-tender close publishes one ORDER_CLOSED; duplicate Idempotency-Key returns original, no second event"
      contains: "ORDER_CLOSED"
  key_links:
    - from: "OrderServiceImpl.closeOrder()"
      to: "FinancePeriodClient.getPeriodStatus()"
      via: "synchronous period-lock check before close (fail-closed -> 423)"
      pattern: "getPeriodStatus|periods/status"
    - from: "OrderServiceImpl.closeOrder()"
      to: "shared-lib EventPublisher (ORDER_CLOSED)"
      via: "@Transactional + IdempotencyService(Idempotency-Key)"
      pattern: "ORDER_CLOSED"
    - from: "RefundServiceImpl.refund()"
      to: "PosAuthorizationService -> OpaClient(pos.rego)"
      via: "OPA refund + approval_limit threshold (fail-closed)"
      pattern: "pos.order.refund"
    - from: "Finance PeriodCloseService.close() -> PosInternalClient.getOpenOrderCount()"
      to: "pos-service InternalPosController GET /internal/orders/open-count"
      via: "existing (Phase 6) Feign contract — path/params/bare-long response MUST match; no edits to tested Finance code"
      pattern: "/internal/orders/open-count"
---

<objective>
Complete the POS write side: till sessions (open/close + reconciliation + `TILL_OPENED`/`TILL_CLOSED`), split-tender payments with idempotent order close (`ORDER_CLOSED` carrying `customerId`, period-lock 423), voids/refunds gated by OPA thresholds, and the internal `/internal/orders/open-count` endpoint (matching Finance's already-implemented Feign client) that Finance period-close depends on. Extend `pos.rego` (+tests) to 100% coverage.

Purpose: `ORDER_CLOSED` is THE core-value event (Inventory depletion, Finance auto-posting, CRM loyalty, Reporting all consume it). Idempotency, exact money, and fail-closed period-lock are non-negotiable.
Output: full order close + tender + till + void/refund flows with ITs proving idempotency, split-tender rounding, reconciliation, OPA thresholds, and period-lock 423; the Finance reverse-contract endpoint; payment/till/void frontend.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-point-of-sale-kitchen-display/07-RESEARCH.md
@.planning/phases/07-point-of-sale-kitchen-display/07-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tills + payments DDL + entities + Finance period Feign client</name>
  <files>
    services/pos-service/src/main/resources/db/migration/V3__pos_tills_payments.sql
    services/pos-service/src/main/java/io/restaurantos/pos/domain/enums/PaymentMethod.java
    services/pos-service/src/main/java/io/restaurantos/pos/domain/enums/TillStatus.java
    services/pos-service/src/main/java/io/restaurantos/pos/domain/model/TillSession.java
    services/pos-service/src/main/java/io/restaurantos/pos/domain/model/OrderPayment.java
    services/pos-service/src/main/java/io/restaurantos/pos/domain/model/OrderRefund.java
    services/pos-service/src/main/java/io/restaurantos/pos/repository/TillSessionRepository.java
    services/pos-service/src/main/java/io/restaurantos/pos/repository/OrderPaymentRepository.java
    services/pos-service/src/main/java/io/restaurantos/pos/repository/OrderRefundRepository.java
    services/pos-service/src/main/java/io/restaurantos/pos/feign/FinancePeriodClient.java
  </files>
  <action>
**1. V3__pos_tills_payments.sql:**
- `till_sessions`: branch_id, cashier_id, opening_float_paisa, expected_closing_paisa, declared_closing_paisa, `variance_paisa BIGINT GENERATED ALWAYS AS (declared_closing_paisa - expected_closing_paisa) STORED`, opened_at, closed_at, status (OPEN/CLOSED). RLS. Partial unique index: `CREATE UNIQUE INDEX uq_open_till_per_cashier ON till_sessions(tenant_id, cashier_id) WHERE status='OPEN'` (US-2.1 AC3).
- `order_payments`: order_id FK, method, amount_paisa, reference_no, recorded_at. RLS.
- `order_refunds`: order_id FK, refund_paisa, reason, refunded_by, scope (FULL/PARTIAL), created_at. RLS.
- `ALTER TABLE orders ADD CONSTRAINT fk_orders_till FOREIGN KEY (till_session_id) REFERENCES till_sessions(id)` (column added nullable in V1).

**2. Enums:** `PaymentMethod {CASH, CARD, LOYALTY_POINTS, BANK_TRANSFER, VOUCHER}`, `TillStatus {OPEN, CLOSED}`.

**3. Entities:** `TillSession` (variance is read-only `@Generated`), `OrderPayment`, `OrderRefund` — extend `TenantAuditableEntity`.

**4. Repositories** (JPQL): `TillSessionRepository.findByCashierIdAndStatus`, `findByIdAndBranchId`; `OrderPaymentRepository.findByOrderId`, `sumAmountByOrderId`; `OrderRefundRepository.findByOrderId`.

**5. FinancePeriodClient (Feign):** `@FeignClient(name="finance-service", configuration=FeignClientConfig.class)` (the pos-service `FeignClientConfig` created in 07-01, mirroring finance-service's per-service config — NOT a shared-lib class) -> `GET /internal/finance/periods/status?branchId=&date=` returning `ApiResponse<{periodId,status,fiscalYear,periodNo}>` (the implemented `InternalFinanceController.periodStatus` wraps in `ApiResponse` and requires the `X-Tenant-Id` header, which `FeignClientConfig` must propagate). Wrap in a Resilience4j fallback that THROWS (fail-closed) — a close must NOT proceed if Finance is unreachable or period is LOCKED/CLOSED (research period-lock section, pitfall 9).
  </action>
  <verify>cd services/pos-service && mvn compile -q</verify>
  <done>V3 migration valid (variance GENERATED, partial unique till index); entities + Feign client compile.</done>
</task>

<task type="auto">
  <name>Task 2: Till open/close + reconciliation + TILL_OPENED/TILL_CLOSED</name>
  <files>
    services/pos-service/src/main/java/io/restaurantos/pos/service/TillService.java
    services/pos-service/src/main/java/io/restaurantos/pos/service/TillServiceImpl.java
    services/pos-service/src/main/java/io/restaurantos/pos/dto/OpenTillRequest.java
    services/pos-service/src/main/java/io/restaurantos/pos/dto/CloseTillRequest.java
    services/pos-service/src/main/java/io/restaurantos/pos/dto/TillSessionDto.java
    services/pos-service/src/main/java/io/restaurantos/pos/web/TillController.java
    services/pos-service/src/test/java/io/restaurantos/pos/TillReconciliationIT.java
  </files>
  <action>
**TillServiceImpl** (`@Transactional`, publish via EventPublisher in-tx):
- `openTill(OpenTillRequest)`: reject if cashier already has OPEN session -> `TillAlreadyOpenException` (409). Create OPEN session with opening_float_paisa. Publish `TILL_OPENED` (`pos.till.opened`) `{tillSessionId, openingFloatPaisa, cashierId}`. OPA `pos.till.open`.
- `closeTill(tillId, CloseTillRequest)` (Idempotency-Key required, US-2.10 AC8): block if any order in this session is NOT in {CLOSED, VOIDED} -> `TillHasOpenOrdersException` (409) (US-2.10 AC4). Compute expected_closing_paisa = opening_float + sum(CASH payments on session orders). Set declared_closing_paisa from request; variance is generated. Set status CLOSED, closed_at. Publish `TILL_CLOSED` (`pos.till.closed`) `{tillSessionId, expectedCashPaisa, countedCashPaisa, variancePaisa, cashierId}`. If |variance| > branch threshold -> include a flag (notification handled by Phase 5 consumer; just emit). `pos.till.reconcile.override` permits force-close with variance.

**TillController:** `POST /api/v1/pos/tills` (open), `POST /api/v1/pos/tills/{id}/close`, `GET /api/v1/pos/tills/{id}`, `GET /api/v1/pos/tills?cashierId=&status=`. `@RequiresFeature("FEATURE_POS")`.

**TillReconciliationIT:** open till -> open second for same cashier -> 409; close with an OPEN order in session -> 409; close clean -> variance computed + TILL_CLOSED outbox row; TILL_OPENED emitted on open.
  </action>
  <verify>cd services/pos-service && mvn -Dtest=TillReconciliationIT test -q</verify>
  <done>Till open/close + one-open-per-cashier + open-order block + variance + both till events proven.</done>
</task>

<task type="auto">
  <name>Task 3: Split-tender + idempotent ORDER_CLOSED + period-lock 423</name>
  <files>
    services/pos-service/src/main/java/io/restaurantos/pos/service/PaymentService.java
    services/pos-service/src/main/java/io/restaurantos/pos/service/PaymentServiceImpl.java
    services/pos-service/src/main/java/io/restaurantos/pos/service/SplitTenderCalculator.java
    services/pos-service/src/main/java/io/restaurantos/pos/service/OrderServiceImpl.java
    services/pos-service/src/main/java/io/restaurantos/pos/dto/CloseOrderRequest.java
    services/pos-service/src/main/java/io/restaurantos/pos/event/PosClosePayloads.java
    services/pos-service/src/main/java/io/restaurantos/pos/web/PaymentController.java
    services/pos-service/src/test/java/io/restaurantos/pos/SplitTenderCalculatorUnitTest.java
    services/pos-service/src/test/java/io/restaurantos/pos/OrderCloseIdempotencyIT.java
    services/pos-service/src/test/java/io/restaurantos/pos/PeriodLockCloseIT.java
  </files>
  <action>
**SplitTenderCalculator** (pure, unit-tested): `validateExact(payments, total)` -> sum must equal total_paisa exactly else `PaymentMismatchException` (422). `equalSplit(total, n)` = `share=total/n`, `remainder=total%n` added to FIRST share only (research POS-03, BLR-1 — NEVER distribute). `byItemSplit(...)` = per-diner subtotal + proportional tax/service-charge with first-share remainder (research Open Question 8).

**PaymentServiceImpl.recordPayment(orderId, payment)** (`@Transactional`): append `order_payments` row; allow multiple (split-tender). Cannot exceed total (partial allowed; order stays open until sum==total). 

**OrderServiceImpl.closeOrder(orderId, CloseOrderRequest, idempotencyKey)** (`@Transactional`):
1. `IdempotencyService.checkAndLock(key, hash, 86400)`; if already complete -> return stored response (no re-publish) (US-2.5 AC11).
2. Load order; reject if 0 items / total 0 (`ZeroValueOrderException`).
3. Validate `sum(order_payments.amount)==order.total_paisa` (SplitTenderCalculator.validateExact) else 422 PAYMENT_MISMATCH.
4. Compute businessDate (BLR-10) and call `FinancePeriodClient.getPeriodStatus(branchId, businessDate)`; LOCKED/CLOSED or unreachable -> `PeriodLockedException` -> **423 PERIOD_LOCKED** (fail-closed; never close).
5. State machine assert -> CLOSED; set closed_at; table -> AVAILABLE.
6. Publish `ORDER_CLOSED` (`pos.order.closed`) with payload `PosClosePayloads.OrderClosedPayload` containing EXACT fields: `{orderId, orderNo, type, customerId(nullable), subtotalPaisa, discountPaisa, serviceChargePaisa, taxPaisa, totalPaisa, payments:[{method, amountPaisa, referenceNo}], items:[{menuItemId, name, qty, unitPricePaisa, lineTotalPaisa}], tillSessionId, cashierId, closedAt}` (research event table — Phase 8/9/12 depend on these names).
7. `IdempotencyService.markComplete(key, responseJson)`.

**PaymentController:** `POST /api/v1/pos/orders/{id}/payments` (record tender), `POST /api/v1/pos/orders/{id}/close` (Idempotency-Key header, `pos.order.close`), `POST /api/v1/pos/orders/{id}/split` (compute split preview, `pos.order.split_bill`).

**Tests:**
- `SplitTenderCalculatorUnitTest`: total 1000/3 -> shares [334,333,333]; sum!=total rejected; by-item proportional tax.
- `OrderCloseIdempotencyIT`: open->item->send->served->pay split (CASH+CARD summing to total)->close -> exactly one ORDER_CLOSED outbox row with correct payload incl null customerId; second close with same Idempotency-Key -> same response, still exactly one ORDER_CLOSED row.
- `PeriodLockCloseIT`: stub FinancePeriodClient (WireMock/mockbean) to return LOCKED -> close -> 423 PERIOD_LOCKED, NO ORDER_CLOSED row; Finance unreachable -> 423 (fail-closed).
  </action>
  <verify>cd services/pos-service && mvn -Dtest=SplitTenderCalculatorUnitTest,OrderCloseIdempotencyIT,PeriodLockCloseIT test -q</verify>
  <done>Split-tender exactness + first-share remainder + idempotent single ORDER_CLOSED + fail-closed 423 period lock all proven.</done>
</task>

<task type="auto">
  <name>Task 4: Voids + refunds (OPA thresholds) + internal open-orders-count + pos.rego</name>
  <files>
    services/pos-service/src/main/java/io/restaurantos/pos/service/RefundService.java
    services/pos-service/src/main/java/io/restaurantos/pos/service/RefundServiceImpl.java
    services/pos-service/src/main/java/io/restaurantos/pos/authz/PosAuthorizationService.java
    services/pos-service/src/main/java/io/restaurantos/pos/dto/VoidOrderRequest.java
    services/pos-service/src/main/java/io/restaurantos/pos/dto/RefundRequest.java
    services/pos-service/src/main/java/io/restaurantos/pos/web/InternalPosController.java
    services/pos-service/src/main/java/io/restaurantos/pos/web/OrderController.java
    policies/restaurantos/pos.rego
    policies/tests/pos_test.rego
    services/pos-service/src/test/java/io/restaurantos/pos/VoidRefundOpaIT.java
    services/pos-service/src/test/java/io/restaurantos/pos/OpenOrdersCountInternalIT.java
    services/pos-service/src/test/java/io/restaurantos/pos/KitchenRoleDeniedPosIT.java
  </files>
  <action>
**PosAuthorizationService:** wraps shared `OpaClient.evaluate("pos", OpaInput...)`. Builds `OpaInput` with user (id/tenant/branch/permissions/attributes incl `approval_limit_paisa`), resource (`order`, id, tenant, branch, created_by, status, amount_paisa=discount-or-refund amount), action. Fail-closed (non-allow / timeout -> `PermissionDeniedException` 403).

**Void** (OrderServiceImpl.voidOrder, `@Transactional`, idempotent via Idempotency-Key): legal only OPEN/SENT_TO_KDS -> VOIDED (state machine). OPA: `void.own` requires created_by==user && status OPEN; else `void.any` (manager). Reason required. table -> AVAILABLE. Publish `ORDER_VOIDED` (`pos.order.voided`) `{orderId, reason, voidedBy}` (Kitchen cancels its tickets — 07-04). 

**RefundServiceImpl.refund(orderId, RefundRequest)** (`@Transactional`, idempotent): legal only CLOSED. FULL -> status REFUNDED; PARTIAL -> linked `order_refunds` row, status stays CLOSED. OPA `pos.order.refund` + threshold: refund_paisa must be <= user `approval_limit_paisa` (enforced in rego) else 403. Publish `ORDER_REFUNDED` (`pos.order.refunded`) `{orderId, refundPaisa, reason, refundedBy}`.

**OrderController additions:** `POST /api/v1/pos/orders/{id}/void`, `POST /api/v1/pos/orders/{id}/refund`.

**InternalPosController (MUST match Finance's already-implemented Feign client exactly):** `GET /internal/orders/open-count?periodStart={ISO date}&periodEnd={ISO date}` returning a **bare `long`** (NOT `ApiResponse`-wrapped — `PosInternalClient.getOpenOrderCount` decodes a primitive `long`; wrapping it breaks the decode). Count orders in non-terminal states {OPEN, SENT_TO_KDS, PARTIAL_READY, READY, SERVED} whose business date (BLR-10) falls within `[periodStart, periodEnd]`, tenant-scoped via the `X-Tenant-Id` header / RLS. `/internal/**` (no OPA, `X-Internal-Service` required).
> DECISION (resolves plan-checker blocker): the documented Doc 4 §4.2 contract (`/internal/pos/branches/{branchId}/open-orders-count?olderThanHours`) was NEVER implemented — Finance Phase 6 shipped & tested `PosInternalClient` against `/internal/orders/open-count?periodStart=&periodEnd=` returning `long`, and period close is tenant-wide (no branch in scope). We adopt the IMPLEMENTED contract to avoid editing tested Finance code. (Follow-up: update Docs/agent-specs/04-internal-api-contracts.md to match — tracked, non-blocking.)

**KitchenRoleDeniedPosIT (reverse-direction isolation, plan-checker warning):** mint a JWT carrying ONLY `pos.kds.view`/`pos.kds.update` (kitchen-style, no `pos.order.*`) via TestFixtures and assert `POST /api/v1/pos/orders` (and close/void) -> **403** at the POS enforcing service. (Complements 07-04's forward-direction `KdsAccessIsolationIT`; together they prove bidirectional isolation at the real enforcing services.)

**OpenOrdersCountInternalIT:** seed orders in terminal + non-terminal states across business dates; assert `/internal/orders/open-count` returns the correct count for a date range and 0 when all are CLOSED/VOIDED (the value FD-14 reads).

**pos.rego** (extend existing void-only file): add allow rules for `pos.order.refund` (with `input.user.attributes.approval_limit_paisa >= input.resource.amount_paisa`), `pos.order.discount.override`, `pos.order.split_bill`, all with `common.same_tenant_and_branch(input)`. Keep existing void.own/void.any.

**pos_test.rego:** allow/deny cases per action; OWNER allow-all; CASHIER denied void.any/refund; refund over approval_limit denied; cross-branch denied. `opa test policies/ --coverage` == 100%.

**VoidRefundOpaIT:** cashier void own OPEN -> ok; cashier void others -> 403; cashier void.any -> 403; manager void.any -> ok; refund within limit -> ok + ORDER_REFUNDED; refund over approval_limit -> 403; void publishes ORDER_VOIDED; idempotent void replay -> single event.
  </action>
  <verify>
    opa test policies/ --coverage  (== 100%)
    cd services/pos-service && mvn -Dtest=VoidRefundOpaIT,OpenOrdersCountInternalIT,KitchenRoleDeniedPosIT test -q
  </verify>
  <done>Void/refund OPA thresholds enforced + idempotent events; internal /internal/orders/open-count matches Finance's Feign contract; kitchen-only JWT 403 at POS; pos.rego at 100% coverage.</done>
</task>

<task type="auto">
  <name>Task 5: Frontend payment + till + void/refund UI</name>
  <files>
    frontend/lib/api-client/schemas/pos.schema.ts
    frontend/lib/adapters/pos.adapter.ts
    frontend/lib/repositories/pos.repository.ts
    frontend/lib/hooks/pos/use-till.ts
    frontend/lib/hooks/pos/use-payments.ts
    frontend/components/pos/payment-panel.tsx
    frontend/components/pos/till-session-bar.tsx
    frontend/components/pos/void-refund-dialog.tsx
    frontend/__tests__/pos/payment-panel.test.tsx
  </files>
  <action>
Extend the four-layer POS abstraction:
- schemas/adapters/repository: add till (open/close/get), payment (record/close/split-preview), void, refund endpoints; close sends an `Idempotency-Key` (uuid v4) header.
- hooks: `use-till.ts`, `use-payments.ts` (mutations invalidate order + till keys).
- `payment-panel.tsx`: split-tender entry (multiple method+amount rows), running remaining balance via `<MoneyDisplay>`, "CHARGE NOW" `h-14 text-lg font-bold` enabled only when sum==total; close on success shows receipt modal stub (Print/Email/WhatsApp deferred — research Open Question 4).
- `till-session-bar.tsx`: open-till (float entry) + close-till (declared cash + variance display).
- `void-refund-dialog.tsx`: reason capture; refund amount (FULL/PARTIAL); buttons permission-gated (`PermissionGuard` on `pos.order.void.any`/`pos.order.refund`) so cashiers don't see manager actions.
- `payment-panel.test.tsx`: charge disabled until exact; split rows sum logic; Idempotency-Key header sent.
- ESLint four-layer boundary clean; tsc zero any.
  </action>
  <verify>cd frontend && pnpm tsc --noEmit && pnpm lint && pnpm vitest run __tests__/pos/payment-panel.test.tsx</verify>
  <done>Payment/till/void-refund UI wired to hooks; charge gated on exact tender; manager actions permission-gated; tests green.</done>
</task>

</tasks>

<verification>
- `mvn -pl services/pos-service verify` exits 0; pos-service coverage >= 70%
- `opa test policies/ --coverage` == 100%
- Till: one OPEN per cashier (409 on second); close blocked by open orders; variance computed; TILL_OPENED + TILL_CLOSED emitted
- Order close: sum(payments)==total enforced; split-tender remainder to first share; idempotent (one ORDER_CLOSED on replay); payload field names exact incl nullable customerId
- Period locked / Finance unreachable on close -> 423 PERIOD_LOCKED, no ORDER_CLOSED
- Void/refund OPA-gated + threshold (approval_limit) + idempotent; ORDER_VOIDED/ORDER_REFUNDED published once
- GET /internal/orders/open-count?periodStart=&periodEnd= returns a bare long matching Finance's implemented PosInternalClient (FD-14)
- KITCHEN_STAFF-style JWT (pos.kds.* only) -> 403 on POST /api/v1/pos/orders (reverse-direction isolation)
- Frontend tsc/lint/test green; manager-only actions permission-gated in UI
</verification>

<success_criteria>
- Tills open/close with reconciliation + events; one-open-per-cashier enforced
- Split-tender idempotent close emits exactly one ORDER_CLOSED with the exact contract payload
- Period-lock is fail-closed (423), never closes into a locked period
- Voids/refunds respect OPA permission + approval-limit threshold and are idempotent
- pos.rego extended to refund/discount.override/split_bill at 100% OPA coverage
- Finance reverse-contract endpoint implemented to the EXACT implemented Feign shape (/internal/orders/open-count, bare long)
- Reverse-direction access isolation proven at POS (kitchen-only JWT 403)
- Payment/till/void-refund frontend on four-layer abstraction, manager actions gated
- mvn verify + opa test + frontend checks all green
</success_criteria>

<output>
After completion, create `.planning/phases/07-point-of-sale-kitchen-display/07-02-SUMMARY.md`
</output>
