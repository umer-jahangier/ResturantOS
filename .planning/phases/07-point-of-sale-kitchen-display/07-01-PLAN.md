---
phase: 07-point-of-sale-kitchen-display
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pom.xml
  - docker/docker-compose.yml
  - .github/workflows/coverage-gates.json
  - services/pos-service/pom.xml
  - services/pos-service/Dockerfile
  - services/pos-service/src/main/java/io/restaurantos/pos/PosServiceApplication.java
  - services/pos-service/src/main/resources/application.yml
  - services/pos-service/src/main/resources/db/migration/V1__pos_schema.sql
  - services/pos-service/src/main/resources/db/migration/V2__pos_infra_tables.sql
  - services/pos-service/src/main/java/io/restaurantos/pos/config/PosSecurityConfig.java
  - services/pos-service/src/main/java/io/restaurantos/pos/config/OpenApiConfig.java
  - services/pos-service/src/main/java/io/restaurantos/pos/config/FeignClientConfig.java
  - services/pos-service/src/main/java/io/restaurantos/pos/domain/enums/OrderStatus.java
  - services/pos-service/src/main/java/io/restaurantos/pos/domain/enums/OrderType.java
  - services/pos-service/src/main/java/io/restaurantos/pos/domain/enums/KdsItemStatus.java
  - services/pos-service/src/main/java/io/restaurantos/pos/domain/enums/TableStatus.java
  - services/pos-service/src/main/java/io/restaurantos/pos/domain/model/MenuCategory.java
  - services/pos-service/src/main/java/io/restaurantos/pos/domain/model/MenuItem.java
  - services/pos-service/src/main/java/io/restaurantos/pos/domain/model/BranchMenuOverride.java
  - services/pos-service/src/main/java/io/restaurantos/pos/domain/model/ModifierGroup.java
  - services/pos-service/src/main/java/io/restaurantos/pos/domain/model/Modifier.java
  - services/pos-service/src/main/java/io/restaurantos/pos/domain/model/DiningTable.java
  - services/pos-service/src/main/java/io/restaurantos/pos/domain/model/Order.java
  - services/pos-service/src/main/java/io/restaurantos/pos/domain/model/OrderItem.java
  - services/pos-service/src/main/java/io/restaurantos/pos/domain/model/OrderItemModifier.java
  - services/pos-service/src/main/java/io/restaurantos/pos/domain/model/OrderDiscount.java
  - services/pos-service/src/main/java/io/restaurantos/pos/domain/model/OrderSequence.java
  - services/pos-service/src/main/java/io/restaurantos/pos/repository/MenuCategoryRepository.java
  - services/pos-service/src/main/java/io/restaurantos/pos/repository/MenuItemRepository.java
  - services/pos-service/src/main/java/io/restaurantos/pos/repository/BranchMenuOverrideRepository.java
  - services/pos-service/src/main/java/io/restaurantos/pos/repository/DiningTableRepository.java
  - services/pos-service/src/main/java/io/restaurantos/pos/repository/OrderRepository.java
  - services/pos-service/src/main/java/io/restaurantos/pos/repository/OrderSequenceRepository.java
  - services/pos-service/src/main/java/io/restaurantos/pos/service/MenuService.java
  - services/pos-service/src/main/java/io/restaurantos/pos/service/MenuServiceImpl.java
  - services/pos-service/src/main/java/io/restaurantos/pos/service/TableService.java
  - services/pos-service/src/main/java/io/restaurantos/pos/service/TableServiceImpl.java
  - services/pos-service/src/main/java/io/restaurantos/pos/service/OrderService.java
  - services/pos-service/src/main/java/io/restaurantos/pos/service/OrderServiceImpl.java
  - services/pos-service/src/main/java/io/restaurantos/pos/service/OrderPricingCalculator.java
  - services/pos-service/src/main/java/io/restaurantos/pos/service/OrderStateMachine.java
  - services/pos-service/src/main/java/io/restaurantos/pos/dto/MenuItemDto.java
  - services/pos-service/src/main/java/io/restaurantos/pos/dto/OrderDto.java
  - services/pos-service/src/main/java/io/restaurantos/pos/dto/CreateOrderRequest.java
  - services/pos-service/src/main/java/io/restaurantos/pos/dto/AddOrderItemRequest.java
  - services/pos-service/src/main/java/io/restaurantos/pos/dto/ApplyDiscountRequest.java
  - services/pos-service/src/main/java/io/restaurantos/pos/event/PosEventPayloads.java
  - services/pos-service/src/main/java/io/restaurantos/pos/web/MenuController.java
  - services/pos-service/src/main/java/io/restaurantos/pos/web/TableController.java
  - services/pos-service/src/main/java/io/restaurantos/pos/web/OrderController.java
  - services/pos-service/src/main/java/io/restaurantos/pos/exception/PosExceptions.java
  - services/pos-service/src/main/java/io/restaurantos/pos/exception/PosGlobalExceptionHandler.java
  - services/pos-service/src/test/java/io/restaurantos/pos/PosTestBase.java
  - services/pos-service/src/test/java/io/restaurantos/pos/OrderStateMachineUnitTest.java
  - services/pos-service/src/test/java/io/restaurantos/pos/OrderPricingCalculatorUnitTest.java
  - services/pos-service/src/test/java/io/restaurantos/pos/OrderLifecycleIT.java
  - services/pos-service/src/test/java/io/restaurantos/pos/OrderRlsIsolationIT.java
  - services/auth-service/src/main/resources/db/changelog/v1.0.0/041-pos-permissions.xml
  - services/auth-service/src/main/resources/db/changelog/db.changelog-master.xml
  - frontend/lib/api-client/schemas/pos.schema.ts
  - frontend/lib/models/pos.model.ts
  - frontend/lib/adapters/pos.adapter.ts
  - frontend/lib/repositories/pos.repository.ts
  - frontend/lib/hooks/pos/use-menu.ts
  - frontend/lib/hooks/pos/use-orders.ts
  - frontend/lib/hooks/query-keys.ts
  - frontend/app/(tenant)/app/pos/page.tsx
  - frontend/components/pos/pos-terminal.tsx
  - frontend/components/pos/menu-grid.tsx
  - frontend/components/pos/order-panel.tsx
  - frontend/components/pos/table-floor-view.tsx
  - frontend/__tests__/pos/pos-repository.test.ts
autonomous: true

must_haves:
  truths:
    - "pos-service boots on 8084 against pos_db; Flyway V1+V2 create all POS tables + shared infra tables (event_outbox, idempotency_keys, processed_events) with RLS on every tenant table"
    - "POST /api/v1/pos/orders creates a DRAFT order (table -> OCCUPIED); adding the first item moves it to OPEN and publishes ORDER_CREATED via the transactional outbox"
    - "The order state machine enforces legal transitions only; an illegal transition (e.g. re-sending a SENT_TO_KDS order) returns 409 STATE_INVALID"
    - "POST .../send-to-kds moves OPEN -> SENT_TO_KDS, sets sent_to_kds_at, and publishes ORDER_SENT_TO_KDS carrying per-item kdsStation resolved from menu_items.kds_station (null -> DEFAULT)"
    - "Per-line tax is floored/half-up per line (never on the order total); a discount can never push a line below zero (line_total = max(0, subtotal - min(discount, subtotal)))"
    - "Effective item price = COALESCE(branch_menu_overrides.price_paisa, menu_items.base_price_paisa); item name + unit price are snapshotted onto order_items at add-time"
    - "orders has client_order_id UUID UNIQUE, customer_id UUID nullable, and a version column for optimistic locking"
    - "auth-service seeds the POS permission catalogue and maps CASHIER (day-to-day subset, NO void.any/refund/discount.override/kds) and MANAGER (privileged superset); OWNER auto-inherits all"
    - "Cross-tenant order read is blocked by RLS (IT proves tenant_B GUC sees no tenant_A orders)"
  artifacts:
    - path: "services/pos-service/src/main/resources/db/migration/V1__pos_schema.sql"
      provides: "menu_categories, menu_items, branch_menu_overrides, modifier_groups, modifiers, dining_tables, orders, order_items, order_item_modifiers, order_discounts, order_sequences with RLS + indexes + UNIQUE(client_order_id)"
      contains: "ENABLE ROW LEVEL SECURITY"
    - path: "services/pos-service/src/main/resources/db/migration/V2__pos_infra_tables.sql"
      provides: "event_outbox, idempotency_keys, processed_events (NON-RLS infra tables, shared-lib ships no migrations)"
      contains: "event_outbox"
    - path: "services/pos-service/src/main/java/io/restaurantos/pos/service/OrderStateMachine.java"
      provides: "central transition guard DRAFT->OPEN->SENT_TO_KDS->PARTIAL_READY->READY->SERVED->CLOSED + VOIDED/REFUNDED; illegal -> StateInvalidException"
      min_lines: 40
    - path: "services/pos-service/src/main/java/io/restaurantos/pos/service/OrderPricingCalculator.java"
      provides: "per-line floored/half-up tax, discount-floor clamp, total composition subtotal - discount + service_charge + tax"
      min_lines: 50
    - path: "services/auth-service/src/main/resources/db/changelog/v1.0.0/041-pos-permissions.xml"
      provides: "POS permission catalogue rows + CASHIER/MANAGER role_permissions mappings"
      contains: "pos.order.send_to_kds"
    - path: "services/pos-service/src/test/java/io/restaurantos/pos/OrderLifecycleIT.java"
      provides: "IT: create->add item(OPEN+ORDER_CREATED outbox row)->send-to-kds(SENT_TO_KDS+ORDER_SENT_TO_KDS outbox row); illegal transition -> 409"
      contains: "ORDER_SENT_TO_KDS"
  key_links:
    - from: "OrderServiceImpl.sendToKds()"
      to: "shared-lib EventPublisher (event_outbox)"
      via: "@Transactional publish in same tx as status mutation"
      pattern: "ORDER_SENT_TO_KDS"
    - from: "OrderPricingCalculator"
      to: "io.restaurantos.shared.money.MoneyUtils"
      via: "per-line tax + discount floor in paisa"
      pattern: "MoneyUtils"
    - from: "auth-service 041-pos-permissions.xml"
      to: "db.changelog-master.xml"
      via: "Liquibase include"
      pattern: "041-pos-permissions"
---

<objective>
Scaffold the new `pos-service` (port 8084, `pos_db`) mirroring the proven finance-service skeleton, and implement the menu/table reference, the order aggregate with its state machine (DRAFT -> OPEN -> SENT_TO_KDS), per-line floored tax, the discount-floor invariant, and the `ORDER_CREATED` / `ORDER_SENT_TO_KDS` events via the transactional outbox. Also seed the POS permission catalogue + CASHIER/MANAGER role mappings in auth-service.

Purpose: pos-service is the heart of the system; `ORDER_SENT_TO_KDS` (this plan) and `ORDER_CLOSED` (07-02) are hard contracts consumed by Kitchen (07-04), Inventory (Phase 8), Finance (Phase 9), CRM (Phase 9), Reporting (Phase 12). Get the event payloads and money math exactly right.
Output: a running pos-service with order CRUD, the state machine up to SENT_TO_KDS, pricing/discount/tax correctness, RLS isolation, the POS RBAC seed, and the POS terminal/floor frontend (four-layer abstraction).
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-point-of-sale-kitchen-display/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: pos-service scaffold + Flyway DDL (schema, RLS, infra tables)</name>
  <files>
    pom.xml
    docker/docker-compose.yml
    services/pos-service/pom.xml
    services/pos-service/Dockerfile
    services/pos-service/src/main/java/io/restaurantos/pos/PosServiceApplication.java
    services/pos-service/src/main/resources/application.yml
    services/pos-service/src/main/resources/db/migration/V1__pos_schema.sql
    services/pos-service/src/main/resources/db/migration/V2__pos_infra_tables.sql
    services/pos-service/src/main/java/io/restaurantos/pos/config/PosSecurityConfig.java
    services/pos-service/src/main/java/io/restaurantos/pos/config/OpenApiConfig.java
    services/pos-service/src/main/java/io/restaurantos/pos/config/FeignClientConfig.java
    services/pos-service/src/main/java/io/restaurantos/pos/domain/enums/OrderStatus.java
    services/pos-service/src/main/java/io/restaurantos/pos/domain/enums/OrderType.java
    services/pos-service/src/main/java/io/restaurantos/pos/domain/enums/KdsItemStatus.java
    services/pos-service/src/main/java/io/restaurantos/pos/domain/enums/TableStatus.java
    services/pos-service/src/main/java/io/restaurantos/pos/domain/model/MenuCategory.java
    services/pos-service/src/main/java/io/restaurantos/pos/domain/model/MenuItem.java
    services/pos-service/src/main/java/io/restaurantos/pos/domain/model/BranchMenuOverride.java
    services/pos-service/src/main/java/io/restaurantos/pos/domain/model/ModifierGroup.java
    services/pos-service/src/main/java/io/restaurantos/pos/domain/model/Modifier.java
    services/pos-service/src/main/java/io/restaurantos/pos/domain/model/DiningTable.java
    services/pos-service/src/main/java/io/restaurantos/pos/domain/model/Order.java
    services/pos-service/src/main/java/io/restaurantos/pos/domain/model/OrderItem.java
    services/pos-service/src/main/java/io/restaurantos/pos/domain/model/OrderItemModifier.java
    services/pos-service/src/main/java/io/restaurantos/pos/domain/model/OrderDiscount.java
    services/pos-service/src/main/java/io/restaurantos/pos/domain/model/OrderSequence.java
  </files>
  <action>
**1. Parent pom.xml:** add `services/pos-service` to `<modules>` (after finance-service).

**2. services/pos-service/pom.xml:** copy `services/finance-service/pom.xml` VERBATIM, changing only `artifactId`/`name` to `pos-service`. Same dependency set: `shared-lib`, `spring-boot-starter-web/data-jpa/validation/actuator/data-redis/amqp`, `spring-cloud-starter-netflix-eureka-client`, `spring-cloud-starter-config`, `spring-cloud-starter-openfeign`, `springdoc-openapi-starter-webmvc-ui`, `flyway-core` + `flyway-database-postgresql`, `postgresql` (runtime), `lombok`, test: `spring-boot-starter-test`, `testcontainers:junit-jupiter`, `testcontainers:postgresql`. Keep the surefire (`**/*IT.java` excluded) + failsafe (ITs) + jacoco `coverage` profile setup identical.

**3. Dockerfile:** copy finance-service Dockerfile (change jar name).

**4. PosServiceApplication.java:** `@SpringBootApplication @EnableFeignClients(basePackages = "io.restaurantos.pos.feign")`. Package root `io.restaurantos.pos`. Do NOT add `@EnableJpaAuditing` (SharedAutoConfiguration is authoritative, decision [03-02-D]).

**5. application.yml:** copy finance-service's, change `server.port: ${SERVER_PORT:8084}`, `spring.application.name: pos-service`, datasource `${POS_DB_URL:jdbc:postgresql://localhost:5432/pos_db}` / `${POS_DB_USER:pos_user}` / `${POS_DB_PASSWORD:pos_pass}`, `jpa.hibernate.ddl-auto: validate`, `flyway.locations: classpath:db/migration`, Eureka `${EUREKA_URI:...}`, Config `${CONFIG_URI:...}`, `restaurantos.internal.secret`, `jwks.uri`. Keep `spring.cloud.openfeign.circuitbreaker.enabled: true`.

**6. V1__pos_schema.sql:** all tables from research "Complete Data Model -> pos_db". Every tenant table: `tenant_id UUID NOT NULL` + 5 audit columns + RLS using the empty-GUC-safe policy `USING (tenant_id = NULLIF(current_setting('app.current_tenant_id', TRUE), '')::UUID)` (research pitfall 3). All money `BIGINT` paisa. Specifics:
- `menu_items`: include `kds_station TEXT NULL`, `tax_rate_pct NUMERIC(5,2)`, `tax_rate_code TEXT`.
- `branch_menu_overrides`: `UNIQUE(tenant_id, branch_id, menu_item_id)`.
- `dining_tables`: status AVAILABLE/OCCUPIED, floor_plan_x/y, floor_plan_shape.
- `orders`: `order_no` (`ORD-YYYYMMDD-NNNN` per-branch daily), `type`, `status`, `table_id`, `cover_count`, `cashier_id`, `till_session_id` (FK added in 07-02; nullable now), money columns (subtotal/tax/discount/service_charge/total), `notes`, `opened_at`, `sent_to_kds_at`, `closed_at`, `voided_at`, `void_reason`, **`client_order_id UUID NOT NULL UNIQUE`**, **`customer_id UUID NULL`** (POS-08), **`version BIGINT NOT NULL DEFAULT 0`** (optimistic lock).
- `order_items`: snapshot columns `item_name_snapshot`, `unit_price_snapshot`; `kds_station`, `kds_status` default PENDING; `discount_paisa`, `tax_paisa`, `line_total_paisa`; FK `order_id ... ON DELETE CASCADE`.
- `order_item_modifiers`: snapshot `modifier_name_snapshot`, `price_delta_paisa`; FK CASCADE.
- `order_discounts` (research open-question 3 — define it): `order_id`, `scope` (LINE/ORDER), `order_item_id` NULL, `type` (FLAT/PERCENT), `value` NUMERIC, `amount_paisa`, `applied_by`.
- `order_sequences`: composite PK `(tenant_id, branch_id, business_date)`, `last_seq INT DEFAULT 0` (per-branch daily order number).
- Indexes: `orders(branch_id,status)`, `orders(client_order_id)`, `order_items(order_id)`.

**7. V2__pos_infra_tables.sql:** create `event_outbox`, `idempotency_keys`, `processed_events` (NON-RLS) — copy the exact DDL finance-service uses (shared-lib ships no migrations; research pitfall 12). Grant to `pos_user`.

**8. Config classes:** copy `FinanceSecurityConfig` -> `PosSecurityConfig` (JWT via JWKS, `/internal/**` requires `X-Internal-Service`, actuator/health open). `OpenApiConfig`, `FeignClientConfig` mirror finance.

**9. Enums:** `OrderStatus {DRAFT, OPEN, SENT_TO_KDS, PARTIAL_READY, READY, SERVED, CLOSED, VOIDED, REFUNDED}`, `OrderType {DINE_IN, TAKEAWAY, DELIVERY}`, `KdsItemStatus {PENDING, COOKING, READY}`, `TableStatus {AVAILABLE, OCCUPIED}`.

**10. Entities** (all tenant tables extend `TenantAuditableEntity`; JPQL repos only, never native `@Query`): `MenuCategory`, `MenuItem`, `BranchMenuOverride`, `ModifierGroup`, `Modifier`, `DiningTable`, `Order` (with `@Version` field, `@OneToMany(cascade=ALL, orphanRemoval=true)` to items + discounts), `OrderItem` (`@OneToMany` to modifiers), `OrderItemModifier`, `OrderDiscount`, `OrderSequence` (`@IdClass`). Never set `@GeneratedValue(UUID)` id before save (decision [03-02-B]).

**11. docker/docker-compose.yml:** add `pos-service` (build: services/pos-service, `8084:8084`, depends_on postgres/eureka/config/rabbitmq), mirroring finance-service. pos_db already pre-created by init-db (do NOT recreate).
  </action>
  <verify>
    cd services/pos-service && mvn compile -q
    Flyway V1/V2 parse on boot (no SQL errors); all entities resolve shared-lib imports
  </verify>
  <done>
    pos-service compiles; V1 has ENABLE ROW LEVEL SECURITY on every tenant table + UNIQUE(client_order_id); V2 has the 3 infra tables; docker-compose has pos-service; `mvn compile` exits 0.
  </done>
</task>

<task type="auto">
  <name>Task 2: Menu + table reference (read APIs) + pricing calculator + state machine</name>
  <files>
    services/pos-service/src/main/java/io/restaurantos/pos/repository/MenuCategoryRepository.java
    services/pos-service/src/main/java/io/restaurantos/pos/repository/MenuItemRepository.java
    services/pos-service/src/main/java/io/restaurantos/pos/repository/BranchMenuOverrideRepository.java
    services/pos-service/src/main/java/io/restaurantos/pos/repository/DiningTableRepository.java
    services/pos-service/src/main/java/io/restaurantos/pos/service/MenuService.java
    services/pos-service/src/main/java/io/restaurantos/pos/service/MenuServiceImpl.java
    services/pos-service/src/main/java/io/restaurantos/pos/service/TableService.java
    services/pos-service/src/main/java/io/restaurantos/pos/service/TableServiceImpl.java
    services/pos-service/src/main/java/io/restaurantos/pos/service/OrderPricingCalculator.java
    services/pos-service/src/main/java/io/restaurantos/pos/service/OrderStateMachine.java
    services/pos-service/src/main/java/io/restaurantos/pos/dto/MenuItemDto.java
    services/pos-service/src/main/java/io/restaurantos/pos/web/MenuController.java
    services/pos-service/src/main/java/io/restaurantos/pos/web/TableController.java
    services/pos-service/src/main/java/io/restaurantos/pos/exception/PosExceptions.java
    services/pos-service/src/test/java/io/restaurantos/pos/OrderPricingCalculatorUnitTest.java
    services/pos-service/src/test/java/io/restaurantos/pos/OrderStateMachineUnitTest.java
  </files>
  <action>
**1. Repositories** (JPQL only): `MenuItemRepository.findByCategoryIdAndActiveTrue`, `findByActiveTrue(Pageable)`; `BranchMenuOverrideRepository.findByBranchIdAndMenuItemId`; `DiningTableRepository.findByBranchId`, `findByIdAndBranchId`.

**2. OrderPricingCalculator** (pure, unit-tested — the money core). Use `io.restaurantos.shared.money.MoneyUtils`:
- `effectivePrice(menuItem, override)` = `COALESCE(override.price_paisa, base_price_paisa)` (US-2.2 AC6).
- `lineSubtotal = (unitPriceSnapshot + sum(modifier price_delta)) * quantity`.
- `discountFloor`: `effectiveDiscount = min(requestedDiscountPaisa, lineSubtotal)`; `lineNet = max(0, lineSubtotal - effectiveDiscount)` (POS-05 — can never be negative).
- `perLineTax`: half-up to nearest paisa on the discounted line base, `tax = roundHalfUp(lineNet * tax_rate_pct / 100)` (research Open Question 1 — use half-up per US-2.2 AC7 wording, NOT MoneyUtils.taxPerLine's floor; leave a `// TODO(shared-lib): reconcile taxPerLine floor vs BLR-1 half-up` comment). NEVER apply tax to the order total.
- `orderTotals`: `subtotal = sum(lineSubtotal)`, `discount = sum(effectiveDiscount)+order-level`, `tax = sum(perLineTax)`, `total = subtotal - discount + service_charge + tax`.

**3. OrderStateMachine**: a `Map<OrderStatus, Set<OrderStatus>>` of legal transitions + `assertTransition(from, to)` throwing `StateInvalidException` (from shared-lib, -> 409). Encode the FD-3 table from research (allow OPEN/SENT_TO_KDS/PARTIAL_READY/READY/SERVED -> CLOSED for takeaway/offline pragmatism per research note). Used by OrderService for every status change.

**4. MenuService/TableService + controllers** (read + minimal manage):
- `GET /api/v1/pos/menu/categories`, `GET /api/v1/pos/menu/items?categoryId=&branchId=` (returns effective price per branch), `GET /api/v1/pos/menu/items/{id}`.
- `GET /api/v1/pos/tables?branchId=`, `PATCH /api/v1/pos/tables/{id}` (status). OPA: `pos.menu.view` / `pos.tables.manage`.
- Menu/table create-manage CRUD is minimal (enough to seed a branch); full menu admin UI is out of phase scope (flag deferred). Provide a small dev seed (a few categories/items with kds_station) via a `seed` Liquibase-style SQL or an `@Profile("dev")` seeder so the POS UI is demoable.

**5. PosExceptions.java:** `OrderNotFoundException` (404 — also used for cross-tenant per BLR-2), `ZeroValueOrderException`, plus reuse shared `StateInvalidException`, `PeriodLockedException`, `IdempotencyConflictException`, `PermissionDeniedException`.

**6. Unit tests:**
- `OrderPricingCalculatorUnitTest`: discount > subtotal clamps to 0 (POS-05); per-line tax floored/half-up matches expected paisa; total composition; modifier deltas; multi-line.
- `OrderStateMachineUnitTest`: every legal transition allowed; OPEN->READY illegal; SENT_TO_KDS->SENT_TO_KDS illegal (re-send) -> StateInvalidException; CLOSED->OPEN illegal.
  </action>
  <verify>
    cd services/pos-service && mvn test -Dtest=OrderPricingCalculatorUnitTest,OrderStateMachineUnitTest -q
  </verify>
  <done>
    Pricing + state-machine unit tests pass; discount-floor and per-line-tax invariants proven; illegal transitions rejected.
  </done>
</task>

<task type="auto">
  <name>Task 3: Order aggregate service + REST API + ORDER_CREATED / ORDER_SENT_TO_KDS outbox + IT</name>
  <files>
    services/pos-service/src/main/java/io/restaurantos/pos/repository/OrderRepository.java
    services/pos-service/src/main/java/io/restaurantos/pos/repository/OrderSequenceRepository.java
    services/pos-service/src/main/java/io/restaurantos/pos/service/OrderService.java
    services/pos-service/src/main/java/io/restaurantos/pos/service/OrderServiceImpl.java
    services/pos-service/src/main/java/io/restaurantos/pos/dto/OrderDto.java
    services/pos-service/src/main/java/io/restaurantos/pos/dto/CreateOrderRequest.java
    services/pos-service/src/main/java/io/restaurantos/pos/dto/AddOrderItemRequest.java
    services/pos-service/src/main/java/io/restaurantos/pos/dto/ApplyDiscountRequest.java
    services/pos-service/src/main/java/io/restaurantos/pos/event/PosEventPayloads.java
    services/pos-service/src/main/java/io/restaurantos/pos/web/OrderController.java
    services/pos-service/src/main/java/io/restaurantos/pos/exception/PosGlobalExceptionHandler.java
    services/pos-service/src/test/java/io/restaurantos/pos/PosTestBase.java
    services/pos-service/src/test/java/io/restaurantos/pos/OrderLifecycleIT.java
    services/pos-service/src/test/java/io/restaurantos/pos/OrderRlsIsolationIT.java
    .github/workflows/coverage-gates.json
  </files>
  <action>
**1. OrderRepository:** `findByIdAndBranchId`, `findByClientOrderId` (offline dedup), `findByBranchIdAndStatusIn(...)` paginated. JPQL only.

**2. OrderSequenceRepository:** `incrementAndGet(tenantId, branchId, businessDate)` via `@Lock(PESSIMISTIC_WRITE)` find + increment (concurrency-safe per-branch daily number). Business date = BLR-10 formula `DATE(opened_at AT branch tz - 4h)`; for now compute from `opened_at` minus 4h (branch tz wiring is a flagged simplification).

**3. OrderServiceImpl** (`@Transactional` on mutating methods; publish via `EventPublisher` inside the SAME tx):
- `createOrder(CreateOrderRequest)`: idempotent on `client_order_id` (catch unique-violation -> return existing order, research idempotency). Set status DRAFT, table -> OCCUPIED (guard table not already OCCUPIED by another open order). No event yet.
- `addItem(orderId, AddOrderItemRequest)`: snapshot name + effective price + kds_station onto order_item + modifiers; recompute totals via OrderPricingCalculator; if first item and status DRAFT -> OPEN and publish `ORDER_CREATED` (`pos.topic`/`pos.order.created`).
- `updateItem` / `removeItem`: only when status OPEN (state machine); recompute totals.
- `applyDiscount(orderId, ApplyDiscountRequest)`: store in order_discounts; recompute with floor; >10% line OR any order-level % -> requires `pos.order.discount.override` (OPA gate added in 07-02; here enforce the permission presence + floor). 
- `sendToKds(orderId)`: assert OPEN->SENT_TO_KDS via state machine; require >=1 item; set `sent_to_kds_at`; publish `ORDER_SENT_TO_KDS` (`pos.order.sent_to_kds`) with payload `{ orderId, items:[{orderItemId, menuItemId, name, qty, kdsStation(=resolved, null->"DEFAULT"), modifiers:[name], notes}] }`.
- `getOrder` / `listOrders`: tenant+branch scoped; cross-tenant -> 404 (BLR-2).
- Reject closing/sending an order with 0 items / total 0 (`ZeroValueOrderException`).

**4. PosEventPayloads.java:** records for `OrderCreatedPayload`, `OrderSentToKdsPayload` (+ item record) matching research event table field names EXACTLY.

**5. OrderController:**
```
POST   /api/v1/pos/orders                      (Idempotency-Key = client_order_id) -> 201 DRAFT
GET    /api/v1/pos/orders            ?branchId=&status=  (pageable)
GET    /api/v1/pos/orders/{id}
POST   /api/v1/pos/orders/{id}/items
PATCH  /api/v1/pos/orders/{id}/items/{itemId}
DELETE /api/v1/pos/orders/{id}/items/{itemId}
POST   /api/v1/pos/orders/{id}/discounts
POST   /api/v1/pos/orders/{id}/send-to-kds     -> 200 SENT_TO_KDS
```
OPA per route: `pos.order.create/view/update/discount.line|order/send_to_kds`. `@RequiresFeature("FEATURE_POS")` on the controller.

**6. PosGlobalExceptionHandler (@RestControllerAdvice):** map `StateInvalidException`->409 STATE_INVALID, `OrderNotFoundException`->404 NOT_FOUND, `ZeroValueOrderException`->422 ZERO_VALUE_ORDER, `IdempotencyConflictException`->409 IDEMPOTENCY_KEY_CONFLICT, `PermissionDeniedException`->403, `PeriodLockedException`->423 (used in 07-02). Reuse shared `ApiError`. (If shared `GlobalExceptionHandler` already covers these, just add POS-specific ones.)

**7. PosTestBase:** copy `FinanceTestBase` static-singleton Testcontainers pattern (postgres:18, Flyway clean+migrate `@BeforeAll`, `@DynamicPropertySource`, mock/stub Redis/Rabbit beans, `TESTCONTAINERS_RYUK_DISABLED=true`).

**8. OrderLifecycleIT:** create order -> add first item -> assert status OPEN + exactly one `ORDER_CREATED` row in event_outbox; send-to-kds -> assert SENT_TO_KDS + `ORDER_SENT_TO_KDS` outbox row whose payload items carry kdsStation (and null station resolved to "DEFAULT"); re-send -> 409 STATE_INVALID; add item with discount > line subtotal -> line_total == 0; duplicate create with same client_order_id -> returns same orderId (no duplicate row).

**9. OrderRlsIsolationIT:** insert order under tenant_A GUC; query under tenant_B GUC -> 0 rows; GET by id cross-tenant -> 404.

**10. coverage-gates.json:** add `"services/pos-service": 70`.
  </action>
  <verify>
    cd services/pos-service && mvn verify -q  (units + ITs green)
    mvn jacoco:report -> pos-service line coverage >= 70%
  </verify>
  <done>
    OrderLifecycleIT + OrderRlsIsolationIT pass; ORDER_CREATED/ORDER_SENT_TO_KDS written to outbox in-tx; discount-floor + idempotent create + state guards proven; coverage >= 70%.
  </done>
</task>

<task type="auto">
  <name>Task 4: auth-service POS permission catalogue + CASHIER/MANAGER role mappings</name>
  <files>
    services/auth-service/src/main/resources/db/changelog/v1.0.0/041-pos-permissions.xml
    services/auth-service/src/main/resources/db/changelog/db.changelog-master.xml
  </files>
  <action>
Add Liquibase changeset `auth-1.0.0-041-pos-permissions` (context `seed` where appropriate) and register it in `db.changelog-master.xml` (after 040).

**1. permissions catalogue rows** (module `pos`): `pos.order.update`, `pos.order.send_to_kds`, `pos.order.refund`, `pos.order.split_bill`, `pos.order.discount.line`, `pos.order.discount.order`, `pos.order.discount.override`, `pos.till.open`, `pos.till.close`, `pos.till.reconcile.override`, `pos.tables.manage`, `pos.menu.view`, `pos.menu.manage`. (`pos.order.create/close/view/void.own/void.any` already exist from changeset 034.) Do NOT add `pos.kds.*` here — that lands in 07-04 with the kitchen role.

**2. role_permissions mappings:**
- **CASHIER** (day-to-day, NON-privileged): add `pos.order.update`, `pos.order.send_to_kds`, `pos.order.discount.line`, `pos.till.open`, `pos.till.close`, `pos.tables.manage`, `pos.menu.view`. (CASHIER already has create/close/view.) DO NOT grant: `void.any`, `refund`, `discount.override`, `discount.order`, `till.reconcile.override`, `split_bill`, or any `pos.kds.*`.
- **MANAGER** (privileged superset): add ALL the new pos.* perms incl. `pos.order.refund`, `pos.order.split_bill`, `pos.order.discount.order`, `pos.order.discount.override`, `pos.till.reconcile.override`, `pos.menu.manage`, plus the cashier subset. (MANAGER already has void.own/void.any.)
- **OWNER / TENANT_ADMIN** auto-inherit via their existing `SELECT code FROM permissions` / all-except-rbac.manage rules — no explicit rows needed, but verify the existing 036 changeset's OWNER/TENANT_ADMIN INSERTs are idempotent or re-run for the new codes (if 036 used a one-time `SELECT code`, add a top-up INSERT here: `INSERT INTO role_permissions SELECT 'OWNER', code FROM permissions p WHERE NOT EXISTS (SELECT 1 FROM role_permissions rp WHERE rp.role_code='OWNER' AND rp.permission_code=p.code)` and the same for TENANT_ADMIN excluding rbac.manage).
- Do NOT grant any POS order perms to ACCOUNTANT/FINANCE_VIEWER/INVENTORY_MANAGER beyond their existing `pos.order.view` (ACCOUNTANT only).
  </action>
  <verify>
    Liquibase validate: cd services/auth-service && mvn -q -DskipTests liquibase:validate (or boot the service against a test DB and confirm changeset applies)
    SELECT permission_code FROM role_permissions WHERE role_code='CASHIER' -> contains send_to_kds, till.open; does NOT contain refund/void.any/discount.override
  </verify>
  <done>
    041 changeset registered in master and applies cleanly; CASHIER has the day-to-day POS subset only; MANAGER has the privileged superset; no kds perms added yet.
  </done>
</task>

<task type="auto">
  <name>Task 5: Frontend POS terminal + floor view (four-layer abstraction)</name>
  <files>
    frontend/lib/api-client/schemas/pos.schema.ts
    frontend/lib/models/pos.model.ts
    frontend/lib/adapters/pos.adapter.ts
    frontend/lib/repositories/pos.repository.ts
    frontend/lib/hooks/pos/use-menu.ts
    frontend/lib/hooks/pos/use-orders.ts
    frontend/lib/hooks/query-keys.ts
    frontend/app/(tenant)/app/pos/page.tsx
    frontend/components/pos/pos-terminal.tsx
    frontend/components/pos/menu-grid.tsx
    frontend/components/pos/order-panel.tsx
    frontend/components/pos/table-floor-view.tsx
    frontend/__tests__/pos/pos-repository.test.ts
  </files>
  <action>
Mirror the finance four-layer pattern (`lib/repositories/finance.repository.ts`, `lib/hooks/finance/*`):
- **L1 schemas** (`pos.schema.ts`): Zod for raw API shapes (`apiMenuItemSchema`, `apiOrderSchema`, `apiOrderItemSchema`). `.parse()` always (never the non-throwing variant).
- **models** (`pos.model.ts`): domain types (`Order`, `OrderItem`, `MenuItem`, money as `bigint`/paisa number per existing MoneyDisplay convention).
- **adapters** (`pos.adapter.ts`): raw -> domain.
- **L2 repository** (`pos.repository.ts`): `getMenuItems`, `getTables`, `createOrder`, `addItem`, `applyDiscount`, `sendToKds`, `getOrder`, `listOrders` via `lib/api-client/request`; parse then adapt. Generate `client_order_id` (uuid v4) and send as `Idempotency-Key` on create.
- **L3 hooks** (`use-menu.ts`, `use-orders.ts`): `useQuery`/`useMutation`, query keys in `query-keys.ts`, `enabled: isAuthenticated && !!branchId`; invalidate order/menu keys on mutation.
- **L4 components**: `pos-terminal.tsx` (menu grid + order panel + send-to-kitchen + charge button placeholder for 07-02), `menu-grid.tsx` (category pills + >=100x100 touch cards, 2/3/4 col), `order-panel.tsx` (line items, qty +/- >=40px, `<MoneyDisplay>` font-mono, running totals), `table-floor-view.tsx` (table status grid). Touch-first per design system 7.1. Page `app/(tenant)/app/pos/page.tsx` `"use client"`, wrapped in `FeatureGuard("FEATURE_POS")` + `PermissionGuard("pos.order.view")`.
- Components MUST NOT import `lib/api-client` or `lib/repositories` (ESLint `no-restricted-imports`, FE-08). `tsc --noEmit` zero `any`.
- **pos-repository.test.ts**: Zod parse + adapter mapping; create sends Idempotency-Key header; bad shape rejected.

Charge/payment + till + offline UI are added in 07-02/07-03; leave clear seams (disabled "CHARGE NOW" button, no offline banner yet).
  </action>
  <verify>
    cd frontend && pnpm tsc --noEmit && pnpm lint && pnpm vitest run __tests__/pos/pos-repository.test.ts
  </verify>
  <done>
    POS terminal + floor render against hooks; four-layer boundary ESLint-clean; tsc zero any; repository test green.
  </done>
</task>

</tasks>

<verification>
- `mvn -pl services/pos-service verify` exits 0 (units + ITs); coverage >= 70%
- V1__pos_schema.sql contains ENABLE ROW LEVEL SECURITY on every tenant table + UNIQUE(client_order_id); V2 has event_outbox/idempotency_keys/processed_events
- create -> add item -> OPEN + ORDER_CREATED outbox row; send-to-kds -> SENT_TO_KDS + ORDER_SENT_TO_KDS outbox row with per-item kdsStation (null->DEFAULT)
- discount > line subtotal clamps line_total to 0; per-line tax never applied to order total; re-send -> 409 STATE_INVALID
- duplicate create with same client_order_id returns the same order (no duplicate)
- cross-tenant order read -> 404 / 0 rows (RLS IT)
- auth 041 changeset applies: CASHIER has POS day-to-day subset (no void.any/refund/override/kds); MANAGER has privileged superset; OWNER inherits all
- pos-service in docker-compose + parent pom modules; frontend tsc/lint/vitest green
</verification>

<success_criteria>
- pos-service scaffolded (8084/pos_db), Flyway V1+V2 run on fresh DB with RLS everywhere
- Order aggregate + state machine enforce DRAFT->OPEN->SENT_TO_KDS; illegal transitions 409
- Per-line floored/half-up tax + discount-floor invariant proven by unit + IT
- ORDER_CREATED + ORDER_SENT_TO_KDS published via transactional outbox in-tx
- orders carry client_order_id (UNIQUE), customer_id (nullable), version (optimistic lock)
- POS RBAC seeded: CASHIER day-to-day subset, MANAGER privileged, OWNER all; finance/inventory roles unchanged
- Frontend POS terminal + floor view on the enforced four-layer abstraction
- mvn verify + frontend tsc/lint/test all green
</success_criteria>

<output>
After completion, create `.planning/phases/07-point-of-sale-kitchen-display/07-01-SUMMARY.md`
</output>
