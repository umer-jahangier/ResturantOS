---
phase: 01-infrastructure-foundation-shared-library
plan: 04
type: execute
wave: 3
depends_on: ["01-01", "01-02", "01-03"]
files_modified:
  - shared-lib/src/main/java/io/restaurantos/shared/context/TenantContext.java
  - shared-lib/src/main/java/io/restaurantos/shared/context/TenantTaskDecorator.java
  - shared-lib/src/main/java/io/restaurantos/shared/messaging/TenantAwareMessageProcessor.java
  - shared-lib/src/main/java/io/restaurantos/shared/web/TenantFilterInterceptor.java
  - shared-lib/src/main/java/io/restaurantos/shared/audit/AuditorAwareImpl.java
  - shared-lib/src/main/java/io/restaurantos/shared/feature/RequiresFeature.java
  - shared-lib/src/main/java/io/restaurantos/shared/feature/FeatureFlagAspect.java
  - shared-lib/src/main/java/io/restaurantos/shared/feature/FeatureFlagService.java
  - shared-lib/src/main/java/io/restaurantos/shared/authz/OpaClient.java
  - shared-lib/src/main/java/io/restaurantos/shared/authz/OpaInput.java
  - shared-lib/src/main/java/io/restaurantos/shared/idempotency/IdempotencyService.java
  - shared-lib/src/main/java/io/restaurantos/shared/security/JwtAuthenticationFilter.java
  - shared-lib/src/main/java/io/restaurantos/shared/security/JwksKeyProvider.java
  - shared-lib/src/main/java/io/restaurantos/shared/security/JwtProperties.java
  - shared-lib/src/main/java/io/restaurantos/shared/security/RestaurantOsAuthentication.java
  - shared-lib/src/main/java/io/restaurantos/shared/api/ApiResponse.java
  - shared-lib/src/main/java/io/restaurantos/shared/api/ApiError.java
  - shared-lib/src/main/java/io/restaurantos/shared/api/GlobalExceptionHandler.java
  - shared-lib/src/main/java/io/restaurantos/shared/exception/
  - shared-lib/src/main/java/io/restaurantos/shared/outbox/EventEnvelope.java
  - shared-lib/src/main/java/io/restaurantos/shared/outbox/OutboxEntry.java
  - shared-lib/src/main/java/io/restaurantos/shared/outbox/OutboxRepository.java
  - shared-lib/src/main/java/io/restaurantos/shared/outbox/DomainEventPublisher.java
  - shared-lib/src/main/java/io/restaurantos/shared/outbox/OutboxRelay.java
  - shared-lib/src/main/java/io/restaurantos/shared/money/MoneyUtils.java
  - shared-lib/src/main/resources/META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports
  - shared-lib/src/test/resources/db/changelog/test-changelog.xml
  - shared-lib/src/test/java/io/restaurantos/shared/integration/BaseIntegrationTest.java
  - shared-lib/src/test/java/io/restaurantos/shared/integration/SharedLibVerificationIT.java
  - shared-lib/src/test/java/io/restaurantos/shared/integration/Widget.java
autonomous: true

must_haves:
  truths:
    - "shared-lib bootstraps in a Spring context: TenantContext, feature-flag, OPA, idempotency, outbox, and Money beans all load (SC3)"
    - "TenantContext propagates across @Async (TenantTaskDecorator) and RabbitMQ consumers (TenantAwareMessageProcessor) (SC3, CRIT-01)"
    - "MoneyUtils does BIGINT-paisa math with correct rounding; the RLS-or-fail guard passes for an RLS table and fails for an unprotected one (SC4)"
    - "DomainEventPublisher writes to event_outbox in the business transaction; OutboxRelay publishes to RabbitMQ; an idempotent consumer processes the event exactly once (SC5)"
  artifacts:
    - path: "shared-lib/src/main/java/io/restaurantos/shared/context/TenantTaskDecorator.java"
      provides: "@Async propagation of TenantContext"
      contains: "TaskDecorator"
    - path: "shared-lib/src/main/java/io/restaurantos/shared/outbox/DomainEventPublisher.java"
      provides: "Transactional outbox write of EventEnvelope JSON"
      contains: "event_outbox"
    - path: "shared-lib/src/main/java/io/restaurantos/shared/money/MoneyUtils.java"
      provides: "BIGINT-paisa arithmetic + rounding"
      contains: "RoundingMode"
    - path: "shared-lib/src/test/java/io/restaurantos/shared/integration/SharedLibVerificationIT.java"
      provides: "One Testcontainers IT proving SC3 + SC4 + SC5"
      contains: "Testcontainers"
    - path: "shared-lib/src/main/resources/META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports"
      provides: "Opt-in auto-configuration so services consume beans (security NOT auto-wired in Phase 1)"
      contains: "AutoConfiguration"
  key_links:
    - from: "shared-lib/src/main/java/io/restaurantos/shared/outbox/OutboxRelay.java"
      to: "RabbitMQ (RabbitTemplate) + event_outbox table"
      via: "polls PENDING rows, publishes, marks SENT in same tx"
      pattern: "RabbitTemplate"
    - from: "shared-lib/src/test/java/io/restaurantos/shared/integration/BaseIntegrationTest.java"
      to: "Testcontainers Postgres 18 + RabbitMQ 4.3 + Redis 8 + Liquibase test-changelog"
      via: "@Testcontainers static containers, Liquibase on context start"
      pattern: "PostgreSQLContainer"
    - from: "shared-lib/src/main/java/io/restaurantos/shared/messaging/TenantAwareMessageProcessor.java"
      to: "TenantContext + set_config('app.current_tenant_id', ...)"
      via: "reads envelope tenantId, sets context + RLS GUC around consumer logic"
      pattern: "app.current_tenant_id"
---

<objective>
Implement the entire `shared-lib` cross-cutting library — tenant propagation, feature flags, OPA, idempotency, standardized API/exception handling, the transactional outbox, Money utilities, and the (shipped-but-not-wired) JWT/security classes — plus the three shared infra-table migrations and ONE Testcontainers integration test that proves SC3, SC4, and SC5 end-to-end.

Purpose: This plan delivers SC3 (shared-lib loads in a Spring context with @Async + RabbitMQ tenant propagation working), SC4 (Money math + the RLS-or-fail guard), and SC5 (transactional outbox → RabbitMQ → idempotent exactly-once consumer). It satisfies the bulk of XCUT-* and LIB-* requirements. Per resolved decisions #3 and #6: verification is a TEST-ONLY `@SpringBootApplication` harness (no sample-service module), and security classes ship but are NOT assembled into a SecurityFilterChain — they are exposed via opt-in auto-configuration for Phase 2.
Output: all shared-lib `src/main` classes from agent-spec 03, the §8.9 infra-table migrations, and the `SharedLibVerificationIT` Testcontainers harness.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-infrastructure-foundation-shared-library/01-RESEARCH.md
@.planning/phases/01-infrastructure-foundation-shared-library/01-02-PLAN.md

# Source of truth — transcribe section-by-section, do not invent:
@Docs/agent-specs/03-shared-lib-specification.md
@Docs/agent-specs/08-database-migration-guide.md
@Docs/agent-specs/09-security-implementation-guide.md
@Docs/agent-specs/10-test-architecture-guide.md
@Docs/agent-specs/02-event-schema-registry.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tenant context + async/consumer propagation + web filter + auditor</name>
  <files>shared-lib/src/main/java/io/restaurantos/shared/context/TenantContext.java, shared-lib/src/main/java/io/restaurantos/shared/context/TenantTaskDecorator.java, shared-lib/src/main/java/io/restaurantos/shared/messaging/TenantAwareMessageProcessor.java, shared-lib/src/main/java/io/restaurantos/shared/web/TenantFilterInterceptor.java, shared-lib/src/main/java/io/restaurantos/shared/audit/AuditorAwareImpl.java</files>
  <action>
    Transcribe agent-spec 03 §3.3 VERBATIM into package `io.restaurantos.shared.context`:
      - `TenantContext`: ThreadLocal holding tenantId, branchId, userId (UUIDs) with set/get/clear; this resolves CRIT-01.
      - `TenantTaskDecorator` (implements Spring `TaskDecorator`): captures the parent thread's TenantContext and restores it inside the @Async worker, clearing in finally — this is the LIB-02 @Async propagation.
      - `TenantAwareMessageProcessor` (RabbitMQ): reads `tenantId`/`branchId`/`userId` from the inbound `EventEnvelope`, sets TenantContext AND issues `set_config('app.current_tenant_id', <tenant>, true)` so RLS applies inside the consumer transaction, runs the handler, then clears in finally — the consumer half of CRIT-01.
    Transcribe agent-spec 03 §3.4 VERBATIM into `io.restaurantos.shared.web`:
      - `TenantFilterInterceptor`: enables the Hibernate `tenantFilter` (defined on TenantAuditableEntity in 01-02) with the current tenant for each session/request.
    Create `io.restaurantos.shared.audit.AuditorAwareImpl` implementing Spring Data `AuditorAware<UUID>` returning `TenantContext.getUserId()` — this backs the @CreatedBy/@LastModifiedBy on TenantAuditableEntity (referenced from 01-02). Annotate the shared `@Configuration` with `@EnableJpaAuditing(auditorAwareRef="auditorAwareImpl")` (placed in the §3.12 config in Task 4).
    Use the exact ThreadLocal semantics from the spec; do not add inheritance to InheritableThreadLocal (decorator handles async).
  </action>
  <verify>`mvn -pl shared-lib -am -q -DskipTests compile` succeeds; `TenantTaskDecorator implements TaskDecorator`; `TenantAwareMessageProcessor` references `app.current_tenant_id`; `AuditorAwareImpl implements AuditorAware`.</verify>
  <done>TenantContext + @Async decorator + RabbitMQ-consumer propagation + Hibernate filter interceptor + auditor all compile (LIB-01, LIB-02, XCUT-01, XCUT-02 tenant-from-context, CRIT-01).</done>
</task>

<task type="auto">
  <name>Task 2: Feature flags, OPA client, idempotency, JWT/security classes (shipped, not wired)</name>
  <files>shared-lib/src/main/java/io/restaurantos/shared/feature/RequiresFeature.java, shared-lib/src/main/java/io/restaurantos/shared/feature/FeatureFlagAspect.java, shared-lib/src/main/java/io/restaurantos/shared/feature/FeatureFlagService.java, shared-lib/src/main/java/io/restaurantos/shared/authz/OpaClient.java, shared-lib/src/main/java/io/restaurantos/shared/authz/OpaInput.java, shared-lib/src/main/java/io/restaurantos/shared/idempotency/IdempotencyService.java, shared-lib/src/main/java/io/restaurantos/shared/security/JwtAuthenticationFilter.java, shared-lib/src/main/java/io/restaurantos/shared/security/JwksKeyProvider.java, shared-lib/src/main/java/io/restaurantos/shared/security/JwtProperties.java, shared-lib/src/main/java/io/restaurantos/shared/security/RestaurantOsAuthentication.java</files>
  <action>
    Transcribe VERBATIM:
      - agent-spec 03 §3.5 → `io.restaurantos.shared.feature`: `@RequiresFeature` annotation, `FeatureFlagAspect` (@Around advice throwing/blocking when flag off), `FeatureFlagService` (Redis-backed, 5-min TTL, resolves flags per tenant). LIB-04.
      - agent-spec 03 §3.6 → `io.restaurantos.shared.authz`: `OpaClient` (fail-closed, 2s timeout, calls OPA `OPA_URL` from §3.12 config, posts branch-aware `OpaInput`, parses allow/deny) and `OpaInput` DTO. LIB-05. Honor RESEARCH: tolerate OPA being optional in dev (no hard failure when OPA_URL unset in the test harness).
      - agent-spec 03 §3.7 → `io.restaurantos.shared.idempotency.IdempotencyService`: stores/looks up `Idempotency-Key` in `idempotency_keys` (24h), returns cached response on replay. LIB-06.
      - agent-spec 09 §9.2 (+ §9.3 tenant population) → `io.restaurantos.shared.security`: `JwtAuthenticationFilter` (RS256 verify against JWKS, build Authentication, populate TenantContext + MDC, clear in finally — resolves tenant from JWT, never from client input, XCUT-02), `JwksKeyProvider` (fetch JWKS at startup, 3600s refresh, keyed by kid), `JwtProperties` (@ConfigurationProperties; decodes the base64 PEM from `generate-keys.sh`), `RestaurantOsAuthentication`. LIB-03.
    CRITICAL (resolved decision #6): DO NOT define a `SecurityFilterChain`/`@EnableWebSecurity` bean in shared-lib. Ship these classes only; do NOT auto-register the filter. Phase 2 assembles the chain. Ensure none of these are component-scanned into an active filter by default (no `@Component` on JwtAuthenticationFilter unless guarded by a `@ConditionalOnProperty` that defaults OFF). XCUT-05 / LIB-06.
  </action>
  <verify>`mvn -pl shared-lib -am -q -DskipTests compile` succeeds; grep shows NO `@EnableWebSecurity` and NO `SecurityFilterChain` bean in shared-lib `src/main`; `JwtProperties` is `@ConfigurationProperties`; `OpaClient` reads `OPA_URL`.</verify>
  <done>Feature-flag aspect, OPA client, idempotency service, and JWT/security classes compile and ship in shared-lib WITHOUT assembling a security filter chain (XCUT-02 tenant-from-JWT, LIB-03/04/05/06).</done>
</task>

<task type="auto">
  <name>Task 3: API response/exception handling, Money utils, transactional outbox, shared config</name>
  <files>shared-lib/src/main/java/io/restaurantos/shared/api/ApiResponse.java, shared-lib/src/main/java/io/restaurantos/shared/api/ApiError.java, shared-lib/src/main/java/io/restaurantos/shared/api/GlobalExceptionHandler.java, shared-lib/src/main/java/io/restaurantos/shared/exception/, shared-lib/src/main/java/io/restaurantos/shared/money/MoneyUtils.java, shared-lib/src/main/java/io/restaurantos/shared/outbox/EventEnvelope.java, shared-lib/src/main/java/io/restaurantos/shared/outbox/OutboxEntry.java, shared-lib/src/main/java/io/restaurantos/shared/outbox/OutboxRepository.java, shared-lib/src/main/java/io/restaurantos/shared/outbox/DomainEventPublisher.java, shared-lib/src/main/java/io/restaurantos/shared/outbox/OutboxRelay.java, shared-lib/src/main/resources/META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports</files>
  <action>
    Transcribe VERBATIM:
      - agent-spec 03 §3.8 → `io.restaurantos.shared.api`: `ApiResponse<T>` + `ApiError` envelope. XCUT-06.
      - agent-spec 03 §3.9 → `io.restaurantos.shared.exception`: the standard exception hierarchy (e.g., `BusinessException`, `ResourceNotFoundException`, `ValidationException`, etc. — exactly the classes listed) AND `GlobalExceptionHandler` (`@RestControllerAdvice` in `io.restaurantos.shared.api`) mapping each exception type to an `ApiError` with the right HTTP status. XCUT-06.
      - agent-spec 03 §3.11 → `io.restaurantos.shared.money.MoneyUtils`: BIGINT-paisa arithmetic with per-line floored tax + half-up rounding (the spec's `RoundingMode` rules; no floating point for money). XCUT-03 / LIB-06.
      - agent-spec 03 §3.10 (+ envelope from agent-spec 02 §2.1) → `io.restaurantos.shared.outbox`: `EventEnvelope`, `OutboxEntry` (@Entity → `event_outbox`), `OutboxRepository` (Spring Data), `DomainEventPublisher` (serializes EventEnvelope to a JSON String and INSERTs an OutboxEntry within the caller's @Transactional business tx — resolves MAJOR-12), and `OutboxRelay` (scheduled poller: reads PENDING rows, publishes via `RabbitTemplate` to the right `*.topic` exchange, marks SENT). CRITICAL (RESEARCH "outbox JSON String (de)serialization" gotcha): persist the payload as a JSON String column and (de)serialize with a single shared ObjectMapper so SC5 round-trips cleanly; ensure status enum + created/processed timestamps match §8.9 columns. CRITICAL (SC5 double-encode gotcha): the relay must NOT call `rabbitTemplate.convertAndSend(jsonString)` — a String body would be re-serialized by the message converter (JSON-of-a-JSON-String, double-encoded). Instead publish the stored JSON as a RAW `org.springframework.amqp.core.Message`: take the stored JSON String, `getBytes(UTF_8)`, build `MessageProperties` with `setContentType("application/json")` (and contentEncoding UTF-8), and call `rabbitTemplate.send(exchange, routingKey, new Message(bytes, props))`. On the consumer side, the `@RabbitListener` must use a `Jackson2JsonMessageConverter` (backed by the same shared ObjectMapper) and declare the listener parameter as the concrete generic type `EventEnvelope<Payload>` so the converter reconstructs the full generic envelope from the `application/json` bytes — so SC5's exactly-once round-trip deserializes back into the original `EventEnvelope<Payload>` without double-encoding.
    Transcribe agent-spec 03 §3.12 → a shared `@AutoConfiguration` class wiring the beans every service loads (TenantTaskDecorator-backed `@Async` executor, `@EnableJpaAuditing(auditorAwareRef=...)`, FeatureFlagService, OpaClient, IdempotencyService, DomainEventPublisher/OutboxRelay, shared ObjectMapper). Register it in `META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports` (per resolved decision #6 — opt-in auto-configuration; security beans excluded/guarded OFF).
  </action>
  <verify>`mvn -pl shared-lib -am -q -DskipTests compile` succeeds; `MoneyUtils` references `RoundingMode`; `OutboxEntry` maps to `event_outbox`; `DomainEventPublisher` serializes `EventEnvelope`; the `AutoConfiguration.imports` file lists the shared auto-config class; `GlobalExceptionHandler` is `@RestControllerAdvice`.</verify>
  <done>API envelope + exception handling, Money utils, and the transactional outbox (publisher + relay) compile and are exposed via opt-in auto-configuration (XCUT-03, XCUT-05, XCUT-06, LIB-06; outbox for SC5).</done>
</task>

<task type="auto">
  <name>Task 4: §8.9 infra-table migrations + Testcontainers harness proving SC3/SC4/SC5</name>
  <files>shared-lib/src/test/resources/db/changelog/test-changelog.xml, shared-lib/src/test/java/io/restaurantos/shared/integration/BaseIntegrationTest.java, shared-lib/src/test/java/io/restaurantos/shared/integration/Widget.java, shared-lib/src/test/java/io/restaurantos/shared/integration/SharedLibVerificationIT.java</files>
  <action>
    Create `shared-lib/src/test/resources/db/changelog/test-changelog.xml` (test-only, per resolved decision #3) that:
      1. Transcribes agent-spec 08 §8.9 VERBATIM for the three shared infra tables — `event_outbox`, `idempotency_keys`, `processed_events` (columns, indexes, rollback, GRANTs). These are RLS-EXEMPT (no RLS changeset) per the 01-02 convention doc.
      2. Adds a sample tenant-scoped `widgets` table (`id`, `tenant_id uuid not null`, `name`, `amount_paisa bigint`, audit cols) FOLLOWED IMMEDIATELY by its RLS changeset (ENABLE + FORCE ROW LEVEL SECURITY + `CREATE POLICY tenant_isolation` + GRANT) per agent-spec 08 §8.6 — this is the positive case the RLS guard must PASS.
    Create `Widget.java` (test @Entity in `...integration`) extending `TenantAuditableEntity` (from 01-02), mapping `widgets`.
    Create `BaseIntegrationTest` by transcribing agent-spec 10 §10.1 VERBATIM into `shared-lib/src/test/java/io/restaurantos/shared/integration/`: a TEST-ONLY `@SpringBootTest`(+`@SpringBootApplication` harness)/`@Testcontainers` base that starts PostgreSQL 18 + RabbitMQ 4.3 + Redis 8 once per JVM, runs Liquibase (`test-changelog.xml`) on context start, and sets the test tenant context before each test (§10.2 fixtures).
    Create `SharedLibVerificationIT extends BaseIntegrationTest` — ONE harness proving all three success criteria (resolved decision #3):
      - SC3: assert the Spring context loaded the shared beans (FeatureFlagService, OpaClient, IdempotencyService, DomainEventPublisher, TenantTaskDecorator executor); assert TenantContext propagates across an `@Async` call (TenantTaskDecorator) AND across a RabbitMQ message via `TenantAwareMessageProcessor` (publish→consume, assert tenant seen in consumer).
      - SC4: assert `MoneyUtils` arithmetic/rounding on representative cases; run the `AbstractRlsCoverageTest` logic (from 01-02) against this schema — `widgets` PASSES (FORCE RLS + policy), and a negative fixture (a tenant_id table created WITHOUT its RLS changeset, added in a disposable schema/transaction) FAILS the guard, proving SC4 "RLS-or-fail".
      - SC5: call `DomainEventPublisher.publish(...)` inside a @Transactional method → row in `event_outbox`; let `OutboxRelay` publish to RabbitMQ; an idempotent consumer (using `IdempotencyService`/`processed_events`) processes it; redeliver the SAME event and assert it is processed EXACTLY ONCE.
    Honor RESEARCH gotchas: outbox JSON String round-trip via the shared ObjectMapper; OPA optional in dev; FORCE-RLS requires `set_config('app.current_tenant_id', ...)` before inserting widget rows in the test.
  </action>
  <verify>`mvn -pl shared-lib -am verify` runs `SharedLibVerificationIT` GREEN under Docker (Testcontainers pulls Postgres 18/RabbitMQ 4.3/Redis 8); the negative RLS fixture causes the guard assertion to fire (proven via the test's own assertThrows, not a build break); JaCoCo `shared-lib` ≥ 70% (agent-spec 10 §10.7).</verify>
  <done>One Testcontainers IT proves SC3 (beans + @Async + RabbitMQ tenant propagation), SC4 (Money math + RLS-or-fail guard pass/fail), and SC5 (outbox → relay → idempotent exactly-once consumer); the three §8.9 infra tables exist and are RLS-exempt.</done>
</task>

</tasks>

<verification>
- `mvn -pl shared-lib -am -DskipTests compile` succeeds for all §3.x classes.
- grep confirms NO `SecurityFilterChain`/`@EnableWebSecurity` bean in shared-lib `src/main` (security shipped, not wired — decision #6).
- `META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports` registers the shared auto-config.
- `mvn -pl shared-lib -am verify` runs `SharedLibVerificationIT` GREEN via Testcontainers (Postgres 18 + RabbitMQ 4.3 + Redis 8), proving SC3 + SC4 + SC5; JaCoCo shared-lib ≥ 70%.
- `event_outbox`, `idempotency_keys`, `processed_events` created by `test-changelog.xml` and excluded from RLS; `widgets` has FORCE RLS + policy.
</verification>

<success_criteria>
shared-lib loads in a Spring context with @Async + RabbitMQ tenant propagation working (SC3); MoneyUtils + the RLS-or-fail guard pass/fail correctly (SC4); transactional outbox delivers to an idempotent exactly-once consumer (SC5). Requirements covered: LIB-01..06, XCUT-02, XCUT-03, XCUT-04, XCUT-05, XCUT-06 (XCUT-01 shared with 01-02).
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-foundation-shared-library/01-04-SUMMARY.md`.
</output>
