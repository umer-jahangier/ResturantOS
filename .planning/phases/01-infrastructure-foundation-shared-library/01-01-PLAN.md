---
phase: 01-infrastructure-foundation-shared-library
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pom.xml
  - shared-lib/pom.xml
  - eureka-server/pom.xml
  - eureka-server/Dockerfile
  - eureka-server/src/main/java/io/restaurantos/eureka/EurekaServerApplication.java
  - eureka-server/src/main/resources/application.yml
  - config-server/pom.xml
  - config-server/Dockerfile
  - config-server/src/main/java/io/restaurantos/configserver/ConfigServerApplication.java
  - config-server/src/main/resources/application.yml
  - deploy/docker-compose.yml
  - deploy/Makefile
  - Makefile
  - deploy/init/clickhouse-init.sql
  - policies/restaurantos/common.rego
  - .gitignore
autonomous: true

must_haves:
  truths:
    - "`make dev-up` brings PostgreSQL, Redis, RabbitMQ, MinIO, OPA, Eureka, Config Server, ClickHouse, Mailpit and pgAdmin up via docker compose"
    - "eureka-server and config-server are real Spring Boot modules whose images are built locally by docker compose (no ghcr.io pull)"
    - "the Maven reactor builds with only shared-lib, eureka-server, config-server declared as modules"
    - "OPA loads at least one policy from the mounted policies/ directory"
  artifacts:
    - path: "pom.xml"
      provides: "Maven parent restaurantos-parent 1.0.0 with Boot 3.3.5 parent, version properties, Phase-1 module list, dependencyManagement (spring-cloud 2023.0.3, testcontainers-bom 1.20.3, shared-lib)"
      contains: "spring-cloud-dependencies"
    - path: "deploy/docker-compose.yml"
      provides: "All 9 infra services + mailpit/pgadmin, eureka/config built locally"
      contains: "build:"
    - path: "deploy/Makefile"
      provides: "make dev-up / dev-down / dev-logs wrapping docker compose + first-run steps"
      contains: "dev-up"
    - path: "eureka-server/src/main/java/io/restaurantos/eureka/EurekaServerApplication.java"
      provides: "@EnableEurekaServer app on port 8761"
      contains: "@EnableEurekaServer"
    - path: "config-server/src/main/java/io/restaurantos/configserver/ConfigServerApplication.java"
      provides: "@EnableConfigServer app on port 8888"
      contains: "@EnableConfigServer"
    - path: "shared-lib/pom.xml"
      provides: "shared-lib jar module placeholder so the reactor resolves (sources land in 01-02/01-04)"
      contains: "shared-lib"
  key_links:
    - from: "deploy/docker-compose.yml"
      to: "eureka-server/Dockerfile"
      via: "build.context=.. build.dockerfile=eureka-server/Dockerfile"
      pattern: "eureka-server/Dockerfile"
    - from: "deploy/docker-compose.yml"
      to: "policies/"
      via: "opa volume mount ../policies:/policies:ro"
      pattern: "/policies"
---

<objective>
Stand up the dev-infrastructure spine and the Maven multi-module skeleton so every later plan has a buildable reactor and a runnable local stack.

Purpose: SC1 requires `make dev-up` to bring the full infrastructure to healthy. The compose file in agent-spec 06 references `ghcr.io/restaurantos/eureka-server:1.0.0` and `config-server:1.0.0` images that do not exist — Phase 1 must scaffold those two Spring Boot modules and build them locally. Without a parent POM listing only the modules that exist, `mvn` fails.
Output: parent `pom.xml`, `shared-lib/pom.xml` (empty jar), `eureka-server` + `config-server` modules with Dockerfiles, `deploy/docker-compose.yml` (local builds for eureka/config), root + `deploy/Makefile` with `dev-up`, the ClickHouse init file, and an OPA placeholder policy.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infrastructure-foundation-shared-library/01-RESEARCH.md

# Source of truth — transcribe, do not invent:
@Docs/agent-specs/01-project-scaffold.md
@Docs/agent-specs/06-dev-docker-compose.md
@Docs/agent-specs/05-environment-variables.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Maven parent POM + shared-lib/eureka/config module skeletons</name>
  <files>pom.xml, shared-lib/pom.xml, eureka-server/pom.xml, eureka-server/src/main/java/io/restaurantos/eureka/EurekaServerApplication.java, eureka-server/src/main/resources/application.yml, config-server/pom.xml, config-server/src/main/java/io/restaurantos/configserver/ConfigServerApplication.java, config-server/src/main/resources/application.yml, .gitignore</files>
  <action>
    Create the root `pom.xml` by transcribing agent-spec 01 §1.2 VERBATIM EXCEPT the `<modules>` block: per resolved decision #1, declare ONLY:
      <module>shared-lib</module>
      <module>eureka-server</module>
      <module>config-server</module>
    Do NOT add the 15 `services/*` modules or `gateway` (they are scaffolded in later phases; listing them now breaks the reactor). Keep `groupId=io.restaurantos`, `artifactId=restaurantos-parent`, `version=1.0.0`, `packaging=pom`, parent `spring-boot-starter-parent 3.3.5`, and properties `java.version=21`, `spring-cloud.version=2023.0.3`, `mapstruct.version=1.6.2` (resolved decision #5 — NOT 1.5.x), `jjwt.version=0.12.6`, `testcontainers.version=1.20.3`.
    In `dependencyManagement`, keep the `spring-cloud-dependencies` import and the `io.restaurantos:shared-lib:1.0.0` entry, and ADD the missing `testcontainers-bom` import (agent-spec 01 / RESEARCH Pitfall 8):
      org.testcontainers:testcontainers-bom:${testcontainers.version}, type=pom, scope=import.

    Create `shared-lib/pom.xml`: a `jar` module with parent restaurantos-parent, `artifactId=shared-lib`, the compile dependencies listed in agent-spec 03 §3.1 (spring-boot-starter-web, -data-jpa, -security, -amqp, -aop, -data-redis; jjwt-api/impl/jackson at ${jjwt.version}; org.mapstruct:mapstruct at ${mapstruct.version}; lombok; net.logstash.logback:logstash-logback-encoder at 7.4; com.nimbusds:nimbus-jose-jwt at 9.40 — RESEARCH "Supporting libs"), plus test deps (spring-boot-starter-test, testcontainers junit-jupiter/postgresql/rabbitmq, org.awaitility:awaitility). Configure `maven-compiler-plugin` `annotationProcessorPaths` in the order lombok → lombok-mapstruct-binding → mapstruct-processor (RESEARCH Pitfall 7). NO sources yet — this is an empty buildable jar; sources arrive in 01-02 and 01-04.

    Create `eureka-server` module (base package `io.restaurantos.eureka` per §1.7): `pom.xml` with parent + `spring-cloud-starter-netflix-eureka-server`; `EurekaServerApplication.java` annotated `@SpringBootApplication @EnableEurekaServer`; `application.yml` with `server.port: ${SERVER_PORT:8761}`, `eureka.client.register-with-eureka: false`, `fetch-registry: false`, and `management.endpoints.web.exposure.include: health` + `management.endpoint.health.probes.enabled: true`.

    Create `config-server` module (base package `io.restaurantos.configserver`): `pom.xml` with parent + `spring-cloud-config-server` + `spring-cloud-starter-netflix-eureka-client`; `ConfigServerApplication.java` annotated `@SpringBootApplication @EnableConfigServer`; `application.yml` with `server.port: ${SERVER_PORT:8888}`, `spring.cloud.config.server.git.uri: ${CONFIG_GIT_URI}`, `eureka.client.service-url.defaultZone: ${EUREKA_URL:http://eureka:8761/eureka}`, health actuator exposed.

    Create `.gitignore` at repo root ignoring `deploy/.env`, `target/`, `*.pem`, and IDE dirs (RESEARCH "Don't Hand-Roll": no committed dev keys).
  </action>
  <verify>`mvn -N validate` at repo root succeeds; `mvn -pl eureka-server,config-server -am -q package` builds both jars; the parent `<modules>` lists exactly shared-lib, eureka-server, config-server.</verify>
  <done>Root reactor resolves and builds with three modules; eureka-server and config-server produce runnable Spring Boot jars; `.gitignore` excludes `deploy/.env` and `*.pem`.</done>
</task>

<task type="auto">
  <name>Task 2: Dockerfiles for eureka/config + docker-compose.yml with local builds</name>
  <files>eureka-server/Dockerfile, config-server/Dockerfile, deploy/docker-compose.yml, deploy/init/clickhouse-init.sql, policies/restaurantos/common.rego</files>
  <action>
    Create `eureka-server/Dockerfile` and `config-server/Dockerfile` as multi-stage builds: stage 1 `maven:3.9-eclipse-temurin-21` with build context = repo root — COPY the parent `pom.xml` and the module dir, run `mvn -pl <module> -am -q -DskipTests package`; stage 2 `eclipse-temurin:21-jre`, COPY the built jar, `ENTRYPOINT ["java","-jar","/app.jar"]`, EXPOSE the module port.

    Create `deploy/docker-compose.yml` by transcribing agent-spec 06 §6.1 VERBATIM, with these resolved-decision changes:
      - Replace the `eureka` service `image: ghcr.io/restaurantos/eureka-server:1.0.0` with a `build:` stanza (`context: ..`, `dockerfile: eureka-server/Dockerfile`) plus `image: restaurantos/eureka-server:1.0.0` (resolved decision #2).
      - Same for `config-server` (`dockerfile: config-server/Dockerfile`, `image: restaurantos/config-server:1.0.0`).
      - Fix the MinIO healthcheck (RESEARCH Pitfall 4): replace `["CMD","mc","ready","local"]` with `["CMD-SHELL","mc ready local || wget -q --spider http://localhost:9000/minio/health/live"]` so it reports healthy on the pinned RELEASE.2024-09-13 image.
    Keep ALL pinned image tags exactly (postgres:16.4, redis:7.4, rabbitmq:3.13-management, minio RELEASE.2024-09-13T20-26-02Z, openpolicyagent/opa:0.65.0, clickhouse/clickhouse-server:24.8, axllent/mailpit:v1.20, dpage/pgadmin4:8.12), the named volumes, the `restaurantos` network, the postgres init mounts (`./init/01-create-databases.sql`, `./init/02-create-roles.sql` — files land in 01-02), the rabbitmq mounts (`./init/rabbitmq-definitions.json`, `./init/rabbitmq.conf` — land in 01-03), the OPA mount `../policies:/policies:ro`, and the clickhouse init mount `./init/clickhouse-init.sql`.

    Create `deploy/init/clickhouse-init.sql` (RESEARCH Pitfall 11 — the mount fails if the file is missing): a minimal file `CREATE DATABASE IF NOT EXISTS clickhouse_analytics;`.

    Create `policies/restaurantos/common.rego` placeholder (RESEARCH Pitfall 9 — so `GET /v1/policies | grep restaurantos` passes): `package restaurantos.common` with the three helper rules from agent-spec 09 §9.4 (`same_tenant_and_branch`, `same_tenant`, `has_permission`). Real per-domain Rego arrives in Phase 2.
  </action>
  <verify>`docker compose -f deploy/docker-compose.yml config -q` validates with no errors; `docker compose -f deploy/docker-compose.yml build eureka config-server` produces `restaurantos/eureka-server:1.0.0` and `restaurantos/config-server:1.0.0`; `grep -c "ghcr.io" deploy/docker-compose.yml` returns 0.</verify>
  <done>Compose validates, builds eureka/config locally, mounts policies + clickhouse init, and contains no nonexistent ghcr.io image references.</done>
</task>

<task type="auto">
  <name>Task 3: Makefile with dev-up / dev-down / dev-logs + first-run steps</name>
  <files>Makefile, deploy/Makefile</files>
  <action>
    Create `deploy/Makefile` with targets wrapping the agent-spec 06 §6.6 first-run flow:
      - `dev-up`: `docker compose up -d --build` then run the §6.6 health/verification steps and create the MinIO bucket (`docker exec restaurantos-minio mc alias set local http://localhost:9000 "$$MINIO_ACCESS_KEY" "$$MINIO_SECRET_KEY"` then `mc mb --ignore-existing local/$$MINIO_BUCKET`).
      - `dev-down`: `docker compose down`.
      - `dev-logs`: `docker compose logs -f`.
      - `dev-ps`: `docker compose ps`.
    Targets must `cd` into `deploy/` (or use `--project-directory`) and load `deploy/.env` (compose auto-loads `.env` from its dir). Add a guard target that fails with a clear message if `deploy/.env` is missing (it is produced by 01-03's generate-keys.sh).

    Create a thin root `Makefile` that delegates: `dev-up: ; $(MAKE) -C deploy dev-up` (and dev-down/dev-logs/dev-ps), so `make dev-up` works from the repo root exactly as SC1 phrases it.
  </action>
  <verify>`make -n dev-up` (dry run) from repo root expands to a `docker compose up -d --build` invocation in deploy/; `make -C deploy dev-down` runs without error after a bring-up.</verify>
  <done>`make dev-up` from the repo root is the single command that builds and starts the stack; dev-down/dev-logs/dev-ps work.</done>
</task>

</tasks>

<verification>
- `mvn -N validate` and `mvn -pl eureka-server,config-server -am package` succeed.
- `docker compose -f deploy/docker-compose.yml config -q` validates; `build eureka config-server` succeeds; zero `ghcr.io` references remain.
- After 01-02 (DB init SQL) and 01-03 (.env + rabbitmq defs) land, `make dev-up` brings all services to healthy (`docker compose ps` shows healthy) — this combined SC1 check is re-asserted in 01-04's verification.
- `make -n dev-up` from repo root delegates into deploy/.
</verification>

<success_criteria>
The Maven reactor builds with exactly three modules; eureka-server and config-server build into local Docker images consumed by compose; `make dev-up` is wired; OPA has a loadable placeholder policy; no broken image references. (Contributes to SC1.)
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-foundation-shared-library/01-01-SUMMARY.md`.
</output>
