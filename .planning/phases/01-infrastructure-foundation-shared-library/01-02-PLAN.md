---
phase: 01-infrastructure-foundation-shared-library
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - deploy/init/01-create-databases.sql
  - deploy/init/02-create-roles.sql
  - shared-lib/src/main/java/io/restaurantos/shared/entity/TenantAuditableEntity.java
  - shared-lib/src/test/java/io/restaurantos/shared/testsupport/AbstractRlsCoverageTest.java
  - docs/conventions/rls-convention.md
autonomous: true

must_haves:
  truths:
    - "psql shows all 13 service databases, each granted to a least-privilege NOSUPERUSER NOBYPASSRLS role"
    - "service roles can set the per-transaction app.current_tenant_id GUC (no static ALTER ROLE SET)"
    - "every entity extends TenantAuditableEntity, which carries tenant_id + audit + soft-delete and defines the Hibernate tenantFilter"
    - "a tenant-scoped table created without an immediate RLS changeset fails the migration/build check"
  artifacts:
    - path: "deploy/init/01-create-databases.sql"
      provides: "CREATE DATABASE for all 13 service databases"
      contains: "CREATE DATABASE platform_db"
    - path: "deploy/init/02-create-roles.sql"
      provides: "13 NOSUPERUSER NOBYPASSRLS login roles + per-db GRANTs"
      contains: "NOSUPERUSER NOBYPASSRLS"
    - path: "shared-lib/src/main/java/io/restaurantos/shared/entity/TenantAuditableEntity.java"
      provides: "@MappedSuperclass with @FilterDef/@Filter tenantFilter, Instant TIMESTAMPTZ audit fields, deleted_at soft delete"
      contains: "@FilterDef(name = \"tenantFilter\""
    - path: "shared-lib/src/test/java/io/restaurantos/shared/testsupport/AbstractRlsCoverageTest.java"
      provides: "Reusable post-Liquibase guard that fails if any tenant_id table lacks FORCE RLS + a policy"
      contains: "pg_policies"
  key_links:
    - from: "shared-lib/src/test/.../AbstractRlsCoverageTest.java"
      to: "information_schema.columns / pg_class / pg_policies"
      via: "native queries after Liquibase runs against Testcontainers Postgres"
      pattern: "relforcerowsecurity"
    - from: "deploy/init/02-create-roles.sql"
      to: "deploy/init/01-create-databases.sql"
      via: "GRANT ALL PRIVILEGES ON DATABASE <db> TO <role> (runs after database creation)"
      pattern: "GRANT ALL PRIVILEGES ON DATABASE"
---

<objective>
Establish multi-tenant data isolation at both the database and ORM layers: the 13 least-privilege databases/roles, the `TenantAuditableEntity` base class that every tenant-scoped entity extends, the RLS-changeset convention, and the runtime guard that makes a missing RLS policy fail the build.

Purpose: SC1 (13 databases each owned by a least-privilege role that can SET `app.current_tenant_id`) and SC4 (a tenant-scoped table without an immediate RLS changeset fails the build) both live here. Defense-in-depth = Hibernate `@Filter` (defined on the entity) + Postgres `FORCE ROW LEVEL SECURITY` with roles that are `NOSUPERUSER NOBYPASSRLS`.
Output: `deploy/init/01-create-databases.sql`, `deploy/init/02-create-roles.sql`, `TenantAuditableEntity`, the reusable `AbstractRlsCoverageTest` guard, and an RLS convention doc executors of later phases will follow.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-infrastructure-foundation-shared-library/01-RESEARCH.md

# Source of truth — transcribe, do not invent:
@Docs/agent-specs/06-dev-docker-compose.md
@Docs/agent-specs/03-shared-lib-specification.md
@Docs/agent-specs/08-database-migration-guide.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: PostgreSQL database + least-privilege role init scripts</name>
  <files>deploy/init/01-create-databases.sql, deploy/init/02-create-roles.sql</files>
  <action>
    Create `deploy/init/01-create-databases.sql` by transcribing agent-spec 06 §6.3 VERBATIM: `CREATE DATABASE` for all 13 service databases — `platform_db, auth_db, user_db, pos_db, inventory_db, finance_db, purchasing_db, hr_db, crm_db, kitchen_db, notification_db, audit_db, file_db`. (auth_db is shared by Auth + Authorization services.)

    Create `deploy/init/02-create-roles.sql` by transcribing agent-spec 06 §6.3 VERBATIM: the `\set *_pw` lines reading each `*_DB_PASSWORD` env var, then `CREATE ROLE <svc>_user LOGIN PASSWORD :'<pw>' NOSUPERUSER NOBYPASSRLS;` for all 13 roles, then `GRANT ALL PRIVILEGES ON DATABASE <db> TO <role>;` for each. Keep the trailing comment that authorization-service connects to auth_db using auth_user.

    CRITICAL (RESEARCH Pitfall 5, resolved decision in `<resolved_decisions>`): do NOT add any `ALTER ROLE ... SET app.current_tenant_id` line. SC1's "role has the app.current_tenant_id SET parameter" is satisfied by the application setting it per-transaction via `set_config('app.current_tenant_id', ..., true)`; a static ALTER ROLE SET would pin one tenant. The custom GUC needs no pre-registration because the name is namespaced (contains a dot). Because roles are NOBYPASSRLS, leave the postgres superuser for Liquibase DDL but document that services connect as their `<svc>_user`.

    Both files are mounted into postgres at `/docker-entrypoint-initdb.d/` by the compose file from 01-01 and run on first volume init only.
  </action>
  <verify>After `docker compose up -d postgres`: `docker exec restaurantos-postgres psql -U postgres -c "\l"` lists all 13 databases; `docker exec restaurantos-postgres psql -U postgres -c "\du"` shows 13 `*_user` roles each with attributes excluding Superuser and Bypass RLS; connecting as a service role and running `SELECT set_config('app.current_tenant_id','00000000-0000-0000-0000-000000000000',true)` succeeds.</verify>
  <done>13 databases exist; 13 NOSUPERUSER NOBYPASSRLS roles own their databases; service roles can set the transaction-local tenant GUC; no static ALTER ROLE SET exists.</done>
</task>

<task type="auto">
  <name>Task 2: TenantAuditableEntity base class + RLS convention doc</name>
  <files>shared-lib/src/main/java/io/restaurantos/shared/entity/TenantAuditableEntity.java, docs/conventions/rls-convention.md</files>
  <action>
    Create `TenantAuditableEntity.java` by transcribing agent-spec 03 §3.2 VERBATIM into package `io.restaurantos.shared.entity`: `@Getter @Setter @MappedSuperclass @EntityListeners(AuditingEntityListener.class)`, `@FilterDef(name="tenantFilter", parameters=@ParamDef(name="tenantId", type=UUID.class))`, `@Filter(name="tenantFilter", condition="tenant_id = :tenantId")`, abstract class implementing Serializable with fields: `tenantId` (UUID, nullable=false, updatable=false), `createdAt`/`updatedAt` (Instant → TIMESTAMPTZ, the XCUT-04 convention), `createdBy`/`updatedBy` (UUID via @CreatedBy/@LastModifiedBy), `deletedAt` (Instant soft delete), and `isDeleted()`. This is the only shared-lib source this plan adds; the `AuditorAware` bean that backs @CreatedBy/@LastModifiedBy is wired in 01-04 (it depends on TenantContext).

    Create `docs/conventions/rls-convention.md` capturing the normative pattern from agent-spec 08 §8.6 so every later phase follows it: (1) every tenant-scoped `createTable` is IMMEDIATELY followed by an RLS changeset that runs `ALTER TABLE <t> ENABLE ROW LEVEL SECURITY; ALTER TABLE <t> FORCE ROW LEVEL SECURITY; CREATE POLICY tenant_isolation ON <t> USING (tenant_id = current_setting('app.current_tenant_id', true)::uuid); GRANT SELECT,INSERT,UPDATE,DELETE ON <t> TO <svc>_user;`; (2) the three shared infra tables (`event_outbox`, `idempotency_keys`, `processed_events`) are explicitly EXEMPT (no RLS — they run outside tenant context); (3) money is BIGINT paisa, timestamps TIMESTAMPTZ, financial FKs use ON DELETE RESTRICT + soft delete (agent-spec 08 §8.12). Note the RESEARCH Pitfall 6 caveat: seeding rows under FORCE RLS requires setting app.current_tenant_id (or seeding before enabling RLS) — relevant for Finance/HR seeds in later phases, not Phase 1.
  </action>
  <verify>`shared-lib` compiles (`mvn -pl shared-lib -am -q -DskipTests compile`); `TenantAuditableEntity` contains the `tenantFilter` @FilterDef and Instant-typed audit fields; the convention doc enumerates the ENABLE+FORCE+POLICY+GRANT sequence and the three exempt tables.</verify>
  <done>TenantAuditableEntity compiles in shared-lib with the tenantFilter definition and TIMESTAMPTZ/Instant audit columns; the RLS convention is documented for later phases.</done>
</task>

<task type="auto">
  <name>Task 3: AbstractRlsCoverageTest — the "RLS-or-fail" runtime guard</name>
  <files>shared-lib/src/test/java/io/restaurantos/shared/testsupport/AbstractRlsCoverageTest.java</files>
  <action>
    Per resolved decision #4 and RESEARCH "How to enforce SC4", create a reusable abstract test in `shared-lib/src/test/java/io/restaurantos/shared/testsupport/` that runs AFTER Liquibase against a Postgres connection (injected `EntityManager` / `DataSource`) and:
      1. SELECTs candidate tenant-scoped tables: `SELECT table_name FROM information_schema.columns WHERE column_name='tenant_id' AND table_schema='public'`.
      2. Excludes the known non-tenant infra tables: `event_outbox`, `idempotency_keys`, `processed_events`.
      3. For each remaining table asserts BOTH `relrowsecurity=true` AND `relforcerowsecurity=true` (`SELECT relrowsecurity, relforcerowsecurity FROM pg_class WHERE relname=?`) AND at least one policy (`SELECT count(*) FROM pg_policies WHERE tablename=?` >= 1).
      4. FAILS (assertion error → `mvn verify` fails) listing every offending table, so a tenant-scoped table shipped without its RLS changeset breaks the build.
    Make it `abstract` and overridable (exclusions list as a protected method) so every service's integration test module can extend it. This class is test-support shipped IN shared-lib so later services inherit it; it is exercised against the `widgets` sample table (passing) and a deliberately-omitted-RLS fixture (failing) by the verification harness built in 01-04.
    Do NOT wire any Testcontainers/Spring bootstrap here (that lives in 01-04's `BaseIntegrationTest`); this class only contains the query+assert logic given an EntityManager.
  </action>
  <verify>`mvn -pl shared-lib -am -q -DskipTests test-compile` compiles the test-support class; it references `information_schema.columns`, `pg_class.relforcerowsecurity`, and `pg_policies`, and excludes the three infra tables.</verify>
  <done>A reusable guard exists that, post-Liquibase, fails the build for any tenant_id-bearing table lacking FORCE RLS + a policy, excluding the three infra tables. (Proven end-to-end in 01-04.)</done>
</task>

</tasks>

<verification>
- `docker compose up -d postgres` then `psql \l` shows 13 databases and `\du` shows 13 NOSUPERUSER NOBYPASSRLS roles.
- `mvn -pl shared-lib -am -DskipTests compile` and `test-compile` succeed with the new entity + guard.
- No `ALTER ROLE ... SET app.current_tenant_id` appears in `02-create-roles.sql`.
- The RLS convention doc documents the ENABLE/FORCE/POLICY/GRANT sequence and the three RLS-exempt infra tables.
</verification>

<success_criteria>
13 least-privilege databases/roles exist (SC1, INFRA-02); TenantAuditableEntity establishes the tenant_id + audit + soft-delete + Hibernate-filter contract (XCUT-01, XCUT-04, contributes to LIB-01); the RLS-or-fail guard is in place to fail the build for any unprotected tenant-scoped table (SC4, XCUT-01).
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-foundation-shared-library/01-02-SUMMARY.md`.
</output>
