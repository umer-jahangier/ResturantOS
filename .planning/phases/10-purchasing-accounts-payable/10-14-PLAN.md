---
phase: 10-purchasing-accounts-payable
plan: 14
type: execute
wave: 3
gap_closure: true
depends_on: ["10-10", "10-11"]
files_modified:
  - frontend/lib/api-client/schemas/finance.schema.ts
  - frontend/lib/adapters/finance.adapter.ts
  - frontend/lib/repositories/finance.repository.ts
  - frontend/lib/hooks/finance/use-finance.ts
  - frontend/app/(tenant)/app/finance/expenses/page.tsx
  - frontend/app/(tenant)/app/finance/ap-aging/page.tsx
  - frontend/app/(tenant)/app/finance/layout.tsx
  - frontend/components/finance/ExpenseFormDialog.tsx
  - frontend/components/finance/ApAgingTable.tsx
  - frontend/mocks/finance.handlers.ts
  - frontend/mocks/handlers.ts
  - frontend/__tests__/lib/finance-expense-journey.test.ts
autonomous: true

must_haves:
  truths:
    - "A user can create an expense from the Finance module and see it PENDING_APPROVAL"
    - "An approver can approve an expense within their OPA limit and see a balanced journal entry posted"
    - "An approver over their OPA limit gets a clear 'exceeds your approval limit' message and NO journal entry is posted"
    - "An approver can reject an expense with a mandatory reason"
    - "A user can see the AP aging report (Current / 31-60 / 61-90 / Over 90) with amounts"
  artifacts:
    - path: "frontend/app/(tenant)/app/finance/expenses/page.tsx"
      provides: "expense list + create + approve/reject actions (no expense UI exists at all today)"
      min_lines: 40
    - path: "frontend/app/(tenant)/app/finance/ap-aging/page.tsx"
      provides: "AP aging buckets (GET /api/v1/finance/ap/aging has zero frontend consumers today)"
    - path: "frontend/components/finance/ApAgingTable.tsx"
      provides: "bucketed AP aging table"
  key_links:
    - from: "frontend/app/(tenant)/app/finance/expenses/page.tsx"
      to: "GET /api/v1/finance/expenses + POST /{id}/approve|reject"
      via: "useExpenses / useApproveExpense / useRejectExpense"
      pattern: "useApproveExpense"
    - from: "frontend/app/(tenant)/app/finance/ap-aging/page.tsx"
      to: "GET /api/v1/finance/ap/aging"
      via: "useApAging -> FinanceRepository.getApAging"
      pattern: "ap/aging"
---

<objective>
Build the FIN-05 user journeys: expense create/approve/reject, and the AP aging report.

Purpose: ROADMAP SC#4 is "AP/AR balances are tracked, and expense approvals respect OPA approval limits". Today `GET /api/v1/finance/ap/aging` works but has NO frontend consumer at any layer and no page (UAT test 9, blocked). And there is NO expense UI at all — create, approve or reject (UAT test 16, blocked). 10-05 shipped a fully OPA-gated `ExpenseService` that no human can invoke. A requirement whose only consumer is an integration test is not shipped.

Output: an expense worklist with approve/reject, and an AP aging page — both on the real backend.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-purchasing-accounts-payable/10-10-SUMMARY.md   # expense list endpoint contract

@frontend/lib/repositories/finance.repository.ts
@frontend/lib/hooks/finance/
@frontend/app/(tenant)/app/finance/layout.tsx
@frontend/app/(tenant)/app/finance/gl/page.tsx      # page + tab conventions to copy
@services/finance-service/src/main/java/io/restaurantos/finance/web/ExpenseController.java
@services/finance-service/src/main/java/io/restaurantos/finance/web/ApArController.java
@services/finance-service/src/main/java/io/restaurantos/finance/dto/CreateExpenseRequest.java

Key constraints:
- Four-layer boundary (04-01-E) + paisa money + `MoneyDisplay`, exactly as in the finance pages that
  already exist (accounts, gl, journal-entries, periods) — copy their structure.
- These are FINANCE-module files (`finance.*` schema/adapter/repository/hooks, `/app/finance/*` routes,
  `mocks/finance.handlers.ts`). They must NOT touch any `purchasing.*` file — that is what lets this plan
  run in the same wave as 10-12 without a conflict. If you find yourself editing
  `frontend/lib/repositories/purchasing.repository.ts`, stop: you are in the wrong plan.
- Approve returns 403 `EXPENSE_APPROVAL_LIMIT_EXCEEDED` when OPA denies (10-05). After 10-07 that path is
  actually reachable against a real OPA — before 10-07 it denied EVERYTHING, so do not "fix" a blanket
  denial you observe locally by weakening the UI; check that 10-07 landed first.
- Vitest tests under `frontend/__tests__/**` (04-05-D).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Finance data layer for expenses + AP aging</name>
  <files>
    frontend/lib/api-client/schemas/finance.schema.ts
    frontend/lib/adapters/finance.adapter.ts
    frontend/lib/repositories/finance.repository.ts
    frontend/lib/hooks/finance/use-finance.ts
    frontend/mocks/finance.handlers.ts
    frontend/mocks/handlers.ts
  </files>
  <action>
Read the Java DTOs first (`CreateExpenseRequest`, `ExpenseDto`, `ApAgingReportDto`) — do not guess.

1. `finance.schema.ts`: `apiExpenseSchema` (id, branchId, expenseDate, expenseAccountCode, description,
   amountPaisa, status PENDING_APPROVAL|APPROVED|REJECTED, requestedBy, approvedBy, approvedAt,
   rejectionReason), `createExpenseInputSchema`, `rejectExpenseInputSchema` ({reason: min 1}),
   `apiApAgingSchema` (buckets: Current / 31-60 / 61-90 / Over 90 with amountPaisa + invoice counts — mirror
   `ApAgingReportDto` exactly).
2. `finance.adapter.ts`: `adaptExpense`, `adaptApAging`.
3. `finance.repository.ts`: `listExpenses(branchId, status?)`, `createExpense(input)`, `approveExpense(id)`,
   `rejectExpense(id, reason)`, `getApAging(branchId, asOf?)`. Always `.parse()`.
4. Hooks: `useExpenses`, `useCreateExpense`, `useApproveExpense(id)`, `useRejectExpense(id)`, `useApAging`.
   Mutations invalidate the expense list key. Errors surface as `ApiError` through TanStack inference
   (04-02-C) so the page can special-case `EXPENSE_APPROVAL_LIMIT_EXCEEDED`.
5. `mocks/finance.handlers.ts` (new) + register it in `mocks/handlers.ts` (this is the ONLY shared mocks
   file this plan touches; 10-12/10-13 touch `purchasing.handlers.ts` only, so there is no conflict).
   Model the OPA limit in the mock: approving an expense above a fixture approval limit returns 403
   `EXPENSE_APPROVAL_LIMIT_EXCEEDED`. Real hex UUIDs.
  </action>
  <verify>
`cd frontend && npx tsc --noEmit && npx eslint lib components` clean; `npx vitest run` — prior tests green.
`grep -rn "purchasing" frontend/lib/repositories/finance.repository.ts` — zero hits (no cross-module leakage).
  </verify>
  <done>Every FIN-05 endpoint (expense CRUD + approve/reject + AP aging) has a Zod-validated repository fn and a hook.</done>
</task>

<task type="auto">
  <name>Task 2: Expenses page (create / approve / reject) + finance nav tab</name>
  <files>
    frontend/app/(tenant)/app/finance/expenses/page.tsx
    frontend/components/finance/ExpenseFormDialog.tsx
    frontend/app/(tenant)/app/finance/layout.tsx
  </files>
  <action>
1. `finance/layout.tsx` — add "Expenses" and "AP Aging" tabs to the finance tab bar, matching the existing
   tab component's shape exactly.

2. `ExpenseFormDialog.tsx` — create form: expense account (select over the CoA expense accounts — use the
   existing accounts hook the `/app/finance/accounts` page uses; do not fetch the CoA a second way),
   expense date, description, amount (rupees in, paisa out). `useCreateExpense`. Modelled on
   `VendorFormDialog.tsx`'s dialog + `createZodResolver` + mutation shape.

3. `finance/expenses/page.tsx` — client component. `useExpenses(branchId)`. Table: date, account,
   description, amount (`MoneyDisplay`), status badge, requested-by. Status filter (default:
   PENDING_APPROVAL — this is an approver's inbox, so show the work first). Per-row Approve / Reject
   actions on PENDING_APPROVAL rows (Reject opens a mandatory-reason dialog).
   Error handling is the point of this page: when approve returns 403 `EXPENSE_APPROVAL_LIMIT_EXCEEDED`,
   show an explicit destructive toast — "This expense exceeds your approval limit" — NOT a generic
   "Something went wrong". The whole requirement is that the limit is visible and respected.
   Loading skeleton + empty state.
  </action>
  <verify>
`cd frontend && npx next build` succeeds.
Real-path manual (stack up, 10-07 landed, real OPA): as a MANAGER whose `approval_limit_paisa` is below the amount, approve a large expense -> destructive toast naming the approval limit, expense stays PENDING_APPROVAL, and `GET /api/v1/finance/gl/balances` shows NO new journal entry. As a user within limit, approve -> APPROVED and a balanced JE (DR expense / CR 1110 bank) appears in the journal-entries page.
  </verify>
  <done>UAT test 16 is executable from the UI, including the over-limit denial with no JE posted.</done>
</task>

<task type="auto">
  <name>Task 3: AP aging page + expense journey test</name>
  <files>
    frontend/app/(tenant)/app/finance/ap-aging/page.tsx
    frontend/components/finance/ApAgingTable.tsx
    frontend/__tests__/lib/finance-expense-journey.test.ts
  </files>
  <action>
1. `ApAgingTable.tsx` — renders the four buckets (Current, 31-60, 61-90, Over 90) with amount
   (`MoneyDisplay`) and invoice count per bucket, plus a total row. Highlight "Over 90" with the warning/
   destructive semantic token (04-04-B) — an aging report whose whole job is to show overdue money should
   make overdue money obvious.
2. `finance/ap-aging/page.tsx` — `useApAging(branchId)`. Renders `ApAgingTable`. Loading skeleton + empty
   state ("No outstanding payables"). This is the first frontend consumer of `GET /api/v1/finance/ap/aging`.
3. `frontend/__tests__/lib/finance-expense-journey.test.ts` — vitest + MSW through the REAL
   repository/hooks:
   - create -> PENDING_APPROVAL
   - approve within limit -> APPROVED
   - approve over limit -> the repository surfaces an `ApiError` with code
     `EXPENSE_APPROVAL_LIMIT_EXCEEDED` (assert the CODE, not just that it threw — a generic throw would
     let the UI show a generic message, which is the failure mode we are fixing)
   - reject with an empty reason -> rejected client-side by the Zod schema
   - `getApAging` parses and adapts the four buckets, and a zero-payables response yields empty buckets,
     not a crash
  </action>
  <verify>
`cd frontend && npx vitest run __tests__/lib/finance-expense-journey.test.ts` — green.
Real-path manual: `/app/finance/ap-aging` renders buckets whose Current-bucket total equals the sum of the unpaid invoices you booked in 10-13.
  </verify>
  <done>UAT test 9 is executable; AP aging has a UI and the expense journey is covered end-to-end.</done>
</task>

</tasks>

<verification>
1. `cd frontend && npx tsc --noEmit && npx eslint . && npx vitest run && npx next build` — all clean.
2. `grep -rn "ap/aging" frontend/lib/repositories/finance.repository.ts` — a real consumer exists (there was none).
3. Real-path: over-limit approve shows the limit-specific message AND posts no journal entry.
4. No file under `frontend/lib/**/purchasing*` or `frontend/components/purchasing/**` was modified by this plan.
</verification>

<success_criteria>
- ROADMAP SC#4 ("AP balances tracked; expense approvals respect OPA approval limits") is demonstrable by a human in the browser, not only by an integration test.
</success_criteria>

<output>
After completion, create `.planning/phases/10-purchasing-accounts-payable/10-14-SUMMARY.md`.
</output>
