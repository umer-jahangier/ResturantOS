---
phase: 10-purchasing-accounts-payable
plan: 06
type: execute
status: pending
wave: 4
depends_on: ["10-03", "10-04", "10-05"]
gap_closure: true
autonomous: true
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/phases/10-purchasing-accounts-payable/10-VERIFICATION.md
  - .planning/phases/10-purchasing-accounts-payable/10-UAT.md

must_haves:
  truths:
    - "REQUIREMENTS.md's PUR/FIN checkbox list and its traceability table AGREE with each other AND with the codebase — every [x]/Complete is backed by shipped, tested code"
    - "Each of PUR-01..PUR-06 and FIN-05 is marked complete ONLY IF a plan in this phase actually built it and its IT is green; anything still unbuilt stays [ ] / Pending with a note"
    - "The traceability table's pre-existing FALSE 'Complete' marks for PUR-05 (price variance never built) and FIN-05 (expense approval never built) are corrected — not propagated into the checkbox list"
    - "10-VERIFICATION.md records all three late-discovered escapes (PUR-02 CLOSED, PUR-05 price variance, FIN-05 expense approval) and how each was closed"
    - "10-UAT.md gains test cases for the newly shipped behavior (PO close, spend analytics, price variance, expense approval)"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Requirement status re-derived from actual coverage, checkbox list and table consistent"
    - path: ".planning/phases/10-purchasing-accounts-payable/10-VERIFICATION.md"
      provides: "Honest record of the three escaped gaps and their closure"
  key_links:
    - from: ".planning/REQUIREMENTS.md checkbox list"
      to: ".planning/REQUIREMENTS.md traceability table"
      via: "both re-derived from the same verified evidence; must not contradict"
---

<objective>
Reconcile the requirement documents ONCE, after every Phase 10 gap plan has landed — re-deriving each
row's status from what actually shipped rather than propagating stale claims.

Purpose: This exists as its own plan for two reasons.
(1) **Truth.** REQUIREMENTS.md's traceability table currently marks PUR-05 and FIN-05 "Complete" when
    they were NOT (price variance and OPA expense approval were never built — that's what 10-03 Task 4
    and 10-05 fix). Blindly syncing the checkbox list to that table would produce a document that is
    internally consistent and factually FALSE, destroying the `[ ]` signal that work is outstanding.
    Worse than the staleness it "fixes."
(2) **Race safety.** 10-03, 10-04 and 10-05 all run in parallel in wave 3. If each edited
    REQUIREMENTS.md we'd have three concurrent writers on one file. Lifting the doc write into a single
    wave-4 plan makes the wave-3 trio genuinely parallel AND lets this plan see the finished state of all
    three before it writes a word.

Output: an honest REQUIREMENTS.md, an updated 10-VERIFICATION.md, and UAT cases for the new behavior.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/10-purchasing-accounts-payable/10-VERIFICATION.md
@.planning/phases/10-purchasing-accounts-payable/10-UAT.md
@.planning/phases/10-purchasing-accounts-payable/10-03-SUMMARY.md
@.planning/phases/10-purchasing-accounts-payable/10-04-SUMMARY.md
@.planning/phases/10-purchasing-accounts-payable/10-05-SUMMARY.md
</context>

<critical_context priority="high">
**Do not trust the existing traceability table.** It is the thing being corrected. Its current state
(PUR-01..05 + FIN-05 = "Complete", PUR-06 = "Pending") is wrong in two rows, and that wrongness is what
let three requirements ship as silently unbuilt. The failure mode this plan exists to break: scoring a
requirement complete against under-scoped must-haves rather than against the requirement's own text.

**Verify before you tick.** For every row, re-read the requirement TEXT, then confirm in the codebase
that each clause of it is actually implemented and covered by a green test. A requirement with three
clauses (e.g. PUR-05: lead-time adherence + fill rate + price variance) is complete only when ALL THREE
exist.
</critical_context>

<tasks>

<task type="auto">
  <name>Task 1: Re-derive every PUR/FIN row from actual coverage and rewrite REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
    For EACH of PUR-01, PUR-02, PUR-03, PUR-04, PUR-05, PUR-06, FIN-05: read the requirement text, then
    verify each clause against the codebase + a green test. Use these checks (all should pass once
    wave 3 has landed — if any does NOT, leave that row `[ ]` / Pending and say why):

    | Req | Clauses to verify | Evidence to confirm |
    |-----|-------------------|---------------------|
    | PUR-01 | vendor CRUD; bank account field-encrypted | VendorService uses EncryptionService; API returns bankAccountLast4 only; `VendorIT` green |
    | PUR-02 | DRAFT→PENDING_APPROVAL→APPROVED→SENT→…→**CLOSED**; tiered OPA approval | `PurchaseOrderApprovalIT` + **`PurchaseOrderCloseIT`** (10-04) green; `grep -rn "PoStatus.CLOSED" services/purchasing-service/src/main/java/` returns a real assignment |
    | PUR-03 | GRN receipt posts GR/IR | `GrnReceiptSimulatorIT` green; DR 1300 / CR 1700 auto-post |
    | PUR-04 | 3-way match → AP; payment posts + AP_PAYMENT_PROCESSED | `ThreeWayMatchIT`, `PurchasingMockE2EIT` green |
    | PUR-05 | lead-time adherence + fill rate + **price variance** per vendor | **`VendorScorecardIT`** (10-03 Task 4) green; `grep -rn "priceVariancePct" services/purchasing-service/src/main/java/` returns hits |
    | PUR-06 | spend by vendor AND category, with period comparison | **`SpendAnalyticsIT`** (10-03) green; GET /analytics/spend returns byVendor + byCategory + prior deltas |
    | FIN-05 | AP/AR tracked **AND** expense approval respects OPA approval limits | `ApArController`/`ApAgingService` (10-02) **AND `ExpenseApprovalIT`** (10-05) green; `grep -rn "finance.expense.approve" services/finance-service/src/main/java/` returns a real OPA call |

    Then update REQUIREMENTS.md in BOTH places so they agree:
    1. The checkbox list (~lines 129-134 for PUR, ~142 for FIN-05): tick `- [x]` only for rows whose
       clauses ALL verified above.
    2. The traceability table (~lines 297-303): set each row's status from the SAME verification. This
       means **correcting** PUR-05 and FIN-05, which the table currently claims are Complete — they only
       become genuinely Complete via 10-03 Task 4 and 10-05 respectively. Point each row's "Phase" cell at
       Phase 10 and, where useful, name the closing plan.

    Do not touch requirements outside PUR-* and FIN-05 (other phases own those rows).
  </action>
  <verify>
    - No row is `[x]` in the checkbox list while its table row says Pending (or vice versa) — grep both and diff by hand.
    - Every ticked row has a named green IT behind it.
    - `grep -n "PUR-0[1-6]\|FIN-05" .planning/REQUIREMENTS.md` shows a consistent story in both sections.
  </verify>
  <done>REQUIREMENTS.md tells the truth: checkbox list and traceability table agree, and every completion claim is backed by shipped code + a green test.</done>
</task>

<task type="auto">
  <name>Task 2: Update 10-VERIFICATION.md and 10-UAT.md</name>
  <files>.planning/phases/10-purchasing-accounts-payable/10-VERIFICATION.md, .planning/phases/10-purchasing-accounts-payable/10-UAT.md</files>
  <action>
    1. `10-VERIFICATION.md` — extend the "Gaps found later (2026-07-12 replan audit)" section so it records
       ALL THREE escapes, not just the first, and how each was closed:
       - PUR-02 PO CLOSED transition — `PoStatus.CLOSED` was dead code → closed by 10-04.
       - PUR-05 price variance — scorecard shipped with only 2 of its 3 required metrics; zero repo-wide
         hits for priceVariance → closed by 10-03 Task 4.
       - FIN-05 expense approval — no Expense entity, no OPA expense action anywhere; the
         `finance.expense.approve` permission sat in auth-service with zero consumers → closed by 10-05.
       Add a short **root-cause** line: all three escaped because the original must-haves were scored
       instead of the requirement text (the "11/11 passed" was measuring the wrong thing). Re-score the
       phase honestly against the requirement text now that the gap plans have landed.
    2. `10-UAT.md` — add user-testable cases for the newly shipped behavior, following the existing
       numbered format (currently 11 tests, so continue from 12):
       - PO close: fully receive a PO → Close PO on the detail page → status CLOSED.
       - PO short-close: partially receive → Close requires a reason → CLOSED with reason recorded.
       - Spend analytics: /app/purchasing/analytics shows spend by vendor and by category with a
         current-vs-prior comparison.
       - Vendor scorecard: the scorecard shows price variance % alongside on-time % and fill rate.
       - Expense approval: approving an expense above your OPA approval limit is rejected (403
         EXPENSE_APPROVAL_LIMIT_EXCEEDED); within limit → APPROVED and a balanced JE posts.
       Update the counts in the `## Summary` block to match.
  </action>
  <verify>10-VERIFICATION.md lists all three escapes with their closing plans and a root-cause line; 10-UAT.md's test count matches its Summary block.</verify>
  <done>The phase record is honest about what was missed, why, and how it was fixed; UAT covers the new behavior.</done>
</task>

</tasks>

<verification>
- REQUIREMENTS.md: checkbox list and traceability table agree for PUR-01..06 + FIN-05, and every completion claim maps to a green IT.
- No requirement is marked complete on the strength of the old (wrong) table alone.
- 10-VERIFICATION.md records the three escapes + root cause.
</verification>

<success_criteria>
- Phase 10's requirement docs are trustworthy: a reader can tell, correctly, what shipped and what didn't.
- The "score the must-haves instead of the requirement" failure mode is named in the record so it does not
  repeat in Phase 11.
</success_criteria>

<notes>
If any wave-3 plan did NOT land cleanly, this plan must leave that requirement `[ ]` / Pending and say so.
Ticking a box for work that isn't done is the exact failure this plan exists to prevent — an inaccurate
"complete" is worse than an honest "pending."
</notes>

<output>
After completion, create `.planning/phases/10-purchasing-accounts-payable/10-06-SUMMARY.md`.
</output>
