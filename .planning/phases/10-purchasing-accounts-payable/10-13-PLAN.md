---
phase: 10-purchasing-accounts-payable
plan: 13
type: execute
wave: 4
gap_closure: true
depends_on: ["10-12"]
files_modified:
  - frontend/lib/api-client/schemas/purchasing.schema.ts
  - frontend/lib/adapters/purchasing.adapter.ts
  - frontend/lib/repositories/purchasing.repository.ts
  - frontend/lib/hooks/purchasing/use-purchasing.ts
  - frontend/app/(tenant)/app/purchasing/invoices/page.tsx
  - frontend/app/(tenant)/app/purchasing/invoices/[id]/page.tsx
  - frontend/app/(tenant)/app/purchasing/payments/page.tsx
  - frontend/components/purchasing/VendorInvoiceFormDialog.tsx
  - frontend/components/purchasing/OverrideMatchDialog.tsx
  - frontend/components/purchasing/ApPaymentDialog.tsx
  - frontend/mocks/purchasing.handlers.ts
  - frontend/__tests__/lib/purchasing-invoice-journey.test.ts
autonomous: true

must_haves:
  truths:
    - "A user can open Invoices from the tab bar and see the branch's vendor invoices with a match-status badge"
    - "A user can book an invoice against a PO and see it land MATCHED or MISMATCHED"
    - "An invoice row links to its detail page, where the existing ThreeWayMatchTable finally has an inbound link"
    - "A permitted user can override a MISMATCHED invoice with a justification and see it become APPROVED_FOR_PAYMENT"
    - "A permitted user can pay a MATCHED / APPROVED_FOR_PAYMENT invoice and see it become PAID"
  artifacts:
    - path: "frontend/app/(tenant)/app/purchasing/invoices/page.tsx"
      provides: "invoice list with match badges + book-invoice action + links to detail"
      min_lines: 40
    - path: "frontend/components/purchasing/OverrideMatchDialog.tsx"
      provides: "justification-mandatory override of a failed 3-way match"
    - path: "frontend/components/purchasing/ApPaymentDialog.tsx"
      provides: "AP payment against an invoice (first frontend consumer of POST /payments)"
  key_links:
    - from: "frontend/app/(tenant)/app/purchasing/invoices/page.tsx"
      to: "GET /api/v1/purchasing/invoices"
      via: "useVendorInvoices -> PurchasingRepository.listInvoices"
      pattern: "useVendorInvoices"
    - from: "OverrideMatchDialog"
      to: "POST /api/v1/purchasing/invoices/{id}/override-match"
      via: "useOverrideMatch mutation"
      pattern: "override-match"
    - from: "ApPaymentDialog"
      to: "POST /api/v1/purchasing/payments"
      via: "useCreateApPayment mutation"
      pattern: "purchasing/payments"
---

<objective>
Build the vendor-invoice and AP-payment journeys: list, book, 3-way-match review, override, pay.

Purpose: UAT tests 4, 5, 6, 7 and 8 are all "blocked — no UI path". `invoices/[id]` (which already renders a working `ThreeWayMatchTable`) has ZERO inbound links. `PurchasingRepository.createInvoice` is dead code — no hook, no caller. `POST /invoices/{id}/override-match` and `POST /api/v1/purchasing/payments` have NO frontend consumer at ANY layer (no schema, no repo fn, no hook, no page). ROADMAP SC#3 ("3-way matched vendor invoices feeding AP; payment posts and publishes AP_PAYMENT_PROCESSED") is entirely unexercisable by a human.

Output: the invoice + payment half of the purchasing module, through the strict four-layer stack.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-purchasing-accounts-payable/10-10-SUMMARY.md   # invoice list contract
@.planning/phases/10-purchasing-accounts-payable/10-12-SUMMARY.md   # data-layer patterns + any missing DTO fields

@frontend/lib/repositories/purchasing.repository.ts
@frontend/lib/hooks/purchasing/use-purchasing.ts
@frontend/components/purchasing/ThreeWayMatchTable.tsx
@frontend/app/(tenant)/app/purchasing/invoices/[id]/page.tsx
@services/purchasing-service/src/main/java/io/restaurantos/purchasing/dto/CreateVendorInvoiceRequest.java
@services/purchasing-service/src/main/java/io/restaurantos/purchasing/dto/CreateApPaymentRequest.java

Key constraints:
- Same four-layer boundary + paisa + real-hex-UUID-in-MSW rules as 10-12. Read 10-12-SUMMARY first; this
  plan extends the SAME purchasing.{schema,adapter,repository} and use-purchasing.ts files, which is why it
  waves after 10-12 rather than beside it.
- `PurchasingRepository.createInvoice(body: unknown)` currently takes `unknown` and is uncalled — TIGHTEN
  it to a real Zod-validated input type as part of this plan; do not leave an `unknown` in the repository.
- 3-way match outcomes come back on the invoice lines as `LineMatchStatus`
  (MATCHED / PRICE_VARIANCE / QTY_VARIANCE / MISSING_GRN) — `ThreeWayMatchTable` already renders them.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Invoice + payment data layer (schema -> adapter -> repository -> hooks + MSW)</name>
  <files>
    frontend/lib/api-client/schemas/purchasing.schema.ts
    frontend/lib/adapters/purchasing.adapter.ts
    frontend/lib/repositories/purchasing.repository.ts
    frontend/lib/hooks/purchasing/use-purchasing.ts
    frontend/mocks/purchasing.handlers.ts
  </files>
  <action>
1. Schemas (match the Java DTOs exactly — read `CreateVendorInvoiceRequest` and `CreateApPaymentRequest`):
   - `createVendorInvoiceInputSchema` — `{ poId, vendorId, branchId, invoiceNumber, invoiceDate,
     lines: [{ poLineId, qty, unitPricePaisa }] }`
   - `overrideMatchInputSchema` — `{ justification: string }` with `min(10)` — the requirement calls for a
     justification, and a 1-char one is not one. If the backend enforces a different minimum, mirror it.
   - `createApPaymentInputSchema` — `{ invoiceId, branchId, amountPaisa, paymentDate, method? }`
   - `apiApPaymentSchema` + `adaptApPayment`.
   - Reuse the existing `apiVendorInvoiceSchema` for list rows.

2. Repository: replace `createInvoice(body: unknown)` with
   `createInvoice(input: VendorInvoiceInput)` that `.parse()`s the input, and add
   `listInvoices(branchId, status?)`, `overrideMatch(id, justification)`, `createApPayment(input)`,
   `listApPayments(branchId)` (only if 10-10 exposed a payments list; if it did not, omit it and drive the
   payments page off invoices with status MATCHED/APPROVED_FOR_PAYMENT/PAID — say which in the SUMMARY).

3. Hooks: `useVendorInvoices(branchId, status?)`, `useCreateVendorInvoice()`, `useOverrideMatch(id)`,
   `useCreateApPayment()`. Invalidate the invoice detail key AND the invoice list key on every mutation
   (a paid invoice that still shows MATCHED in the list is a UAT bounce).

4. MSW handlers in `purchasing.handlers.ts`: book-invoice returns MATCHED for an exact match and
   MISMATCHED with a PRICE_VARIANCE line when the unit price is >2% over the PO (mirror the real
   `ThreeWayMatchService` tolerance so the mock does not teach the tests a lie); override-match flips
   MISMATCHED -> APPROVED_FOR_PAYMENT; payment flips -> PAID. Real hex UUIDs (10-03-D).
  </action>
  <verify>
`cd frontend && npx tsc --noEmit` clean (in particular: `createInvoice` no longer takes `unknown`).
`npx eslint lib components` clean. `npx vitest run` — prior tests still green.
  </verify>
  <done>Every invoice and payment endpoint has a Zod-validated repository fn and a hook; `createInvoice` is no longer dead `unknown`-typed code.</done>
</task>

<task type="auto">
  <name>Task 2: Invoice list page, book-invoice dialog, and the override-match action</name>
  <files>
    frontend/app/(tenant)/app/purchasing/invoices/page.tsx
    frontend/app/(tenant)/app/purchasing/invoices/[id]/page.tsx
    frontend/components/purchasing/VendorInvoiceFormDialog.tsx
    frontend/components/purchasing/OverrideMatchDialog.tsx
  </files>
  <action>
1. `invoices/page.tsx` — client component. `useVendorInvoices(branchId)`. Table: invoice number, vendor,
   PO number, invoice date, total (`MoneyDisplay`), status badge (MATCHED / MISMATCHED /
   APPROVED_FOR_PAYMENT / PAID — reuse/extend the `MatchStatusBadge` already in `ThreeWayMatchTable.tsx`,
   do not write a third badge). Status filter. Every row links to
   `/app/purchasing/invoices/{id}` — **the inbound link the detail route has never had**.
   "Book Invoice" button opens `VendorInvoiceFormDialog`. Loading skeleton + empty state.

2. `VendorInvoiceFormDialog.tsx` — pick a PO (select fed by `usePurchaseOrders(branchId, ["SENT",
   "PARTIALLY_RECEIVED","FULLY_RECEIVED"])` — you can only invoice a PO that has been sent), then prefill
   one line per PO line with the PO's qty and unit price, both EDITABLE. Editing them is how UAT tests 4/5/6
   are driven: leave them equal -> MATCHED (F1); book against a PO with no receipt -> MISSING_GRN (F4);
   bump a unit price >2% -> PRICE_VARIANCE -> MISMATCHED (F6). Show the resulting status in a toast on
   success and route to the invoice detail page.

3. `OverrideMatchDialog.tsx` + wire it into `invoices/[id]/page.tsx`: on a MISMATCHED invoice, render an
   "Override match" button (gated on the user holding `vendor.invoice.override` — read permissions the same
   way other permission-gated UI in this codebase does; if no such helper exists, render it and let the 403
   surface as a toast, and note that in the SUMMARY). The dialog takes a mandatory justification
   (>= 10 chars, submit disabled until then) and calls `useOverrideMatch`. On success the invoice becomes
   APPROVED_FOR_PAYMENT and the `ThreeWayMatchTable` re-renders with the override recorded.
  </action>
  <verify>
`cd frontend && npx next build` succeeds.
Real-path manual: Purchasing -> Invoices -> Book Invoice against a fully-received PO with matching qty/price -> MATCHED. Book another against a SENT-but-unreceived PO -> MISMATCHED with a MISSING_GRN line. Book a third with a unit price 5% over -> MISMATCHED with PRICE_VARIANCE -> Override with a justification -> APPROVED_FOR_PAYMENT.
  </verify>
  <done>UAT tests 4, 5, 6 and 8 are executable from the UI; the ThreeWayMatchTable is reachable by clicking, not by pasting a UUID.</done>
</task>

<task type="auto">
  <name>Task 3: AP payments page + invoice-journey test</name>
  <files>
    frontend/app/(tenant)/app/purchasing/payments/page.tsx
    frontend/components/purchasing/ApPaymentDialog.tsx
    frontend/__tests__/lib/purchasing-invoice-journey.test.ts
  </files>
  <action>
1. `payments/page.tsx` — the Payments tab 10-11 added. Show the payables worklist: invoices with status
   MATCHED or APPROVED_FOR_PAYMENT (i.e. payable), with vendor, invoice number, amount, days outstanding,
   and a "Pay" action per row. Also show recently PAID invoices (or the payment list, if 10-10 exposed one)
   so the user can see the result of what they just did.

2. `ApPaymentDialog.tsx` — amount (defaulting to the invoice total, editable for a partial payment if the
   backend `CreateApPaymentRequest` supports it — check; if it does not, make the amount read-only and say
   so), payment date, method. Calls `useCreateApPayment`. On success: toast, invalidate the invoice list,
   the invoice detail and the payables list; the invoice shows PAID.
   This is the first frontend consumer of `POST /api/v1/purchasing/payments` — the endpoint that posts
   AP -> Bank in finance and publishes `AP_PAYMENT_PROCESSED` (ROADMAP SC#3).

3. `frontend/__tests__/lib/purchasing-invoice-journey.test.ts` — vitest + MSW through the REAL
   repository/hooks (mirror `purchasing-close.test.ts`):
   - book an exactly-matching invoice -> MATCHED
   - book a >2% price-drift invoice -> MISMATCHED with a PRICE_VARIANCE line
   - override-match with a justification -> APPROVED_FOR_PAYMENT; overriding with a 3-char justification is
     rejected client-side by the Zod schema (assert the parse throws — the justification requirement is a
     contract, not a placeholder)
   - createApPayment -> PAID, and the request body carries `amountPaisa` as a paisa integer string (not a
     float rupee value — the classic money bug this codebase deliberately avoids)
  </action>
  <verify>
`cd frontend && npx vitest run __tests__/lib/purchasing-invoice-journey.test.ts` — green.
Real-path manual: pay a MATCHED invoice from the Payments tab -> invoice becomes PAID; confirm in the finance GL that the AP -> Bank journal entry posted (GET /api/v1/finance/gl/balances, or the journal-entries page) — the payment is only real if the money moved.
  </verify>
  <done>UAT test 7 is executable; an AP payment posts to finance and the invoice reaches PAID from the UI.</done>
</task>

</tasks>

<verification>
1. `cd frontend && npx tsc --noEmit && npx eslint . && npx vitest run && npx next build` — all clean.
2. `grep -rn "createInvoice" frontend/lib/repositories/purchasing.repository.ts` — no `unknown` in the signature, and `grep -rn "useCreateVendorInvoice" frontend/` shows a real caller (it was dead code).
3. Real-path click-through: book -> mismatch -> override -> pay, entirely from the browser, with the finance JE confirmed.
</verification>

<success_criteria>
- ROADMAP SC#3 (3-way match -> AP -> payment -> AP_PAYMENT_PROCESSED) is demonstrable by a human clicking through the app.
- Zero purchasing repository functions remain without a caller.
</success_criteria>

<output>
After completion, create `.planning/phases/10-purchasing-accounts-payable/10-13-SUMMARY.md`.
</output>
