---
phase: 08-inventory-recipe-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pom.xml
  - scripts/start-dev.ps1
  - services/inventory-service/pom.xml
  - services/inventory-service/src/main/java/io/restaurantos/inventory/InventoryServiceApplication.java
  - services/inventory-service/src/main/resources/application.yml
  - services/inventory-service/src/main/java/io/restaurantos/inventory/config/InventoryRabbitConfig.java
  - services/inventory-service/src/main/resources/db/migration/V1__inventory_schema.sql
  - services/inventory-service/src/main/resources/db/migration/V2__shared_infra_tables.sql
  - services/inventory-service/src/main/java/io/restaurantos/inventory/entity/ProcessedEventEntity.java
  - services/inventory-service/src/main/java/io/restaurantos/inventory/repository/ProcessedEventJpaRepository.java
  - services/inventory-service/src/main/java/io/restaurantos/inventory/service/ProcessedEventService.java
  - services/inventory-service/src/main/java/io/restaurantos/inventory/event/InventoryEventPayloads.java
autonomous: true
requirements: [INV-01, INV-03, INV-07]
user_setup: []

must_haves:
  truths:
    - "inventory-service is a member of the root Maven reactor and compiles (mvn -pl services/inventory-service -am test-compile succeeds)."
    - "Every tenant-scoped inventory table has ENABLE ROW LEVEL SECURITY, FORCE ROW LEVEL SECURITY, and a NULLIF-guarded tenant_isolation policy."
    - "event_outbox, idempotency_keys, and processed_events exist, are RLS-exempt, and are granted to inventory_user."
    - "InventoryEventPayloads defines the inbound OrderClosedPayload + ItemEntry (mirroring pos-service) plus every outbound inventory event record."
    - "InventoryRabbitConfig declares inventory.topic and consumes inventory.order-closed.queue with the DLQ + retry contract."
  artifacts:
    - services/inventory-service/pom.xml
    - services/inventory-service/src/main/resources/db/migration/V1__inventory_schema.sql
    - services/inventory-service/src/main/resources/db/migration/V2__shared_infra_tables.sql
    - services/inventory-service/src/main/java/io/restaurantos/inventory/event/InventoryEventPayloads.java
    - services/inventory-service/src/main/java/io/restaurantos/inventory/service/ProcessedEventService.java
    - services/inventory-service/src/main/java/io/restaurantos/inventory/config/InventoryRabbitConfig.java
  key_links:
    - "root pom.xml <modules> lists services/inventory-service (after services/kitchen-service)."
    - "InventoryRabbitConfig.INVENTORY_ORDER_CLOSED_QUEUE equals the literal inventory.order-closed.queue already declared in deploy/init/rabbitmq-definitions.json."
    - "ProcessedEventService.tryProcess(consumer, eventId, Runnable) backs the (consumer,event_id) PK on processed_events."
  prohibitions:
    - "No RLS policy uses the unguarded empty-string-fragile GUC cast; every policy wraps the GUC in NULLIF(...,'') before ::UUID."
    - "event_outbox, idempotency_keys, and processed_events are NOT given ROW LEVEL SECURITY (they are RLS-exempt infra tables)."
    - "V1 does not stamp recipe version onto pos-service or alter the ORDER_CLOSED event contract (D-01 resolution is inventory-local)."
---

<objective>
Stand up the new `services/inventory-service` Maven module so it compiles inside the reactor, boots on port 8085 against `inventory_db`, has a complete Flyway schema (all Phase-8 domain tables with FORCE RLS from V1), the three RLS-exempt shared-infra tables, the idempotency scaffolding, and the full inventory event-payload contract. This is the Wave-1 foundation every downstream plan depends on.

Purpose: Everything in Phase 8 (masters, recipes, depletion, receipts, transfers, counts) needs a compiling module, a schema, and the shared event/idempotency scaffolding to exist before it can be built or tested.
Output: A bootable, migration-carrying inventory-service skeleton plus `InventoryEventPayloads`, `ProcessedEventService`, and RabbitMQ topology.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-inventory-recipe-management/08-CONTEXT.md
@.planning/phases/08-inventory-recipe-management/08-RESEARCH.md
@.planning/phases/08-inventory-recipe-management/08-PATTERNS.md
</context>

<tasks>

<!-- planner-discipline-allow: FORCE ROW LEVEL SECURITY -->
<!-- planner-discipline-allow: ENABLE ROW LEVEL SECURITY -->

<task type="auto">
  <name>Task 1: Maven module wiring, Application, application.yml, RabbitMQ topology, dev-stack registration</name>
  <files>pom.xml, scripts/start-dev.ps1, services/inventory-service/pom.xml, services/inventory-service/src/main/java/io/restaurantos/inventory/InventoryServiceApplication.java, services/inventory-service/src/main/resources/application.yml, services/inventory-service/src/main/java/io/restaurantos/inventory/config/InventoryRabbitConfig.java</files>
  <read_first>
    - services/kitchen-service/pom.xml (exact dependency set + surefire/failsafe plugin config to mirror)
    - services/kitchen-service/src/main/java/io/restaurantos/kitchen/KitchenServiceApplication.java (@SpringBootApplication + @EntityScan/@EnableJpaRepositories including io.restaurantos.shared)
    - services/kitchen-service/src/main/resources/application.yml (datasource, flyway, rabbitmq listener retry block, eureka, config, restaurantos.internal.secret/jwks/opa)
    - services/kitchen-service/src/main/java/io/restaurantos/kitchen/config/KitchenRabbitConfig.java (queue + DLX + binding + Jackson2JsonMessageConverter pattern)
    - pom.xml (root reactor <modules> ordering — inventory goes after services/kitchen-service)
    - scripts/start-dev.ps1 (Start-ServiceWindow pattern lines ~156-170; the port-kill list ~line 30)
  </read_first>
  <action>
    Add `<module>services/inventory-service</module>` to the root pom.xml `<modules>` block immediately after `services/kitchen-service`. Create services/inventory-service/pom.xml with artifactId `inventory-service`, parent `restaurantos-parent:1.0.0` (relativePath ../../pom.xml), and the SAME dependency set + build plugin block (spring-boot-maven-plugin lombok exclude, maven-compiler-plugin lombok annotation path, surefire excluding **/*IT.java, failsafe including **/*IT.java with TESTCONTAINERS_RYUK_DISABLED=true) as kitchen-service/pom.xml; add the test-scoped org.testcontainers:rabbitmq dependency alongside junit-jupiter + postgresql. Create InventoryServiceApplication in package io.restaurantos.inventory annotated @SpringBootApplication, @EnableScheduling (needed by the later nightly expiry sweep), @EntityScan(basePackages={"io.restaurantos.inventory.domain.model","io.restaurantos.inventory.entity","io.restaurantos.shared"}) and @EnableJpaRepositories(basePackages={"io.restaurantos.inventory.repository","io.restaurantos.shared"}). Create application.yml mirroring kitchen-service's: spring.application.name inventory-service, server.port ${SERVER_PORT:8085}, datasource url ${INVENTORY_DB_URL:jdbc:postgresql://localhost:5432/inventory_db} user ${INVENTORY_DB_USER:inventory_user} password ${INVENTORY_DB_PASSWORD:inventory_pass}, jpa.hibernate.ddl-auto validate, flyway enabled locations classpath:db/migration, the rabbitmq listener.simple block with acknowledge-mode manual, default-requeue-rejected false, retry enabled initial-interval 2000 max-attempts 3 multiplier 2.0 max-interval 8000, eureka + cloud.config + openfeign.circuitbreaker + management prometheus + restaurantos.internal.secret/jwks.uri/opa.url identical to kitchen. Create config/InventoryRabbitConfig mirroring KitchenRabbitConfig: constants INVENTORY_ORDER_CLOSED_QUEUE="inventory.order-closed.queue", POS_TOPIC_EXCHANGE="pos.topic", INVENTORY_TOPIC_EXCHANGE="inventory.topic", DLX="restaurantos.dlx"; @Bean TopicExchange for pos.topic and inventory.topic; @Bean Queue inventoryOrderClosedQueue (durable, x-dead-letter-exchange=DLX, x-dead-letter-routing-key=queue+".dlq"); @Bean Binding to posTopic with routing key pos.order.closed; @Bean Jackson2JsonMessageConverter. Register inventory-service in scripts/start-dev.ps1 (add a Start-ServiceWindow "inventory-service" "services/inventory-service" line after kitchen-service, and add 8085 to the port-kill foreach list).
  </action>
  <acceptance_criteria>
    - `mvn -pl services/inventory-service -am test-compile -q` exits 0 (module resolves in the reactor and compiles).
    - `grep -c "services/inventory-service" pom.xml` returns at least 1 (module registered).
    - InventoryRabbitConfig defines the constant value inventory.order-closed.queue and INVENTORY_TOPIC_EXCHANGE value inventory.topic.
    - application.yml server.port default resolves to 8085 and the rabbitmq retry block has max-attempts 3 / max-interval 8000.
    - scripts/start-dev.ps1 contains an inventory-service Start-ServiceWindow entry and 8085 appears in the port-kill list.
  </acceptance_criteria>
  <verify>
    <automated>mvn -pl services/inventory-service -am test-compile -q</automated>
  </verify>
  <done>Module compiles in the reactor; boots on 8085 config; RabbitMQ topology + dev-stack registration in place.</done>
</task>

<task type="auto">
  <name>Task 2: V1__inventory_schema.sql — complete domain schema with FORCE RLS + NULLIF-guarded policies</name>
  <files>services/inventory-service/src/main/resources/db/migration/V1__inventory_schema.sql</files>
  <read_first>
    - services/kitchen-service/src/main/resources/db/migration/V1__kitchen_schema.sql (the CORRECT NULLIF-guarded policy form + audit-column set matching TenantAuditableEntity)
    - services/finance-service/src/main/resources/db/migration/V1__finance_schema.sql (table + unique-constraint + CHECK style — note it OMITS FORCE; you must ADD FORCE)
    - .planning/phases/08-inventory-recipe-management/08-RESEARCH.md § "Pattern 4" and Pitfalls 2 & 3 (the exact ENABLE+FORCE+NULLIF block; qty=NUMERIC(18,4), to_base_factor=NUMERIC(18,8), money=BIGINT paisa)
    - shared-lib/src/main/java/io/restaurantos/shared/entity/TenantAuditableEntity.java (the audit columns every table must carry: tenant_id, created_at, updated_at, created_by, updated_by, deleted_at)
  </read_first>
  <action>
    Create V1__inventory_schema.sql defining these tenant-scoped tables, each with the full TenantAuditableEntity audit-column set (tenant_id UUID NOT NULL, created_at/updated_at TIMESTAMPTZ DEFAULT now(), created_by/updated_by UUID, deleted_at TIMESTAMPTZ) and, immediately after each CREATE TABLE, the block: ALTER TABLE t ENABLE ROW LEVEL SECURITY; ALTER TABLE t FORCE ROW LEVEL SECURITY; CREATE POLICY tenant_isolation ON t USING (tenant_id = NULLIF(current_setting('app.current_tenant_id', TRUE), '')::UUID). Tables: (1) units_of_measure — code VARCHAR(20), name VARCHAR(120), base_unit_code VARCHAR(20), to_base_factor NUMERIC(18,8) NOT NULL DEFAULT 1, UNIQUE(tenant_id, code). (2) ingredients — name VARCHAR(160), sku VARCHAR(60), base_uom_code VARCHAR(20) NOT NULL, category VARCHAR(80), reorder_point NUMERIC(18,4) NOT NULL DEFAULT 0, is_active BOOLEAN NOT NULL DEFAULT true, UNIQUE(tenant_id, sku). (3) ingredient_branch_stock — branch_id UUID NOT NULL, ingredient_id UUID NOT NULL REFERENCES ingredients(id), qty_on_hand NUMERIC(18,4) NOT NULL DEFAULT 0, avg_cost_paisa BIGINT NOT NULL DEFAULT 0, last_counted_at TIMESTAMPTZ, UNIQUE(tenant_id, branch_id, ingredient_id). (4) stock_lots — branch_id UUID NOT NULL, ingredient_id UUID NOT NULL, stock_id UUID NOT NULL REFERENCES ingredient_branch_stock(id), qty NUMERIC(18,4) NOT NULL DEFAULT 0 CHECK (qty >= 0), expiry_date DATE, receipt_unit_cost_paisa BIGINT NOT NULL DEFAULT 0, received_at TIMESTAMPTZ NOT NULL DEFAULT now(), source_movement_id UUID; plus INDEX idx_inventory_lots_fefo ON stock_lots (tenant_id, branch_id, ingredient_id, expiry_date). (5) recipes — menu_item_id UUID NOT NULL, version INT NOT NULL, is_current BOOLEAN NOT NULL DEFAULT true, effective_from TIMESTAMPTZ NOT NULL, yield_servings NUMERIC(18,4) NOT NULL DEFAULT 1, name VARCHAR(160), UNIQUE(tenant_id, menu_item_id, version); plus INDEX idx_inventory_recipe_effective ON recipes (tenant_id, menu_item_id, effective_from). (6) recipe_lines — recipe_id UUID NOT NULL REFERENCES recipes(id), ingredient_id UUID NOT NULL, qty NUMERIC(18,4) NOT NULL, uom_code VARCHAR(20) NOT NULL, yield_pct NUMERIC(6,2) NOT NULL DEFAULT 100. (7) inventory_movements — branch_id UUID NOT NULL, ingredient_id UUID NOT NULL, movement_type VARCHAR(24) NOT NULL CHECK (movement_type IN ('OPENING_BALANCE','RECEIPT','DEPLETION','TRANSFER_OUT','TRANSFER_IN','COUNT_VARIANCE','WASTAGE','TRANSFER_VARIANCE')), qty NUMERIC(18,4) NOT NULL, unit_cost_paisa BIGINT NOT NULL DEFAULT 0, total_cost_paisa BIGINT NOT NULL DEFAULT 0, reference_type VARCHAR(40), reference_id UUID, movement_at TIMESTAMPTZ NOT NULL DEFAULT now(); plus INDEX idx_inventory_movements_branch_time ON inventory_movements (tenant_id, branch_id, movement_at). (8) stock_transfers — from_branch_id UUID NOT NULL, to_branch_id UUID NOT NULL, status VARCHAR(16) NOT NULL DEFAULT 'SHIPPED' CHECK (status IN ('SHIPPED','RECEIVED','CANCELLED')), shipped_at TIMESTAMPTZ, received_at TIMESTAMPTZ. (9) stock_transfer_lines — transfer_id UUID NOT NULL REFERENCES stock_transfers(id), ingredient_id UUID NOT NULL, qty_shipped NUMERIC(18,4) NOT NULL, qty_received NUMERIC(18,4), variance_qty NUMERIC(18,4) NOT NULL DEFAULT 0, unit_cost_paisa BIGINT NOT NULL DEFAULT 0. (10) stock_counts — branch_id UUID NOT NULL, status VARCHAR(16) NOT NULL DEFAULT 'DRAFT' CHECK (status IN ('DRAFT','POSTED')), counted_at TIMESTAMPTZ, posted_at TIMESTAMPTZ. (11) stock_count_lines — count_id UUID NOT NULL REFERENCES stock_counts(id), ingredient_id UUID NOT NULL, system_qty NUMERIC(18,4) NOT NULL DEFAULT 0, counted_qty NUMERIC(18,4) NOT NULL DEFAULT 0, variance_qty NUMERIC(18,4) NOT NULL DEFAULT 0, variance_cost_paisa BIGINT NOT NULL DEFAULT 0. Apply the ENABLE+FORCE+NULLIF policy block to all 11 tables.
  </action>
  <acceptance_criteria>
    - `grep -c "FORCE ROW LEVEL SECURITY" services/inventory-service/src/main/resources/db/migration/V1__inventory_schema.sql` returns 11 (one per domain table).
    - `grep -c "NULLIF(current_setting('app.current_tenant_id', TRUE), '')::UUID" services/inventory-service/src/main/resources/db/migration/V1__inventory_schema.sql` returns 11 (every policy uses the guarded GUC form).
    - The file declares all 11 tables named above with qty columns typed NUMERIC(18,4), to_base_factor NUMERIC(18,8), and all cost columns BIGINT.
    - Runtime application of the migration is proven by 08-02's SchemaMigrationIT (Wave-2 dependency).
  </acceptance_criteria>
  <verify>
    <automated>grep -c "FORCE ROW LEVEL SECURITY" services/inventory-service/src/main/resources/db/migration/V1__inventory_schema.sql</automated>
  </verify>
  <done>Complete domain schema with FORCE RLS + NULLIF-guarded policies on every tenant table; correct numeric/paisa typing.</done>
</task>

<task type="auto">
  <name>Task 3: V2 infra tables, processed-events idempotency scaffolding, and the inventory event-payload contract</name>
  <files>services/inventory-service/src/main/resources/db/migration/V2__shared_infra_tables.sql, services/inventory-service/src/main/java/io/restaurantos/inventory/entity/ProcessedEventEntity.java, services/inventory-service/src/main/java/io/restaurantos/inventory/repository/ProcessedEventJpaRepository.java, services/inventory-service/src/main/java/io/restaurantos/inventory/service/ProcessedEventService.java, services/inventory-service/src/main/java/io/restaurantos/inventory/event/InventoryEventPayloads.java</files>
  <read_first>
    - services/finance-service/src/main/resources/db/migration/V2__shared_infra_tables.sql (copy verbatim; swap finance_user→inventory_user, index-name prefix idx_finance_→idx_inventory_)
    - services/kitchen-service/src/main/java/io/restaurantos/kitchen/service/ProcessedEventService.java (copy verbatim, retarget package)
    - services/kitchen-service/src/main/java/io/restaurantos/kitchen/entity/ProcessedEventEntity.java and repository/ProcessedEventJpaRepository.java (copy verbatim, retarget package; keep existsByConsumerAndEventId)
    - services/pos-service/src/main/java/io/restaurantos/pos/event/PosClosePayloads.java (exact inbound OrderClosedPayload + ItemEntry wire shape to mirror)
    - services/kitchen-service/src/main/java/io/restaurantos/kitchen/event/KitchenEventPayloads.java (flat Java record payload style)
  </read_first>
  <action>
    Create V2__shared_infra_tables.sql by copying finance-service's V2 verbatim, changing the three GRANT targets to inventory_user and the two index name prefixes to idx_inventory_; these three tables (event_outbox, idempotency_keys, processed_events) get NO ROW LEVEL SECURITY. Copy ProcessedEventEntity, ProcessedEventJpaRepository (existsByConsumerAndEventId), and ProcessedEventService (tryProcess(String consumerName, UUID eventId, Runnable action) @Transactional) verbatim from kitchen-service into the io.restaurantos.inventory.{entity,repository,service} packages. Create event/InventoryEventPayloads.java defining, as flat Java records: the INBOUND OrderClosedPayload(UUID orderId, String orderNo, String type, UUID customerId, long subtotalPaisa, long discountPaisa, long serviceChargePaisa, long taxPaisa, long totalPaisa, List<PaymentEntry> payments, List<ItemEntry> items, UUID tillSessionId, UUID cashierId, Instant closedAt) and ItemEntry(UUID menuItemId, String name, int qty, long unitPricePaisa, long lineTotalPaisa) and PaymentEntry (mirror pos-service's shape; the consumer only reads items + closedAt but the record must deserialize the full envelope payload); and the OUTBOUND records: StockDepletedPayload(UUID orderId, List<DepletedLine> lines, long totalCogsPaisa) with DepletedLine(UUID ingredientId, java.math.BigDecimal qtyBaseDepleted, long cogsPaisa); StockReceivedPayload(UUID ingredientId, UUID branchId, BigDecimal qty, long unitCostPaisa, long newAvgCostPaisa, UUID lotId, java.time.LocalDate expiryDate); LowStockAlertPayload(UUID ingredientId, UUID branchId, BigDecimal qtyOnHand, BigDecimal reorderPoint); ExpiryAlertPayload(UUID lotId, UUID ingredientId, UUID branchId, java.time.LocalDate expiresOn, BigDecimal qty); CountVariancePostedPayload(UUID countId, UUID branchId, List<CountVarianceLine> lines, long totalVarianceCostPaisa) with CountVarianceLine(UUID ingredientId, BigDecimal varianceQty, long varianceCostPaisa); WastageRecordedPayload(UUID ingredientId, UUID branchId, BigDecimal qty, long costPaisa, String reason); TransferShippedPayload(UUID transferId, UUID fromBranchId, UUID toBranchId, List<TransferLine> lines) with TransferLine(UUID ingredientId, BigDecimal qty, long unitCostPaisa); TransferReceivedPayload(UUID transferId, UUID toBranchId, List<TransferLine> lines); TransferVariancePayload(UUID transferId, List<TransferVarianceLine> lines) with TransferVarianceLine(UUID ingredientId, BigDecimal varianceQty, long varianceCostPaisa). Also add public static final String constants for the event TYPEs (STOCK_DEPLETED, STOCK_RECEIVED, LOW_STOCK_ALERT, EXPIRY_ALERT, COUNT_VARIANCE_POSTED, WASTAGE_RECORDED, TRANSFER_SHIPPED, TRANSFER_RECEIVED, TRANSFER_VARIANCE) and their inventory.topic routing keys (inventory.stock.depleted, inventory.stock.received, inventory.stock.low, inventory.lot.expiry, inventory.count.variance, inventory.wastage.recorded, inventory.transfer.shipped, inventory.transfer.received, inventory.transfer.variance).
  </action>
  <acceptance_criteria>
    - `mvn -pl services/inventory-service -am test-compile -q` exits 0 with ProcessedEventService, ProcessedEventEntity, ProcessedEventJpaRepository, and InventoryEventPayloads present.
    - `grep -c "ENABLE ROW LEVEL SECURITY" services/inventory-service/src/main/resources/db/migration/V2__shared_infra_tables.sql` returns 0 (infra tables are RLS-exempt).
    - `grep -c "inventory_user" services/inventory-service/src/main/resources/db/migration/V2__shared_infra_tables.sql` returns 3 (three GRANT statements retargeted).
    - InventoryEventPayloads declares OrderClosedPayload with an Instant closedAt field and ItemEntry with an int qty field, and declares StockDepletedPayload with a totalCogsPaisa field.
  </acceptance_criteria>
  <verify>
    <automated>mvn -pl services/inventory-service -am test-compile -q</automated>
  </verify>
  <done>Idempotency scaffolding + full event contract compile; infra tables RLS-exempt and granted to inventory_user.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RabbitMQ → inventory consumer | Untrusted at-least-once ORDER_CLOSED delivery crosses into stock mutation |
| DB connection → tenant rows | Every query must be tenant-isolated at the row level regardless of app-layer bugs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-8-XTEN | Elevation of Privilege | every domain table (V1 schema) | high | mitigate | ENABLE + FORCE ROW LEVEL SECURITY + NULLIF-guarded tenant_isolation policy on all 11 tables (must_haves.truths #2); asserted at runtime by 08-02 SchemaMigrationIT. |
| T-8-IDEM | Repudiation / Tampering | processed_events + ProcessedEventService | high | mitigate | (consumer, event_id) PK + tryProcess guard scaffolding created here; consumed by 08-05 DepletionService; duplicate-delivery proven in 08-05 DepletionConsumerIT. |
| T-8-SC | Tampering (supply chain) | Maven dependencies | low | accept | No new third-party packages introduced — every dependency is copied from the already-vetted kitchen-service pom (RESEARCH § Package Legitimacy Audit: not applicable). No npm/pip/cargo install; no legitimacy checkpoint required. |
</threat_model>

<verification>
- `mvn -pl services/inventory-service -am test-compile -q` succeeds.
- `grep -c "FORCE ROW LEVEL SECURITY" .../V1__inventory_schema.sql` == 11; `grep -c "ENABLE ROW LEVEL SECURITY" .../V2__shared_infra_tables.sql` == 0.
- Module registered in root pom.xml and scripts/start-dev.ps1.
</verification>

<success_criteria>
inventory-service compiles in the reactor with a complete FORCE-RLS schema, RLS-exempt infra tables, processed-events idempotency scaffolding, RabbitMQ topology, and the full inventory event-payload contract.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 08-01)

New Maven module `services/inventory-service` (base package `io.restaurantos.inventory`, port 8085, `inventory_db`). New symbols/artifacts created here:
- `InventoryServiceApplication` (class), `application.yml` (config), `config/InventoryRabbitConfig` (class; constants INVENTORY_ORDER_CLOSED_QUEUE, POS_TOPIC_EXCHANGE, INVENTORY_TOPIC_EXCHANGE, DLX)
- Migrations `V1__inventory_schema.sql`, `V2__shared_infra_tables.sql`
- DB tables: units_of_measure, ingredients, ingredient_branch_stock, stock_lots, recipes, recipe_lines, inventory_movements, stock_transfers, stock_transfer_lines, stock_counts, stock_count_lines, event_outbox, idempotency_keys, processed_events
- `entity/ProcessedEventEntity`, `repository/ProcessedEventJpaRepository`, `service/ProcessedEventService` (classes)
- `event/InventoryEventPayloads` (records: OrderClosedPayload, ItemEntry, PaymentEntry, StockDepletedPayload, DepletedLine, StockReceivedPayload, LowStockAlertPayload, ExpiryAlertPayload, CountVariancePostedPayload, CountVarianceLine, WastageRecordedPayload, TransferShippedPayload, TransferReceivedPayload, TransferVariancePayload, TransferLine, TransferVarianceLine; event-type + routing-key constants)
- RabbitMQ exchange `inventory.topic`; consumed queue `inventory.order-closed.queue`
- Root pom.xml `<module>` entry; scripts/start-dev.ps1 inventory-service entry
</artifacts_produced>

<output>
Create `.planning/phases/08-inventory-recipe-management/08-01-SUMMARY.md` when done.
</output>
