---
phase: 08-inventory-recipe-management
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - services/inventory-service/src/test/java/io/restaurantos/inventory/InventoryTestBase.java
  - services/inventory-service/src/test/java/io/restaurantos/inventory/TestFixtures.java
  - services/inventory-service/src/test/java/io/restaurantos/inventory/SchemaMigrationIT.java
autonomous: true
requirements: [INV-01, INV-03]
user_setup: []

must_haves:
  truths:
    - "InventoryTestBase boots a Testcontainers PostgreSQL, applies V1+V2 via Flyway, and mocks RabbitTemplate/StringRedisTemplate/OpaClient so ITs run without a live broker."
    - "SchemaMigrationIT proves the migrations apply cleanly and that a sample domain table reports relforcerowsecurity = true with a tenant_isolation policy present."
    - "TestFixtures provides tenant/branch-scoped JWT + TenantContext helpers, including an INVENTORY_MANAGER role variant."
  artifacts:
    - services/inventory-service/src/test/java/io/restaurantos/inventory/InventoryTestBase.java
    - services/inventory-service/src/test/java/io/restaurantos/inventory/TestFixtures.java
    - services/inventory-service/src/test/java/io/restaurantos/inventory/SchemaMigrationIT.java
  key_links:
    - "InventoryTestBase datasource points at inventory_db/inventory_user Testcontainers instance; spring.flyway.enabled=false with @BeforeAll Flyway.migrate()."
    - "SchemaMigrationIT queries pg_class.relforcerowsecurity and pg_policies to assert FORCE RLS + tenant_isolation (validates 08-01 T-8-XTEN mitigation)."
  prohibitions:
    - "InventoryTestBase does not depend on any Phase-8 domain entity class (entities are created in Wave-3 plans); it operates at the migration/JDBC level only."
    - "No test uses a watch/continuous mode flag."
---

<objective>
Create the inventory-service Testcontainers integration-test harness (`InventoryTestBase` + `TestFixtures`) that every downstream feature IT builds on, and a `SchemaMigrationIT` that proves the 08-01 schema applies and actually enforces FORCE RLS. This closes the Nyquist Wave-0 test-infrastructure gap for the phase.

Purpose: Downstream feature plans (masters, recipes, depletion, receipts, transfers, counts) all extend `InventoryTestBase` for their ITs and rely on `TestFixtures` for tenant/JWT context; the schema's FORCE-RLS guarantee (T-8-XTEN) needs a runtime assertion.
Output: A reusable IT base class, fixtures, and a green schema/RLS smoke IT.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-inventory-recipe-management/08-VALIDATION.md
@.planning/phases/08-inventory-recipe-management/08-RESEARCH.md
@services/inventory-service/src/main/resources/db/migration/V1__inventory_schema.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: InventoryTestBase + TestFixtures (Testcontainers harness, service-agnostic)</name>
  <files>services/inventory-service/src/test/java/io/restaurantos/inventory/InventoryTestBase.java, services/inventory-service/src/test/java/io/restaurantos/inventory/TestFixtures.java</files>
  <read_first>
    - services/kitchen-service/src/test/java/io/restaurantos/kitchen/KitchenTestBase.java (exact @SpringBootTest + PostgreSQLContainer + @BeforeAll Flyway.migrate() + @DynamicPropertySource + @MockitoBean RabbitTemplate/StringRedisTemplate/OpaClient pattern)
    - .planning/phases/08-inventory-recipe-management/08-PATTERNS.md § "InventoryTestBase" (the exact base-class body + the RabbitTemplate-mock vs RabbitMQContainer decision)
    - services/kitchen-service/src/test/java (a TestFixtures/fixture helper if present, for the JWT/TenantContext builder shape)
    - shared-lib/src/main/java/io/restaurantos/shared/tenant/TenantContext.java (set(tenantId, userId, branchId, ...) signature used to activate tenant context in tests)
  </read_first>
  <action>
    Create InventoryTestBase as an abstract @SpringBootTest base mirroring KitchenTestBase: a static PostgreSQLContainer("postgres:16") with databaseName inventory_db, username inventory_user, password inventory_pass, started in a static block with TESTCONTAINERS_RYUK_DISABLED=true; a @BeforeAll applyMigrations() that runs Flyway.configure().dataSource(jdbcUrl,user,pass).locations("classpath:db/migration").cleanDisabled(false).baselineOnMigrate(false).load() then clean()+migrate(); a @DynamicPropertySource wiring spring.datasource.* to the container, spring.jpa.hibernate.ddl-auto=none, spring.flyway.enabled=false, eureka.client.enabled=false, spring.cloud.config.enabled=false, TESTCONTAINERS_RYUK_DISABLED=true, restaurantos.opa.url=http://127.0.0.1:1; and @MockitoBean fields for RabbitTemplate, StringRedisTemplate, OpaClient (mock the broker per PATTERNS.md — the depletion consumer IT in 08-05 will drive DepletionService directly rather than via a live queue). Create TestFixtures as a static helper providing: methods to build a tenant/branch UUID pair and activate TenantContext for a test (set(tenantId, userId, branchId, null)), a helper to run a block with app.current_tenant_id GUC set on a JDBC connection (for RLS-scoped raw seeding), and JWT/authentication builder variants for OWNER, MANAGER, and INVENTORY_MANAGER roles carrying the inventory.item.view / inventory.item.manage permissions. Do NOT reference any Phase-8 domain entity class here.
  </action>
  <acceptance_criteria>
    - `mvn -pl services/inventory-service -am test-compile -q` compiles the test sources.
    - InventoryTestBase exposes @MockitoBean RabbitTemplate and applies Flyway migrations from classpath:db/migration against a Testcontainers PostgreSQL.
    - TestFixtures exposes an INVENTORY_MANAGER role/JWT builder and a TenantContext activation helper.
    - InventoryTestBase source contains no import of io.restaurantos.inventory.domain.model.* (entity-independent).
  </acceptance_criteria>
  <verify>
    <automated>mvn -pl services/inventory-service -am test-compile -q</automated>
  </verify>
  <done>Reusable Testcontainers IT base + fixtures compile and are entity-independent.</done>
</task>

<task type="auto">
  <name>Task 2: SchemaMigrationIT — migrations apply and FORCE RLS is enforced</name>
  <files>services/inventory-service/src/test/java/io/restaurantos/inventory/SchemaMigrationIT.java</files>
  <read_first>
    - services/inventory-service/src/test/java/io/restaurantos/inventory/InventoryTestBase.java (from Task 1 — extend it)
    - services/inventory-service/src/main/resources/db/migration/V1__inventory_schema.sql (table names + policy names to assert)
    - .planning/phases/08-inventory-recipe-management/08-RESEARCH.md Pitfall 2 & Assumption A2 (FORCE RLS assertion via pg_catalog; existing services do NOT set FORCE, so this IT is the first to assert it)
  </read_first>
  <action>
    Create SchemaMigrationIT extends InventoryTestBase. After migrations apply, use a JdbcTemplate/DataSource query to assert: (1) all 14 expected tables exist in information_schema.tables (11 domain + event_outbox + idempotency_keys + processed_events); (2) for a representative domain table (ingredient_branch_stock), pg_class.relrowsecurity = true AND pg_class.relforcerowsecurity = true (query: SELECT relrowsecurity, relforcerowsecurity FROM pg_class WHERE relname = 'ingredient_branch_stock'); (3) a tenant_isolation policy exists for ingredient_branch_stock in pg_policies; (4) the three infra tables (event_outbox, idempotency_keys, processed_events) have relrowsecurity = false. Because the Testcontainers superuser bypasses RLS row-visibility, assert on the pg_catalog metadata (policy + FORCE flags) rather than attempting a cross-tenant row-visibility read (per RESEARCH Assumption A2 / decision [03-03-B]).
  </action>
  <acceptance_criteria>
    - `mvn -pl services/inventory-service verify -Dit.test=SchemaMigrationIT -DfailIfNoTests=false` passes (Docker required).
    - The IT asserts relforcerowsecurity = true for ingredient_branch_stock and a tenant_isolation policy row in pg_policies.
    - The IT asserts relrowsecurity = false for event_outbox, idempotency_keys, and processed_events.
  </acceptance_criteria>
  <verify>
    <automated>mvn -pl services/inventory-service verify -Dit.test=SchemaMigrationIT -DfailIfNoTests=false</automated>
  </verify>
  <done>Schema applies cleanly; FORCE RLS + tenant_isolation proven on domain tables; infra tables proven RLS-exempt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB connection → tenant rows | The runtime guarantee that RLS is FORCE-enforced must be verified, not assumed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-8-XTEN | Elevation of Privilege | RLS on domain tables | high | mitigate | SchemaMigrationIT asserts relforcerowsecurity=true + tenant_isolation policy on domain tables and relrowsecurity=false on infra tables — the runtime proof of 08-01's FORCE-RLS mitigation (must_haves.truths #2). |
| T-8-SC | Tampering (supply chain) | test dependencies | low | accept | Testcontainers/JUnit5/AssertJ/Awaitility resolve from the vetted parent-POM managed versions; no new package introduced. |
</threat_model>

<verification>
- `mvn -pl services/inventory-service -am test-compile -q` compiles test sources.
- `mvn -pl services/inventory-service verify -Dit.test=SchemaMigrationIT -DfailIfNoTests=false` green.
</verification>

<success_criteria>
A reusable Testcontainers IT harness exists and the schema's FORCE-RLS isolation is proven at runtime, unblocking all Wave-3+ feature ITs.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 08-02)

- `test/InventoryTestBase` (abstract IT base class), `test/TestFixtures` (JWT/TenantContext helpers incl. INVENTORY_MANAGER), `test/SchemaMigrationIT` (schema + FORCE-RLS assertion IT).
</artifacts_produced>

<output>
Create `.planning/phases/08-inventory-recipe-management/08-02-SUMMARY.md` when done.
</output>
