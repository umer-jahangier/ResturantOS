---
phase: 08-inventory-recipe-management
plan: 03
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - services/inventory-service/src/main/java/io/restaurantos/inventory/domain/model/UnitOfMeasure.java
  - services/inventory-service/src/main/java/io/restaurantos/inventory/domain/model/Ingredient.java
  - services/inventory-service/src/main/java/io/restaurantos/inventory/domain/model/IngredientBranchStock.java
  - services/inventory-service/src/main/java/io/restaurantos/inventory/domain/model/StockLot.java
  - services/inventory-service/src/main/java/io/restaurantos/inventory/domain/model/InventoryMovement.java
  - services/inventory-service/src/main/java/io/restaurantos/inventory/repository/UnitOfMeasureRepository.java
  - services/inventory-service/src/main/java/io/restaurantos/inventory/repository/IngredientRepository.java
  - services/inventory-service/src/main/java/io/restaurantos/inventory/repository/IngredientBranchStockRepository.java
  - services/inventory-service/src/main/java/io/restaurantos/inventory/repository/StockLotRepository.java
  - services/inventory-service/src/main/java/io/restaurantos/inventory/repository/InventoryMovementRepository.java
  - services/inventory-service/src/main/java/io/restaurantos/inventory/service/MacCalculator.java
  - services/inventory-service/src/main/java/io/restaurantos/inventory/service/IngredientService.java
  - services/inventory-service/src/main/java/io/restaurantos/inventory/service/OpeningBalanceService.java
  - services/inventory-service/src/main/java/io/restaurantos/inventory/web/IngredientController.java
  - services/inventory-service/src/main/java/io/restaurantos/inventory/web/UnitOfMeasureController.java
  - services/inventory-service/src/main/java/io/restaurantos/inventory/web/OpeningBalanceController.java
  - services/inventory-service/src/main/java/io/restaurantos/inventory/dto/InventoryDtos.java
  - services/inventory-service/src/test/java/io/restaurantos/inventory/InventoryFixtures.java
  - services/inventory-service/src/test/java/io/restaurantos/inventory/MacCalculatorTest.java
  - services/inventory-service/src/test/java/io/restaurantos/inventory/IngredientAdminIT.java
  - services/inventory-service/src/test/java/io/restaurantos/inventory/OpeningBalanceIT.java
  - gateway/src/main/resources/application.yml
  - gateway/src/main/java/io/restaurantos/gateway/support/RouteFeatureMap.java
autonomous: true
requirements: [INV-01, INV-07]
user_setup: []

must_haves:
  truths:
    - "A manager can create/update ingredients, units of measure (with to_base_factor conversion), and per-ingredient reorder points via /api/v1/inventory/** endpoints, gated by FEATURE_INVENTORY."
    - "Opening stock is recorded as an OPENING_BALANCE inventory_movements row and sets the ingredient_branch_stock qty_on_hand + avg_cost_paisa."
    - "MacCalculator computes the weighted moving-average cost in BigDecimal with HALF_UP rounding (mirrors MoneyUtils.fromPkr, never the FLOOR taxPerLine)."
    - "IngredientBranchStockRepository.findForUpdate(tenantId, branchId, ingredientId) acquires a PESSIMISTIC_WRITE lock; StockLotRepository returns lots oldest-expiry-first for FEFO."
    - "Negative reorder points / negative opening quantities are rejected at the API boundary (@Valid)."
  artifacts:
    - services/inventory-service/src/main/java/io/restaurantos/inventory/service/MacCalculator.java
    - services/inventory-service/src/main/java/io/restaurantos/inventory/repository/IngredientBranchStockRepository.java
    - services/inventory-service/src/main/java/io/restaurantos/inventory/repository/StockLotRepository.java
    - services/inventory-service/src/main/java/io/restaurantos/inventory/service/OpeningBalanceService.java
    - services/inventory-service/src/test/java/io/restaurantos/inventory/InventoryFixtures.java
  key_links:
    - "Every domain entity extends shared-lib TenantAuditableEntity (inherits tenantId + audit + tenantFilter)."
    - "OpeningBalanceService uses MacCalculator to set avg_cost_paisa and writes movement_type='OPENING_BALANCE'."
    - "gateway inventory-route (Path=/api/v1/inventory/**, lb://inventory-service) is active and RouteFeatureMap maps /api/v1/inventory/ to FEATURE_INVENTORY."
  prohibitions:
    - "MacCalculator does NOT use RoundingMode.FLOOR or MoneyUtils.taxPerLine for cost math."
    - "Opening-balance and reorder-point endpoints do NOT accept a client-supplied tenant_id (tenant resolved from JWT/TenantContext only)."
    - "The pre-existing pos-route gateway gap is NOT modified (out of scope; only the inventory-route is activated)."
---

<objective>
Build the shared inventory stock-domain model (entities + repositories), the moving-average-cost calculator, the ingredient/UOM/reorder-point master-data CRUD, and opening-balance recording (INV-01, INV-07). Activate the gateway route so `/api/v1/inventory/**` is reachable. Provide `InventoryFixtures` so downstream feature ITs can seed ingredient/stock/lot rows.

Purpose: This plan is the domain substrate for the whole phase — depletion (08-05), receipts (08-06), transfers (08-07), and counts (08-08) all read these entities/repositories, `MacCalculator`, and `InventoryFixtures`.
Output: Master-data + opening-balance API, the pessimistic-lock + FEFO repositories, the MAC utility, and the test fixture helper.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-inventory-recipe-management/08-CONTEXT.md
@.planning/phases/08-inventory-recipe-management/08-RESEARCH.md
@.planning/phases/08-inventory-recipe-management/08-PATTERNS.md
@services/inventory-service/src/main/resources/db/migration/V1__inventory_schema.sql
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Stock-domain entities, repositories (findForUpdate + FEFO), MacCalculator, and InventoryFixtures</name>
  <files>services/inventory-service/src/main/java/io/restaurantos/inventory/domain/model/UnitOfMeasure.java, services/inventory-service/src/main/java/io/restaurantos/inventory/domain/model/Ingredient.java, services/inventory-service/src/main/java/io/restaurantos/inventory/domain/model/IngredientBranchStock.java, services/inventory-service/src/main/java/io/restaurantos/inventory/domain/model/StockLot.java, services/inventory-service/src/main/java/io/restaurantos/inventory/domain/model/InventoryMovement.java, services/inventory-service/src/main/java/io/restaurantos/inventory/repository/UnitOfMeasureRepository.java, services/inventory-service/src/main/java/io/restaurantos/inventory/repository/IngredientRepository.java, services/inventory-service/src/main/java/io/restaurantos/inventory/repository/IngredientBranchStockRepository.java, services/inventory-service/src/main/java/io/restaurantos/inventory/repository/StockLotRepository.java, services/inventory-service/src/main/java/io/restaurantos/inventory/repository/InventoryMovementRepository.java, services/inventory-service/src/main/java/io/restaurantos/inventory/service/MacCalculator.java, services/inventory-service/src/test/java/io/restaurantos/inventory/InventoryFixtures.java, services/inventory-service/src/test/java/io/restaurantos/inventory/MacCalculatorTest.java</files>
  <read_first>
    - shared-lib/src/main/java/io/restaurantos/shared/entity/TenantAuditableEntity.java (base class every entity extends; the inherited columns must match V1 exactly)
    - services/pos-service/src/main/java/io/restaurantos/pos/repository/OrderSequenceRepository.java (exact @Lock(PESSIMISTIC_WRITE) @Query findForUpdate shape)
    - services/inventory-service/src/main/resources/db/migration/V1__inventory_schema.sql (column names/types the entities map)
    - shared-lib/src/main/java/io/restaurantos/shared/money/MoneyUtils.java (fromPkr HALF_UP rounding to mirror; taxPerLine FLOOR to avoid)
    - .planning/phases/08-inventory-recipe-management/08-RESEARCH.md § "MAC recompute" code example + Pitfall 4 (BigDecimal recompute, HALF_UP, oldQty+recvQty==0 guard, oldQty<=0 policy)
    - services/inventory-service/src/test/java/io/restaurantos/inventory/InventoryTestBase.java (for InventoryFixtures seeding via repositories/JdbcTemplate under tenant GUC)
  </read_first>
  <behavior>
    - MacCalculatorTest: recompute(oldQty=10, oldAvg=500, recvQty=10, recvCost=700) returns 600 (weighted average, HALF_UP).
    - MacCalculatorTest: recompute where the exact quotient is fractional rounds HALF_UP to the nearer paisa (e.g. a .5 case rounds up), never FLOOR.
    - MacCalculatorTest: recompute onto empty stock (oldQty=0, oldAvg=0, recvQty=5, recvCost=350) returns 350 (receipt unit cost).
    - MacCalculatorTest: recompute onto negative on-hand (oldQty=-2 from D-02 oversell, oldAvg=400, recvQty=10, recvCost=600) applies the documented oldQty<=0 policy: reset avg to the receipt unit cost 600.
  </behavior>
  <action>
    Create the five JPA entities (UnitOfMeasure, Ingredient, IngredientBranchStock, StockLot, InventoryMovement) extending TenantAuditableEntity with @Id UUID @GeneratedValue and @Column mappings to the V1 columns; qty fields are BigDecimal (NUMERIC(18,4)), to_base_factor BigDecimal (NUMERIC(18,8)), cost fields long paisa, movement_type/status String. Create the repositories: UnitOfMeasureRepository (findByCode within tenant), IngredientRepository (findBySku, findByIsActiveTrue), IngredientBranchStockRepository with findForUpdate(@Param tenantId, @Param branchId, @Param ingredientId) annotated @Lock(LockModeType.PESSIMISTIC_WRITE) + JPQL "SELECT s FROM IngredientBranchStock s WHERE s.tenantId=:tenantId AND s.branchId=:branchId AND s.ingredientId=:ingredientId" plus a plain findByBranchIdAndIngredientId, StockLotRepository with findByStockIdOrderByExpiryDateAsc(UUID stockId) (FEFO: NULL expiry ordered last via NULLS LAST) and findByTenantIdAndExpiryDateLessThanEqualAndQtyGreaterThan(...) for the later sweep, InventoryMovementRepository (basic save + findByReferenceId). Create service/MacCalculator as a static/stateless utility recomputeAvgCostPaisa(BigDecimal oldQty, long oldAvgCostPaisa, BigDecimal recvQty, long recvUnitCostPaisa): compute oldValue=oldQty*oldAvg, recvValue=recvQty*recvCost, newQty=oldQty+recvQty; if newQty.signum()==0 return recvUnitCostPaisa; if oldQty.signum()<=0 return recvUnitCostPaisa (documented D-02-oversell policy — a receipt onto zero/negative on-hand resets MAC to the receipt unit cost); otherwise return oldValue.add(recvValue).divide(newQty, 0, RoundingMode.HALF_UP).longValueExact(). Create MacCalculatorTest asserting the four behaviors above. Create test/InventoryFixtures with repository/JdbcTemplate helpers to seed a UnitOfMeasure, an Ingredient (with reorder point), an IngredientBranchStock (qty + avg_cost), and a StockLot (qty + expiry + receipt cost) for a given tenant/branch under the tenant GUC — these are reused by all downstream feature ITs without modification.
  </action>
  <acceptance_criteria>
    - `mvn -pl services/inventory-service test -Dtest=MacCalculatorTest` passes (all four behaviors green, no Docker).
    - IngredientBranchStockRepository.findForUpdate is annotated @Lock(LockModeType.PESSIMISTIC_WRITE).
    - StockLotRepository exposes findByStockIdOrderByExpiryDateAsc (FEFO ordering).
    - `grep -c "RoundingMode.HALF_UP" services/inventory-service/src/main/java/io/restaurantos/inventory/service/MacCalculator.java` returns at least 1; `grep -c "FLOOR" services/inventory-service/src/main/java/io/restaurantos/inventory/service/MacCalculator.java` returns 0.
    - All five entities extend TenantAuditableEntity.
  </acceptance_criteria>
  <verify>
    <automated>mvn -pl services/inventory-service test -Dtest=MacCalculatorTest</automated>
  </verify>
  <done>Domain entities, pessimistic-lock + FEFO repositories, HALF_UP MAC calculator, and reusable fixtures compile and pass unit tests.</done>
</task>

<task type="auto">
  <name>Task 2: Ingredient / UOM / reorder-point master CRUD + gateway route activation (INV-01)</name>
  <files>services/inventory-service/src/main/java/io/restaurantos/inventory/service/IngredientService.java, services/inventory-service/src/main/java/io/restaurantos/inventory/web/IngredientController.java, services/inventory-service/src/main/java/io/restaurantos/inventory/web/UnitOfMeasureController.java, services/inventory-service/src/main/java/io/restaurantos/inventory/dto/InventoryDtos.java, services/inventory-service/src/test/java/io/restaurantos/inventory/IngredientAdminIT.java, gateway/src/main/resources/application.yml, gateway/src/main/java/io/restaurantos/gateway/support/RouteFeatureMap.java</files>
  <read_first>
    - services/pos-service/src/main/java/io/restaurantos/pos/web/MenuController.java (@RestController + @RequestMapping + @RequiresFeature + ApiResponse.ok(...) wrapping + @Valid request bodies)
    - shared-lib/src/main/java/io/restaurantos/shared/api/ApiResponse.java (envelope; controllers return ResponseEntity<ApiResponse<T>>)
    - shared-lib/src/main/java/io/restaurantos/shared/feature/RequiresFeature.java (annotation usage; FEATURE_INVENTORY already exists per PLATFORM-10)
    - gateway/src/main/resources/application.yml (the commented inventory-route stub to uncomment: Path=/api/v1/inventory/**, uri lb://inventory-service, circuit breaker; adjacent finance-route as the shape to mirror)
    - gateway/src/main/java/io/restaurantos/gateway/support/RouteFeatureMap.java (existing route→FEATURE map entries; add /api/v1/inventory/ → FEATURE_INVENTORY)
    - services/inventory-service/src/test/java/io/restaurantos/inventory/InventoryTestBase.java + TestFixtures.java (IT base + INVENTORY_MANAGER JWT)
  </read_first>
  <action>
    Create dto/InventoryDtos.java holding request/response records for ingredients and UOM: CreateIngredientRequest(@NotBlank String name, @NotBlank String sku, @NotBlank String baseUomCode, String category, @NotNull @PositiveOrZero BigDecimal reorderPoint), UpdateIngredientRequest, IngredientDto, CreateUomRequest(@NotBlank String code, @NotBlank String name, String baseUnitCode, @NotNull @Positive BigDecimal toBaseFactor), UomDto. Create IngredientService with create/update/list/get for ingredients (tenant resolved from TenantContext, never from the request) and create/list for UOM; setting reorder_point per ingredient. Create IngredientController @RequestMapping("/api/v1/inventory/ingredients") @RequiresFeature("FEATURE_INVENTORY") with GET list, GET {id}, POST (@Valid CreateIngredientRequest), PUT {id} (@Valid), returning ResponseEntity<ApiResponse<...>>; and UnitOfMeasureController @RequestMapping("/api/v1/inventory/uom") @RequiresFeature("FEATURE_INVENTORY") with GET list + POST (@Valid CreateUomRequest). Activate the gateway inventory-route in gateway/application.yml (uncomment/add Path=/api/v1/inventory/** → lb://inventory-service with an inventoryCircuitBreaker, mirroring finance-route) and add a /api/v1/inventory/ → FEATURE_INVENTORY entry to RouteFeatureMap; leave the pos-route stub untouched. Create IngredientAdminIT extends InventoryTestBase asserting: an INVENTORY_MANAGER can POST a valid ingredient (201/200 + persisted row) and list/get it; a POST with a negative reorderPoint is rejected 400; a UOM with a non-positive toBaseFactor is rejected 400.
  </action>
  <acceptance_criteria>
    - `mvn -pl services/inventory-service verify -Dit.test=IngredientAdminIT -DfailIfNoTests=false` passes.
    - POST /api/v1/inventory/ingredients with a valid body persists and returns the ingredient; negative reorderPoint returns 400 (validation rejects negative manager input — T-8-NEGQTY).
    - IngredientController and UnitOfMeasureController carry @RequiresFeature("FEATURE_INVENTORY").
    - gateway/application.yml contains an active route with Path predicate /api/v1/inventory/** → lb://inventory-service and RouteFeatureMap maps /api/v1/inventory/ to FEATURE_INVENTORY.
  </acceptance_criteria>
  <verify>
    <automated>mvn -pl services/inventory-service verify -Dit.test=IngredientAdminIT -DfailIfNoTests=false</automated>
  </verify>
  <done>Ingredient/UOM/reorder CRUD works with feature gating + validation; gateway route reaches inventory-service.</done>
</task>

<task type="auto">
  <name>Task 3: Opening-balance recording via OPENING_BALANCE movement (INV-07)</name>
  <files>services/inventory-service/src/main/java/io/restaurantos/inventory/service/OpeningBalanceService.java, services/inventory-service/src/main/java/io/restaurantos/inventory/web/OpeningBalanceController.java, services/inventory-service/src/test/java/io/restaurantos/inventory/OpeningBalanceIT.java</files>
  <read_first>
    - services/inventory-service/src/main/java/io/restaurantos/inventory/service/MacCalculator.java (Task 1 — used to set initial avg_cost_paisa)
    - services/inventory-service/src/main/java/io/restaurantos/inventory/repository/IngredientBranchStockRepository.java + StockLotRepository.java + InventoryMovementRepository.java (Task 1)
    - services/inventory-service/src/main/java/io/restaurantos/inventory/dto/InventoryDtos.java (Task 2 — add the opening-balance request record here)
    - .planning/phases/08-inventory-recipe-management/08-CONTEXT.md D-02/D-04 (lots floor at zero; opening balance seeds initial stock + a lot)
  </read_first>
  <action>
    Add RecordOpeningBalanceRequest(@NotNull UUID ingredientId, @NotNull UUID branchId, @NotNull @Positive BigDecimal qty, @NotNull @PositiveOrZero long unitCostPaisa, java.time.LocalDate expiryDate) to InventoryDtos. Create OpeningBalanceService.recordOpeningBalance(request) as @Transactional: acquire the stock row via findForUpdate (creating the ingredient_branch_stock row if absent), set qty_on_hand to the opening qty and avg_cost_paisa via MacCalculator.recomputeAvgCostPaisa(existingQty, existingAvg, qty, unitCostPaisa) (on a fresh row this yields the unit cost), create a StockLot(qty, expiryDate, receipt_unit_cost_paisa=unitCostPaisa) so FEFO has physical inventory to walk, and write an inventory_movements row with movement_type='OPENING_BALANCE', signed qty, unit_cost_paisa, total_cost_paisa, reference_type='OPENING_BALANCE'. No event is published for opening balance (internal movement only, per CONTEXT). Create OpeningBalanceController @RequestMapping("/api/v1/inventory/opening-balance") @RequiresFeature("FEATURE_INVENTORY") POST (@Valid). Create OpeningBalanceIT extends InventoryTestBase asserting: recording an opening balance sets qty_on_hand and avg_cost_paisa, creates exactly one OPENING_BALANCE movement, and creates a stock_lot with the given expiry; a negative/zero opening qty is rejected 400.
  </action>
  <acceptance_criteria>
    - `mvn -pl services/inventory-service verify -Dit.test=OpeningBalanceIT -DfailIfNoTests=false` passes.
    - After recording, ingredient_branch_stock.qty_on_hand equals the opening qty, avg_cost_paisa equals the opening unit cost, and exactly one inventory_movements row with movement_type='OPENING_BALANCE' exists.
    - A stock_lots row with the supplied expiry_date and receipt_unit_cost_paisa is created.
    - A non-positive opening qty returns 400.
  </acceptance_criteria>
  <verify>
    <automated>mvn -pl services/inventory-service verify -Dit.test=OpeningBalanceIT -DfailIfNoTests=false</automated>
  </verify>
  <done>Opening stock is recorded as an OPENING_BALANCE movement that sets stock qty + MAC and seeds a FEFO lot.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/v1/inventory/** | Manager input (ingredient, reorder point, opening qty) crosses into master data |
| gateway → inventory-service | Route + feature-flag enforcement for the inventory module |
| DB connection → tenant rows | Master data must remain tenant/branch isolated |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-8-XTEN | Elevation of Privilege | ingredient/uom/stock/movement queries | high | mitigate | FORCE RLS (08-01) + every repository query scopes tenantId explicitly; tenant resolved from JWT/TenantContext, never from request body (must_haves.prohibitions #2). |
| T-8-AC | Elevation of Privilege | master-data + opening-balance endpoints | medium | mitigate | @RequiresFeature("FEATURE_INVENTORY") + JWT auth + inventory.item.manage permission; gateway RouteFeatureMap gate. |
| T-8-NEGQTY | Tampering / input validation (V5) | CreateIngredientRequest, RecordOpeningBalanceRequest, CreateUomRequest | medium | mitigate | @Valid + @PositiveOrZero/@Positive reject negative reorder point, non-positive UOM factor, and non-positive opening qty at the API boundary (acceptance criteria in Tasks 2 & 3). |
| T-8-SC | Tampering (supply chain) | dependencies | low | accept | No new packages. |
</threat_model>

<verification>
- `mvn -pl services/inventory-service test -Dtest=MacCalculatorTest` green.
- `mvn -pl services/inventory-service verify -Dit.test=IngredientAdminIT,OpeningBalanceIT -DfailIfNoTests=false` green.
- Gateway route + RouteFeatureMap entry present.
</verification>

<success_criteria>
Managers manage ingredients/UOM/reorder points and opening stock is recorded via an OPENING_BALANCE movement; the shared stock-domain model, MAC calculator, FEFO/lock repositories, and fixtures exist for downstream plans.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 08-03)

- Entities: `UnitOfMeasure`, `Ingredient`, `IngredientBranchStock`, `StockLot`, `InventoryMovement`
- Repositories: `UnitOfMeasureRepository`, `IngredientRepository`, `IngredientBranchStockRepository` (findForUpdate), `StockLotRepository` (FEFO), `InventoryMovementRepository`
- Services: `MacCalculator`, `IngredientService`, `OpeningBalanceService`
- Controllers: `IngredientController`, `UnitOfMeasureController`, `OpeningBalanceController` (routes under /api/v1/inventory/**)
- DTOs: `InventoryDtos` (CreateIngredientRequest, UpdateIngredientRequest, IngredientDto, CreateUomRequest, UomDto, RecordOpeningBalanceRequest)
- Test helpers: `InventoryFixtures`, `MacCalculatorTest`, `IngredientAdminIT`, `OpeningBalanceIT`
- Gateway: active inventory-route (`/api/v1/inventory/**` → lb://inventory-service); `RouteFeatureMap` entry `/api/v1/inventory/` → FEATURE_INVENTORY
</artifacts_produced>

<output>
Create `.planning/phases/08-inventory-recipe-management/08-03-SUMMARY.md` when done.
</output>
