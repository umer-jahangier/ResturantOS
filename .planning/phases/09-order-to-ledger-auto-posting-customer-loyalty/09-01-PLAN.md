---
phase: 09-order-to-ledger-auto-posting-customer-loyalty
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/finance-service/src/main/resources/db/migration/V3__posted_source_events.sql
  - services/finance-service/src/main/java/io/restaurantos/finance/autopost/PostedSourceEventEntity.java
  - services/finance-service/src/main/java/io/restaurantos/finance/autopost/PostedSourceEventRepository.java
  - services/finance-service/src/main/java/io/restaurantos/finance/autopost/ProcessedEventService.java
  - services/finance-service/src/main/java/io/restaurantos/finance/autopost/AccountResolver.java
  - services/finance-service/src/main/java/io/restaurantos/finance/autopost/AutoPostingRecipeEngine.java
  - services/finance-service/src/main/java/io/restaurantos/finance/autopost/consumer/OrderClosedConsumer.java
  - services/finance-service/src/main/java/io/restaurantos/finance/autopost/consumer/StockDepletedConsumer.java
  - services/finance-service/src/main/java/io/restaurantos/finance/autopost/consumer/OrderRefundedConsumer.java
  - services/finance-service/src/main/java/io/restaurantos/finance/autopost/consumer/WastageConsumer.java
  - services/finance-service/src/main/java/io/restaurantos/finance/autopost/consumer/CountVarianceConsumer.java
  - services/finance-service/src/main/java/io/restaurantos/finance/autopost/consumer/TransferShippedConsumer.java
  - services/finance-service/src/main/java/io/restaurantos/finance/autopost/consumer/TransferReceivedConsumer.java
  - services/finance-service/src/main/java/io/restaurantos/finance/config/RabbitListenerConfig.java
  - services/finance-service/src/test/java/io/restaurantos/finance/autopost/OrderCloseAutoPostingIT.java
  - services/finance-service/src/test/java/io/restaurantos/finance/autopost/InventoryAutoPostingIT.java
  - deploy/init/rabbitmq-definitions.json
autonomous: true

must_haves:
  truths:
    - "ORDER_CLOSED produces a balanced revenue JE (DR payment accounts, CR revenue + output tax) posted to the open period"
    - "STOCK_DEPLETED produces a balanced COGS JE (DR COGS, CR inventory) using totalCogsPaisa from the inventory event"
    - "Re-delivering the same eventId or same (source_type, source_id) produces no duplicate JE — idempotent via processed_events + posted_source_events + journal_entries"
    - "ORDER_REFUNDED, WASTAGE_RECORDED, COUNT_VARIANCE_POSTED, TRANSFER_SHIPPED, and TRANSFER_RECEIVED each post a balanced JE per M3.4 recipes"
    - "Posting to a LOCKED period fails without creating a posted_source_events row (fail-safe)"
  artifacts:
    - path: "services/finance-service/src/main/java/io/restaurantos/finance/autopost/AutoPostingRecipeEngine.java"
      provides: "M3.4 recipe implementations resolving accounts by system_tag"
      min_lines: 80
    - path: "services/finance-service/src/main/resources/db/migration/V3__posted_source_events.sql"
      provides: "posted_source_events table with UNIQUE(tenant_id, source_type, source_id)"
      contains: "posted_source_events"
    - path: "services/finance-service/src/main/java/io/restaurantos/finance/autopost/consumer/OrderClosedConsumer.java"
      provides: "Idempotent consumer on finance.order-closed.queue"
      contains: "finance.order-closed.queue"
    - path: "services/finance-service/src/test/java/io/restaurantos/finance/autopost/OrderCloseAutoPostingIT.java"
      provides: "IT proving revenue + COGS + dedup"
      min_lines: 60
  key_links:
    - from: "OrderClosedConsumer"
      to: "AutoPostingRecipeEngine.postOrderRevenue"
      via: "ProcessedEventService.tryProcess → TenantAwareMessageProcessor"
      pattern: "ORDER_REVENUE|postOrderRevenue"
    - from: "StockDepletedConsumer"
      to: "AutoPostingRecipeEngine.postOrderCogs"
      via: "finance.stock-depleted.queue → totalCogsPaisa"
      pattern: "ORDER_COGS|totalCogsPaisa"
    - from: "AutoPostingRecipeEngine"
      to: "JournalEntryService.autoPostInternal"
      via: "InternalAutoPostJeRequest with source_type + source_id"
      pattern: "autoPostInternal"
---

<objective>
Extend finance-service with the auto-posting engine and RabbitMQ consumers that close the order-to-ledger loop. Implements Phase 9 SC1 (partial FIN-03): order close revenue+COGS, refund, wastage, stock count variance, and inter-branch transfer recipes — all balanced, idempotent, and routed through the existing JE engine from Phase 6.

Purpose: Core value — POS order → balanced double-entry JE.  
Output: `posted_source_events` table, recipe engine, 7 consumers, new RabbitMQ bindings, Testcontainers ITs.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-order-to-ledger-auto-posting-customer-loyalty/09-RESEARCH.md

@Docs/RestaurantERP_SaaS_Specification.md
@Docs/agent-specs/02-event-schema-registry.md
@Docs/agent-specs/08-database-migration-guide.md

@services/finance-service/src/main/java/io/restaurantos/finance/service/JournalEntryServiceImpl.java
@services/finance-service/src/main/java/io/restaurantos/finance/web/InternalFinanceController.java
@services/finance-service/src/main/java/io/restaurantos/finance/seed/PakistanRestaurantCoaTemplate.java
@shared-lib/src/main/java/io/restaurantos/shared/messaging/TenantAwareMessageProcessor.java
@shared-lib/src/main/java/io/restaurantos/shared/event/EventEnvelope.java
@deploy/init/rabbitmq-definitions.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: posted_source_events + recipe engine + order-close consumers + RabbitMQ bindings</name>
  <files>services/finance-service/src/main/resources/db/migration/V3__posted_source_events.sql, services/finance-service/src/main/java/io/restaurantos/finance/autopost/PostedSourceEventEntity.java, services/finance-service/src/main/java/io/restaurantos/finance/autopost/PostedSourceEventRepository.java, services/finance-service/src/main/java/io/restaurantos/finance/autopost/ProcessedEventService.java, services/finance-service/src/main/java/io/restaurantos/finance/autopost/AccountResolver.java, services/finance-service/src/main/java/io/restaurantos/finance/autopost/AutoPostingRecipeEngine.java, services/finance-service/src/main/java/io/restaurantos/finance/autopost/consumer/OrderClosedConsumer.java, services/finance-service/src/main/java/io/restaurantos/finance/autopost/consumer/StockDepletedConsumer.java, services/finance-service/src/main/java/io/restaurantos/finance/config/RabbitListenerConfig.java, deploy/init/rabbitmq-definitions.json</files>
  <action>
    Add Flyway migration `V3__posted_source_events.sql`:
    ```sql
    CREATE TABLE posted_source_events (
      id           UUID PRIMARY KEY DEFAULT gen_random_uuid(),
      tenant_id    UUID NOT NULL,
      source_type  VARCHAR(50) NOT NULL,
      source_id    UUID NOT NULL,
      je_id        UUID NOT NULL REFERENCES journal_entries(id),
      posted_at    TIMESTAMPTZ NOT NULL DEFAULT now(),
      UNIQUE (tenant_id, source_type, source_id)
    );
    GRANT SELECT, INSERT ON posted_source_events TO finance_user;
    ```
    NON-RLS (finance internal bookkeeping).

    `AccountResolver`: given tenantId + system_tag (CASH, BANK, REVENUE, OUTPUT_TAX, COGS, INVENTORY, etc.) → account code from `chart_of_accounts`. Throw `AccountNotConfiguredException` if missing (fail loud in IT).

    `ProcessedEventService`: copy pattern from notification-service Phase 5 — `tryProcess(consumer, eventId, Runnable)`.

    `AutoPostingRecipeEngine`:
    - `postOrderRevenue(EventEnvelope)` — parse Doc 02 ORDER_CLOSED payload. Map payments: CASH→CASH, CARD/WALLET→BANK, LOYALTY_POINTS→loyalty liability account (2400 or system_tag `LOYALTY_LIABILITY`). DR payment splits; CR REVENUE for (subtotalPaisa - discountPaisa); CR OUTPUT_TAX for taxPaisa; if discountPaisa > 0, DR discount account (4910). Resolve open period via `AccountingPeriodService`. Check `posted_source_events` + call `jeService.autoPostInternal` with source_type=`ORDER_REVENUE`, source_id=orderId. Insert posted_source_events after successful post.
    - `postOrderCogs(EventEnvelope)` — parse STOCK_DEPLETED payload `totalCogsPaisa`. DR COGS / CR INVENTORY. source_type=`ORDER_COGS`, source_id=orderId.

    `OrderClosedConsumer` on `finance.order-closed.queue` (already bound).
    `StockDepletedConsumer` on NEW `finance.stock-depleted.queue`.

    Update `deploy/init/rabbitmq-definitions.json`:
    - Add queue `finance.stock-depleted.queue` + DLQ, bound to `inventory.topic` routing key `inventory.stock.depleted`
    - (Existing `finance.order-closed.queue` unchanged)

    Enable `@EnableRabbit` if not already on FinanceServiceApplication.
  </action>
  <verify>`mvn -pl services/finance-service -am -q -DskipTests compile`; `grep "posted_source_events" services/finance-service/src/main/resources/db/migration/V3__posted_source_events.sql`; `grep "finance.stock-depleted.queue" deploy/init/rabbitmq-definitions.json`; `grep "ORDER_REVENUE" services/finance-service/.../AutoPostingRecipeEngine.java`</verify>
  <done>Recipe engine posts balanced revenue JE on ORDER_CLOSED and COGS JE on STOCK_DEPLETED; idempotency tables wired; RabbitMQ binding for stock-depleted added.</done>
</task>

<task type="auto">
  <name>Task 2: Inventory/POS auto-post consumers + ITs proving SC1 + FIN-03 Phase-9 scope</name>
  <files>services/finance-service/src/main/java/io/restaurantos/finance/autopost/consumer/OrderRefundedConsumer.java, services/finance-service/src/main/java/io/restaurantos/finance/autopost/consumer/WastageConsumer.java, services/finance-service/src/main/java/io/restaurantos/finance/autopost/consumer/CountVarianceConsumer.java, services/finance-service/src/main/java/io/restaurantos/finance/autopost/consumer/TransferShippedConsumer.java, services/finance-service/src/main/java/io/restaurantos/finance/autopost/consumer/TransferReceivedConsumer.java, services/finance-service/src/test/java/io/restaurantos/finance/autopost/OrderCloseAutoPostingIT.java, services/finance-service/src/test/java/io/restaurantos/finance/autopost/InventoryAutoPostingIT.java, deploy/init/rabbitmq-definitions.json</files>
  <action>
    Extend `AutoPostingRecipeEngine` with M3.4 recipes:
    - `postOrderRefund` — ORDER_REFUNDED: DR Sales Refunds (4920) + reverse output tax; CR CASH/BANK. source_type=`ORDER_REFUND`, source_id=orderId.
    - `postWastage` — WASTAGE_RECORDED: DR Wastage (5200), CR INVENTORY at costPaisa. source_type=`WASTAGE`, source_id=wastageId.
    - `postCountVariance` — COUNT_VARIANCE_POSTED: per line positive variance → loss recipe (5220/1300); negative → gain (1300/5221). source_type=`COUNT_VARIANCE`, source_id=countId.
    - `postTransferShipped` — TRANSFER_SHIPPED: DR INVENTORY_TRANSIT (1320), CR INVENTORY. source_type=`TRANSFER_SHIP`, source_id=transferId.
    - `postTransferReceived` — TRANSFER_RECEIVED: DR INVENTORY, CR INVENTORY_TRANSIT (use shipped cost from payload or paired event). source_type=`TRANSFER_RECV`, source_id=transferId.

    Consumers on new queues (add all to definitions.json with DLQ pattern):
    | Queue | Exchange | Routing key |
    |-------|----------|-------------|
    | finance.order-refunded.queue | pos.topic | pos.order.refunded |
    | finance.wastage.queue | inventory.topic | inventory.wastage.recorded |
    | finance.count-variance.queue | inventory.topic | inventory.count.variance |
    | finance.transfer-shipped.queue | inventory.topic | inventory.transfer.shipped |
    | finance.transfer-received.queue | inventory.topic | inventory.transfer.received |

    `OrderCloseAutoPostingIT` (Testcontainers Postgres + RabbitMQ + seeded tenant COA + open period):
    1. Publish ORDER_CLOSED envelope → assert 1 POSTED JE, debits=credits, posted_source_events ORDER_REVENUE row.
    2. Publish STOCK_DEPLETED same orderId → COGS JE balanced.
    3. Re-publish same ORDER_CLOSED eventId → processed_events dedup; still exactly 1 revenue JE.
    4. Re-publish same orderId different eventId → posted_source_events blocks duplicate.

    `InventoryAutoPostingIT`: publish wastage, count variance (loss + gain lines), transfer ship+receive → each produces balanced JE.

    **Deferred explicitly (NOT in this plan):** GR/IR (`finance.stock-received`), vendor invoice, payroll — Phase 10/11.
  </action>
  <verify>`mvn -pl services/finance-service -am verify` GREEN; ITs pass; JaCoCo finance ≥ 75% per coverage-gates.json; `grep "finance.order-refunded.queue" deploy/init/rabbitmq-definitions.json`; each consumer class references correct queue name.</verify>
  <done>Phase 9 auto-posting recipes for order close, refund, wastage, count variance, and transfers are live, idempotent, and proven by ITs. Core-value loop (order → JE) is closed pending Phase 7/8 event publishers.</done>
</task>

</tasks>

<verification>
- `mvn -pl services/finance-service -am verify` GREEN with Testcontainers.
- ORDER_CLOSED → balanced revenue JE; STOCK_DEPLETED → balanced COGS JE.
- Duplicate events → no duplicate JEs (processed_events + posted_source_events).
- Refund, wastage, count variance, transfer events each post balanced JEs.
- All new queues present in rabbitmq-definitions.json with DLQ bindings.
</verification>

<success_criteria>
Phase 9 SC1 and SC2 met for finance: triggering events produce balanced auto-posted JEs; idempotency proven. FIN-03 satisfied for Phase-9-scope recipes (order, refund, wastage, count, transfer). GR/IR/vendor/payroll explicitly deferred to Phases 10/11.
</success_criteria>

<output>
After completion, create `.planning/phases/09-order-to-ledger-auto-posting-customer-loyalty/09-01-SUMMARY.md` recording: posted_source_events schema, recipe list + source_type values, queue bindings added, idempotency layers, IT coverage, and explicit deferrals for Phase 10/11 FIN-03 recipes.
</output>
