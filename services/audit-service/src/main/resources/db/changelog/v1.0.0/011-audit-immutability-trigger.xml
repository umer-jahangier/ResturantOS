<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
            http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.29.xsd">

    <!-- Defense-in-depth immutability: trigger fires even if someone bypasses GRANT.
         Combines with REVOKE UPDATE/DELETE on audit_writer for two-layer enforcement. -->
    <changeSet id="audit-1.0.0-011-immutability-trigger" author="restaurantos-agent">
        <sql splitStatements="false">
            CREATE OR REPLACE FUNCTION prevent_audit_mutation() RETURNS trigger AS $$
            BEGIN
                RAISE EXCEPTION 'audit_events is append-only';
            END;
            $$ LANGUAGE plpgsql;
        </sql>
        <sql splitStatements="false">
            CREATE TRIGGER audit_events_immutable
                BEFORE UPDATE OR DELETE ON audit_events
                FOR EACH ROW EXECUTE FUNCTION prevent_audit_mutation();
        </sql>
        <rollback>
            <sql>DROP TRIGGER IF EXISTS audit_events_immutable ON audit_events;</sql>
            <sql>DROP FUNCTION IF EXISTS prevent_audit_mutation();</sql>
        </rollback>
    </changeSet>

</databaseChangeLog>
