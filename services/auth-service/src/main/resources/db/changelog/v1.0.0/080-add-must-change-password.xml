<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

    <!-- Add must_change_password flag to users table for provisioning-created admin accounts -->
    <changeSet id="auth-1.0.0-080-must-change-password" author="restaurantos-agent">
        <preConditions onFail="MARK_RAN">
            <not>
                <columnExists tableName="users" columnName="must_change_password"/>
            </not>
        </preConditions>
        <addColumn tableName="users">
            <column name="must_change_password" type="BOOLEAN" defaultValueBoolean="false">
                <constraints nullable="false"/>
            </column>
        </addColumn>
        <rollback>
            <dropColumn tableName="users" columnName="must_change_password"/>
        </rollback>
    </changeSet>

</databaseChangeLog>
