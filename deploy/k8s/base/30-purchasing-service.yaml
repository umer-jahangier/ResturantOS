apiVersion: v1
kind: Service
metadata:
  name: purchasing-service
  labels: { app: purchasing-service }
spec:
  selector: { app: purchasing-service }
  ports: [{ name: http, port: 8087, targetPort: 8087 }]
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: purchasing-service
  labels: { app: purchasing-service }
spec:
  replicas: 1
  selector: { matchLabels: { app: purchasing-service } }
  strategy:
    type: RollingUpdate
    rollingUpdate: { maxSurge: 1, maxUnavailable: 0 }
  template:
    metadata:
      labels: { app: purchasing-service, tier: app }
    spec:
      initContainers:
        - name: wait-for-deps
          image: busybox:1.36
          command: ["sh","-c","until nc -z postgres 5432 && nc -z eureka-server 8761; do echo waiting for postgres+eureka; sleep 3; done"]
          resources: { requests: { memory: "8Mi", cpu: "10m" }, limits: { memory: "32Mi" } }
      containers:
        - name: purchasing-service
          image: restaurantos/purchasing-service:latest
          ports: [{ containerPort: 8087, name: http }]
          envFrom:
            - configMapRef: { name: restaurantos-config }
            - secretRef:    { name: restaurantos-secrets }
          env:
            - { name: SERVER_PORT, value: "8087" }
            - { name: PURCHASING_DB_URL, value: "jdbc:postgresql://postgres:5432/purchasing_db" }
            - { name: PURCHASING_DB_USER, value: "purchasing_user" }
            - { name: PURCHASING_DB_PASSWORD, valueFrom: { secretKeyRef: { name: restaurantos-secrets, key: PURCHASING_DB_PASSWORD } } }
          resources:
            requests: { memory: "220Mi", cpu: "50m" }
            limits:   { memory: "640Mi" }
          startupProbe:
            httpGet: { path: /actuator/health, port: 8087 }
            periodSeconds: 5
            failureThreshold: 60
          readinessProbe:
            httpGet: { path: /actuator/health, port: 8087 }
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            tcpSocket: { port: 8087 }
            periodSeconds: 20
            failureThreshold: 6
