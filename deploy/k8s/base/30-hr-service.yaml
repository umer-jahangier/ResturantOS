apiVersion: v1
kind: Service
metadata:
  name: hr-service
  labels: { app: hr-service }
spec:
  selector: { app: hr-service }
  ports: [{ name: http, port: 8088, targetPort: 8088 }]
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: hr-service
  labels: { app: hr-service }
spec:
  replicas: 1
  selector: { matchLabels: { app: hr-service } }
  strategy:
    type: RollingUpdate
    rollingUpdate: { maxSurge: 1, maxUnavailable: 0 }
  template:
    metadata:
      labels: { app: hr-service, tier: app }
    spec:
      initContainers:
        - name: wait-for-deps
          image: busybox:1.36
          command: ["sh","-c","until nc -z postgres 5432 && nc -z eureka-server 8761; do echo waiting for postgres+eureka; sleep 3; done"]
          resources: { requests: { memory: "8Mi", cpu: "10m" }, limits: { memory: "32Mi" } }
      containers:
        - name: hr-service
          image: restaurantos/hr-service:latest
          ports: [{ containerPort: 8088, name: http }]
          envFrom:
            - configMapRef: { name: restaurantos-config }
            - secretRef:    { name: restaurantos-secrets }
          env:
            - { name: SERVER_PORT, value: "8088" }
            - { name: HR_DB_URL, value: "jdbc:postgresql://postgres:5432/hr_db" }
            - { name: HR_DB_USER, value: "hr_user" }
            - { name: HR_DB_PASSWORD, valueFrom: { secretKeyRef: { name: restaurantos-secrets, key: HR_DB_PASSWORD } } }
          resources:
            requests: { memory: "220Mi", cpu: "50m" }
            limits:   { memory: "640Mi" }
          startupProbe:
            httpGet: { path: /actuator/health, port: 8088 }
            periodSeconds: 5
            failureThreshold: 60
          readinessProbe:
            httpGet: { path: /actuator/health, port: 8088 }
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            tcpSocket: { port: 8088 }
            periodSeconds: 20
            failureThreshold: 6
