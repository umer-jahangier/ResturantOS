apiVersion: v1
kind: Service
metadata:
  name: nlq-service
  labels: { app: nlq-service }
spec:
  selector: { app: nlq-service }
  ports: [{ name: http, port: 8094, targetPort: 8094 }]
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: nlq-service
  labels: { app: nlq-service }
spec:
  replicas: 1
  selector: { matchLabels: { app: nlq-service } }
  strategy:
    type: RollingUpdate
    rollingUpdate: { maxSurge: 1, maxUnavailable: 0 }
  template:
    metadata:
      labels: { app: nlq-service, tier: app }
    spec:
      initContainers:
        - name: wait-for-deps
          image: busybox:1.36
          command: ["sh","-c","until nc -z postgres 5432 && nc -z eureka-server 8761; do echo waiting for postgres+eureka; sleep 3; done"]
          resources: { requests: { memory: "8Mi", cpu: "10m" }, limits: { memory: "32Mi" } }
      containers:
        - name: nlq-service
          image: restaurantos/nlq-service:latest
          ports: [{ containerPort: 8094, name: http }]
          envFrom:
            - configMapRef: { name: restaurantos-config }
            - secretRef:    { name: restaurantos-secrets }
          env:
            - { name: SERVER_PORT, value: "8094" }
            - { name: NLQ_DB_URL, value: "jdbc:postgresql://postgres:5432/nlq_db" }
            - { name: NLQ_DB_USER, value: "nlq_user" }
            - { name: NLQ_DB_PASSWORD, valueFrom: { secretKeyRef: { name: restaurantos-secrets, key: NLQ_DB_PASSWORD } } }
          resources:
            requests: { memory: "300Mi", cpu: "50m" }
            limits:   { memory: "640Mi" }
          startupProbe:
            httpGet: { path: /actuator/health, port: 8094 }
            periodSeconds: 5
            failureThreshold: 60
          readinessProbe:
            httpGet: { path: /actuator/health, port: 8094 }
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            tcpSocket: { port: 8094 }
            periodSeconds: 20
            failureThreshold: 6
