apiVersion: v1
kind: Service
metadata:
  name: user-service
  labels: { app: user-service }
spec:
  selector: { app: user-service }
  ports: [{ name: http, port: 8082, targetPort: 8082 }]
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-service
  labels: { app: user-service }
spec:
  replicas: 1
  selector: { matchLabels: { app: user-service } }
  strategy:
    type: RollingUpdate
    rollingUpdate: { maxSurge: 1, maxUnavailable: 0 }
  template:
    metadata:
      labels: { app: user-service, tier: app }
    spec:
      initContainers:
        - name: wait-for-deps
          image: busybox:1.36
          command: ["sh","-c","until nc -z postgres 5432 && nc -z eureka-server 8761; do echo waiting for postgres+eureka; sleep 3; done"]
          resources: { requests: { memory: "8Mi", cpu: "10m" }, limits: { memory: "32Mi" } }
      containers:
        - name: user-service
          image: restaurantos/user-service:latest
          ports: [{ containerPort: 8082, name: http }]
          envFrom:
            - configMapRef: { name: restaurantos-config }
            - secretRef:    { name: restaurantos-secrets }
          env:
            - { name: SERVER_PORT, value: "8082" }
            - { name: USER_DB_URL, value: "jdbc:postgresql://postgres:5432/user_db" }
            - { name: USER_DB_USER, value: "user_service" }
            - { name: USER_DB_PASSWORD, valueFrom: { secretKeyRef: { name: restaurantos-secrets, key: USER_DB_PASSWORD } } }
          resources:
            requests: { memory: "200Mi", cpu: "50m" }
            limits:   { memory: "560Mi" }
          startupProbe:
            httpGet: { path: /actuator/health, port: 8082 }
            periodSeconds: 5
            failureThreshold: 60
          readinessProbe:
            httpGet: { path: /actuator/health, port: 8082 }
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            tcpSocket: { port: 8082 }
            periodSeconds: 20
            failureThreshold: 6
