apiVersion: v1
kind: Service
metadata:
  name: audit-service
  labels: { app: audit-service }
spec:
  selector: { app: audit-service }
  ports: [{ name: http, port: 8093, targetPort: 8093 }]
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: audit-service
  labels: { app: audit-service }
spec:
  replicas: 1
  selector: { matchLabels: { app: audit-service } }
  strategy:
    type: RollingUpdate
    rollingUpdate: { maxSurge: 1, maxUnavailable: 0 }
  template:
    metadata:
      labels: { app: audit-service, tier: app }
    spec:
      initContainers:
        - name: wait-for-deps
          image: busybox:1.36
          command: ["sh","-c","until nc -z postgres 5432 && nc -z eureka-server 8761; do echo waiting for postgres+eureka; sleep 3; done"]
          resources: { requests: { memory: "8Mi", cpu: "10m" }, limits: { memory: "32Mi" } }
      containers:
        - name: audit-service
          image: restaurantos/audit-service:latest
          ports: [{ containerPort: 8093, name: http }]
          envFrom:
            - configMapRef: { name: restaurantos-config }
            - secretRef:    { name: restaurantos-secrets }
          env:
            - { name: SERVER_PORT, value: "8093" }
            - { name: AUDIT_DB_URL, value: "jdbc:postgresql://postgres:5432/audit_db" }
            - { name: AUDIT_DB_USER, value: "audit_writer" }
            - { name: AUDIT_DB_PASSWORD, valueFrom: { secretKeyRef: { name: restaurantos-secrets, key: AUDIT_DB_PASSWORD } } }
            - { name: AUDIT_DB_ADMIN_URL, value: "jdbc:postgresql://postgres:5432/audit_db" }
            - { name: AUDIT_DB_ADMIN_USER, value: "audit_user" }
            - { name: AUDIT_DB_ADMIN_PASSWORD, valueFrom: { secretKeyRef: { name: restaurantos-secrets, key: AUDIT_DB_PASSWORD } } }
          resources:
            requests: { memory: "220Mi", cpu: "50m" }
            limits:   { memory: "640Mi" }
          startupProbe:
            httpGet: { path: /actuator/health, port: 8093 }
            periodSeconds: 5
            failureThreshold: 60
          readinessProbe:
            httpGet: { path: /actuator/health, port: 8093 }
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            tcpSocket: { port: 8093 }
            periodSeconds: 20
            failureThreshold: 6
