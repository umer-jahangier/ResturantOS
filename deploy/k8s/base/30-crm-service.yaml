apiVersion: v1
kind: Service
metadata:
  name: crm-service
  labels: { app: crm-service }
spec:
  selector: { app: crm-service }
  ports: [{ name: http, port: 8089, targetPort: 8089 }]
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: crm-service
  labels: { app: crm-service }
spec:
  replicas: 1
  selector: { matchLabels: { app: crm-service } }
  strategy:
    type: RollingUpdate
    rollingUpdate: { maxSurge: 1, maxUnavailable: 0 }
  template:
    metadata:
      labels: { app: crm-service, tier: app }
    spec:
      initContainers:
        - name: wait-for-deps
          image: busybox:1.36
          command: ["sh","-c","until nc -z postgres 5432 && nc -z eureka-server 8761; do echo waiting for postgres+eureka; sleep 3; done"]
          resources: { requests: { memory: "8Mi", cpu: "10m" }, limits: { memory: "32Mi" } }
      containers:
        - name: crm-service
          image: restaurantos/crm-service:latest
          ports: [{ containerPort: 8089, name: http }]
          envFrom:
            - configMapRef: { name: restaurantos-config }
            - secretRef:    { name: restaurantos-secrets }
          env:
            - { name: SERVER_PORT, value: "8089" }
            - { name: CRM_DB_URL, value: "jdbc:postgresql://postgres:5432/crm_db" }
            - { name: CRM_DB_USER, value: "crm_user" }
            - { name: CRM_DB_PASSWORD, valueFrom: { secretKeyRef: { name: restaurantos-secrets, key: CRM_DB_PASSWORD } } }
          resources:
            requests: { memory: "300Mi", cpu: "50m" }
            limits:   { memory: "640Mi" }
          startupProbe:
            httpGet: { path: /actuator/health, port: 8089 }
            periodSeconds: 5
            failureThreshold: 60
          readinessProbe:
            httpGet: { path: /actuator/health, port: 8089 }
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            tcpSocket: { port: 8089 }
            periodSeconds: 20
            failureThreshold: 6
