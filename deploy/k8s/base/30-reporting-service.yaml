apiVersion: v1
kind: Service
metadata:
  name: reporting-service
  labels: { app: reporting-service }
spec:
  selector: { app: reporting-service }
  ports: [{ name: http, port: 8092, targetPort: 8092 }]
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: reporting-service
  labels: { app: reporting-service }
spec:
  replicas: 1
  selector: { matchLabels: { app: reporting-service } }
  strategy:
    type: RollingUpdate
    rollingUpdate: { maxSurge: 1, maxUnavailable: 0 }
  template:
    metadata:
      labels: { app: reporting-service, tier: app }
    spec:
      initContainers:
        - name: wait-for-deps
          image: busybox:1.36
          command: ["sh","-c","until nc -z postgres 5432 && nc -z eureka-server 8761; do echo waiting for postgres+eureka; sleep 3; done"]
          resources: { requests: { memory: "8Mi", cpu: "10m" }, limits: { memory: "32Mi" } }
      containers:
        - name: reporting-service
          image: restaurantos/reporting-service:latest
          ports: [{ containerPort: 8092, name: http }]
          envFrom:
            - configMapRef: { name: restaurantos-config }
            - secretRef:    { name: restaurantos-secrets }
          env:
            - { name: SERVER_PORT, value: "8092" }
            - { name: REPORTING_DB_URL, value: "jdbc:postgresql://postgres:5432/reporting_db" }
            - { name: REPORTING_DB_USER, value: "reporting_user" }
            - { name: REPORTING_DB_PASSWORD, valueFrom: { secretKeyRef: { name: restaurantos-secrets, key: REPORTING_DB_PASSWORD } } }
          resources:
            requests: { memory: "300Mi", cpu: "50m" }
            limits:   { memory: "640Mi" }
          startupProbe:
            httpGet: { path: /actuator/health, port: 8092 }
            periodSeconds: 5
            failureThreshold: 60
          readinessProbe:
            httpGet: { path: /actuator/health, port: 8092 }
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            tcpSocket: { port: 8092 }
            periodSeconds: 20
            failureThreshold: 6
