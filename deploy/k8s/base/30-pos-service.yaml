apiVersion: v1
kind: Service
metadata:
  name: pos-service
  labels: { app: pos-service }
spec:
  selector: { app: pos-service }
  ports: [{ name: http, port: 8084, targetPort: 8084 }]
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: pos-service
  labels: { app: pos-service }
spec:
  replicas: 1
  selector: { matchLabels: { app: pos-service } }
  strategy:
    type: RollingUpdate
    rollingUpdate: { maxSurge: 1, maxUnavailable: 0 }
  template:
    metadata:
      labels: { app: pos-service, tier: app }
    spec:
      initContainers:
        - name: wait-for-deps
          image: busybox:1.36
          command: ["sh","-c","until nc -z postgres 5432 && nc -z eureka-server 8761; do echo waiting for postgres+eureka; sleep 3; done"]
          resources: { requests: { memory: "8Mi", cpu: "10m" }, limits: { memory: "32Mi" } }
      containers:
        - name: pos-service
          image: restaurantos/pos-service:latest
          ports: [{ containerPort: 8084, name: http }]
          envFrom:
            - configMapRef: { name: restaurantos-config }
            - secretRef:    { name: restaurantos-secrets }
          env:
            - { name: SERVER_PORT, value: "8084" }
            - { name: POS_DB_URL, value: "jdbc:postgresql://postgres:5432/pos_db" }
            - { name: POS_DB_USER, value: "pos_user" }
            - { name: POS_DB_PASSWORD, valueFrom: { secretKeyRef: { name: restaurantos-secrets, key: POS_DB_PASSWORD } } }
          resources:
            requests: { memory: "300Mi", cpu: "50m" }
            limits:   { memory: "640Mi" }
          startupProbe:
            httpGet: { path: /actuator/health, port: 8084 }
            periodSeconds: 5
            failureThreshold: 60
          readinessProbe:
            httpGet: { path: /actuator/health, port: 8084 }
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            tcpSocket: { port: 8084 }
            periodSeconds: 20
            failureThreshold: 6
