apiVersion: v1
kind: Service
metadata:
  name: platform-admin-service
  labels: { app: platform-admin-service }
spec:
  selector: { app: platform-admin-service }
  ports: [{ name: http, port: 8096, targetPort: 8096 }]
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: platform-admin-service
  labels: { app: platform-admin-service }
spec:
  replicas: 1
  selector: { matchLabels: { app: platform-admin-service } }
  strategy:
    type: RollingUpdate
    rollingUpdate: { maxSurge: 1, maxUnavailable: 0 }
  template:
    metadata:
      labels: { app: platform-admin-service, tier: app }
    spec:
      initContainers:
        - name: wait-for-deps
          image: busybox:1.36
          command: ["sh","-c","until nc -z postgres 5432 && nc -z eureka-server 8761; do echo waiting for postgres+eureka; sleep 3; done"]
          resources: { requests: { memory: "8Mi", cpu: "10m" }, limits: { memory: "32Mi" } }
      containers:
        - name: platform-admin-service
          image: restaurantos/platform-admin-service:latest
          ports: [{ containerPort: 8096, name: http }]
          envFrom:
            - configMapRef: { name: restaurantos-config }
            - secretRef:    { name: restaurantos-secrets }
          env:
            - { name: SERVER_PORT, value: "8096" }
            - { name: PLATFORM_DB_URL, value: "jdbc:postgresql://postgres:5432/platform_db" }
            - { name: PLATFORM_DB_USER, value: "platform_user" }
            - { name: PLATFORM_DB_PASSWORD, valueFrom: { secretKeyRef: { name: restaurantos-secrets, key: PLATFORM_DB_PASSWORD } } }
          resources:
            requests: { memory: "220Mi", cpu: "50m" }
            limits:   { memory: "640Mi" }
          startupProbe:
            httpGet: { path: /actuator/health, port: 8096 }
            periodSeconds: 5
            failureThreshold: 60
          readinessProbe:
            httpGet: { path: /actuator/health, port: 8096 }
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            tcpSocket: { port: 8096 }
            periodSeconds: 20
            failureThreshold: 6
