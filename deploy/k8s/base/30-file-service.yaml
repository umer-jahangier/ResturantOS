apiVersion: v1
kind: Service
metadata:
  name: file-service
  labels: { app: file-service }
spec:
  selector: { app: file-service }
  ports: [{ name: http, port: 8095, targetPort: 8095 }]
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: file-service
  labels: { app: file-service }
spec:
  replicas: 1
  selector: { matchLabels: { app: file-service } }
  strategy:
    type: RollingUpdate
    rollingUpdate: { maxSurge: 1, maxUnavailable: 0 }
  template:
    metadata:
      labels: { app: file-service, tier: app }
    spec:
      initContainers:
        - name: wait-for-deps
          image: busybox:1.36
          command: ["sh","-c","until nc -z postgres 5432 && nc -z eureka-server 8761; do echo waiting for postgres+eureka; sleep 3; done"]
          resources: { requests: { memory: "8Mi", cpu: "10m" }, limits: { memory: "32Mi" } }
      containers:
        - name: file-service
          image: restaurantos/file-service:latest
          ports: [{ containerPort: 8095, name: http }]
          envFrom:
            - configMapRef: { name: restaurantos-config }
            - secretRef:    { name: restaurantos-secrets }
          env:
            - { name: SERVER_PORT, value: "8095" }
            - { name: FILE_DB_URL, value: "jdbc:postgresql://postgres:5432/file_db" }
            - { name: FILE_DB_USER, value: "file_service" }
            - { name: FILE_DB_PASSWORD, valueFrom: { secretKeyRef: { name: restaurantos-secrets, key: FILE_DB_PASSWORD } } }
          resources:
            requests: { memory: "220Mi", cpu: "50m" }
            limits:   { memory: "640Mi" }
          startupProbe:
            httpGet: { path: /actuator/health, port: 8095 }
            periodSeconds: 5
            failureThreshold: 60
          readinessProbe:
            httpGet: { path: /actuator/health, port: 8095 }
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            tcpSocket: { port: 8095 }
            periodSeconds: 20
            failureThreshold: 6
